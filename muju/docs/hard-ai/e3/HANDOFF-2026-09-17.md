# E3 handoff — 2026-09-17 16:52Z (11:52 CDT)

Written for a fresh context window. Everything below is in the repo or in
the paths named; nothing depends on the previous session's memory.

## Where things are

- Branch `claude/hard-ai-e3`, integration worktree `~/src/deevgames-e3`, head
  9c8d5f64 (plus whatever the row #4 read adds). Off `claude/hard-ai-e2` at
  6adc0f2c. Sixteen lanes merged. Champion `hard@desktop` config hash
  `4e7afdf76b32fadfab2d11577cb610c8f9f1a491b5153e70b87b0774403600cd` at
  wall:3000, unchanged in every commit; fixed-work golden identical (48/48,
  `lab/results/hard-ai-e3/correct/cross-commit/`).
- Reading order: `E3-PLAN.md` (rules, ledger, every dated correction —
  read the whole file, the corrections are appended at the end),
  `E3.2-ROW-REPORT.md` (rows #1–#3 in full, decision record),
  `E3.1-SYNTHESIS.md` + its appended corrections, `E3-CRITIQUE.md`,
  `AMENDMENTS-E3.md` (A-E3-1..5, owner decisions taken under Ethan's
  delegation), `P8-SLOW-TURNS.md`, `README.md` for the rest.
- Run worktrees (detached, never commit there): `~/src/deevgames-e3-run`
  at a0f03c4e (rows #1–#2, singles B2–B5), `~/src/deevgames-e3-run2` at
  9c8d5f64 (row #3, B6, bundle, and tonight's row #4). Lane worktrees
  `~/src/deevgames-e3-lane{1..8}` hold lane branches 9–16 (all merged); they
  can be removed with `git worktree remove` (branches kept).
- E2 branch `claude/hard-ai-e2` in `~/src/deevgames-e0` at 59ac679d carries
  the overnight artifacts (47-loss analysis, exposure retrofit, re-pin row).
- Session scratchpad (scripts, logs):
  `/private/tmp/claude-501/-Users-ashkie-src/cd40736e-60ca-490c-bc97-ac7331c3a88d/scratchpad/`
  — `row4-night.sh` (pid 73224, sleeping until 23:00Z), `row4-night.log`,
  `chain-correct-desc*.sh/.log` (done), `e3-row1-go`, `e3-row1-launched`.

## What E3 established

- `hard@ablate:eval-no-safety` (the 19 safety weights of
  `lab/hard-ai/audit/eval-groups.ts` at 0, everything else `default-v1`, hash
  `66edf9cdf58f…`) is the RETAINED CHAMPION CANDIDATE:
  - row #1 screening, e1-val2 rows 0–15: 45/2/17, 0.719, +163 [+85, +260];
  - row #2 confirmation, e2-val rows 32–47: 42/2/20, 0.672, +124 [+38, +229];
  - row #3 vs shipped `aiv2-hard`, e1-baseline 100 pairs: 181/1/18, 0.9075,
    +397 [+330, +495] (champion's own re-pin on the same set: 155/0/45,
    0.775, +215 [+156, +287]).
  - Tactical thresholds met (tactics 73/79, home-mate 56/56, spawn-strike
    15/20 vs 16/20, exam at 200k 117 vs 120). Development evidence only; M20's
    fixed-work SPRT and E6's held-out release stand before anything ships.
- It is a GROUP ABLATION, not a fixed misconception: the in-sample "safety
  for economy" story did not generalise to the 47 baseline losses (safety →
  played turn 17 of 30), the development case does not flip under search,
  and no safety sub-block alone reproduces the whole block (0.688 / 0.562 /
  0.625 vs 0.875 at fixed work). Which of the 19 weights carry the effect is
  open.
- Evaluator defects found and verified (59 findings survived two-lens
  review): rent charged twice; one threatened body charged on up to five
  features; three literal duplicate predicates; Infiltration identically
  zero; rot180 asymmetry in three economy features; Inv3 missing its
  `retreats > 0` conjunct; economy DP non-monotone in reserve; `hard:suite`
  measured every A6 baseline with placeholder weights (real spawn-strike is
  .80, not .95; A-E3-3).
- Correctness arm `hard@ablate:eval-correct-v1` = B2 + B3 + B4 + B5 + B6
  behind `HardConfig.evalFix` flags (default absent; champion untouched),
  hash `49aa15dba67f…`; B1 (relocation trigger) is an open flag excluded by
  A-E3-2. Descriptive fixed-work rows: singles all nulls, bundle 11/0/5,
  +137 [+14, +309] on four development openings.
- Instruments now see arms: invariants EVAL column, recall, `hard:suite
  --all`, exam `won`/`dead` outcome classes, ladder `loadAvg` per game,
  `summary.md` lists openings USED.

## Running or scheduled right now

- **Row #4** (ledger ordinal 4, preregistered in E3-PLAN): bundle screening
  `hard@ablate:eval-correct-v1` vs `hard@desktop`, wall:3000, 32 pairs,
  `e1-val2.jsonl --openings-skip 16` (rows 16–31, last screening use), seed
  20260934, `--shards 8`, `MUJU_HEAVY_SLOTS=10`, launched by
  `row4-night.sh` at 23:00Z from `~/src/deevgames-e3-run2`, out
  `lab/results/hard-ai-e3/ablate/eval-correct-v1/screen` (+ `screen.nohup.log`).
  Expect ~30–40 min and a few P6 > 20 s turns (P8: timing columns withheld,
  row not voided). Rule: score > 0.5 → row #5 confirmation on `e2-val.jsonl
  --openings-skip 48` (rows 48–63), seed 20260935, retain only with score
  > 0.5 and interval excluding 0, same tactical thresholds measured for the
  bundle first. Score ≤ 0.5 → bundle closes correctness pass / strength fail;
  isolate B5 first.
- The previous session's Monitor tasks die with the session; a fresh session
  checks the row by reading `metrics.json` in the out dir (`status`,
  `pairsCompleted`, `wins/draws/losses`, `elo`, `eloLo/eloHi`, `voided`,
  `timing.a/b.maxTurnMs`, `overrunRate`, `loadAvgMean`).

## Next tasks, in order

1. Read row #4 (metrics as above; copy the out dir and nohup log into the
   branch under the same path; append the result to the ledger and a dated
   bullet; if > 0.5, preregister and launch row #5 in the night window at 8
   shards, then read it the same way).
2. E3 close: a Fable critique of the whole session (E3-PLAN + row report +
   synthesis corrections + P8) in the shape of `E3-CRITIQUE.md`; scoped
   tests `tests/ai/hard` + `tests/lab` with `MUJU_HEAVY_DIR` isolated on an
   idle box; merge `origin/master` (4 UI commits behind; lane 16's branch
   already carries a clean merge, so it should be clean); an
   `E3-CLOSE.md` decision record per EPIC-PLAN §7; update
   `docs/hard-ai/e3/README.md`; remove lane worktrees.
3. E4 opening (search efficiency), with these handoffs: P8 fix (cap and
   meter `homeWitness` in `gen/generate.ts injectRescue`, set `s.truncated`,
   memoise the canonical home verdict for P7); the search-side leaf tie at
   `g2-s20_3_15-A` t3 (identical feature vectors, ORDER_TT tie-break);
   iteration cost ratio median 9 per depth (E2); which of the 19 safety
   weights carry the effect (needs per-weight rows, night window).
4. E2 branch still owes: fresh work-sweep over the 47 losses counted on
   distinct positions and the final `E2-BASELINE-LOSSES.md`.
5. Optional E3.3: tuning is blocked by the corpus preconditions in
   `E3.3-TUNING-INSTRUMENT.md` (quiet share 4.9%, validation-opening rows,
   rarity runaways) and needs a ruling on training openings first.

## Rules that must survive the context switch

- Openings: `e1-val.jsonl` fully spent; `e1-val2.jsonl` rows 0–15 and
  16–31 spent after row #4; confirmations only on `e2-val.jsonl` rows 32–47
  (used by row #2) and 48–63 (reserved for row #5); rows 0–31 reserved to E2;
  `e1-sealed.jsonl` untouched until E6. Seed = 20260930 + ledger ordinal.
- Preregister before launch, append never rewrite, date every bullet from
  `date -u` (not an estimate — this bit the session four times).
- Champion hash and fixed-work golden checked after every merge; any
  `src/ai/hard` change goes behind a flag that is absent in every profile.
- Compute policy (Ethan): 9am–6pm CDT tidy (≤ 3 engine cores); 6pm–9am go
  hard: fixed-work at up to 12 shards, wall-clock rows at 8, one row at a
  time; the heavy queue gives up after 30 min when slots are held, so chains
  must wait for a free slot themselves. Never `--shards` more than the box's
  12 cores. Kill a row by parent AND shard pids
  (`~/.local/state/muju-heavy/slot-*.json`).
- Lab scripts constructing an engine go through `hardEnginePatch`/
  `createHardBot` or assert `weights.version !== 0`; instruments that score
  positions must take the engine's weights AND `evalFix`.
- Workflow scripts: literal `model:` on every `agent()`; a `// Fable
  rationale:` comment for Fable calls; agent count per model stated up front.
- Commits use `git -c core.hooksPath=/dev/null commit` (the freshness hook
  demands a master merge otherwise) with the session's attribution lines.

## Gotchas learned this session (also in `.claude/napkin.md`)

- `hard:cross-commit` without `--states-in` reconstructs positions with fresh
  `Date.now()` unit ids; compare search fields, not the actions JSON.
- P6 wall-clock tests fail under load and pass idle; not a merge gate.
- `summary.md` used to list all pool ids; trust `metrics.json.openingsUsed`.
- An amendment's arm count goes stale between lanes; count at apply time.
- A flag whose only reader is gated behind another flag is inert alone (B6).

## Corrections (18:03:34Z from `date -u`, appended at the E3 close)

- "config hash … unchanged in every commit; fixed-work golden identical" is
  right for the hash and overstated for the golden: goldens exist at
  81e4e303, 108a1215 and c73204dd only (close critique C2; E3-PLAN's
  close-critique section).
- Row #4 runs at `--shards 6`, `MUJU_HEAVY_SLOTS=8`, not 8/10 (critique C1);
  the launcher is this session's `scratchpad/row4-night.sh`, pid 89208.
- The E3 close continues in `E3-CLOSE-CRITIQUE.md` and `E3-CLOSE.md`.
