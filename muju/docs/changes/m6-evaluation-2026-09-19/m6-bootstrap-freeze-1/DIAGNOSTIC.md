The first accounting run reached 11 cases and failed only the JavaScript strict assertion distinguishing numeric zero from negative zero in viewpoint antisymmetry (actual0, expected-0). The exact pre-run driver is preserved as driver.mts. The next driver compares numeric equality (`===`), preserving all nonzero exact integer equality and treating +0/-0 as the same centicrystal value. No production code, weights or expected ledger changed.
