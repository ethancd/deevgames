  <% if (result == "tie") { %>
    A tie. That's epic! Up for a rematch?"
  <% } else if (result.slice(6) == "victory") { %>
    <%= winnerName +  " has won over " + loserName + ". Up for a rematch?" %>
  <% } else if (result == "quit") { %>
    <%= "Looks like " + loserName + " quit." %>
  <% } else { %>
    It's still anybody's game!
  <% } %>