# Hard AI under Phasing — handoff addendum (2026-09-21)

Read this before [HANDOFF.md](HANDOFF.md). It was written from a cloud session on master `eda73b2`
(the merge of PR #25) after verifying every claim in the 09-20 handoff against that commit: the
hard test suite and the two failing lab files were run here, the CI logs for the last two master runs
were read, and nine read-only reader agents plus a critic checked each handoff item against the code
(raw output in `reports/audit-2026-09-21/reader-reports.md`). Every line number below is at `eda73b2`.
Nothing in this addendum changes engine behaviour; the only non-doc addition is the freeze artifact
`weights/phasing-hand-priors-v1.json` (see §2.2).

## 0. The one-paragraph state

PR #25 landed two commits on top of the handoff base `a02bbb7`: `b2a85e6` (lab arms, an env weights
hook, 32 weight JSONs, reports, the handoff) and `71b41a3`, which did the first two parts of handoff
step 1: the pending-summon credit in `src/ai/hard/engine.ts:355-364` is now **unconditional** (base
`stage0 + stage1` at :359, the credit `(pendCostSum[mover] − pendCostSum[1−mover]) × 100` at :363,
no `+pc` label gate anywhere in `src/`, `lab/` or `tests/`), and `DEFAULT_WEIGHTS` at
`src/ai/hard/eval/weights.ts:62` is now `makeWeights('phasing-hand-priors-v1')`: 43 non-zero
weights, `weightsHash` **`14d06ba8`**, `WEIGHTS_VERSION` still 2. Production really uses it
(`engine.ts:268` substitutes `DEFAULT_WEIGHTS`, `eval/evaluate.ts:89` defaults to it), but only
behind `?phasingAi=1`; nothing in PR #25 touched a guard, a default or a server file. **Everything
else in step 1 is still open, and master is red because of it**: CI run
[35552456980](https://github.com/ethancd/deevgames/actions/runs/35552456980) on `eda73b2` fails
**7 files / 24 tests** (the 4 environment-only lab failures the handoff already knew about, plus
**20 new ones caused entirely by the weights change**; the unconditional credit broke nothing).
Locally on this checkout: `npm run hard:test` → 2 files / 9 tests failing;
`npx vitest run tests/lab/ablate.test.ts tests/lab/phasing-tune-guards.test.ts` → 11 failing.
No scorer mirror, no DAG record, no contract amendment, no superseding floor contract, no
correctness run, no Round 3, no Rush work, no cutover work has started.

## 1. Start here: finish step 1 in one PR

Do the sub-steps in this order. 1.1 is a decision that the test edits in 1.2 depend on; 1.3 and
1.4 touch files inside the floor contract's engine-source hash, so they must land **before** §3.

### 1.1 Three decisions to write down first (dated amendment files, never edits to frozen docs)

1. **BankExcess 100 → 25 deviates from the M6 bootstrap contract.**
   `docs/hard-ai/phasing/M6-BOOTSTRAP-CONTRACT.md` line 1 says "No engine outcomes informed these
   coefficients … deviations recorded and reviewed before outcomes", and its freeze paragraph
   (the "Freeze weights 2/3 at 100 and 58 at 1 … unless the accounting model is explicitly
   revised" paragraph, around line 106) pins exactly the weight `71b41a3` changed. Outcomes were
   seen first (the sweep in `results/ALL-PROGRESS-LINES.txt`: bank discount 100→25 is the master
   switch, 1–0–31 → 12–0–20 vs Rush, spend 24% → 93%). Recommendation: **keep 25** and write
   `docs/hard-ai/phasing/M6-BOOTSTRAP-AMENDMENT-2026-09-21.md` (new file) stating: BankExcess
   100→25; the 38 restored priors; the unconditional pending-summon credit in the within-turn
   scorer; and that the outcomes preceded this record. Then change `ACCOUNTING_PINS` in
   `lab/hard-ai/tune/texel.ts:82` from `{2:100, 3:100, 58:1}` to `{2:100, 3:25, 58:1}` and the
   matching pin in `tests/lab/phasing-tune-guards.test.ts:146-148` (it fails today with
   `texel: accounting w[3] must stay pinned at 100`).
2. **Two M6 escrow-conservation invariants are false by design now.**
   `tests/ai/hard/phasing-bootstrap.test.ts:88` asserts `full()` of the arrived unit equals the
   pending score (received −2340, expected −2115) and `:104` asserts
   `score = 200 + 300 + service` (received −1945, expected 670). Under the hand priors an arrived
   unit earns positional credit (SpawnArea, AnchorDepth, …) that a pending one does not, so these
   are not numeric re-pins. Also `:75` ("no cash cliff at eight") now sees the 9th crystal worth 25
   instead of 100, which is the intended discount. Recommendation: keep testing the contract they
   describe by building the Evaluator in that file from the **frozen bootstrap** instead of
   `DEFAULT_WEIGHTS` — `weights/bootstrap-control.json` (hash `0ae24a95`, non-zero exactly
   `[[0,100],[2,100],[3,100],[23,100],[58,1]]`, verified here; the same vector is frozen under
   `docs/changes/m6-evaluation-2026-09-19/m6-bootstrap-freeze-*`) — and add one new test that
   `DEFAULT_WEIGHTS` keeps the pins that still hold (Material 100, BankLiquid 100, EconDelta 100,
   PendingValue 1, features 59–61 zero) and hashes to `14d06ba8`. Alternative: assert conservation
   on the accounting slice only (`stage0 + w[PendingValue]·f[PendingValue]`).
3. **`WEIGHTS_VERSION`: keep 2, fix the comment.**
   `weights.ts:14` says the number is "bumped whenever `DEFAULT_WEIGHTS`' numbers change", and
   `71b41a3` changed the numbers without bumping, so the constant contradicts its own comment. By
   the code's actual semantics the version is a persisted-vector schema gate (`loadWeights`
   `:111-112`, `assertCurrentWeights` `:124`, tuner preflight `lab/hard-ai/tune/rows.ts:178`
   hard-codes `WEIGHTS_VERSION !== 2`), while every identity that must distinguish vectors already
   binds the values: `weightsHash` (`:157-173`, excludes the label), the ladder resolved-config hash
   (`lab/hard-ai/ladder/identity.ts`, includes `w`, `material`, version and label; it has already
   moved, see 1.2), and the suite `weightsSha256` (`lab/hard-ai/suites/phasing/engine-adapter.ts`).
   A bump would orphan the 32 JSONs under `weights/`, the two M6 freeze artifacts, the
   `MUJU_HARD_WEIGHTS` hook and the `--weights` path of `measure.ts` (all use `loadWeights`),
   `rows.ts:178`, and five pins in `tests/lab/phasing-tune-guards.test.ts` (:18, :22, :36, :140,
   :145) for no gain. The release DAG's hard-ai node says "bump weightsVersion … when weights
   change", while `docs/changes/m6-evaluation-2026-09-19/m6-dev-continuation.md:142` allows weight
   artifacts to be re-pinned (new record, exact hashes, schema/version) only after the dev/val
   process and frozen floors succeed; answer both in the change record (§1.6) rather than
   ignoring either.
   There is no opening-book file in the tree, so "bump book magic" is vacuous at HEAD.

### 1.2 The exact test edits (20 failing assertions, plus the ones hidden behind them)

The first failing `expect` stops each test, so several lines below did not show in the run but will
fail next. New values were computed on this checkout from `DEFAULT_WEIGHTS` and from
`resolvedConfigHash` (`lab/hard-ai/ladder/identity.ts`); they are the values, not guesses.

**`tests/ai/hard/eval.test.ts`** (5 tests)

| Line | Today | Change to |
|---|---|---|
| :131-132 | `material + 100 * (5 + 2)` (1300) | `material + 100 * 5 + 25 * 2` (1150), and the BLACK negation |
| :155 | `w[F.HomeInvaded]` is 0 | −4000 |
| :217 | `w[F.DrawPressure]` is 0 | −8 |
| :296-306 | non-zero list is the 5 bootstrap pairs; invariant slice all zero | the 43-pair list and the invariant slice below; rename the `describe`/`it` (they say "sparse accounting vector") |
| :345 | `w[F.SpawnArea]` is 0 (after mutating `TUNED_WEIGHTS`) | 30 |

The 43 non-zero pairs (`[index, value]`) of `DEFAULT_WEIGHTS.w`:

```
[[0,100],[2,100],[3,25],[4,-4000],[7,30],[8,8],[9,-800],[10,25],[11,90],[12,-60],[13,-400],
 [14,-180],[15,220],[16,90],[17,-20],[18,-8],[19,40],[23,100],[28,-50],[30,-25],[31,-10],[32,20],
 [33,35],[34,-40],[35,-120],[36,-150],[37,300],[38,-800],[39,-300],[40,-250],[41,-100],[43,-120],
 [44,-600],[45,-150],[46,-150],[47,-400],[49,-40],[50,-200],[51,-200],[53,-200],[56,-150],
 [57,-250],[58,1]]
```

The invariant slice `w[38..57]` (INV_BASE 38, 20 entries):
`[-800,-300,-250,-100,0,-120,-600,-150,-150,-400,0,-40,-200,-200,0,-200,0,0,-150,-250]`.
`w[59..61]` stay `[0,0,0]`; `material` is unchanged (cost × 100 for all 18 definitions).
Zero by contract and still zero: Rent(1), PstMine(5), BankConvertible(6), ElementCoverage(22),
features 24–27, HangingBuy(29), Inv5(42), Inv11(48), Inv15(52), Inv17(54), Inv18(55), and 59–61.

**`tests/ai/hard/phasing-bootstrap.test.ts`** (4 tests): `:57-58` pins the 5-pair list; `:75`,
`:88`, `:104` are decision 1.1(2). If you take the fixture route, `:57-58` becomes a test of the
fixture (unchanged values) plus the new `DEFAULT_WEIGHTS` test.

**`tests/lab/ablate.test.ts`** (10 tests; all but three flow from one constant)

| Line | Today | Change to |
|---|---|---|
| :198 `DESKTOP_WALL3000_HASH` | `4464b191…` | `2c485153f22afad810639da52dc59a3e7e13c1187cc2cce9cb0bf9611e90abdd` |
| :499 `search-iter-fit` | `a18b84f8…` | `6c8f9bb176bf1f365d6a4e6ca2e285c8117e3d5d7b73b6bb963d974cd562754e` |
| :532 `search-reach-cache` | `1aef0a8b…` | `6e3d2936d0564c13b331fe5435da15452e4ae4572636cdb898bfd045bd52973c` |
| :724 … :734 live counts | economy 4, home 0, safety 0, space 0, invariants 0, stage2 2 | economy 6, home 10, safety 17, space 9, invariants 15, stage2 26 (group sizes unchanged: 15/11/21/14/20/39) |
| :759 `A_A_WEIGHT_ARMS` | seven vacuous arms | `[]` — no weight arm is A/A any more; :778 "arms that bite" becomes all ten |
| :808 | every safety weight is 0 | exactly four are zero: indices 29, 54, 59, 60; the other 17 are live |

Use sites that pass once the constant moves: :595, :687, :913, :968, :994. The file's convention
(:170-208) is to keep every superseded hash as a named constant with the reason it moved; this is
the **fourth move** of `DESKTOP_WALL3000_HASH` and the reason is the M6 one again: `71b41a3`
replaced `DEFAULT_WEIGHTS`, which `hardEnginePatch` substitutes into every `hard@*` configuration.
Rewrite the comment blocks at :696-716 (describes the five-weight bootstrap) and :802-807, and the
test title at :755 ("A/A against the M6 bootstrap"). Consequence for records: every `hard@*`
ladder row recorded before `71b41a3` quotes `4464b191…`; rows must never be pooled across it.

**`tests/lab/phasing-tune-guards.test.ts:146-148`** — decision 1.1(1).

**The 4 environment-only failures** (red since before the handoff; `5a6d2f9` fixed only the v2
half of the third):

- `tests/lab/gate1.test.ts:124` and `:297` — `git show a0551c8:…PHASING-PREREGISTRATION…md`
  fails on the shallow CI checkout (`lab/ai/gate1.ts:105`).
- `tests/lab/gate1-shard.test.ts:462` — `spawnSync rg ENOENT`; `lab/ai/gate1-sources.ts:60`,
  `lab/ai/phasing-selfplay.ts:86` and `lab/ai/run.ts:22` all shell out to ripgrep.
- `tests/lab/suites-phasing-contract.test.ts:51` — the **v1** contract test hits
  `measure.ts:286`'s result-path refusal because the shallow graft root "touches" every
  `results/` file.

Fix: `.github/workflows/deploy.yml` — add `with: fetch-depth: 0` to `actions/checkout` and install
ripgrep before `npm test`; or gate the three files with `it.skipIf(process.env.CI)` as
`tests/ai/hard/p6-stoppable-generation.test.ts:240` does. Until CI is green the Playwright steps
never run, and note that a green run still does **not** publish Pages: the deploy step is
`if: env.CLOUDFLARE_API_TOKEN != ''` (`deploy.yml:57`) and has been skipped on every run.

### 1.3 Mirror the scorer credit through one helper

Eight duplicated within-turn scorer closures still return plain `stage0 + stage1`, so every lab
instrument now drives a generator the engine no longer ships (the analyser compares the engine's
played turn against a regenerated candidate list, so a played BUY can be reported as "never
offered"; recall's cheap/reference/ceiling lists and the audit's family counts are likewise off).
Sites: `lab/hard-ai/recall/run.ts:492`, `lab/hard-ai/analyze/engine.ts:180`,
`lab/hard-ai/audit/gen-view.ts:75`, `lab/hard-ai/audit/score.ts:59`,
`lab/hard-ai/coverage/run.ts:221`, `lab/hard-ai/bench/p6-turn-time.ts:159`,
`tests/ai/hard/generate.test.ts:52`, `tests/ai/hard/gen-trace.test.ts:52`.

Do it as one exported helper rather than nine copies (that copy-drift is how this item arose):
`withinTurnScore(ev, p, mover, sc, ply)` in `src/ai/hard/eval/evaluate.ts` next to
`terminalScore` = `terminalScore(p, mover, ply) ?? (ev.stage0 + ev.stage1 + (pendCostSum[mover]
− pendCostSum[1−mover]) × CC)`, called from `engine.ts:355-364` (byte-identical arithmetic; confirm
with `npm run hard:determinism`) and the eight sites. **Do not** add the credit to
`src/ai/hard/search/pvs.ts:694` (depth-1 futility stand-pat, off on DESKTOP) or to
`eval/evaluate.ts:171` / `:185` (`full()` already includes stage 2, whose `PendingValue` prices
pending principal; adding it there double-counts). No test pins a literal within-turn score, so the
mirror changes no numeric assertion; add a unit test for the helper. Follow-ons: append a dated
entry to `docs/hard-ai/design/DEVIATIONS.md` after the 2026-09-15 within-turn-scorer entry
(:1791-1802); expect the M13 recall gate artifact `lab/results/hard-ai-verify/M13.json` to move
once recall carries the credit and re-baseline it (and the brackets in
`lab/hard-ai/verify/gates.ts` if needed).

### 1.4 Clean up the scratch code that was merged

`lab/hard-ai/ablate/arms.ts:953-984` ("SCRATCH … NOT in the repo") and
`lab/hard-ai/bots/hard.ts:298-300` ("SCRATCH EXPERIMENT HOOK (not for merge as-is)") both merged.
`handPriorsPatch` (`arms.ts:956`) clones **`DEFAULT_WEIGHTS`**, which is now the hand priors, so
at HEAD (resolved-config hashes at `wall:3000`):

| Arm | Hash | What it actually is now |
|---|---|---|
| `hard@desktop` / `hard@lab` / `hard@ablate:base` | `2c485153…` | the champion, `phasing-hand-priors-v1` |
| `hard@ablate:hand-priors` | `6687f4ab…` | numerically the champion under label `scratch-hand-priors-v1` (A/A) |
| `hard@ablate:hand-priors-pc` | `d93b450c…` | same, label `scratch-hand-priors-v1+pc` (A/A) |
| `hard@ablate:bank25-pc` | `a5d8488b…` | same numbers again (A/A) |
| `hard@ablate:bootstrap-pc` | `203e88f9…` | hand priors with BankExcess 100 — **not** the bootstrap |

So the handoff's "scorer fix ALONE gives 25–0–7" row is no longer reproducible by arm name; run it
as `MUJU_HARD_WEIGHTS=$PWD/docs/hard-ai/phasing/repair-2026-09-20/weights/bootstrap-control.json`
with `--a hard@desktop`. Fix: build the four arms from the frozen bootstrap literal (or delete
them), drop the `+pc` semantics from their `change` strings and from the four `*+pc.json` copies
(byte-identical to their plain copies except the label line), and either remove the
`MUJU_HARD_WEIGHTS` hook or label-scope it (it overrides **both** seats of a hard-vs-hard row;
Round 3 in §4 needs a scoped form). Update `tests/lab/ablate.test.ts` accordingly.

### 1.5 Prose that now misdescribes production

`src/ai/hard/eval/weights.ts:1-6` ("Phasing accounting bootstrap … all overlapping coefficients
are zero"), `:14` (version comment), `:19` ("Frozen pre-outcome vector"), `:58-61` ("The approved
accounting bootstrap"); `vitest.config.ts:65` (eval-correct "14/27 fail") and `:71-79` (recall
"blocked because the bootstrap sets only five weights" — `eval-no-safety` now zeroes 17 live
weights, so try `MUJU_RUN_QUARANTINE=1 npx vitest run tests/ai/hard/eval-correct.test.ts
tests/lab/recall.test.ts` and restore whichever passes); `lab/hard-ai/ablate/arms.ts:44-52` and
`:258-272`; the `ablate.test.ts` blocks named in 1.2; `HANDOFF.md` lines listed in §6;
`repair.patch:10` still shows the label-gated form (historical, do not re-apply);
`reports/understand/*.md` describe the pre-repair tree.

### 1.6 The change record and the entry points

- `python3 tools/muju-content-dag.py plan --kind ai` lists 13 nodes, all `pending`: wasm-tactics,
  ai-search, hard-ai, ai-strength, mcp-tools, agent-guides, balance-analysis, game-validation,
  static-package, server-package, static-deploy, server-deploy, release-verification. Redirect the
  plan to `muju/docs/changes/2026-09-20-hard-phasing-repair.md` and fill in changed / verified
  unchanged / blocked with evidence as the commands run (`CONTENT_DAG.md`). The hard-ai node's
  verify command is `npm run hard:types && npm run hard:test`; the ai-strength node's suite
  command `npm run hard:suite` is the Standard-era runner — the Phasing floors instrument is
  `npm run hard:suite:phasing:measure` (`package.json:67`). static-deploy is blocked on the
  Cloudflare token; server-deploy ("Render auto-builds master") rests on `ONLINE.md` and the
  Dockerfile only, there is no `render.yaml`, so whether `eda73b2` is live must be checked against
  the host, not assumed.
- Then point `CLAUDE.md`'s "start here" section at the change record (today it points here).

## 2. Values you will need (all computed on `eda73b2`)

### 2.1 Identities

| Thing | Value |
|---|---|
| `DEFAULT_WEIGHTS.label` / `weightsHash` / `WEIGHTS_VERSION` | `phasing-hand-priors-v1` / `14d06ba8` / 2 |
| Frozen M6 bootstrap (`weights/bootstrap-control.json`) | label `bootstrap-control`, hash `0ae24a95` |
| Old v2 floor contract `engineSourceSha256` | `e46f735eaaea2aa4df2d7eda276e7c6db366e1db587260fcefc8a0d678391dcd` |
| Old v2 floor contract `weightsSha256` (bootstrap) | `1addc7d64b574c2ea27df209d32a5e37bc0800e8e4948c2bd3f514c1f64c92e0` |
| Measurement ledger seq 1 `chain` | `d7774d6d6a7265383d4c69c8d32fa469fd2195c5ff2702d5493b14464a25d430` |
| Ledger seq 1 `contractCommit` | `1864090d0d13897f90c398a42b7681dbb7066bf4` |
| `p1-val` openings sha | `cbd427df…` (32 rows) |
| Round-3 vectors (`+pc` copies) | `combined-best-guess` `030f6597` (31 non-zero), `phasing-priors-v1` `fd5a13e5` (30), `econ-only-best-guess` `1d6085c7` (7) |

### 2.2 Freeze artifact

No file in `weights/` equalled the shipped default in both `w` and `material` (the closest,
`hp-tier13+pc.json`, has the same `w` but hash `d0154290` because its material differs). This
addendum adds `weights/phasing-hand-priors-v1.json` = `serializeWeights(DEFAULT_WEIGHTS)` at
`eda73b2`; it round-trips through `loadWeights` to hash `14d06ba8`. Use it wherever a row needs the
production vector by file.

## 3. Superseding the floor contract (before any suite measurement, after 1.3 and 1.4)

`lab/hard-ai/suites/phasing/fixtures/v2/floor-contract.json:7-8` pins the bootstrap by hash, so
`npm run hard:suite:phasing:measure` refuses at HEAD (`measure.ts:366 assertContractBuild`), and
because the v2 manifest has already been measured once, a re-measurement must declare
`supersedes` (`measure.ts:230-232`, `contract.ts:45-61`). Recipe:

1. Finish 1.3 and 1.4 first: `engineSourceSha256` covers `src/ai/hard/**` **and** the
   `LAB_DEPENDENCIES` at `lab/hard-ai/suites/phasing/engine-adapter.ts:67` (`bots/hard.ts`,
   `ablate/arms.ts`, `audit/eval-groups.ts`, `ladder/identity.ts`, `ladder/ruleset.ts`,
   `suites/phasing/engine-adapter.ts`, `suites/phasing/canonical.ts`), so a contract written
   before those edits is void (`engine-adapter.ts:143` throws "Adapter source drift").
2. Write a new contract file (do not edit the existing one, the ledger, or anything under
   `results/`): same schema `muju-phasing-suite-floor-v2`, same `manifestSha256`
   (`454fe137aa5bf97f4703a209985e4743eb995719c09cb6bcf8ea6d130bc39453`), same `allowedMiss`,
   `weightsSha256 = weightIdentity(DEFAULT_WEIGHTS)`, `engineSourceSha256 =
   hashJson(engineSourceHashes())` at the final source, and
   `supersedes: { ledgerSeq: 1, chain: "d7774d6d6a7265383d4c69c8d32fa469fd2195c5ff2702d5493b14464a25d430" }`.
3. Commit it **alone** (the contract test refuses a commit that also carries results) and push.
4. In a later commit run `npm run hard:suite:phasing:measure` and record pass/fail per family
   **without moving floors**. Last measurement (`results/v2-measure-2-2026-09-19`, bootstrap
   weights): `floorPass: false`; tactics, home-mate and home-fortify passed; invariants, economy
   and summon-disruption failed; 79 cases pass. Append the amendment to
   `M5-FLOOR-PREREGISTRATION-v2.md` rather than editing its frozen sections.

## 4. Evidence status and what the rules require

- **Correctness veto has not been run post-repair.** Run `npm run hard:perft && npm run hard:fuzz
  && npm run hard:determinism` and record the artifacts under the change record's ai-strength
  node. The ladder rows' 0 illegal / 0 divergence counters are not a substitute.
- **The 53–0–11 row is development evidence, not a strength claim.** `p1-val` is the VAL
  (model-selection) allocation and has been read by **three** rows, not once:
  `ladder-h2h-desktop-vs-aiv2hard-w1500` (master, 4–0–60, VOID),
  `confirm-val-hppc-vs-aiv2hardturn-w6000` (53–0–11), `confirm-val-hppc-vs-aiv2hard-w1500`
  (49–0–15, VOID). Gate 2 as preregistered (`PHASING-PREREGISTRATION-2026-09-18.md` §Gate 2)
  needs `p1-sealed` rows 0–31 (owner's machine only, not in the repo), seed 20260953, 32 pairs,
  handicaps 0 and 3, `wall:8000`, strict legality, SPRT(0, +100; α=β=0.05), and a **new dated**
  `RELEASE-<date>-phasing.md` (never edit `RELEASE-2026-09-18.md`). Append amendment A6 to the
  preregistration naming the candidate identity (`phasing-hand-priors-v1`, `14d06ba8`, the final
  engine source hash) and disclosing the three val reads. Keep all further comparisons on
  `p1-dev`.
- **Guards stay closed until ai-strength passes** (`content-dag.json` hard-ai node); opening them is
  the owner's call, see §5.4.

## 5. After step 1, in order

### 5.1 Round 3 (queued, never run) — recipe for this checkout

`scripts/round3.sh` hard-codes a laptop scratch path, waits on a `pgrep` loop, uses 8 shards, and
its seventh (K48) row is a no-op stub. Equivalent here (4 cores; use `--shards 4` or fewer,
`MUJU_HEAVY_SLOTS` ≤ 4; the default heavy queue of 2 is safest for the 1.5 s rows):

```
cd muju && npm ci
W=$PWD/docs/hard-ai/phasing/repair-2026-09-20/weights
for v in combined-best-guess phasing-priors-v1 econ-only-best-guess; do
  MUJU_HARD_WEIGHTS=$W/$v+pc.json node --import tsx lab/hard-ai/ladder/run.ts --a hard@desktop --b Rush \
    --work wall:1500 --handicaps 0 --pairs 16 --seed 33 --shards 4 \
    --openings lab/hard-ai/ladder/openings/p1-dev.jsonl --replays on --out /tmp/round3/$v-pc-vs-Rush-w1500
  MUJU_HARD_WEIGHTS=$W/$v+pc.json node --import tsx lab/hard-ai/ladder/run.ts --a hard@desktop --b aiv2-hard-turn \
    --work wall:6000 --handicaps 0 --pairs 16 --seed 31 --shards 4 \
    --openings lab/hard-ai/ladder/openings/p1-dev.jsonl --replays on --out /tmp/round3/$v-pc-vs-aiv2hardturn-w6000
done
node docs/hard-ai/phasing/repair-2026-09-20/scripts/stats.mjs /tmp/round3/<run>
```

Seeds 33 / 31 make these paired comparisons against the hand-priors rows (12–0–20 vs Rush,
28–0–4 vs aiv2-hard-turn). About 12–23 game-minutes per Rush row and 53–58 per 6 s row; ~3.3–4
CPU-hours for all six. The manifest names every run `hard@desktop`, so record label + hash per
run. Check `overrunRate` in each `summary.md` stays under 5%. Commit `summary.md` and
`metrics.json` only, under `results/round3-<name>/`, and append to `ALL-PROGRESS-LINES.txt`.
Optional seventh row: `--a hard@ablate:k48` with `MUJU_HARD_WEIGHTS` unset.

### 5.2 Time use — the bigger defect is the cold profile, not the rung rounding

Arms exist and, since `71b41a3`, every `hard@ablate:<arm>` already runs on the repair:
`k48` (`arms.ts:481`), `calib` (`:519-522`, `time.calibrateCold`, rejected on Standard at
−27 Elo), `work-fit` (`:531`), `search-iter-fit`, `work-fit-deep`. None has a Phasing row. The
larger finding: `INITIAL_UNITS_PER_MS = 200` (`src/ai/hard/config.ts:587`) sizes every game's
first search at the cold profile and it is deadline-aborted — `firstSearchAborted` 64 of 64 games
(`confirm-val-hppc-vs-aiv2hard-w1500`), 91 of 96 (`ladder-h2h-desktop-vs-aiv2hardturn-w1500`),
63 of 64 (`confirm-val-hppc-vs-aiv2hardturn-w6000`), 32 of 32 (`confirm-dev-hppc-vs-Rush-w1500`).
Re-measure `calib` under Phasing before building anything new; then the rung arms vs
aiv2-hard-turn at `wall:6000` (1.5 s rows against the old engine go VOID on timing).

### 5.3 Rush (days, generator work) — do as lab arms first

Every site the handoff cites is as described at HEAD: `config.ts:543` (`maxMultisets 35,
maxPlans 12, keepPerMultiset 3`), `gen/purchase.ts:441-463`, `gen/promote.ts:196-197` and
`:252-253`, `eval/pending.ts:116`, `gen/generate.ts:879` / `:915`. Two corrections: "sort before
truncating" is necessary but not sufficient, because the multiset list itself is DFS-truncated at
`maxMultisets 35` of 209 (buffer ceiling `MAX_MULTISETS 64`, `purchase.ts:70`) and at bank ≥ 14
all 35 contain fire_1, so `maxPlans 36 + keepPerMultiset 1` still yields 35 fire-containing plans
— the selection has to score the multiset stage too; and the cheapest-first order lives in
`core/catalog.ts:188` (tier-1 ids sorted by cost, then id), not in `config.ts`. `Mission` at `promote.ts:57` is
`FORTIFY, SURVIVE, INCOME, REACH, ANCHOR` — no STRENGTH; adding one makes
`tests/ai/hard/promote.test.ts:137-160` fail by design. The knob reports conclude Hard now spends
93–97% and loses to Rush by elimination against ~126 fire_1 per game, so composition (a water_1
counter-build the menu cannot reach) is the lever and fire_1→fire_2 second. Add a `purchase`
`ArmFactor` and a `purchase-wide` arm so `hard@desktop`'s hash does not move while you measure;
the `ablate.test.ts` one-factor-per-arm and hash-uniqueness tests need the new factor.

### 5.4 Phasing-only cutover — corrections and one independent fix

Not started; PR #25 touched no guard. Corrections to the handoff's list: `server/mcp.ts:39` is the
`instructions` string, the defaults are `:66-69`; there are 2 `COALESCE(...,'standard')` sites
(`server/rooms.ts:299`, `:320`) plus five `?? 'standard'` fallbacks (`rooms.ts:522`,
`observation.ts:23`, `analysis/core.ts:93`, `:113`, `analysis/index.ts:31`);
`AnalysisScreen.tsx:30` and four `phasing = false` prop defaults are missing from the list; the
worker guard (`ai/worker/handler.ts:113`) reads the request marker set by `useAI.ts:109-116`, not
`readPhasingAiPreview()` itself. The cutover fallout numbers (14 files / 64 tests, or 49 / 245 if
`board.ts:204` flips) were measured without rejecting `ruleset:'standard'` on create. Today, with
no flag: Standard + Hard sends `engine:'hard'` (`useAI.ts:125` has no ruleset check), the engine
pack-errors (`core/state.ts` `pack`, :609-616; the file header says it refuses any
non-Phasing state) and falls back to AIEngineV2 every turn; Phasing + any AI cannot
start at all. Independent of the cutover, add `state.ruleset === 'phasing'` to the `useHard`
condition at `useAI.ts:125` so a Standard Hard game stops pack-erroring every turn. Also: no e2e
spec exercises the shipped Phasing engine in CI — `e2e/hard-ai.spec.ts` starts Standard games (so
the DAG's "fallback counters stay at zero in e2e/hard-ai.spec.ts" never touches HardEngine),
`e2e/phasing-ai.spec.ts` sits behind `?phasingAi=1`, and neither is in
`playwright.online.config.ts`'s `testMatch`.

### 5.5 Texel — last

Confirmed at HEAD: seven `'muju-phasing-1'` literals, not one (`lab/hard-ai/tune/corpus.ts:269`,
`:279`, `:375`; `rows.ts:146`, `:190`, `:227`, `:290`; the live ruleset is `muju-phasing-2`);
`rows.ts:178` hard-codes version 2; `texel.ts:82` pins BankExcess at 100 so `hard:texel` throws on
`DEFAULT_WEIGHTS` before fitting; `package.json:64-65` still defines `hard:spsa` →
`lab/hard-ai/tune/spsa.ts` and `hard:book` → `lab/hard-ai/book/build.ts`, and neither file nor
directory exists. `reports/understand/gap-tuner.md` found a fit on lopsided replays learns bot
identity; nothing here is needed to ship the hand priors.

## 6. Corrections to HANDOFF.md, by line

- :13, :46 "scorer fix ALONE gives 25–0–7" — reproducible only via `MUJU_HARD_WEIGHTS=…/bootstrap-control.json`, not by arm (§1.4).
- :18 scorer at `engine.ts:355-360` → `:355-364` at HEAD (credit at :363).
- :29-33 (root cause 4) — "8 of 51" is a 12-position probe; population figures are 339 / 3,842 and 504 / 5,525 promotions offered / legal; sort-before-truncate is insufficient alone (§5.3).
- :34-35 (root cause 5) — the cold-profile first-search abort is the larger defect (§5.2).
- :59 "p1-val has now been looked at once" → three rows (§4).
- :64-65 "see commit history for whether it is still label-gated" → resolved, unconditional (`71b41a3`).
- :66 arms `bootstrap-pc` / `bank25-pc` → no longer the bootstrap; `hand-priors*` are A/A (§1.4).
- :70-71 "13 reviewed vectors … +pc copies" → `weights/` holds 32 JSONs (28 distinct hashes) + `make-variants.ts`; the shipped hand priors are not one of the 13; now frozen as `phasing-hand-priors-v1.json` (§2.2).
- :72-73 "+pc turns the scorer credit on" → inert at HEAD.
- :95-100 step 1 → parts (a) and (b) landed in `71b41a3`; tests, mirrors, version comment, DAG walk open (§1).
- :97 test list → incomplete; the full list is §1.2.
- :98 "suite manifests / freeze artifacts that pin the label" → no manifest pins the label; the binding pin is the floor contract's `weightsSha256` (§3); freeze artifacts are historical and must not be edited.
- :109 → seven `muju-phasing-1` literals across `corpus.ts` and `rows.ts` (§5.5).
- :110 → correct: `hard:spsa` / `hard:book` exist in `package.json:64-65` and point at missing files.
- :114-119 cutover list → see §5.4 corrections.
- :128-129 → CI is red for 7 files / 24 tests at `eda73b2`, not 4; Pages is frozen by the missing token regardless of CI colour; "Render auto-builds master" is a doc claim, not verified.

## 7. How this was verified (commands, so you can trust rather than redo)

```
cd muju && npm ci                                   # node 22, npm 10
npx vitest run tests/ai/hard --reporter=dot          # 2 files / 9 tests fail, 170 s
npx vitest run tests/lab/ablate.test.ts tests/lab/phasing-tune-guards.test.ts   # 11 fail
node --import tsx <script>  # printed DEFAULT_WEIGHTS non-zero pairs, hash, serialized form;
                            # resolvedConfigHash for hard@desktop and the ablate arms at wall:3000;
                            # live counts per EVAL_GROUPS; loadWeights over weights/*.json
python3 tools/muju-content-dag.py plan --kind ai     # 13 nodes, all pending; `check` passes
```

GitHub Actions on master: run 35475103182 (`a02bbb7`, before the repair) 3 files / 4 tests; run
35552456980 (`eda73b2`) 7 files / 24 tests, 894 s. The 24 FAIL lines are listed in
`reports/audit-2026-09-21/reader-reports.md` (item `ci`). Nothing in this session ran a ladder,
suite, perft, fuzz or determinism command, and no source file under `src/` or `lab/` was changed.
