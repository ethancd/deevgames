<!-- Generated with `python3 tools/muju-content-dag.py plan --kind rules`, then every
     node's disposition filled in by hand from the lane reports and the integration runs. -->

# Draw clock 20 — `muju-phasing-1` -> `muju-phasing-2` (2026-09-19)

Branch `claude/phasing-2-drawclock`, base `11d6cd57` ("Preregistration amendment A4"),
worktree `/Users/ashkie/src/deevgames-claude-drawclock`. **Uncommitted at the time of writing;
the coordinator commits.**

## The change

The inactivity draw limit goes from **10 plies to 20**. The in-game warning keeps its
three-ply margin: `INACTIVITY_WARNING` 7 -> 17. **What resets the clock is unchanged** —
only an attack that removes a unit. Nothing else in the rules moves.

Standard and Phasing share the one constant in `muju/src/game/inactivity.ts` and it was
deliberately NOT forked: the owner is retiring Standard, and a second copy of this number would
be new dead weight from the day it was written.

Historical evidence was never edited. `lab/results/**`, `docs/hard-ai/RELEASE-2026-09-18.md`,
the e0-e5 records, the archived corpora and their hash tests, and the whole
`lab/harness/results/p1-scripted-2026-09-18/` directory (`shasum -c SHA256SUMS`: all 7 files OK)
are untouched. Where an old save's outcome depended on the ten-ply limit, the replay pins the
old limit through a parameter instead: `resolveInactivityDraw(state, limit = INACTIVITY_LIMIT)`
with an exported `LEGACY_INACTIVITY_LIMIT = 10`. **That pin is used in exactly two places** —
`src/utils/persistence.ts` for pre-schema-8 saves, and the regression test that proves the
default no longer draws at 10 while the pinned call still does.

## Release status

Prepared, not deployed. This branch is not `master`; nothing was built, packaged or published,
and the Academy media bundle was never fetched. Academy notice text is prepared; R09/R10
re-narration is pending owner speech authorization.

# Node-by-node dispositions (all 27)

The headings, reasons, paths and Update/Verify bullets below are the generator's output
verbatim, so a reader can check that nothing was quietly dropped. The generator's own preamble
said "Review plan only: no edits, tests, media generation or deployments have run" and its
`[ ]` boxes are its default state; **the `Disposition` line under each node is what actually
happened**, and it supersedes both. Unmapped paths were investigated by hand where they
mattered and that is said in the node.

## 1. [ ] rule-contract — Agreed rules and change intent

Reason: change kind: rules

Paths: `muju/SPEC.md`, `muju/JUDGMENT_LOG.md`

- Update: State exact old/new behavior, affected units, edge cases and intended rules revision. User-approved changes take precedence over old specs. Update SPEC and append rationale to JUDGMENT_LOG without rewriting historical decisions.
- Update: When adding, promoting or retiring a whole rule set or variant, name the surviving normative text, fold variant documents into SPEC or mark them superseded, and state which rules revision each stored room, save, replay, opening corpus and strength record belongs to.
- Verify: Resolve differences between intended rules, executable behavior and published descriptions. Historical documents are not current authority.

**Disposition (1/27 — `rule-contract`):** **CHANGED.** `muju/SPEC.md` is at v3.0 with a version-history entry and a rewritten
§9 inactivity clause; `muju/JUDGMENT_LOG.md` gains **J-021**, which supersedes J-016's
threshold, preserves J-016's timing and J-019's definition of a quiet turn, records what
was deliberately NOT changed (only an attack that removes a unit resets the clock) and
states the reversal cost. Both name the revision `muju-phasing-1` -> `muju-phasing-2`.
Amendment A4 in `muju/docs/hard-ai/PHASING-PREREGISTRATION-2026-09-18.md` is the owner
decision this record executes and was not edited.
Evidence: `tests/game/upkeep-draw.test.ts` "muju-phasing-2 quiet clock" pins
`INACTIVITY_LIMIT === 20`, `INACTIVITY_WARNING === 17` and the three-ply margin outright,
so the contract and the code cannot drift apart silently.

## 2. [ ] catalogue — Canonical piece definitions and elemental relationships

Reason: upstream changed: rule-contract

Paths: `muju/src/game/units.ts`, `muju/src/game/types.ts`, `muju/src/game/elements.ts`

- Update: Change canonical definitions, stable IDs, stats, costs and elemental relationships. Check promotion cost differences and tier/stock limits. Read the applicable SPEC sections even when starting here.
- Verify: Exercise changed stat boundaries and elemental matchups against real combat, movement, mining and promotion behavior; verify all consumers use current definitions.

**Disposition (2/27 — `catalogue`):** **VERIFIED UNCHANGED.** No stat, cost, stable ID, tier or elemental relationship was
touched. Evidence: `git diff --stat -- muju/src/game/units.ts muju/src/game/elements.ts
muju/src/game/types.ts` is empty; a grep of `src/game` for this rule's number returns
`src/game/inactivity.ts` only. The Academy export re-derived all 324 ordered matchups and
`catalog.json`, `map.json` and `bonk-matrix.json` came back byte-identical (node 16), which
is an independent check that the catalogue did not move.

## 3. [ ] board-rules — Map, setup and shared rule constants

Reason: upstream changed: rule-contract

Paths: `muju/src/game/board.ts`, `muju/src/game/resourceMap.ts`, `muju/src/game/rules.ts`

- Update: Update board reserves, setup, action budgets and handicap limits as applicable. Check palette, map painter, starting income and symmetry expectations.
- Verify: Test setup and economy against the intended map and rule constants; preserve explicitly historical map fixtures.

**Disposition (3/27 — `board-rules`):** **VERIFIED UNCHANGED.** `board.ts` still seeds `inactivityPlies: 0`; no reserve layout,
action budget (`ACTIONS_PER_TURN` 4), handicap cap (`MAX_BLACK_CRYSTAL_HANDICAP` 20) or
map constant (`INITIAL_MAP_RESOURCES` 504, `MAX_RESOURCE_RESERVE` 16) is involved.
Evidence: `git diff --stat -- muju/src/game/board.ts muju/src/game/resourceMap.ts
muju/src/game/rules.ts` is empty, and `hard:perft --check` reproduces the frozen Standard
triple 14959/1053/797 and the Phasing initial 14959/1850/797 unchanged.

## 4. [ ] transitions — Canonical legality and state transitions

Reason: upstream changed: catalogue; upstream changed: board-rules

Paths: `muju/src/game/`, `muju/src/ai/simulate.ts`, `muju/src/hooks/useGameState.ts`, `muju/lab/harness/`

- Update: Review legality, combat, movement, promotion, spawning, upkeep, mining, turn timing, inactivity, victory and home-checkmate. Keep UI, server and simulations on the same transition; adjust lab invariants where the rule itself changes.
- Verify: Add or adapt meaningful regression cases for the changed rule, including illegal-action behavior and identical resulting states in local, AI and server paths.

**Disposition (4/27 — `transitions`):** **CHANGED** — this is the node the change lives in.
`muju/src/game/inactivity.ts`: `INACTIVITY_LIMIT` 10 -> 20, `INACTIVITY_WARNING` 7 -> 17,
new `LEGACY_INACTIVITY_LIMIT = 10`, and `resolveInactivityDraw(state, limit = INACTIVITY_LIMIT)`
so an archived `muju-phasing-1` record can be adjudicated under the limit it was recorded
with WITHOUT editing the archive. Standard and Phasing deliberately share the one constant;
it was not forked. What resets the clock is unchanged: only an attack that removes a unit
(`src/ai/simulate.ts:102`, `turn.ts#handOffTurn`), verified by reading both.
Evidence: `npx vitest run tests/game tests/hooks` 40 files / 556 tests green;
`tests/game/upkeep-draw.test.ts` walks a full twenty-ply playout proving the game is still
PLAYING at the old ten-ply threshold and draws at twenty, holds the whole 17/18/19 warning
band open, and proves the legacy limit only adjudicates when pinned explicitly.
NEW, and the strongest evidence here: `tests/ai/hard/cross-engine-draw-clock.test.ts`
walks one scripted quiet game ply by ply through canonical `applyAction` AND the Hard
replica's `make()`, asserting at EVERY ply that the two engines' digests agree, that the
clock is not a draw at any of plies 1..19, that it draws at exactly 20, and that the
replica unwinds the whole twenty-ply game back to a byte-identical start digest.

## 5. [ ] rules-docs — Current rules, AI explanation and public design copy

Reason: upstream changed: transitions

Paths: `muju/SPEC.md`, `muju/AI_ENGINE_README.md`, `muju/docs/AI_IMPLEMENTATION_STATUS.md`, `muju/docs/PHASING-2026-09-16.md`, `muju/docs/STRATEGY_GUIDE-2026-09-12.md`, `muju/docs/strategy-guide-codex-vs-claude.md`, `docs/game-design-dossier.md`, `portfolio/index.html`

- Update: Synchronize current rule tables, examples, costs, terminology and supported strategy claims. Review dated guides used as current advice; mark superseded claims or issue a new revision instead of silently rewriting historical evidence.
- Verify: Search by piece ID, display name, stat label and old numeric phrase across active docs. Check examples with the current engine. Do not claim old balance studies establish new strategic strength.

**Disposition (5/27 — `rules-docs`):** **CHANGED.** Current claims corrected: `muju/SPEC.md`, `muju/AI_ENGINE_README.md`,
`muju/docs/AI_IMPLEMENTATION_STATUS.md`, `docs/game-design-dossier.md`.
`muju/docs/PHASING-2026-09-16.md` was APPENDED to, not rewritten.
Dated evidence and advice got supersession notes in each file's own established banner
style rather than rewrites, per this node's rule about not falsifying prior results:
`docs/STRATEGY_GUIDE-2026-09-12.md` (banner plus an inline note under §7 "The ten-turn draw
clock"), `docs/strategy-guide-codex-vs-claude.md`, `docs/DRAW_TEN-2026-09-08.md`,
`docs/UPKEEP_DRAW-2026-09-08.md`.
Deliberately left as historical record: `SPEC.md:48` ("v1.9 (ten-turn draw at turn end)"),
`JUDGMENT_LOG.md` J-013/J-016/J-017, and the dated 2026-09-09/2026-09-12 study reports —
rewriting them would falsify the record J-021 supersedes by reference.
Evidence: a full-repo grep over `.ts/.tsx/.md/.py/.html` for fourteen spellings of the old
number (ten quiet | ten-turn | ten turns | 10 turns | ten plies | 10 plies | ten-ply |
10-ply | ten consecutive | 10 consecutive | tenth quiet | ten player turns | drawAtQuietTurns
| amber at seven) leaves only the historical files named above, each of which states the
rules its own study ran under. `portfolio/index.html` carries no draw-clock claim.

## 6. [ ] browser-ui — Browser controls, instructions and visual representations

Reason: upstream changed: transitions

Paths: `muju/src/components/`, `muju/src/App.tsx`, `muju/src/main.tsx`, `muju/src/hooks/useAI.ts`, `muju/src/index.css`, `muju/src/utils/colors.ts`, `muju/src/sound/`, `muju/src/music/`, `muju/public/music/`, `muju/index.html`, `muju/public/previews/`

- Update: Inspect UnitShop, UnitInfo, InstructionsModal, VisualKey, AnalysisScreen, MapPainter, action/reach previews and unit artwork for copied facts or numeric assumptions. Reuse canonical data where possible. Regenerate affected previews.
- Verify: Inspect desktop and phone play: instructions, stat cards, legal highlights, purchase/promotion, turn endings and analysis. Ensure the AI worker still runs.

**Disposition (6/27 — `browser-ui`):** **CHANGED.** `src/components/InstructionsModal.tsx` (the Phasing controls line) and
`src/components/ModeSelect.tsx` (the mode blurb) were the last two hardcoded statements;
both now interpolate `INACTIVITY_LIMIT`. `GameScreen.tsx`, `VictoryScreen.tsx` and the main
instructions draw paragraph already read the constants and were re-read to confirm it.
`src/index.css` VERIFIED UNCHANGED: `.progress-clock` already carries `white-space: nowrap`
with `overflow: hidden` / `text-overflow: ellipsis` on its spans, so the one-character-longer
"17/20 turns without a kill" degrades inside the strip at phone widths instead of overflowing
the page. No preview art was regenerated because no board, piece or palette changed.
Evidence: `tests/hooks` and `tests/components` green inside the full suite; the three e2e
specs that drive the clock (`e2e/action-budget.spec.ts`, `e2e/upkeep-draw.spec.ts`,
`e2e/analysis.spec.ts`) were UPDATED but NOT EXECUTED — see node 20.

## 7. [ ] persistence — Saved games, migrations, history and replay compatibility

Reason: upstream changed: transitions

Paths: `muju/src/game/migrate.ts`, `muju/src/game/replay.ts`, `muju/src/game/moveHistory.ts`, `muju/src/utils/persistence.ts`, `muju/src/components/TurnReplay.tsx`, `muju/src/online/RoomHistory.tsx`

- Update: Decide how existing saves, active rooms and historical replays behave under the change. Add migration/version handling if needed; preserve old evidence and stored histories.
- Update: Retiring a rule set: never reinterpret a stored state under different rules. Bump the save schema, decide convert / archive read-only / reject per stored ruleset, keep only the presentation needed to view retired games, and keep a captured fixture of a retired-rules game.
- Verify: Load an existing save/room, undo where supported, restart the server and replay a completed game. Explicitly report incompatibilities and their handling.

**Disposition (7/27 — `persistence`):** **CHANGED.** `src/utils/persistence.ts`: `SCHEMA_VERSION` 7 -> 8 with
`READABLE_SCHEMA_VERSIONS = [5,6,7,8]`. A pre-v8 save is adjudicated ONCE under the pinned
`LEGACY_INACTIVITY_LIMIT`, so a game that had already drawn keeps that result; a
still-playing position resumes with its quiet clock restarted at 0 rather than silently
gaining ten extra plies, and is re-stamped v8 with board, players and score preserved.
That is the same choice `migrateLegacyGame` made the last time this clock's reset rule
changed. No stored state is reinterpreted under rules its players did not agree to.
**This is one of exactly two places the pinned legacy limit is used** (the other is a
regression test); no archive, corpus or historical record was edited anywhere.
Evidence: `tests/save-rules-revision.test.ts` (6 tests) covers a schema-6 and a schema-7
save mid quiet stretch, a finished pre-v8 inactivity draw keeping its recorded 10, a pre-v8
PLAYING position at exactly 10 plies adjudicated as a draw under the pinned legacy limit, a
current v8 save round-tripping a clock of 11 untouched, and schemas 1-4 and 9 still refused.
Blast radius of the schema bump re-checked: `tests/game` + `tests/hooks` 41 files / 562 tests
green.

## 8. [ ] wasm-tactics — Compiled tactical solver and canonical witness validation

Reason: upstream changed: transitions

Paths: `muju/assembly/tactics.ts`, `muju/src/ai/wasm/`, `muju/asconfig.json`

- Update: Review duplicated tactical semantics and ABI assumptions. kernel.ts packs stats and attack powers from canonical definitions; do not invent a second stat table. Rebuild tactics.wasm when affected.
- Verify: Replay solver witnesses through canonical legality/transitions. Compare TS and WASM on changed combat/movement/promotion cases, including bounded-search unknown results.
- Command (cwd `muju`): `npm run ai:wasm` — Rebuilds generated WASM; also runs automatically before npm test/build.

**Disposition (8/27 — `wasm-tactics`):** **VERIFIED UNCHANGED, and the kernel does not know the draw limit.**
`grep -rniE 'inactiv|progress|draw|clock|quiet' assembly src/ai/wasm/kernel.ts` returns ZERO
hits. The only 10s in `assembly/tactics.ts` are board geometry (`STRIDE` = 10 words per
unit, `p % 10` / `p / 10` coordinate maths, +/-10 for vertical neighbours), and `kernel.ts`
packs position, owner, definition, damage, flags and attack words only.
Evidence: `git diff --stat -- muju/assembly muju/src/ai/wasm` is empty, so `npm run ai:wasm`
was correctly NOT run and `tactics.wasm` is the committed binary unchanged.

## 9. [ ] ai-search — Deterministic game AI, evaluation, planning and worker

Reason: upstream changed: transitions; upstream changed: wasm-tactics

Paths: `muju/src/ai/`, `muju/lab/ai/`, `muju/lab/docs/`, `muju/AI_ENGINE_PLAN.md`, `muju/AI_ENGINE_QUESTIONS.md`

- Update: Review moves, evaluation, beam plans, MCTS, placement strategies, budgets, tactical sharpening and worker protocol. Rules changes may alter reach, affordability and threat assumptions even when stats import automatically. Historical design plans are context, not implementation authority.
- Verify: Check legal complete turns, reproducibility under fixed seeds/work budgets, worker execution and deadlines. Separate rule correctness from strength; run targeted matchups when balance claims are made.
- Command (cwd `muju`): `npm run ai:tactics` — Runs tactical cases and writes a fresh lab result directory; inspect results.

**Disposition (9/27 — `ai-search`):** **VERIFIED UNCHANGED.** The legacy AIEngineV2 stack (engine-v2, planner, beam, MCTS,
evaluation, sharpener, worker) has no draw-horizon term at all: it only scores
`getGameResult(...) === 'draw'` as 0 (`evaluation.ts:35,325`, `planner/scoring.ts:17`), and
the one other hit is `simulate.ts:102`, the kill-resets writer, which this change does not
touch. No budget, beam width, MCTS depth or eval term is keyed to the limit, so a longer
clock needs no AI edit.
Evidence: `grep -rni 'inactiv|pressure|horizon|plies|draw' src/ai --exclude-dir=hard` returns
only those four sites; `tests/ai` (excluding `hard/**`) green. The new cross-engine test
additionally asserts the V2 terminal oracle `getGameResult` reports no draw at any of plies
1..19 and `{status:'draw', reason:'inactivity'}` at exactly ply 20, so "unchanged" here means
"agrees with the new limit", not "was never asked".

## 10. [ ] hard-ai — Hard engine: packed rules replica, turn generator, prover, evaluation and search

Reason: upstream changed: transitions; upstream changed: ai-search

Paths: `muju/src/ai/hard/`, `muju/src/ai/hardOptIn.ts`, `muju/docs/hard-ai/`

- Update: src/ai/hard is a complete second rules engine: core/state.ts mirrors legality, transitions, turn boundaries, upkeep and terminals; tactics/prover.ts mirrors analyzeHomeDefense; gen/ encodes the macro-turn grammar and forced injections; tables/, core/income.ts and eval/weights.ts encode timing and economy assumptions. Any change to turn order, purchases, upkeep, mining, movement cost or home-checkmate needs a matching replica change, not just a re-import. pack must reject states it cannot represent rather than drop fields. Keep the worker and UI guards closed for unsupported rules until ai-strength passes.
- Update: Hash every state field that changes future boards into Kpos/Kturn. Bump weightsVersion, book magic and the ladder identity hash when rules, features or weights change.
- Verify: Every emitted turn replays through canonical isLegalAction/applyAction with matching position keys (verify/replay.ts). Runtime fallback and divergence counters stay at zero in e2e/hard-ai.spec.ts. Easy/medium must still avoid loading the hard chunk.
- Command (cwd `muju`): `npm run hard:types && npm run hard:test` — Type-checks the hard lab and runs tests/ai/hard.

**Disposition (10/27 — `hard-ai`):** **CHANGED.** The replica now FOLLOWS the canonical constant instead of restating it:
`src/ai/hard/core/state.ts` re-exports `INACTIVITY_LIMIT` / `INACTIVITY_WARNING` from
`src/game/inactivity.ts` (permitted by `core: ['src/game', ...]` in `lab/hard-ai/deps.ts`),
and `MAX_CLOCK = INACTIVITY_LIMIT`.
ZOBRIST, append-only: the clock plane goes from 11 to 21 keys with the frozen 11-key prefix
(`clock` 0..10) left at its original position in the RNG stream and the ten new keys drawn
LAST, after `pend` and `progress`, so every `Kpos`/`Kturn` of every position with clock <= 10
is bit-identical to `muju-phasing-1`. `CLOCK_LEGACY_VALUES = 11` is a historical constant
never derived from the limit.
EVAL: invariant 16's literal 7 is now `INACTIVITY_WARNING`, and `DrawPressure` is renormalised
from raw `sign(lead)·clock²` (which would have silently quadrupled to 400 against an unchanged
coefficient) to `sign(lead)·trunc(clock²·100/LIMIT²)` — identical value-for-value at limit 10,
still 0..100 at limit 20. No weight changed; `DEFAULT_WEIGHTS.w[F.DrawPressure]` is still 0.
BOOK: `bookCompatibility` carried a hardcoded `'muju-phasing-1'`; it now carries
`PHASING_RULES_REVISION = 'muju-phasing-2'` AND `INACTIVITY_LIMIT` itself, so a phasing-1 book
falls through to search rather than scoring under a clock it was not built for.
Evidence: `npm run hard:types` clean; `npm run hard:deps`
`{"filesScanned":46,"violations":[],"layeringViolations":0,"nondeterminismViolations":0,"bigintViolations":0}`;
`npx vitest run tests/ai/hard` 66 files / 902 tests green (now 67/905 with the new
cross-engine file). `tests/ai/hard/zobrist.test.ts` redraws the stream segment-by-segment AND
redraws it in the exact pre-A4 order, asserting clock keys 0..10 and every other plane are
word-for-word identical. `grep` confirms nothing outside `core` and `eval` reads `p.clock`:
no TT packing, no bit-packed clock field, no clock-indexed table.

## 11. [ ] ai-strength — AI correctness veto and preregistered strength evidence

Reason: upstream changed: hard-ai; upstream changed: ai-search

Paths: `muju/lab/hard-ai/`, `muju/tests/ai/hard/`, `muju/tests/lab/`, `muju/docs/hard-ai/RELEASE-2026-09-18.md`, `muju/docs/hard-ai/EPIC-PLAN-2026-09-16.md`

- Update: A rules change invalidates every existing strength claim; the records remain as evidence for their own rules revision. Re-pin replica parity first: perft fixtures, differential fuzz on transition, legality and prover surfaces, fixed-work goldens, determinism and cross-commit. Re-verify or re-author suites whose expected moves depend on the changed rule. Make the harness, scripted bots and ladder identity hash carry the rules revision.
- Update: Strength is claimed only from a preregistered rule written before the run: openings generated by scripted bots (never by an engine under test) with a sha-pinned dev/val/sealed allocation, tuning on dev only, a sealed row consumed once, and an independent baseline that has passed its own sanity gate. Rows with illegal actions, replica divergences or engine fallbacks are void. Append a new dated release record; never edit an old one.
- Verify: Correctness is a hard veto: zero illegal actions, zero replica divergences, zero fallbacks, perft equal, suites at or above their pinned floors. Report score, Elo interval, SPRT outcome, seeds, identity hashes and responsiveness percentiles, and list what remains unmeasured. Do not pool rows across identity hashes or rules revisions.
- Command (cwd `muju`): `npm run hard:perft && npm run hard:fuzz && npm run hard:determinism` — Replica parity and reproducibility gates; long-running.
- Command (cwd `muju`): `npm run hard:suite` — Tactical and positional suites against pinned floors.
- Command (cwd `muju`): `npm run hard:ladder` — Paired seat-mirrored ladder rows; configure openings, seeds and budgets per the preregistration. Very long-running.

**Disposition (11/27 — `ai-strength`):** **CHANGED for replica parity and the scripted substrate; BLOCKED for every strength claim.**

CHANGED — parity re-pinned first, as this node requires. `hard:perft --check` was re-run on
the merged tree under BOTH engines:
- `--engine canonical`: `fixturesChecked 7, fixturesMismatch 0, digestMismatches 0,
  mismatches 0, replicaAgreed true, standardTriple "checked"`, Standard triple 14959/1053/797,
  Phasing initial 14959/1850/797.
- `--engine replica`: `fixturesChecked 7, fixturesMismatch 0, digestMismatches 0,
  mismatches 0, replicaAgreed true, standardTriple "skipped-phasing-replica"`.
Exactly ONE perft fixture was re-frozen — `arrival-and-refund`, digest a0ecef98 -> dea9d2d6,
counts 121/121/121 -> 141/141/141 — and only after canonical and replica agreed on the new
numbers; the drift was shown to be caused by the rule change (the walker's games no longer end
at a ten-ply draw) rather than by the predicate edit. `upkeep-review-pending` was RECOVERED
rather than re-frozen: a loose `sets >= 2` predicate had started matching a degenerate
two-unit endgame (424/425/424 collapsing to 2/3/2), and tightening it to the fixture's own
documented purpose re-selected the identical position at digest 2fb1fec9 and counts
424/425/424, unchanged. The other five fixtures' digests and counts did not move.

CHANGED — a bounded all-surface `hard:fuzz` on the merged tree (1,200,000 walk actions, seed
20260919, all five surfaces, 2m04s): divergences 0, legalitySetMismatches 0,
pendingLegalityMismatches 0, unmakeMismatches 0, rehashMismatches 0, roundTripMismatches 0,
invariantViolations 0; arrival surface 0 on all nine mismatch counters; prover surface
fixtureMismatch 0 / fuzzVerdictMismatch 0 / nodeMismatch 0 / witnessIllegal 0 /
witnessNotRemoved 0 / clockFixtureOk true; gate-preservation surface mismatches 0,
incrementalProverVerdictMismatches 0, proverOrderInvarianceViolations 0, gateProverCapHits 0.
**maxClockSeen 20 of 20**, 949 of 4,544 walk games played with the ATTACK weight zeroed, and
1,940 games actually ended as inactivity draws at 20 (plus 819 more on the gate surface). The
clock histogram puts 180,361 observations in the 11..20 band — states that did not exist under
`muju-phasing-1` at all. Non-vacuity: buys 131,888, arrivals 122,962, refunds 6,639,
`arrivalRefundGameFraction` 0.607, prover cutoffCases 278, proverMaxNodes 182 of 20,000.

CHANGED — the scripted-bot reference was re-run under the new revision (see
`lab/harness/results/p2-scripted-2026-09-19/`, and the old-vs-new table below). The frozen
sanity bands were re-derived from the UNCHANGED formula.

BLOCKED — A4 voids every existing strength claim and these were NOT re-run: Gate 1 rows A1/A2
and both pilots, the M5 v1 suite measurement (`m6-suite-measure-1`), the ladder, and
`npm run hard:verify`'s full gate chain (out of lane and over the ~15-minute command budget;
the commands its M5/M10 rows invoke were each run individually and are all green). The M5
criterion in `lab/hard-ai/verify/gates.ts` was STRENGTHENED with
`fuzz.maxClockSeen >= INACTIVITY_LIMIT` and `fuzz.quietGames > 0` so an archived pre-A4
artifact cannot pass it. **No strength claim is made in this record.**

BLOCKED (coverage) — `lab/hard-ai/positions/fuzz-1000.jsonl` was NOT re-sampled: it is an
archived corpus many historical artifacts reference. Its `inactivityRule: "on"` rows top out
at clock 9 of 20, so the reference audit's `draw` position set holds nothing imminent under
the new limit. That is recorded in code at the new `ARCHIVED_DRAW_CLOCK_FLOOR = 7` constant in
`lab/hard-ai/verify/reference.ts`, which deliberately is NOT expressed as `LIMIT - 3` because
a floor of 17 would select ZERO rows and silently empty the set. The fuzz clock gate supplies
that coverage instead.

## 12. [ ] server-runtime — Authoritative multiplayer host, schemas and online client

Reason: upstream changed: transitions; upstream changed: persistence

Paths: `muju/server/rooms.ts`, `muju/server/schema.ts`, `muju/server/http.ts`, `muju/server/index.ts`, `muju/server/clockPressure.ts`, `muju/server/matchPolicy.ts`, `muju/server/matchScope.ts`, `muju/tools/engine-seat`, `muju/docs/ENGINE-SEAT-MATCH-2026-09-19.md`, `muju/src/online/`, `muju/ONLINE.md`

- Update: Review room transitions, validation, clocks, staging, invitations, observations, persistence and client/server compatibility. Document any protocol change and handling of active games.
- Update: Retiring a rule set: issue a new rulesVersion, state which stored versions upgrade in place and which become read-only archives, keep mutating calls on retired rooms returning RULES_CHANGED, and never reset or rewrite the production room database.
- Verify: Check create/join/play, revision and idempotency handling, clocks/staging where relevant, persisted rooms and reconnects.

**Disposition (12/27 — `server-runtime`):** **CHANGED.** `server/rooms.ts` advances Standard `muju-online-4` -> `muju-online-6` and
Phasing `muju-phasing-1` -> `muju-phasing-2`, both exported and threaded through `read()`,
`listActive()`'s SQL and `create()`. `muju-online-5` is SKIPPED because it is reserved by the
unmerged T5 branch `codex/phasing-only-canonical` (confirmed at its `rooms.ts:21`); that note
lives in this file's header comment and in `ONLINE.md` so T5 sees it on rebase.
Rooms stored at the retired versions take the EXISTING changed-rules path unchanged: 409
`RULES_CHANGED` on every call, the SQLite row untouched, dropped from the active lobby, still
summarised in the archived list. No archive system was invented and the `muju-online-2/3`
in-place upgrade is untouched (it restarts the quiet clock, so it never carried a stale count).
`src/online/OnlineLobby.tsx` renders `{INACTIVITY_LIMIT}` instead of "Draw after 10
consecutive turns".
Evidence: `npm run server:types` clean. `tests/server/rooms.test.ts` gains "stamps the
twenty-ply rules revisions on new rooms" (reads `rulesVersion` straight out of SQLite) and
"refuses to play %s rooms under the new clock without losing them" for both retired versions:
it stores a real room, rewrites its `rulesVersion`, sets `inactivityPlies = 8`, reopens the
store, asserts `RULES_CHANGED` from get / authenticated get / act / preview-act / moveHistory /
restore, then re-reads the raw row to prove `rulesVersion`, the clock and the board are
byte-intact and that `listActive()` omits it.
OWNER DECISION PENDING (recorded, not invented): archived rooms at the retired versions can
still be LISTED with their result but can no longer be OPENED for review, because `read()`
throws `RULES_CHANGED` — the pre-existing behaviour for any retired version. Read-only archive
access for retired rules is T5's territory. No room database was touched.

## 13. [ ] mcp-tools — MCP rules, legal actions, previews and analysis helpers

Reason: upstream changed: server-runtime; upstream changed: ai-search

Paths: `muju/server/mcp.ts`, `muju/server/stdio.ts`, `muju/server/observation.ts`, `muju/server/agentSchema.ts`, `muju/server/notation.ts`, `muju/server/analysis/`, `muju/tools/benchmark-analysis.ts`

- Update: Review muju_rules text and catalogue, tool descriptions, schemas/defaults, legal actions, preview and play parity. Recheck geometry, economy, units, tactics and exchange helpers for hardcoded budgets/claims and changed search bounds.
- Verify: Assert rules/analysis return current stats and legal witnesses, then exercise HTTP MCP and stdio as applicable. Distinguish bounded estimates from proofs; update benchmark evidence when helper behavior changes.

**Disposition (13/27 — `mcp-tools`):** **CHANGED.** `server/observation.ts`: both `rules.victory` (Standard) and
`rulesFor('phasing').victory` are now template strings over `INACTIVITY_LIMIT`, so a future
revision cannot leave stale prose behind; `observe()` already exported
`quietTurns` / `drawAtQuietTurns` from the constant. `server/analysis/index.ts:249`: the
headline draw pair was the one bare literal in this node (`draw: [plies, 10]`) and now reads
`[s.inactivityPlies ?? 0, INACTIVITY_LIMIT]` — it would otherwise have reported the wrong
denominator to every MCP analysis client.
`server/mcp.ts` tool descriptions and the server `instructions` string state no numeric draw
limit — VERIFIED UNCHANGED by grep. `server/analysis/economy.ts` stops its stay-in-place ledger
at the engine's terminal, so `terminal:inactivity` now arrives after 20 plies automatically,
and its default horizon of 12 own harvests (24 plies) still spans the limit.
Evidence: `tests/server/draw-clock-statements.test.ts` (new, 6 tests) asserts both rulesets'
victory text contains the constant and matches NONE of four retired sentences, that the whole
`rules` JSON is free of them, and that `observe()` reports
`drawAtQuietTurns === INACTIVITY_LIMIT`. `tests/server/analysis.test.ts` gains "reports the
draw headline as [quietPlayerTurns, canonical limit]" plus a one-ply-earlier `stop === 'horizon'`
case. The new cross-engine test asks `observe()` at EVERY one of the twenty plies and asserts
`drawAtQuietTurns - quietTurns` equals the true plies remaining each time.

## 14. [ ] agent-guides — Published agent skills and MCP usage documentation

Reason: upstream changed: mcp-tools; upstream changed: rules-docs

Paths: `muju/public/skills/`, `muju/server/skills.ts`, `muju/docs/MCP_TOOL_TAPS.md`, `muju/docs/ANALYSIS_TOOLS.md`, `muju/ONLINE.md`

- Update: Update public Muju and time-awareness skill files, TAPs, analysis documentation and live tool examples. Vite copies public skills into dist; the production server serves those copies.
- Verify: Compare documentation to tool discovery and actual responses. Check both /SKILL.md on the Node host and the static /muju/skills/ copies after release.

**Disposition (14/27 — `agent-guides`):** **CHANGED.** `public/skills/muju-hono-tanka/SKILL.md` and `ONLINE.md` now say twenty plies /
ten hand-offs each and point at `quietTurns` vs `drawAtQuietTurns`; `ONLINE.md`'s
rules-version paragraph documents the 6 / phasing-2 bump, the reserved `muju-online-5` and the
`RULES_CHANGED` handling. `docs/ANALYSIS_TOOLS.md` names the limit on the layer-0 draw pair and
records that a stay-in-place forecast now runs to 20 plies. `docs/MCP_TOOL_TAPS.md` gains one
observation TAP for `quietTurns` within three of the limit.
`public/skills/muju-time-awareness/**` mentions no draw rule — VERIFIED UNCHANGED.
Evidence: `tests/server/draw-clock-statements.test.ts` READS all four files plus
`OnlineLobby.tsx` off disk and proves the published text states twenty and carries no ten-ply
claim. That guard is what keeps these documents tied to the constant rather than to a memory of
it. NOT VERIFIED: the copies served live (`/SKILL.md` on the Node host, `/muju/skills/` in the
static site) — nothing is deployed from this branch.

## 15. [ ] balance-analysis — Current static balance report and relevant fresh experiments

Reason: upstream changed: transitions; upstream changed: ai-search

Paths: `muju/lab/solver/`, `muju/lab/results/current-static/current.json`, `muju/lab/results/current-static/current.md`

- Update: Review solver assumptions and regenerate current-static. Run new experiments only when needed to support changed claims; record source/rules hashes, configuration and seeds. Leave frozen map studies, baselines and dated results intact.
- Verify: Check catalogue/model freshness and explain changed dominance or witness results rather than weakening checks merely to pass. Static values are separate from AI weights.
- Command (cwd `muju`): `npm run balance:static` — Regenerates current-static outputs.
- Command (cwd `muju`): `npm run balance:check && npm run balance:types` — Checks current outputs and solver types.

**Disposition (15/27 — `balance-analysis`):** **VERIFIED UNCHANGED.** The static solver has no draw, clock, quiet or inactivity term at
all: `grep -rni 'inactiv|quiet|draw' lab/solver/*.ts` returns zero hits, so the current-static
report's dominance and witness results cannot move with this rule.
Evidence: `git diff --stat -- muju/lab/solver muju/lab/results/current-static` is empty;
`npm run balance:types` is covered by the clean `lab/solver` typecheck. `balance:static` was
deliberately NOT re-run, because regenerating an unchanged report would only add noise to the
diff. The scripted-bot BALANCE measurement that this rule does move is the harness campaign,
which belongs to node 11 and to the reference comparison below.

## 16. [ ] academy-data — Academy current rule snapshots, catalogues, map and matchup matrices

Reason: upstream changed: transitions

Paths: `muju/academy/export-rules.ts`, `muju/academy/rules-snapshot/`, `muju/academy/catalog.json`, `muju/academy/map.json`, `muju/academy/bonk-matrix.json`, `muju/academy/rules-verification.json`, `muju/academy/metal-v29-provenance.json`, `muju/academy/production/R??/source-rules/`, `muju/academy/production/R??/src/catalog.json`, `muju/academy/production/R??/src/map.json`, `muju/academy/production/R??/src/bonk-matrix.json`

- Update: Read Academy README and STATUS first. Adapt export-rules.ts assertions/version to the intended rule change, export from live src/game, then propagate current data to every affected active episode. Audit imports and provenance of rules-snapshot and each episode source-rules: they contain historical assumptions and rendering subsets, so do not blindly replace them.
- Verify: Check all ordered full-health matchups and affected demonstrations, map totals and copies. A single attack/defense change can alter incoming/outgoing matrices in every element lesson R11–R16. Regeneration of JSON alone does not update narration or rendered videos.
- Command (cwd `muju`): `node --import tsx academy/export-rules.ts` — Writes Academy JSON after assertions pass. Review/update expectations before running.

**Disposition (16/27 — `academy-data`):** **CHANGED, and one real break was found and fixed at the cause.**
`academy/export-rules.ts` asserted a draw from `inactivityPlies: 9` and would have failed
under the new limit, BLOCKING this whole node; it now derives the demonstration from
`INACTIVITY_LIMIT` and carries a comment about R09's stale narration.
Evidence: `npx tsx academy/export-rules.ts` -> "324 ordered matchups and revised movement,
Cleave, promotion and draw demonstrations passed." It regenerated EXACTLY ONE line of
`academy/rules-verification.json` — the R09 draw demonstration — and `catalog.json`,
`map.json` and `bonk-matrix.json` came back byte-identical, which independently confirms node 2.
`academy/rules-snapshot/**`, `academy/production/R??/source-rules/**` and `academy/archive/**`
hold 15 further copies of `inactivity.ts` still at 10; these are per-lesson PROVENANCE for what
each recording was made against and were deliberately not rewritten — `academy/STATUS.md`
records that they refresh through `export-rules.ts` in whichever release re-records R09.
OPEN LABEL (owner/Academy-release decision, not guessed here): `rules-verification.json` still
carries `"rules": "v2.9"` while now verifying a v3.0 rule. Nothing reads it —
`build-release.py` and `verify-live.py` take per-episode rules from `release.json` — so it was
left rather than assigned a value this lane has no authority to choose.

## 17. [ ] academy-lessons — Academy lesson scripts, examples and renderer sources

Reason: upstream changed: academy-data; upstream changed: rules-docs; upstream changed: browser-ui; upstream changed: server-runtime

Paths: `muju/academy/README.md`, `muju/academy/STATUS.md`, `muju/academy/BIBLE.md`, `muju/academy/ALL-SCRIPTS.md`, `muju/academy/CURRICULUM-AND-BATCHES.md`, `muju/academy/PIPELINE-NOTES.md`, `muju/academy/production/R??/episode.json`, `muju/academy/production/R??/src/`, `muju/academy/production/R??/scripts/`, `muju/academy/supplement.tsx`, `muju/academy/update-visuals.py`, `muju/academy/production/R??/public/art/PROMPTS.md`, `muju/academy/production/R??/package.json`, `muju/academy/production/R??/tsconfig.json`, `muju/academy/production/*.tsx`, `muju/academy/revise.py`, `muju/academy/propagate-economy.py`, `muju/academy/.gitignore`

- Update: Audit active R01–R16 episode.json and renderer sources for spoken facts, cards, boards, examples, captions and summaries. Record affected episode IDs and why others are unchanged. Preserve repaired current scripts; revise.py and propagate-economy.py are one-time historical migrations and must not be rerun over current production.
- Verify: Validate each changed example against current engine state, action budget and timing. Cross-element counter matchups matter. Check visible revision/date labels and current source provenance; do not resurrect retired R17–R27.

**Disposition (17/27 — `academy-lessons`):** **CHANGED (notice prepared) + BLOCKED (re-narration pending owner speech authorization).**
`academy/STATUS.md` gains an affected-lesson note naming **R09 "The Ten Quiet Turns"** with
every superseded narration line quoted verbatim — the title, three narration lines and the
recap line, including "five complete rounds" and "amber at seven". **R10 was checked and
recorded VERIFIED UNCHANGED**: its capstone says "quiet count" and never states the number.
The prepared course notice in `academy/build-release.py` now states that the quiet-turn draw is
twenty plies, names R09 as still saying ten, and restates that what resets the clock is
unchanged; `academy/verify-live.py` gained three matching assertions and keeps all its existing
ones. No lesson media, `episode.json`, renderer source or render script was touched.
Evidence: `python3 -m py_compile academy/build-release.py academy/verify-live.py` OK.
BLOCKED: R09 needs re-narration in a future v9 release. That is a speech-generation decision
the owner has not authorized, so it is recorded rather than performed; the live page carries the
correction through the prepared notice in the meantime.
`revise.py` and `propagate-economy.py` were correctly NOT rerun (one-time historical migrations).

## 18. [ ] academy-audio — Academy narration, local transcription and caption timing

Reason: upstream changed: academy-lessons

Paths: `muju/academy/audio-batch.py`, `muju/academy/prepare-audio.py`, `muju/academy/retake-openai.mjs`, `muju/academy/elevenlabs-speech.py`, `muju/academy/transcribe-new.mjs`, `muju/academy/transcribe-local.py`, `muju/academy/align-current.py`, `muju/academy/timeline.mjs`, `muju/academy/production/R??/public/audio/`, `muju/academy/production/R??/speech-directions.json`

- Update: Regenerate only changed speech with the established OpenAI cast and pronunciation, preserve unchanged takes, and refresh the changed-clip inventory consumed by transcribe-local.py. Verify locally with faster-whisper, align captions, and regenerate timelines. Inspect hardcoded temporary runtime/model paths before running scripts.
- Verify: No unresolved spoken-fact, missing-word, pronunciation or caption-timing errors. Bind ASR and caption evidence to current audio hashes. Old clip inventories and successful historical logs do not verify new narration.

BLOCKED — obtain missing production inputs:
- `muju/academy/production/R??/public/audio/`
Academy narration and mastered speech media is archived outside git; the tracked pipeline sources and per-line timelines remain in the repository. Obtain the media bundle before executing these nodes; do not skip Academy propagation.
~/Archives/muju-media-2026-09-18/academy (hashes in ~/Archives/muju-media-2026-09-18/MANIFEST.sha256); original copy in /Users/ashkie/src/deevgames/muju/academy; copy offsite before deleting either

**Disposition (18/27 — `academy-audio`):** **BLOCKED, on two independent grounds.**
(1) The DAG's own input check: `python3 -B tools/muju-content-dag.py check` reports the
`muju/academy/production/R??/public/audio/` media as archived outside git and unavailable in
this worktree. The bundle was not fetched.
(2) The work itself is R09 re-narration, which needs owner authorization for speech generation
(node 17). Nothing was generated, transcribed, aligned or re-timed, and no ASR or caption
evidence is claimed. The affected spoken lines are inventoried verbatim in
`academy/STATUS.md` so the future release does not have to rediscover them.

## 19. [ ] academy-video — Final videos, mastering, posters, transcripts and visual QA

Reason: upstream changed: academy-audio

Paths: `muju/academy/render-batch.py`, `muju/academy/render.mjs`, `muju/academy/master.mjs`, `muju/academy/remaster-batch.py`, `muju/academy/final-frames.py`, `muju/academy/stills-batch.py`, `muju/academy/review-final.py`, `muju/academy/production/R??/output/`, `muju/academy/production/R??/qa/`, `muju/academy/production/R??/public/music/`, `muju/academy/production/R??/public/art/*.png`

- Update: Render affected episodes, master with existing soundtracks, and regenerate affected posters/transcripts. Review actual final exports before recording review-final.py --visual-reviewed evidence. Inspect batch script/runtime paths and current episode configuration first.
- Verify: Require full decode, source-audio correlation, complete duration, quiet holds, loudness/peak checks, readable cards/boards and correct matrices. final-review.json must identify exact final video and timeline hashes; never mechanically assert that unseen frames were reviewed.

BLOCKED — obtain missing production inputs:
- `muju/academy/production/R??/output/`
- `muju/academy/production/R??/qa/`
- `muju/academy/production/R??/public/music/`
- `muju/academy/production/R??/public/art/*.png`
Academy music beds are archived outside git; the tracked cue sheets and pipeline sources remain in the repository. Obtain the media bundle before executing these nodes; do not skip Academy propagation.
~/Archives/muju-media-2026-09-18/academy (hashes in ~/Archives/muju-media-2026-09-18/MANIFEST.sha256); original copy in /Users/ashkie/src/deevgames/muju/academy; copy offsite before deleting either
Academy raster art is archived outside git; the authored image prompts beside it (PROMPTS.md) are tracked. Obtain the media bundle before executing these nodes; do not skip Academy propagation.
~/Archives/muju-media-2026-09-18/academy (hashes in ~/Archives/muju-media-2026-09-18/MANIFEST.sha256); original copy in /Users/ashkie/src/deevgames/muju/academy; copy offsite before deleting either
Academy rendered episodes, posters and thumbnails are archived outside git; the render scripts and Remotion sources that produce them are tracked. Obtain the media bundle before executing these nodes; do not skip Academy propagation.
~/Archives/muju-media-2026-09-18/academy (hashes in ~/Archives/muju-media-2026-09-18/MANIFEST.sha256); original copy in /Users/ashkie/src/deevgames/muju/academy; copy offsite before deleting either
Academy QA capture media (frame grabs, audio exports, ASR evidence) is archived outside git; the QA scripts that regenerate it are tracked. Obtain the media bundle before executing these nodes; do not skip Academy propagation.
~/Archives/muju-media-2026-09-18/academy (hashes in ~/Archives/muju-media-2026-09-18/MANIFEST.sha256); original copy in /Users/ashkie/src/deevgames/muju/academy; copy offsite before deleting either

**Disposition (19/27 — `academy-video`):** **BLOCKED.** Downstream of node 18 and blocked by the same missing media bundle
(rendered episodes, posters, thumbnails, music beds, raster art and QA capture are all archived
outside git, per the DAG check). No episode was rendered, mastered or reviewed, and no
`final-review.json` evidence is asserted. Correctly so: mechanically claiming unseen frames were
reviewed is exactly what this node forbids.

## 20. [ ] game-validation — Game, AI, server, MCP and browser regression gates

Reason: upstream changed: rules-docs; upstream changed: browser-ui; upstream changed: persistence; upstream changed: wasm-tactics; upstream changed: ai-search; upstream changed: hard-ai; upstream changed: ai-strength; upstream changed: server-runtime; upstream changed: mcp-tools; upstream changed: agent-guides; upstream changed: balance-analysis

Paths: `muju/tests/`, `muju/e2e/`, `muju/vitest.config.ts`, `muju/playwright.config.ts`, `muju/playwright.online.config.ts`, `muju/playwright.hard.config.ts`, `muju/server/tsconfig.json`, `muju/tsconfig.json`

- Update: Adapt meaningful changed-rule regression cases and fixtures. Retain explicit historical compatibility fixtures; do not replace every occurrence of an old number. Investigate failures against intended behavior.
- Verify: Run targeted checks during iteration, then release gates. For general local browser tests first serve the built game at MUJU_BASE_URL (default http://127.0.0.1:8927/muju/); the online suite starts its own isolated host.
- Command (cwd `muju`): `npm run server:types && npm test && npm run build` — Checks types/tests and rebuilds WASM plus browser artifacts.
- Command (cwd `muju`): `npm run test:online:e2e` — Runs Playwright against an isolated in-memory Node host on port 8928.
- Command (cwd `muju`): `npm run test:e2e -- e2e/ai-worker.spec.ts` — Requires the built game served at MUJU_BASE_URL and an installed browser.

**Disposition (20/27 — `game-validation`):** **CHANGED.** FULL DEFAULT SUITE, JSON reporter (`npx vitest run --reporter=json`), final run on the merged
tree: **194 test files, 635 suites, 2694 tests — 0 failed files, 0 failed suites, 0 failed
tests, 0 skipped**, `success: true`.
The run BEFORE the seam fixes was 193 files / 633 suites / 2691 tests with **2 failed files, 4
failed suites, 2 failed tests** — both files, `tests/lab/suites-phasing-invariants.test.ts`
("inv16-clock-discipline: premise and temporal evidence are canonical") and
`tests/lab/suites-phasing-runner.test.ts` (the six-family bundle runner), had ONE root cause and
it is written up under "Seam fixes" below.

TYPECHECKS, all seven configurations, all clean and all re-run after the final seam fixes:
`npx tsc --noEmit -p .`, `-p lab`, `-p lab/ai`, `-p lab/hard-ai`, `-p server`,
`-p tools/engine-seat`, `-p lab/solver`. Plus `npm run hard:deps`
`{"filesScanned":46,"violations":[],"layeringViolations":0,"nondeterminismViolations":0,"bigintViolations":0}`,
`python3 -B ../tools/muju-content-dag.py check` ("Valid DAG: 27 nodes, 49 edges; required
repository paths exist") and `python3 -B ../tools/test-muju-content-dag.py` (14 tests OK).

NEW REGRESSION COVERAGE, none of it a re-pinned literal: the constants and the three-ply margin
pinned outright; a full twenty-ply playout; the whole 17/18/19 warning band held open; the
legacy-limit adjudication; the published-text guard that reads five files off disk; the six
save-migration cases; the two room-revision cases; and the new cross-engine walk. Every fixture
that had spelled "one ply before the draw" as a literal 9 now derives it from
`INACTIVITY_LIMIT`, so the next change to this number cannot silently hollow them out.
**No assertion anywhere was weakened, skipped or deleted**, and no test was added to
`vitest.config.ts`'s quarantine list (which is unchanged, and whose four `tests/ai/hard`
entries were confirmed by grep to contain no clock pin).

NOT EXECUTED, and why: the Playwright suites. `e2e/action-budget.spec.ts`,
`e2e/upkeep-draw.spec.ts` and `e2e/analysis.spec.ts` were updated for the new limit
(`action-budget` now drives twenty End-turn clicks instead of ten and should be TIMED on its
first real run), and `npm run test:online:e2e` was not run either. Both need a built app or a
live host and sit outside the ~15-minute command budget this run was held to. Several other e2e
specs hardcode `schemaVersion: 6`; with the v8 bump their seeded saves now load with the quiet
clock restarted, which is harmless only because none of them sets `inactivityPlies` — that was
checked, not assumed. `npm run build` was not run.

## 21. [ ] static-package — Complete deevgames static release artifact

Reason: upstream changed: game-validation

Paths: `build-all.sh`, `tools/verify_site.py`, `tools/smoke-site.cjs`, `muju/vite.config.ts`, `muju/package.json`, `muju/package-lock.json`, `index.html`, `README.md`

- Update: Build all three games with locked dependencies and Node 24; stage only the generated _site directory. Confirm current rules, skill assets and compiled AI land in the artifact. Check hub/game navigation and public dossier copy.
- Verify: The full build verifies links, required pages and file sizes. Serve _site on an unused port and run tools/smoke-site.cjs against it; inspect actual gameplay.
- Command (cwd `.`): `bash build-all.sh` — Builds muju, forge and oracle and replaces generated _site; their dependencies must already be installed.

**Disposition (21/27 — `static-package`):** **PREPARED, NOT DEPLOYED — not attempted on this branch.** `bash build-all.sh` was not run
and no `_site` artifact was produced or staged. This is `claude/phasing-2-drawclock`, not
`master`, and the packaging node's job is to build the RELEASE artifact; running it here would
produce an artifact nobody will publish while overwriting the generated `_site` directory.
The source this node would package is green at the type and unit level (node 20). Not claimed:
link/page/size verification, `tools/smoke-site.cjs`, or any inspection of actual gameplay in a
built bundle.

## 22. [ ] server-package — Node host release with persistent room storage

Reason: upstream changed: game-validation

Paths: `muju/Dockerfile`, `muju/compose.yaml`, `muju/.dockerignore`

- Update: Build the Node host including its own browser dist and copied public skill files. Verify deployed source revision, persistent disk configuration, public URL/CORS and protocol compatibility.
- Verify: Test the built image and restart persistence. Preserve /app/data/rooms.sqlite and existing room data. Static Pages publication does not update this service.

**Disposition (22/27 — `server-package`):** **PREPARED, NOT DEPLOYED — not attempted on this branch.** No Docker image was built and
`muju/Dockerfile`, `compose.yaml` and `.dockerignore` are untouched. The host change itself is
complete and tested in-process (node 12): new rules identities, `RULES_CHANGED` on retired
rooms, and the room database never touched. Not claimed: built-image behaviour, restart
persistence of `/app/data/rooms.sqlite`, public URL/CORS, or protocol compatibility against a
deployed peer.

## 23. [ ] academy-package — Versioned Academy site package and historical redirects

Reason: upstream changed: academy-video

Paths: `muju/academy/build-release.py`, `muju/academy/verify-live.py`, `muju/academy/verify-withdrawal.py`

- Update: Review the explicit mixed v7/v8 Metal release revisions, summaries, catalogue and economy in builder/verifier. Locate a current ashkie-pages checkout, review its instructions/workflow, choose a fresh archive outside the site, then validate before packaging. Do not assume the old /private/tmp checkout still exists.
- Verify: Run build-release.py with production root, site checkout and fresh external archive using --check-only before packaging. Verify exact hashed videos/posters/transcripts, course page, release.json, offline-manifest handling and static-before-wildcard redirects. The builder explicitly verifies the Metal v2.9 mixed v7/v8 release.
- External: ethancd/ashkie-pages: muju-academy/, _redirects, offline-manifest.json, ./check, tools/verify_muju_videos.py and deployment workflow

**Disposition (23/27 — `academy-package`):** **BLOCKED.** Downstream of node 19, which is blocked on the missing media bundle, so the
package cannot be assembled or hash-verified. `build-release.py` and `verify-live.py` were
EDITED (the prepared notice and its three new assertions) but NOT RUN: `--check-only` needs a
current `ashkie-pages` checkout and a fresh external archive, and the builder verifies exact
hashed videos, posters and transcripts that are not present here. Both files compile
(`python3 -m py_compile`). No packaging, redirect or offline-manifest claim is made.

## 24. [ ] static-deploy — Publish Cloudflare Pages deevgames

Reason: upstream changed: static-package

Paths: `.github/workflows/deploy.yml`

- Update: Follow root README deployment flow. Publish validated _site through the workflow or documented Wrangler command; master is production and other branches are previews. Check fetched origin/master freshness and preserve unrelated work.
- Verify: Record source revision, artifact identity, actual publish result and deployment URL. A green workflow can mean checks-only when credentials are absent; confirm the deploy step actually ran.
- External: Cloudflare Pages project deevgames; documented public base https://deevgames.pages.dev
- External: Publishing paused since 2026-09-18 (no deploy credentials): record this node as blocked, not complete. Muju's canonical release is the Render host; the Pages /muju/ copy is frozen.

**Disposition (24/27 — `static-deploy`):** **BLOCKED, for two reasons, and it would be blocked even with an artifact.**
(1) Nothing was packaged (node 21) and this branch is not `master`.
(2) The DAG records that Pages publishing has been PAUSED since 2026-09-18 for want of deploy
credentials; Muju's canonical release is the Render host and the Pages `/muju/` copy is frozen.
No workflow was run, no Wrangler command was issued, and no deployment URL or artifact identity
is claimed.

## 25. [ ] server-deploy — Publish authoritative multiplayer/MCP host

Reason: upstream changed: server-package

Paths: external release surface

- Update: Inspect the current Render service and deploy the verified matching source/image through its configured release path. The repository has no verified automatic server deployment workflow; discover the live service configuration instead of inventing a deploy command. Follow ONLINE.md persistent disk and health settings.
- Verify: Record service/release identity and revision. Verify /api/muju/health, /muju/, /SKILL.md and MCP discovery/rules. Exercise a changed-rule preview and legal action in a fresh disposable room; verify existing storage survived.
- External: Documented Node/MCP host https://deevgames-muju.onrender.com; Render web service with /app/data persistence

**Disposition (25/27 — `server-deploy`):** **BLOCKED — prepared, not deployed.** Nothing was published to
`https://deevgames-muju.onrender.com`. No Render service was inspected, no release was
triggered, and no `/api/muju/health`, `/muju/`, `/SKILL.md`, MCP discovery, changed-rule
preview or fresh-room check was performed against the live host. The room database was not
touched. When this does ship it carries a user-visible consequence that belongs to the owner,
not to a deploy step: existing archived rooms at `muju-online-4` / `muju-phasing-1` become
listable but no longer openable for review (node 12).

## 26. [ ] academy-deploy — Publish Muju Academy on ashkie.com

Reason: upstream changed: academy-package

Paths: external release surface

- Update: Publish the prepared Academy-only changes via the current ashkie-pages workflow, following that checkout's release checks. Preserve unrelated website changes and previous media archives.
- Verify: Record website revision/deployment URL. Install the adapted verify-live.py as tools/verify_muju_videos.py in the website checkout before running it there. Verify page and release manifest, exact media bytes/hashes, range seeking, historical redirects and retired assets, then inspect the live course.
- External: https://ashkie.com/muju-academy/; ethancd/ashkie-pages deployment workflow

**Disposition (26/27 — `academy-deploy`):** **BLOCKED.** Downstream of node 23. Nothing was published to
`https://ashkie.com/muju-academy/`, `verify-live.py` was not installed into the website
checkout as `tools/verify_muju_videos.py`, and no website revision, media hash, range-seek,
redirect or live course inspection is claimed. The prepared course notice is ready in
`build-release.py` and will carry the twenty-ply correction to readers on whichever release
ships first; R09's narration still says ten until it is re-recorded (node 17).

## 27. [ ] release-verification — Cross-surface completion evidence

Reason: upstream changed: static-deploy; upstream changed: server-deploy; upstream changed: academy-deploy

Paths: `muju/docs/changes/`

- Update: Write a change-specific completion record with every affected node disposition, exact source/artifact revisions, tests, affected lesson IDs, publish results and live verification evidence. Update current Academy STATUS and append decision context as appropriate.
- Verify: Compare the changed piece/rule across browser UI, local AI behavior, online engine, MCP rules and analysis, public agent skills and live Academy lessons/transcripts. Every applicable release must be verified or explicitly blocked. Do not call an end-to-end update complete when a surface is still stale or a publish is unverified.

**Disposition (27/27 — `release-verification`):** **CHANGED (this document) — and the cross-surface live comparison is BLOCKED.**
This file is the change-specific completion record the node asks for: every one of the 27 nodes
carries a disposition with evidence, `academy/STATUS.md` was updated with the affected-lesson
inventory, and J-021 in `JUDGMENT_LOG.md` carries the decision context.
What IS verified end to end, on this tree: the rule is stated by ONE constant and agrees across
the canonical engine, the Hard replica, the legacy V2 terminal oracle, the server observation,
the analysis headline, the browser instructions, the lobby, the saved-game migration, the online
room identity, the four published agent documents and the Academy export — proved by the
cross-engine walk, the published-text guard, both perft engines and a 1.2M-action all-surface
fuzz that reaches clock 20 and draws there.
What is NOT: any of the three release targets. Nothing was built, packaged, published or
verified live on Pages, Render or ashkie.com, and the Academy media bundle was never fetched.
**This change is therefore NOT complete end to end** — it is complete in the repository and
prepared for release, which is the most this branch can honestly claim.


---

# Seam fixes made by the integrator

Four lanes edited this worktree concurrently on disjoint files. Three seams survived the merge;
all three were fixed at the cause, not papered over.

## 1. `inv16-clock-discipline` — the only test failure on the merged tree

**File:** `muju/lab/hard-ai/suites/phasing/build-invariants.ts` (one case, lines 172-183).

This case is owned by the Phasing-suites workflow running in a different worktree, so the edit
is deliberately the minimal mechanical one and nothing was redesigned. The case authored its
violating member at `clock: 9` with a root premise of `inactivity-plies eq 9`, and its probe
expected `{kind: terminal, winner: null, reason: inactivity}` after one passive Black handoff.
Its MEANING is "one passive handoff from the draw"; at a twenty-ply limit, clock 9 -> 10 is
still `playing`, so the probe failed and the same single case then failed the six-family bundle
runner's authoring check (1 of 225 cases). That is two failed test files from one authored
number.

The fix imports `INACTIVITY_LIMIT` and derives the premise from it:

```
const lastQuietPly = INACTIVITY_LIMIT - 1;
add(16, 'clock-discipline', `...counter${lastQuietPly} versus2. One passive Black handoff
  reaches inactivity${INACTIVITY_LIMIT}/draw versus3/playing...`,
  state(units, { clock: lastQuietPly }), state(units, { clock: 2 }),
  root({ kind: 'inactivity-plies', value: { eq: lastQuietPly } }),
  root({ kind: 'inactivity-plies', value: { eq: 2 } }), ...)
```

The motif, the diagram, the correct member (clock 2, ending at 3 and still playing), both
probes, the classification and the work budget are untouched. Only the number moved, and it is
now derived rather than written out, so the next revision cannot hollow the case out again.

**For the other branch to merge cleanly:** this is the entire edit — one added import of
`INACTIVITY_LIMIT` from `../../../../src/game/inactivity`, one added `lastQuietPly` const, the
rationale string turned into a template literal, and `9` -> `lastQuietPly` in the `state(...)`
call and the root predicate. `lab/hard-ai/suites/invariants.positions.jsonl` (the Standard-era
v1 corpus), `lab/hard-ai/suites/build-invariants.ts`, `lab/hard-ai/suites/invariants.suite.json`
and the frozen `lab/hard-ai/suites/phasing/fixtures/v1/**` measurement artifacts were **NOT**
touched: they are historical evidence, they are void per A4 anyway, and the bundle runner
authors into a temp directory rather than comparing against them.

## 2. A superseded record silently reinterpreted as Standard

**File:** `muju/lab/hard-ai/bench/profile.ts:567`.

Found and reported by the harness lane, which correctly did not fix it in its own lane. The
line read:

```
ruleset: record.rulesVersion === LADDER_RULES_VERSION ? 'phasing' : 'standard',
```

`LADDER_RULES_VERSION` is now `'muju-phasing-2'`, so a `muju-phasing-1` record falls to the
`else` and reconstructs as **Standard** — the exact "never reinterpret an old corpus" hazard
`lab/hard-ai/ladder/ruleset.ts` exists to prevent, and it fails silently because the two rule
sets share every action type. It now asks the module that owns the question:

```
ruleset: rulesetForRevision(record.rulesVersion, `profile: in-memory record for ${opening.id}`),
```

which returns `'phasing'` for both revisions and THROWS on an unknown one instead of guessing.
`LADDER_RULES_VERSION` is no longer imported by this file.

## 3. Nothing else needed a seam fix

`lab/ai/gate1*` and `tests/lab/gate1*.test.ts` were green on the merged tree and needed no
mechanical update; no edit was made to them. `tests/game/tier3-cap.test.ts` and
`tests/game/action-budget.test.ts` were edited cross-lane by the server lane as a direct
consequence of the `SCHEMA_VERSION` bump and did not clash with the canonical lane's edits to
`tests/game/**` — confirmed by the clean full suite.

`lab/experiments/depth-economy/validate.ts:23` asserts `INACTIVITY_LIMIT === 10` and was
deliberately left alone: it imports from its own frozen `./.sandbox/src/game/inactivity`, is a
self-contained historical experiment with its own copy of the engine, is in no tsconfig and is
run by no test. Re-pinning it would falsify what that experiment measured.

---

# Reference comparison — the harness lane's scripted numbers, restated exactly

840 games, same 15 scripted bots, same seed `20260955` and seed derivation, same mirroring,
handicaps h0/h3, same match options, band FORMULA byte-for-byte unchanged, manifest written
before the first game. `totals.json` records `changedDuringRun: []`, so all 840 games were
played against one tree even though other lanes were editing the worktree.
Evidence: `muju/lab/harness/results/p2-scripted-2026-09-19/` (manifest.json, games.jsonl,
summary.csv with 210 rows, sanity-bands.json, totals.json, verification.json, REPORT.md,
SHA256SUMS). The 2026-09-18 directory is untouched and its SHA256SUMS still verify.

| Metric | 2026-09-18 (10 plies) | 2026-09-19 (20 plies) |
| --- | ---: | ---: |
| Inactivity draws | 416 / 840 (49.5238%) | **227 / 840 (27.0238%)** |
| Inactivity draws, Wilson 95% | [46.15%, 52.90%] | [24.13%, 30.13%] |
| Draws by any other rule | 0 | 0 |
| Mean completed player turns | 29.98095 | **38.22024** |
| Mean recorded round number | 15.45952 | 19.70595 |
| Mean single decisions | 214.97857 | 264.84405 |
| Maximum recorded round / decisions | 91 / 1078 | 91 / 1119 |
| Mean peak inactivity clock | 7.52024 | 11.12381 |
| Purchases per game, both seats | 38.35952 | **43.09048** |
| Elimination wins | 239 | 326 |
| Upkeep-elimination wins | 98 | 179 |
| Home occupation / home checkmate wins | 48 / 39 | 55 / 53 |
| Illegal actions / invariants / cap adjudications | 0 / 0 / 0 | 0 / 0 / 0 |

**It is a true paired comparison.** Seed, seed derivation and pairing order are identical, so
game *i* of one campaign is the same bots, seats, handicap and seed as game *i* of the other —
all 840 of 840. 424 games are identical in winner, win type and length (they had already ended
before ply 10); **189 games that drew under 10 plies resolved under 20; ZERO went the other
way.** 189 is exactly 416 - 227: the rule removed draws from this population and added none.

Every one of the 15 bots fell. Largest: Balanced -37.50 pp, Random -32.14 pp, Mono-plant
-31.25 pp. Smallest: Rush -8.04 pp and Mono-fire -10.71 pp — the two that already drew least.
Expand and AntiRush remain the highest at 55.36%.

New frozen bands, from the unchanged formula: purchases **[1.6696, 188.2857]** (was
[1.3839, 186.9286]); inactivity **[0, 0.7261348375]** (was [0, 0.8658137861]).

Opening books were NOT regenerated, and A4's claim about them was verified by replay rather
than trusted: all 48 p1-dev and 32 p1-val rows, at both handicaps — 160 replays — end with
`inactivityPlies` of **exactly 1** and `progressThisTurn` false. The maximum clock over the
whole unsealed corpus is 1: six below the old warning and nineteen below the new limit. No
opening sits near either boundary, so the bytes and their pinned hashes are valid under either
revision. The sealed book was never opened.

**The report deliberately does not editorialise** on whether a 27.02% inactivity-draw rate is
the game the owner wants. It reports the numbers and the paired structure that makes them
attributable to the rule alone.

---

# Commands run by the integrator, and their results

| Command | Result |
| --- | --- |
| `npx tsc --noEmit -p .` / `-p lab` / `-p lab/ai` / `-p lab/hard-ai` / `-p server` / `-p tools/engine-seat` / `-p lab/solver` | all clean, exit 0; re-run after the seam fixes |
| `npm run hard:deps` | `{"filesScanned":46,"violations":[],"layeringViolations":0,"nondeterminismViolations":0,"bigintViolations":0}` |
| `python3 -B ../tools/muju-content-dag.py check` | "Valid DAG: 27 nodes, 49 edges; required repository paths exist" + the 5 pre-existing local-only Academy media paths |
| `python3 -B ../tools/test-muju-content-dag.py` | 14 tests, OK |
| `npx vitest run --reporter=json` (full default suite) | **194 files / 635 suites / 2694 tests, 0 failed** (was 2 failed files / 4 failed suites / 2 failed tests before the seam fixes) |
| `npm run hard:perft -- --check --engine canonical` | `fixturesChecked 7, fixturesMismatch 0, digestMismatches 0, mismatches 0, replicaAgreed true, standardTriple "checked"` |
| `npm run hard:perft -- --check --engine replica` | `fixturesChecked 7, fixturesMismatch 0, digestMismatches 0, mismatches 0, replicaAgreed true, standardTriple "skipped-phasing-replica"` |
| `npm run hard:fuzz -- --actions 1200000 --seed 20260919` (all five surfaces) | exit 0; every counter 0; **maxClockSeen 20/20**; details below |

## The fuzz run in full

Artifact: `muju/lab/results/hard-ai-verify/fuzz-integration-2026-09-19.json`, which records its
own provenance honestly — `git 11d6cd57d9be6ad89ae5746b3b558bcb50b95926`, `gitDirtyFiles 93`,
`node v24.11.1`, `at 2026-09-19T16:04:27.324Z`. It is a NEW dated artifact beside the lane's
`fuzz-walk.json` / `fuzz-prover-arrival.json` / `fuzz-gate.json`, none of which was overwritten.

Walk (1,200,000 actions, 4,544 games, 60.4 s): `divergences 0, legalitySetMismatches 0,
pendingLegalityMismatches 0, unmakeMismatches 0, rehashMismatches 0, roundTripMismatches 0,
invariantViolations 0`. Coverage: `buys 131888, arrivals 122962, refunds 6639,
arrivalRefundGameFraction 0.607, quietGames 949/4544`.
**`maxClockSeen 20` of 20**; `inactivity:draw@END_PLACE_PHASE` **1,940** of 4,544 games.
Clock histogram: `0:224227 1:107747 2:94572 3:83498 4:73176 5:64758 6:56383 7:50294 8:43292
9:38510 10:33691 11:30343 12:26710 13:23999 14:21231 15:19161 16:17072 17:15562 18:13759
19:12524 20:149491`. The **11..20 band — the states `muju-phasing-1` did not have — carries
180,361 observations**, so this run's zeros say something about the change rather than
re-testing the old rules.

Arrival surface (400 cases): all nine mismatch counters 0; `casesWithArrival 309,
casesWithRefund 220, casesWithBoth 143` (non-vacuous).
Prover surface (20,000 cases): `fixtureMismatch 0, fuzzVerdictMismatch 0, nodeMismatch 0,
witnessChecked 12892, witnessIllegal 0, witnessNotRemoved 0, cutoffCases 278,
**clockFixtureOk true**`.
Gate-preservation surface (1,200,000 actions, 16,122 games): `mismatches 0,
incrementalProverVerdictMismatches 0, incrementalProverNodeMismatches 0,
freshPackProverMismatches 0, proverOrderInvarianceViolations 0, gateProverCapHits 0`;
non-vacuity `proofsCompared 44534, incrementalProverCases 1262754, proverOrderPermuted 130272,
proverMaxNodes 182 of 20000`; its own walk produced 819 inactivity draws, so it reaches clock
20 too.

Note that the clock-coverage clause is now a FAILURE condition in `lab/hard-ai/fuzz/run.ts` and
in the M5 row of `lab/hard-ai/verify/gates.ts`: a run of >= 4,000 actions that never reaches the
limit, or never produces an inactivity draw, fails rather than reporting green.

---

# Pins that moved, and pins that deliberately did not

Every pin below encodes THIS rule's number and legitimately moves to 20. They are listed per
lane; the pins listed as not moved are about other things and were left alone on purpose.

## Canonical engine, UI and documentation

- `src/game/inactivity.ts`: `INACTIVITY_LIMIT` 10 -> 20; `INACTIVITY_WARNING` 7 -> 17; NEW
  `LEGACY_INACTIVITY_LIMIT = 10` (archive replay only, not a live pin).
- `tests/game/turn.test.ts`: three cases re-expressed against the constant (seed 9 -> `LIMIT-1`,
  expected 10 -> `LIMIT`, "draws at exactly ten" -> "twenty", 8 -> `LIMIT-2`).
- `tests/game/upkeep-draw.test.ts`: the describe "10 completed quiet turns" -> "20"; five seeds
  9 -> `LIMIT-1`; "draws exactly at 10" -> "at 20"; the lab rule-off case 12 -> `LIMIT+2` (kept
  strictly above the limit so it still proves something); the expired-save case 12 -> `LIMIT+2`;
  the pending-choice clock 7 -> `INACTIVITY_WARNING` (keeping its meaning: inside the band).
- `tests/game/phasing.test.ts`, `tests/hooks/pass-play.test.ts`,
  `tests/hooks/upkeep-undo.test.ts`, `tests/hooks/local-history.test.tsx`: seeds 9 -> `LIMIT-1`.
- `e2e/action-budget.spec.ts` (title "draw at ten" -> "at twenty", loop 10 -> `LIMIT`, strings
  interpolated), `e2e/upkeep-draw.spec.ts` (base clock 7 -> `WARNING`, seed 9 -> `LIMIT-1`),
  `e2e/analysis.spec.ts` (seed 9 -> `LIMIT-1`).
- `academy/export-rules.ts`: draw demonstration seed 9 -> `LIMIT-1`, "tenth quiet turn"
  interpolated; `academy/rules-verification.json` regenerated — the single R09 line.
- NOT MOVED: `tests/game/upkeep-draw.test.ts`'s "chip damage does not reset" and "upkeep removal,
  promotion and placement do not reset progress" keep their literal 7 — they assert NON-RESET,
  not a threshold, and re-pinning them would quietly change what they test.
  `tests/game/action-budget.test.ts`'s `inactivityPlies: 9` is a schema-5 legacy-migration
  fixture whose expectation is a clock restarted at 0 regardless of the limit.

## Hard replica

- `tests/ai/hard/constants.test.ts`: 10 -> 20 and 7 -> 17 — the pin the whole change hangs off.
  Two assertions ADDED, none removed: the three-ply margin, and that the replica's
  `MAX_CLOCK`/`INACTIVITY_LIMIT`/`INACTIVITY_WARNING` EQUAL the canonical exports, so a future
  fork of the constant fails loudly.
- `tests/ai/hard/zobrist.test.ts`: clock plane 11 -> 21 keys. The single FILL_ORDER test was
  replaced by FOUR stronger ones (segment-wise redraw; a pre-A4 legacy-order redraw proving clock
  keys 0..10 are word-identical; distinctness and non-zero-ness of all 21 keys; `Kpos` separating
  all 21 values). Nothing weakened.
- `tests/ai/hard/eval.test.ts`: `DrawPressure` 81 (clock 9 of 10) -> 90 (clock 19 of 20),
  restated as the formula rather than a bare number, plus new assertions for full scale 100 at
  the draw, 0 at clock 0, monotonicity and the <= 100 bound.
- `tests/ai/hard/invariants.test.ts` (threshold 7 -> `WARNING`, "one below" 6 -> `WARNING-1`,
  plus `LIMIT - WARNING === 3`), `terminal-order.test.ts` (helper `clock9` renamed
  `atLastQuietPly`, six pins moved), `prover.test.ts`, `phasing-macro.test.ts`,
  `phasing-invariants.test.ts`, `phasing-economy.test.ts` (one fixture each, 9 -> `LIMIT-1`).
- `lab/hard-ai/fuzz/prover-surface.ts` `clockFixture` 9 -> `LIMIT-1` (this is the fixture the
  M10 gate's `clockFixtureOk` criterion reads).
- `lab/hard-ai/perft/fixtures.json`: `arrival-and-refund` ONLY — digest a0ecef98 -> dea9d2d6,
  counts 121 -> 141.
- `lab/hard-ai/perft/phasing-fixtures.ts`: `arrival-and-refund`'s clock guard 7 -> `LIMIT - 3`.
- `src/ai/hard/book/probe.ts`: book `rulesKey` -> `muju-phasing-2`, plus `INACTIVITY_LIMIT` in
  the descriptor. This deliberately invalidates every phasing-1 book against this build.
- NOT MOVED: perft `upkeep-review-pending` digest 2fb1fec9 / counts 424/425/424 (recovered by
  tightening its predicate, not re-frozen); the other five perft fixtures; the Standard initial
  triple 14959/1053/797; the Phasing initial 14959/1850/797; `ZOBRIST_SEED 0x4d554a55`; every
  non-clock Zobrist plane size; `PROOF_NODES 20000`; prover `fixtureCases 28`;
  `MAX_BLACK_CRYSTAL_HANDICAP 20`; `ACTIONS_PER_TURN 4`; tier-1 prices [3,3,4,4,5,5];
  `INITIAL_MAP_RESOURCES 504`; `MAX_RESOURCE_RESERVE 16`; every eval weight including
  `DEFAULT_WEIGHTS.w[F.DrawPressure] = 0`.

## Server, MCP and saves

- `server/analysis/index.ts:249` headline draw pair; `server/observation.ts:178,191` both
  rulesets' victory text; `server/rooms.ts:23` `RULES_VERSION` `muju-online-4` -> `muju-online-6`
  (5 reserved by T5) and the new-Phasing-room version -> `muju-phasing-2`;
  `src/online/OnlineLobby.tsx:151`; `src/utils/persistence.ts:9` `SCHEMA_VERSION` 7 -> 8.
- `tests/server/rooms.test.ts` (draw loop 1..10 -> 1..`LIMIT`, "ten" -> "twenty"),
  `tests/server/clock-pressure.test.ts` (loop and `completedTurns` 5/4 -> `LIMIT/2` and
  `LIMIT/2-1`), `tests/server/analysis.test.ts` and `phasing-analysis.test.ts` (seeds 9 ->
  `LIMIT-1`), `tests/ai/phasing.test.ts`, `tests/ai/upkeep-clock.test.ts` (seed 9 -> `LIMIT-1`,
  harness `maxInactivityPlies` 10 -> `LIMIT`, turns 5 -> `LIMIT/2`).
- Cross-lane consequences of the save bump: `tests/game/tier3-cap.test.ts:51`
  `SCHEMA_VERSION` 7 -> 8, and `tests/game/action-budget.test.ts:52`'s written-back literal 7
  replaced by `SCHEMA_VERSION` (strengthened rather than re-pinned).
- NOT MOVED: `tests/game/resource-map.test.ts`'s schema-5/6 resume matrix (it never asserts the
  clock) and the `muju-online-2/3` in-place upgrade expectations (that migration already zeroed
  the clock).

## Harness and ladder

- `tests/lab/baseline-identity.test.ts`: `LADDER_RULES_VERSION` -> `muju-phasing-2`, and
  STRENGTHENED to assert it equals BOTH `RULES_VERSION` and `HARNESS_RULES_VERSION`, pinning the
  single source of truth as single.
- `tests/lab/phasing-harness.test.ts`: the quiet-game draw boundary derived from the constant
  (controls 10 -> `LIMIT`, plies 20 -> `2*LIMIT`), plus three new assertions.
- `tests/lab/analyze-phasing.test.ts`: `record.rulesVersion` -> `muju-phasing-2`.
- `tests/lab/ablate.test.ts`: `DESKTOP_WALL3000_HASH` and two configHashes. **Attribution:**
  these were not pasted on faith — forcing `rulesVersion` back to `muju-phasing-1` on today's
  resolved configuration reproduces each superseded pin byte for byte, so the revision is the
  SOLE cause and no concurrent `src/ai/hard/**` edit contributed. The phasing-1 values are kept
  in comments per that file's convention, and a new `DESKTOP_WALL3000_HASH_PHASING_1` is
  asserted DIFFERENT from the live hash.
- NOT MOVED: p1-dev/p1-val row counts, byte sizes and sha256 in `ALLOCATION-P1.md` (re-verified
  unchanged); the sealed book's hash; the whole `p1-scripted-2026-09-18` directory;
  `phasing-evidence.test.ts`'s expectation that the 2026-09-18 records are `muju-phasing-1`;
  `DESKTOP_WALL3000_HASH_STANDARD` and `DESKTOP_WALL3000_HASH_PHASING_M4`.

---

# Unresolved, and who owns it

1. **R09 "The Ten Quiet Turns" needs re-narration** — title, three narration lines and the recap
   line (including "five complete rounds" and "amber at seven"). Inventoried verbatim in
   `academy/STATUS.md`; blocked on owner speech authorization and the offline media bundle. R10
   was checked and needs nothing. The prepared course notice carries the correction meanwhile.
2. **No release target was built, packaged, published or verified live** — Pages, the Render
   Node/MCP host and the Academy all remain prepared-not-deployed, and Pages publishing has been
   paused since 2026-09-18 for want of credentials.
3. **Playwright was not run.** The three clock-driving e2e specs were updated but not executed,
   and `npm run test:online:e2e` was not run either; both need a built app
   (`npm run build`, whose `prebuild` rewrites the tracked `src/ai/wasm/tactics.wasm`) and sit
   outside this run's command budget. `e2e/action-budget.spec.ts` now drives twenty End-turn
   clicks instead of ten and should be TIMED on its first real run.
4. **Every strength claim is still void per A4** and was not re-run: Gate 1 rows A1/A2 and both
   pilots, the M5 v1 suite measurement, the ladder, and the full `npm run hard:verify` chain.
5. **`lab/hard-ai/positions/fuzz-1000.jsonl` was not re-sampled**, so the reference audit's
   `draw` set holds no position that is imminent under the new limit (its rows stop at clock 9
   of 20). Documented in code at `ARCHIVED_DRAW_CLOCK_FLOOR`; the fuzz clock gate covers 11..20
   instead. Re-sampling is a deliberate archive rotation, not a fix to slip into this change.
6. **`academy/rules-verification.json` still says `"rules": "v2.9"`** while verifying a v3.0
   rule. Nothing reads it; choosing its new value is the Academy release lane's call.
7. **Archived online rooms at retired revisions become unopenable**, not just unplayable — a
   deliberate consequence of following the existing `RULES_CHANGED` path rather than inventing a
   read-only archive. Read-only access for retired rules belongs to T5
   (`codex/phasing-only-canonical`), which also reserves `muju-online-5`.
8. **Four `tests/ai/hard` files remain quarantined** by `vitest.config.ts`
   (`p6-stoppable-generation`, `p8-rescue-cap`, `eval-correct`, `approach-tie`), as does
   `tests/lab/reference.test.ts`. Pre-existing and unrelated: confirmed by grep that none
   contains a clock pin. The quarantine list was not touched.

---

# Proposed commit split

Six commits, one per lane plus the seams and this record. **Read this first: only the SIXTH
commit is green repo-wide.** The change flips a single shared constant that four subsystems
read, so no intermediate commit can pass the full suite and `git bisect` over this series is
not meaningful. The split is for REVIEWABILITY. Each commit below names what it is green on by
itself.

## 1. Canonical engine, browser copy and rules documentation

Green by itself on `npx vitest run tests/game tests/hooks` (40 files / 556 tests) **except**
`tests/game/tier3-cap.test.ts` and `tests/game/action-budget.test.ts`, which pin the save schema
and land with commit 3.

```
muju/src/game/inactivity.ts
muju/src/components/InstructionsModal.tsx
muju/src/components/ModeSelect.tsx
muju/tests/game/turn.test.ts
muju/tests/game/upkeep-draw.test.ts
muju/tests/game/phasing.test.ts
muju/tests/hooks/pass-play.test.ts
muju/tests/hooks/upkeep-undo.test.ts
muju/tests/hooks/local-history.test.tsx
muju/e2e/action-budget.spec.ts
muju/e2e/upkeep-draw.spec.ts
muju/e2e/analysis.spec.ts
muju/SPEC.md
muju/JUDGMENT_LOG.md
muju/AI_ENGINE_README.md
muju/docs/AI_IMPLEMENTATION_STATUS.md
muju/docs/PHASING-2026-09-16.md
muju/docs/STRATEGY_GUIDE-2026-09-12.md
muju/docs/strategy-guide-codex-vs-claude.md
muju/docs/DRAW_TEN-2026-09-08.md
muju/docs/UPKEEP_DRAW-2026-09-08.md
docs/game-design-dossier.md
muju/academy/export-rules.ts
muju/academy/rules-verification.json
muju/academy/STATUS.md
muju/academy/build-release.py
muju/academy/verify-live.py
```

```
Draw clock: twenty quiet plies, warning at seventeen (muju-phasing-2)

The inactivity draw fires after 20 plies instead of 10, and the in-game warning
keeps its three-ply margin at 17. What resets the clock does not change: only an
attack that removes a unit. Standard and Phasing keep sharing the one constant;
forking it would only be a second number to forget.

resolveInactivityDraw takes an optional limit defaulting to the live constant,
and LEGACY_INACTIVITY_LIMIT = 10 is exported beside it, so an archived
muju-phasing-1 record can be adjudicated under the limit it was recorded with
without anyone editing the archive.

Every player-facing statement now interpolates the constant rather than spelling
the number out, and every test that had written "one ply before the draw" as a
literal 9 now derives it from INACTIVITY_LIMIT. SPEC is at v3.0 and J-021
records the decision, what was deliberately not changed, and the reversal cost.
Dated guides get supersession notes rather than rewrites.

academy/export-rules.ts asserted a draw from inactivityPlies: 9 and would have
failed, blocking the whole Academy export; it now derives from the constant and
regenerates exactly one demonstration line.
```

## 2. Hard replica and its lab

Green by itself on `npm run hard:types && npm run hard:deps && npx vitest run tests/ai/hard`
once commit 1 is in.

```
muju/src/ai/hard/core/state.ts
muju/src/ai/hard/core/zobrist.ts
muju/src/ai/hard/types.ts
muju/src/ai/hard/config.ts
muju/src/ai/hard/book/probe.ts
muju/src/ai/hard/eval/features.ts
muju/src/ai/hard/eval/invariants.ts
muju/lab/hard-ai/fuzz/differential.ts
muju/lab/hard-ai/fuzz/run.ts
muju/lab/hard-ai/fuzz/prover-surface.ts
muju/lab/hard-ai/perft/phasing-fixtures.ts
muju/lab/hard-ai/perft/fixtures.json
muju/lab/hard-ai/verify/gates.ts
muju/lab/hard-ai/verify/reference.ts
muju/lab/hard-ai/audit/inv-pairs.ts
muju/tests/ai/hard/constants.test.ts
muju/tests/ai/hard/zobrist.test.ts
muju/tests/ai/hard/eval.test.ts
muju/tests/ai/hard/invariants.test.ts
muju/tests/ai/hard/terminal-order.test.ts
muju/tests/ai/hard/prover.test.ts
muju/tests/ai/hard/phasing-macro.test.ts
muju/tests/ai/hard/phasing-invariants.test.ts
muju/tests/ai/hard/phasing-economy.test.ts
muju/lab/results/hard-ai-verify/perft.json
muju/lab/results/hard-ai-verify/fuzz-walk.json
muju/lab/results/hard-ai-verify/fuzz-prover-arrival.json
muju/lab/results/hard-ai-verify/fuzz-gate.json
```

```
Hard replica: follow the canonical clock instead of restating it

core/state.ts re-exports INACTIVITY_LIMIT and INACTIVITY_WARNING from
src/game/inactivity.ts, so the replica can no longer hold a second opinion about
this number, and a new constants test asserts the equality so a future fork
fails loudly.

The Zobrist clock plane grows from 11 keys to 21 APPEND-ONLY: the frozen 11-key
prefix stays where it was in the RNG stream and the ten new keys are drawn last,
after pend and progress. A test redraws the stream in the exact pre-A4 order and
proves clock keys 0..10 and every other plane are word-for-word identical, so
every position with clock <= 10 keeps its muju-phasing-1 hash.

DrawPressure was raw clock^2, which maxed at 100 under a ten-ply limit and would
have silently quadrupled to 400 against an unchanged coefficient. It is now
trunc(clock^2 * 100 / LIMIT^2): identical value-for-value at limit 10, still
0..100 at limit 20, and no weight changed.

The book descriptor carries the revision AND the limit, so a phasing-1 book
falls through to search rather than scoring under a clock it was not built for.

hard:fuzz gains --quiet-games (default 0.2) and makes insufficient clock
coverage a FAILURE: the default walker weights ATTACK at 22, so an ordinary walk
of any length would have stayed in the low clock values and looked identical at
limit 10 or 20. One perft fixture (arrival-and-refund) is re-frozen after both
engines agreed on the new numbers; upkeep-review-pending was recovered at its
original digest by tightening a predicate that had started matching a degenerate
two-unit endgame.
```

## 3. Server, MCP, saves and agent guides

Green by itself on `npm run server:types` and
`npx vitest run tests/server tests/ai --exclude 'tests/ai/hard/**'` plus
`tests/save-rules-revision.test.ts`.

```
muju/server/rooms.ts
muju/server/observation.ts
muju/server/analysis/index.ts
muju/src/online/OnlineLobby.tsx
muju/src/utils/persistence.ts
muju/public/skills/muju-hono-tanka/SKILL.md
muju/ONLINE.md
muju/docs/MCP_TOOL_TAPS.md
muju/docs/ANALYSIS_TOOLS.md
muju/tests/server/draw-clock-statements.test.ts
muju/tests/save-rules-revision.test.ts
muju/tests/server/rooms.test.ts
muju/tests/server/analysis.test.ts
muju/tests/server/phasing-analysis.test.ts
muju/tests/server/clock-pressure.test.ts
muju/tests/ai/phasing.test.ts
muju/tests/ai/upkeep-clock.test.ts
muju/tests/game/action-budget.test.ts
muju/tests/game/tier3-cap.test.ts
```

```
Online and saved games get new rules identities for the twenty-ply clock

No stored game may be replayed under a clock its players did not agree to.

Rooms: Standard advances muju-online-4 -> muju-online-6 and Phasing
muju-phasing-1 -> muju-phasing-2. Five is skipped because the unmerged T5 branch
codex/phasing-only-canonical already reserves muju-online-5; that is recorded in
rooms.ts and ONLINE.md so T5 sees it on rebase. Rooms at retired versions take
the existing path unchanged: 409 RULES_CHANGED on every call, row untouched in
SQLite, dropped from the active lobby, still summarised in the archived list.

Saves: SCHEMA_VERSION 7 -> 8, which is what makes an old save distinguishable at
all. A pre-v8 save is adjudicated once under the pinned LEGACY_INACTIVITY_LIMIT,
so a game that had already drawn keeps that result; a still-playing position
resumes with its quiet clock restarted rather than silently gaining ten extra
plies. That is the same choice migrateLegacyGame made last time this clock's
reset rule changed.

The analysis headline's draw pair was the one remaining bare literal and would
have reported the wrong denominator to every MCP client. The muju_rules victory
texts are now template strings over the constant, and a new test reads SKILL.md,
ONLINE.md, MCP_TOOL_TAPS.md, ANALYSIS_TOOLS.md and OnlineLobby.tsx off disk to
prove the published text states twenty and carries no ten-ply claim.
```

## 4. Harness, ladder and the new scripted reference

Green by itself on both lab typechecks and `npx vitest run tests/lab` **except** the two
`suites-phasing-*` files, which commit 5 fixes.

```
muju/lab/harness/types.ts
muju/lab/harness/runner.ts
muju/lab/harness/phasing-round-robin.ts
muju/lab/hard-ai/ladder/openings/phasing.ts
muju/lab/hard-ai/ladder/ruleset.ts
muju/lab/hard-ai/ladder/run.ts
muju/lab/hard-ai/ladder/openings/ALLOCATION-P1.md
muju/tests/lab/phasing-evidence.test.ts
muju/tests/lab/openings-p1.test.ts
muju/tests/lab/phasing-harness.test.ts
muju/tests/lab/baseline-identity.test.ts
muju/tests/lab/ladder-runner.test.ts
muju/tests/lab/ablate.test.ts
muju/tests/lab/analyze-phasing.test.ts
muju/lab/harness/results/p2-scripted-2026-09-19/
```

```
Harness: one rules revision, no cross-revision pooling, and a new reference

HARNESS_RULES_VERSION is now the single definition of 'muju-phasing-2'; the
literal used to be hand-written in four places, which is why it is one constant
re-exported by the ladder and the P1 openings now.

rulesetForRevision still answers 'phasing' for muju-phasing-1, because the
analyst must be able to open A1/A2 evidence. assertPoolableRevision answers the
separate question and refuses anything that is not the current revision. That
gap was real: when Phasing replaced Standard the two populations were kept apart
mostly by ACCIDENT, because their opening books were different files and every
guard compared openings.sha256. phasing-2 changed one constant and nothing else
-- same bots, same engines, same p1-dev.jsonl down to the byte -- so a --resume
into a phasing-1 directory would have merged two populations into one Elo and
one SPRT in silence. run.ts now compares rules.rulesVersion first.

A4's claim about the opening books is verified by replay, not trusted: all 160
replays (48 dev + 32 val, at h0 and h3) end with inactivityPlies of exactly 1,
six below the old warning and nineteen below the new limit, so no opening sits
near either boundary and nothing needed regenerating.

p2-scripted-2026-09-19 is the first measurement of what the rule does: 840
games, same bots, same seed, same derivation, 0 illegal actions, 0 invariant
failures. Inactivity draws fall from 416/840 (49.52%) to 227/840 (27.02%). It is
a true paired comparison -- 424 games identical, 189 that drew under 10 plies
resolved under 20, ZERO the other way -- and 189 is exactly the fall in the
count. The 2026-09-18 directory is untouched and its SHA256SUMS still verify.
```

## 5. Integration seams

The commit that makes the tree green.

```
muju/lab/hard-ai/suites/phasing/build-invariants.ts
muju/lab/hard-ai/bench/profile.ts
muju/tests/ai/hard/cross-engine-draw-clock.test.ts
muju/lab/results/hard-ai-verify/fuzz-integration-2026-09-19.json
```

```
Integration: fix the two cross-lane seams and prove the engines agree

inv16-clock-discipline authored its violating member at clock 9 and expected the
draw after one passive handoff. At twenty plies that position is still playing,
which failed the invariant pair AND the six-family bundle runner -- two failed
test files from one authored number. The premise now derives from
INACTIVITY_LIMIT. The motif, the diagram, the correct member and both probes are
untouched, and nothing under suites/phasing/fixtures/v1 was edited. This case
belongs to the Phasing-suites workflow; the edit is deliberately the minimal
mechanical one so that branch merges cleanly.

bench/profile.ts decided a record's rule set with
`rulesVersion === LADDER_RULES_VERSION ? 'phasing' : 'standard'`. With
LADDER_RULES_VERSION now muju-phasing-2, a muju-phasing-1 record fell to the
else and reconstructed as STANDARD -- silently, because the two rule sets share
every action type. It asks rulesetForRevision now, which answers 'phasing' for
both and throws on an unknown revision instead of guessing.

The new test is the one that would have caught a split between the four readers
of this clock: it walks a scripted quiet game ply by ply through canonical
applyAction AND the Hard replica's make(), asserting at every ply that the two
digests agree, that no ply in 1..19 is a draw, that ply 20 is, that the V2
terminal oracle agrees, and that the host observation's quietTurns and
drawAtQuietTurns report the same plies remaining. The replica then unwinds all
twenty plies back to a byte-identical start digest.
```

## 6. The DAG work record

```
muju/docs/changes/2026-09-19-draw-clock-20.md
```

```
Record the twenty-ply draw clock against the content DAG

Every one of the 27 nodes carries a disposition with evidence: changed, verified
unchanged, or blocked. Release targets are prepared, not deployed -- this branch
is not master, nothing was built or published, and the Academy media bundle was
never fetched. Academy notice text is prepared; R09 re-narration is pending
owner speech authorization and R10 needs nothing.

The record also carries the old-vs-new scripted reference numbers, the exact
pins that moved and the pins that deliberately did not, and eight unresolved
items with their owners.
```
