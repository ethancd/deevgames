# Playlist and continuous full mixes

The listening page is http://127.0.0.1:8766/playlist/ . It contains only completed full-length mixes, now nine tracks: six element themes plus the selected lobby, match and rematch pieces. One audio element plays tracks in sequence, with Previous/Next, native seek and volume controls, level matching, downloads, and mutually exclusive Repeat track / Repeat disc toggles. Repeat disc is on by default; neither mode autoplays on initial page load. Settings are stored locally. The mixes-and-notes page remains separate.

The first five full versions had preserved the audition endings, including their fade-outs, followed by quieter restarts. Those are superseded in the player by v2 blended mixes. Original v1 files are retained. The local edits overlap active phrases with equal-power crossfades, using measured rhythmic attacks for Fire, Plant, Shadow and Metal, and a four-second sustained overlap for Water. The rest of the PCM is retained before MP3 encoding, apart from a -0.0115 dB headroom adjustment to Shadow. Removing the fades and redundant starts shortens the mixes to 3:14–3:21.

Electricity uses the explicitly accepted 19D and is 3:30. It retains only the active first 37.5 seconds before generating the continuation. The source fade begins later, so it is excluded. The request keeps the woodwind-free plucked-string/electric palette, charged pauses and answering hook.

All 36 composition joins across the nine full mixes were checked using stereo RMS at 100 ms resolution within three seconds either side of each join. No checked interval fell below -45 dB RMS. Decode, durations, source hashes and player-file hashes also passed. These are technical continuity checks, not a claim of by-ear musical review. Earlier five-second checks missed the original gap and are superseded for this issue.

Playlist verification covers ordinary advance, stopping at disc end without repeat, repeat-disc wraparound, repeat-track behavior and a one-track disc. The actual page toggles were checked for mutual exclusion and restored to Repeat disc. Audio loaded without an error, and the user's active playback was left running.

The earlier non-element favorites are 03C Open Room, Lantern On; 04C Unequal Routes; and 05B Another Game Before Dawn. Three 3:30 continuation plans are prepared, each retaining an active opening before its fade. Those older files were not stored for inpainting and required three reference uploads. Automatic approval review blocked that batch, requiring explicit permission for the uploads. The user explicitly approved the three uploads and extensions. All three uploads and 3:30 generations completed successfully; their source and generation receipts are retained. Estimated additional cost: $1.575 for generation plus $0.3375 for references, at prior rates. The 05C vocal alternate remains a liked alternate, not the selected closer.


The final disc is 30:32 long, ordered: Open Room, Lantern On; Hono; Umeme; Muju; Tanka; Straumr; Gölge; Unequal Routes; Another Game Before Dawn. All source files referenced by the playlist exist and all selected full mixes are at least three minutes. The nine-track page was checked in a fresh browser tab; the user's existing active player was left uninterrupted. The active narrow browser panel also had no horizontal overflow (304px client and scroll widths).

The three new non-element joins and all their subsequent composition boundaries stayed above -45 dB RMS in every 100ms check window, with minimum levels near -18.7 to -22.8 dB. No automatic retries were needed. Incremental estimated paid usage for this turn was $0.525 for Umeme and $1.9125 for the three additional tracks and reference uploads; the five blend repairs and playlist were local work.

## Seeking repair and Tanka middle revision

The old Python `http.server` returned the entire MP3 with HTTP 200 even when the browser requested a byte range. That made the scrubber reset. `python3 -u muju/tools/serve-music.py` now serves the same output directory on localhost:8766 with correct HTTP 206 ranges, lengths and seekable media metadata. Use this command for future restarts. Regression checks cover exact middle/tail byte responses, suffix and open-ended ranges, invalid ranges, HEAD, full GET and If-Range. Browser seeks to 1:30, 2:30 and back to 1:00 stayed at the requested position while paused. Audio URLs include a cache-busting query so old whole-file responses are not reused. The playlist also has a Jump to time form; existing favorites and note IDs are retained.

The user located Tanka's issue at 1:59–2:28 and clarified that the composition loses too much energy, returning to the very quiet opening. A single focused ElevenLabs inpaint requests sustained bass and bronze accompaniment beneath the original wooden flute, with a low-medium energy floor. It uses the already-stored full source, with no new uploads. The 33-second replacement includes two-second edges at 1:57–1:59 and 2:28–2:30; complementary raised-cosine blends insert it into the existing full mix. The previous opening repair and PCM outside 1:57–2:30 are retained before MP3 encoding.

The playlist and full-version player now use `tanka-flute-full-v3-low-mid.mp3`, still 3:18. Its original v1 and v2 files remain available. The revised passage button starts at 1:54 for context. New edge checks show no silence at 100 ms resolution, the MP3 decodes without clipping, and the measured 2:20–2:28 section rises from -23.67 to -22.40 dB RMS; its surrounding phrases remain at their previous levels. These checks establish technical continuity and level, not subjective musical quality. Listening review remains with the user. Estimated additional usage is $0.1725 at the prior full-output rate, with one successful request and no retries. The full prompt, provenance and measurements are recorded in `SOUNDTRACK_TANKA_MIDDLE_REVISION-2026-09-12.json` and the v3 receipts.
