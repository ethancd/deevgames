# Reader reports behind HANDOFF-2026-09-21.md (raw evidence)

Nine read-only verification agents plus one critic, run 2026-09-21 against master `eda73b2`. This is the raw output, kept so the next agent can see the evidence behind the addendum without re-reading the tree. Where a reader and the addendum disagree, the addendum wins: the critic section at the end adjudicates every contradiction found (line ranges off by two in the `rush` and `evidence` reports, the 43 non-zero count, the three-row `p1-val` count, and so on). Nothing here was edited by hand.

```text
====================================================================================================
ITEM pins  STATUS not-started  EFFORT hours
SUMMARY: The handoff (step 1) says the tests that pin the bootstrap must be updated and names eval.test.ts:155,217,297 plus "suite manifests / freeze artifacts that pin the label". At HEAD (eda73b2) no file under muju/tests has changed since the handoff base a02bbb7 (git diff a02bbb7..HEAD --stat -- muju/tests is empty), while 71b41a3 made DEFAULT_WEIGHTS = makeWeights('phasing-hand-priors-v1') with exactly 43 non-zero entries (not ~45: BankExcess is set to 100 at weights.ts:24 and overwritten to 25 at :31). The default-selected vitest run therefore now fails in four files: tests/ai/hard/eval.test.ts (:155, :217, :298-301, :304, :345), tests/ai/hard/phasing-bootstrap.test.ts (:57-58), tests/lab/phasing-tune-guards.test.ts (:146-148, because lab/hard-ai/tune/texel.ts:82 pins w[3]=100 and HEAD has 25), and tests/lab/ablate.test.ts (the pinned champion/arm resolved-config hashes at :198/:499/:532 move because the ladder identity hashes DEFAULT_WEIGHTS w+material+version+label via bots/hard.ts:296-300 and identity.ts:344-354/406-408; the live-weight counts at :718-734, the A/A arm list at :755-778 and the all-zero safety assertion at :808 are all now wrong). No suite manifest pins the label; the weights identity is pinned by lab/hard-ai/suites/phasing/fixtures/v2/floor-contract.json:7-8 (weightsSha256 1addc7d6… = weightIdentity of the bootstrap, label excluded) and recorded historically in the hash-chained measurement-ledger.jsonl, the v2-measure-2 results, the docs/changes/m6-evaluation-2026-09-19 freeze bundle, M6-BOOTSTRAP-CONTRACT.md:86-96,106 and M5-FLOOR-PREREGISTRATION-v2.md:912-921, none of which may be edited (a new contract with `supersedes` and a new dated amendment are the paths). No e2e spec under muju/e2e asserts a weights label (muju/tests/e2e does not exist), and the worker protocol never carries one. Separately, the four SCRATCH arms b2a85e6 added at arms.ts:953-984 clone DEFAULT_WEIGHTS, so after 71b41a3 `bootstrap-pc` is no longer the bootstrap and `hand-priors`/`bank25-pc` are byte-identical to the champion.
REMAINING:
  - Decide the accounting pin before editing tests: either change lab/hard-ai/tune/texel.ts:82 ACCOUNTING_PINS to {2:100, 3:25, 58:1} and tests/lab/phasing-tune-guards.test.ts:146 to match, and record the deviation from docs/hard-ai/phasing/M6-BOOTSTRAP-CONTRACT.md:86-96,106 in a NEW dated contract/amendment document under docs/hard-ai/phasing/ (never edit the M6 contract), or restore w[3]=100 in src/ai/hard/eval/weights.ts:31 (the handoff says the bank discount is the master switch, so this needs a measured decision).
  - Decide WEIGHTS_VERSION in src/ai/hard/eval/weights.ts:15: keep 2 and rewrite the :14 comment to state why a hand-prior change does not bump it, or bump to 3 and update lab/hard-ai/tune/rows.ts:178, tests/lab/phasing-tune-guards.test.ts:18,22,36,140,145, regenerate the 33 files in docs/hard-ai/phasing/repair-2026-09-20/weights/ and note the book/abi consequence.
  - Update muju/tests/ai/hard/eval.test.ts: :155 to -4000, :217 to -8, :298-301 to the 43-pair list [[0,100],[2,100],[3,25],[4,-4000],[7,30],[8,8],[9,-800],[10,25],[11,90],[12,-60],[13,-400],[14,-180],[15,220],[16,90],[17,-20],[18,-8],[19,40],[23,100],[28,-50],[30,-25],[31,-10],[32,20],[33,35],[34,-40],[35,-120],[36,-150],[37,300],[38,-800],[39,-300],[40,-250],[41,-100],[43,-120],[44,-600],[45,-150],[46,-150],[47,-400],[49,-40],[50,-200],[51,-200],[53,-200],[56,-150],[57,-250],[58,1]], :304 to [-800,-300,-250,-100,0,-120,-600,-150,-150,-400,0,-40,-200,-200,0,-200,0,0,-150,-250], :345 to 30, and rewrite the describe title at :296 and header at :5-11 that still describe a sparse bootstrap.
  - Update muju/tests/ai/hard/phasing-bootstrap.test.ts:57-58 to the same 43-pair list (or to assert the accounting slots 0/2/23/58 plus 59-61 zero separately) and rename the describe at :45.
  - Update muju/tests/lab/ablate.test.ts:718-734 live counts to economy 6, home 10, safety 17, space 9, invariants 15, stage2 26; :755-778 to vacuous = [] and all ten ALL_WEIGHT_ARMS biting; :808 to assert the four remaining zero safety indices (29, 54, 59, 60) and 17 live; and rewrite the comment blocks at :696-716 and :802-807.
  - Run `npx vitest run tests/lab/ablate.test.ts` once (after another process releases node_modules) to read the new hard@desktop wall:3000 hash and the search-iter-fit / search-reach-cache hashes, then record them at tests/lab/ablate.test.ts:198, :499 and :532 as a fourth documented move, keeping '4464b191…', 'a18b84f8…' and '1aef0a8b…' as superseded constants/comments per the file's convention at :170-208.
  - Fix stale prose that now misdescribes HEAD: src/ai/hard/eval/weights.ts:1-6 and :58-61 header, lab/hard-ai/ablate/arms.ts:258-272 label example, and vitest.config.ts:62-79 quarantine reasons/counts; then run tests/ai/hard/eval-correct.test.ts and tests/lab/recall.test.ts under MUJU_RUN_QUARANTINE=1 to see whether either can leave the list.
  - Repair the four SCRATCH arms at lab/hard-ai/ablate/arms.ts:953-984: build them from the frozen bootstrap literal (docs/hard-ai/phasing/repair-2026-09-20/weights/bootstrap-control.json values) instead of cloneWeights(DEFAULT_WEIGHTS), drop or rename the '+pc' labels now that src/ai/hard/engine.ts:357-363 is unconditional, and correct the change strings (bootstrap-pc currently equals hand priors with BankExcess 100).
  - Write a new floor contract for the v2 manifest (new file, e.g. lab/hard-ai/suites/phasing/fixtures/v2/floor-contract-supersedes-1.json or a v3 directory) carrying weightIdentity(new DEFAULT_WEIGHTS), hashJson(engineSourceHashes()) at the final source state and supersedes {ledgerSeq: 1, chain: <measurement-ledger.jsonl line 1 chain>}, committed ALONE before any measurement; do not touch fixtures/v2/floor-contract.json, measurement-ledger.jsonl or any results/ file.
  - Append a dated addendum to docs/hard-ai/phasing/repair-2026-09-20/HANDOFF.md (or a new status document) stating that the production default moved from phasing-accounting-bootstrap-v1 to phasing-hand-priors-v1 at 71b41a3, that reports/understand/*.md describe the pre-repair tree, and how to reproduce the 'scorer fix only' row now (MUJU_HARD_WEIGHTS=weights/bootstrap-control.json rather than hard@ablate:bootstrap-pc).
  - Walk `python3 tools/muju-content-dag.py plan --kind ai` nodes hard-ai and ai-strength with evidence: the planner requires bumping weightsVersion and the ladder identity hash when weights change and appending a new dated release record rather than editing an old one.
RISKS:
  - The three moved hash literals (tests/lab/ablate.test.ts:198, :499, :532) cannot be computed without running node; they must be read from a test run and recorded as a documented identity move, not pinned to fit (M6-STATUS.md:137 and the DAG's hard-ai node both say so).
  - Changing tests/ai/hard/phasing-bootstrap.test.ts:57-58 and texel.ts:82 without a recorded amendment silently retires the coordinator-approved M6 contract (M6-BOOTSTRAP-CONTRACT.md:1,106); the deviation must be written down before outcomes are claimed.
  - Bumping WEIGHTS_VERSION cascades beyond the handoff's 'invalidates every JSON in weights/': lab/hard-ai/tune/rows.ts:178 hard-codes 2, phasing-tune-guards.test.ts pins 2 in five places, loadWeights (weights.ts:112) rejects the 33 repair vectors, and the floor contract abi records weightsVersion.
  - Editing fixtures/v2/floor-contract.json, measurement-ledger.jsonl, any results/ file, the docs/changes/m6-evaluation-2026-09-19 bundle, M5-FLOOR-PREREGISTRATION-v2.md or M6-BOOTSTRAP-CONTRACT.md would break the blob/chain checks in measure.ts:118-146,266-283 and the DAG rule against editing preregistrations; hard:suite:phasing:measure will refuse (measure.ts:366) until a superseding contract exists.
  - The SCRATCH arms at arms.ts:953-984 drift with DEFAULT_WEIGHTS, so the handoff results table rows labelled bootstrap-pc / hand-priors-pc can no longer be reproduced by arm name; the MUJU_HARD_WEIGHTS hook (bots/hard.ts:287-300, marked 'not for merge as-is') overrides every hard@* bot in-process, so hard-vs-hard rows with it are invalid.
  - The default vitest selection is red at HEAD in four files (eval.test.ts, phasing-bootstrap.test.ts, phasing-tune-guards.test.ts, ablate.test.ts) on top of the four environment-only lab failures the handoff already reports at :128; Render auto-builds master with no test gate.
  - Every hard@* ladder row recorded before 71b41a3 carries a resolved-config hash that this tree can no longer mint; the handoff's results table rows are therefore under a superseded engine identity and cannot be pooled with new rows (identity.ts:44-63).
CORRECTIONS:
  - Step 1's pin list is incomplete: besides eval.test.ts:155 and :217 (confirmed) and :297 (the assertion is at :298-301), eval.test.ts:304 (all-zero invariant slice) and :345 (SpawnArea 0) also fail; so do tests/ai/hard/phasing-bootstrap.test.ts:57-58, tests/lab/phasing-tune-guards.test.ts:146-148 (via texel.ts:82 ACCOUNTING_PINS w[3]=100 vs HEAD 25), and tests/lab/ablate.test.ts at :198/:499/:532 (moved hash literals), :718-734 (live counts), :755-778 (A/A arm list) and :808 (all-zero safety).
  - 'Suite manifests ... that pin the label' is inaccurate: no suite manifest (fixtures/v2/manifest.json) pins the weights label; the pin is the v2 floor contract's weightsSha256 (fixtures/v2/floor-contract.json:8, a hash of version/w/material with the label excluded, engine-adapter.ts:74-76) plus engineSourceSha256 (:7), and the v1 contract pins nothing about weights (contract.ts:87-88).
  - The handoff's 'see commit history for whether [the scorer credit] is still label-gated on +pc' is resolved at HEAD: engine.ts:357-363 is unconditional (71b41a3), so the '+pc' arm labels and weight-file copies no longer change behaviour.
  - The 'scorer fix only' row (bootstrap-pc) and the hand-priors arms are no longer reproducible by arm name: arms.ts:953-984 derive from DEFAULT_WEIGHTS, which is now the hand priors, so bootstrap-pc is hand priors with BankExcess 100 and hand-priors/bank25-pc equal the champion; the frozen bootstrap survives only as weights/bootstrap-control.json (hash 0ae24a95) and the m6 freeze bundle.
  - The handoff lists texel.ts:82 only under step 6 ('Texel is NOT ready'); it also breaks a default-selected test now (phasing-tune-guards.test.ts:148) and encodes the M6 contract freeze of w[3]=100 that HEAD's BankExcess=25 violates.
  - The task text's '~45 non-zero weights' is 43: BankExcess is assigned 100 at weights.ts:24 and overwritten to 25 at :31, leaving 5 accounting slots (one changed) plus 38 priors.
  - The quarantine reasons in vitest.config.ts:65 ('eval-correct 14/27 fail') and :71-79 ('recall blocked because the bootstrap sets only five weights') are stale at HEAD: eval-correct.test.ts:205,279,520 (-250 / 90) are now satisfied and eval-no-safety zeroes 17 live weights, so both files' counts changed and recall may be restorable.
  - weights.ts:14 states WEIGHTS_VERSION is 'bumped whenever DEFAULT_WEIGHTS' numbers change'; 71b41a3 changed the numbers without bumping, so the handoff's 'decide on WEIGHTS_VERSION' is an open inconsistency at HEAD, not just a future choice.
====================================================================================================
ITEM mirrors  STATUS not-started  EFFORT hours
SUMMARY: The handoff (step 1) says to mirror the pending-summon credit from src/ai/hard/engine.ts into lab/hard-ai/recall/run.ts and "the ~8 duplicated scorer closures in lab/tests". At HEAD (eda73b2) commit 71b41a3 made the credit unconditional in engine.ts:355-364 (base stage0+stage1 at line 359, credit `(p.pendCostSum[mover] - p.pendCostSum[1 - mover]) * 100` at line 363), but `git diff a02bbb7..HEAD -- muju/lab muju/tests` touched only ablate/arms.ts and bots/hard.ts, so none of the eight duplicated within-turn closures (recall/run.ts:488-493, analyze/engine.ts:176-181, audit/gen-view.ts:71-76, audit/score.ts:45-60, coverage/run.ts:218-222, bench/p6-turn-time.ts:156-160, tests/ai/hard/generate.test.ts:49-53, tests/ai/hard/gen-trace.test.ts:49-53) carries it; all still return plain stage0+stage1. Every one of them feeds the SAME callback slot the engine fills (`WithinTurnScorer`, consumed at gen/generate.ts:879 for the K cut and gen/actionsearch.ts:654 for the beam keep-set), so each instrument now silently drives a generator the engine no longer ships: recall's cheap/reference/ceiling lists, the analyser's gen-missed vs search-missed classification (analyze.ts:550,606-612 compares the engine's actually-played turn against CandidateLister's list, so a played BUY can be reported as never offered), the audit's buy-family counts, and coverage's rung attribution. pvs.ts:694 and evaluate.ts:171/185 are different concepts and must NOT get the credit: evaluate()/full() already include stage 2, whose F.PendingValue (features.ts:508, w=1 at weights.ts:26) prices CC*pendCost plus service PV, so the credit there would double-count principal; pvs.ts:694 is a depth-1 futility stand-pat of the parent node compared to a stage-2-scale alpha, disabled on DESKTOP (config.ts:600 useFutility:false; only `lab-refined` at bots/hard.ts:256 enables it). No test pins a literal within-turn score, so the mirror changes no numeric assertion; the minimal correct change is one exported helper `withinTurnScore(ev, p, mover, sc, ply)` in src/ai/hard/eval/evaluate.ts (every site already imports `Evaluator, terminalScore` from that module) that engine.ts and all eight sites call.
REMAINING:
  - In muju/src/ai/hard/eval/evaluate.ts, next to terminalScore, add and export `withinTurnScore(ev: Evaluator, p: PackedState, mover: Side, sc: Scratch, ply: number): Centi` = terminalScore(p, mover, ply) ?? (ev.stage0(p, mover) + ev.stage1(p, mover, sc, ply) + (p.pendCostSum[mover] - p.pendCostSum[1 - mover]) * CC), with a doc comment citing DESIGN §5.4 and the 2026-09-20 handoff.
  - In muju/src/ai/hard/engine.ts:355-364, replace the closure body with `score: (p, scratch, ply) => withinTurnScore(evaluator, p, ctx.scoreMover, scratch, ply)` (identical arithmetic; use CC instead of the literal 100).
  - In muju/lab/hard-ai/recall/run.ts:488-493, replace the closure body with `withinTurnScore(this.evaluator, p, this.scoreMover, sc, ply)` so the cheap, reference, reply and ceiling lists match production.
  - In muju/lab/hard-ai/analyze/engine.ts:174-181, replace the closure body with `withinTurnScore(this.evaluator, p, this.scoreMover, sc, ply)` and reword the 'copied from engine.ts' comment to point at the helper.
  - In muju/lab/hard-ai/audit/gen-view.ts:71-76, replace the closure body with `withinTurnScore(this.evaluator, p, this.scoreMover, sc, ply)`.
  - In muju/lab/hard-ai/audit/score.ts:58-59, replace the terminal check and return with `return withinTurnScore(this.evaluator, p, mover, this.sc, 1)` and update the header (lines 4-6) to name the helper.
  - In muju/lab/hard-ai/coverage/run.ts:218-222, replace the closure body with `withinTurnScore(this.evaluator, p, this.scoreMover, sc, ply)`.
  - In muju/lab/hard-ai/bench/p6-turn-time.ts:154-160, replace the closure body with `withinTurnScore(this.evaluator, p, this.mover, sc, ply)` and fix the comment.
  - In muju/tests/ai/hard/generate.test.ts:47-53 and muju/tests/ai/hard/gen-trace.test.ts:49-53, replace the `score` function bodies with `return withinTurnScore(evaluator, p, scoreMover, s, ply)`.
  - Add a unit test (e.g. muju/tests/ai/hard/within-turn.test.ts, or a case in tests/ai/hard/eval.test.ts) asserting that on a packed fixture with a pending summon of cost c for the mover, withinTurnScore equals stage0+stage1+c*100, that an opponent pending summon subtracts, and that a decided position returns terminalScore.
  - Leave muju/src/ai/hard/search/pvs.ts:694, muju/src/ai/hard/eval/evaluate.ts:171/185, muju/lab/hard-ai/audit/eval-audit.ts:253/358-359 and muju/lab/hard-ai/bench/run.ts:305/547 untouched; optionally add a one-line comment at pvs.ts:694 noting the stand-pat is on the stage-1 scale without the pending credit.
  - In muju/lab/hard-ai/ablate/arms.ts:44-52 rewrite the 'wired in code ... both build it as stage0 + stage1' paragraph to reference withinTurnScore, and at 980-984 either drop the three '+pc' arms or reword their `change` strings since the suffix no longer gates anything.
  - Add a dated DEVIATIONS entry in muju/docs/hard-ai/design/DEVIATIONS.md after the 2026-09-15 within-turn-scorer entry (1791-1802) describing the pending-principal credit, and add a pointer at muju/docs/hard-ai/DESIGN.md:1009-1010.
  - When node_modules are free, run `cd muju && npm run hard:types && npm run hard:test` (hard-ai DAG node), then re-run the M13 recall gate (`npm run hard:recall -- --corpus fuzz-1000.jsonl --positions 200 --reply-positions 100 --k 24 --deep 2000 --shards 12 --out lab/results/hard-ai-verify/M13.json`) and re-baseline muju/lab/results/hard-ai-verify/M13.json and, if needed, the brackets at muju/lab/hard-ai/verify/gates.ts:488-503.
RISKS:
  - Recall's ceiling and top1/regret numbers move once its closure gains the credit (the ceiling list is ranked by within-turn score, recall/run.ts:813-818); the committed M13.json (top1 0.295, ceilingTop1 0.64, top1Share 0.4609) will not reproduce and the gates.ts:501-503 bracket ceilingTop1 ∈ [0.55, 0.8] may need re-baselining.
  - Copy-pasting the credit into nine sites instead of a helper repeats the drift that caused this item: engine.ts already diverged from the eight copies once, and arms.ts:44-49 documents only two of the nine sites.
  - pvs.ts:694 futility stand-pat is on the stage-1 scale while alpha is on the stage-2 scale (which includes PendingValue), so a mover holding pending summons is under-estimated and pruned more aggressively; pre-existing, off on DESKTOP, live only under `lab-refined` — a separate decision, not part of this mirror.
  - coverage/run.ts:213 and bench/p6-turn-time.ts:145 build `new Evaluator(this.rep)` with DEFAULT_WEIGHTS (now hand priors) and ignore arm weights, so those instruments still cannot reproduce an arm's exact ranking even after the mirror.
  - The engine refactor to the helper must be byte-identical (same arithmetic order); confirm with fixed-work goldens/determinism (`npm run hard:determinism`) since ladder identity hashes and the strength claims in the handoff depend on it.
  - Analyser and audit artifacts produced before the mirror (E1/E2 loss analyses, family counts) were computed with the uncredited scorer and should not be compared with post-mirror runs.
  - The credit is Phasing-specific (pendCostSum is always 0 under Standard, so the helper is harmless there, but HardEngine is Phasing-only per core/state.ts:615 anyway).
CORRECTIONS:
  - Handoff cites the scorer at `src/ai/hard/engine.ts:355-360`; that was correct at a02bbb7 (6-line closure). At HEAD the closure is 355-364 with stage0+stage1 at 359 and the credit at 363.
  - Handoff says 'see commit history on this branch for whether it is still label-gated on +pc or unconditional': resolved — 71b41a3 made it unconditional; no `endsWith('+pc')` remains in src, so the `+pc` weight labels in weights/*.json and the `hand-priors-pc`/`bootstrap-pc`/`bank25-pc` arms (arms.ts:982-984) no longer switch anything on.
  - Handoff says 'lab/hard-ai/recall/run.ts and the ~8 duplicated scorer closures in lab/tests': the exact count is 8 within-turn closures INCLUDING recall (7 others: analyze/engine.ts, audit/gen-view.ts, audit/score.ts, coverage/run.ts, bench/p6-turn-time.ts, tests generate.test.ts, tests gen-trace.test.ts). Sites that call stage0/stage1 but are NOT within-turn scorers (eval-audit.ts:253/358-359, bench/run.ts:305/547, interfaces.test.ts:950, pvs.ts:694, evaluate.ts:171/185) must not be mirrored.
  - None of the eight mirrors has been started at HEAD: git diff a02bbb7..HEAD under muju/lab and muju/tests touches only ablate/arms.ts and bots/hard.ts.
  - The handoff frames the mirror as a follow-up; in practice recall does not 'compare engine output to its own re-scoring' (it never instantiates HardEngine), but analyze.ts:550,606-612 does compare the real engine's played turn to CandidateLister's regenerated list, so the analyser is the instrument that will now emit spurious 'not in cheap list' findings for BUY turns.
====================================================================================================
ITEM version  STATUS partially-done  EFFORT hours
SUMMARY: The handoff (HANDOFF.md:99) leaves "decide on WEIGHTS_VERSION" open and warns a bump invalidates every JSON in weights/; the readers' gap report (gap-hand-priors-vs-v2.md:6,125) proposed bumping to 3. At HEAD, 71b41a3 changed DEFAULT_WEIGHTS' numbers and label (weights.ts:27-44,62) but left WEIGHTS_VERSION = 2, which contradicts the constant's own comment "Bumped whenever DEFAULT_WEIGHTS' numbers change (book/probe compatibility)" (weights.ts:14) — a Standard-era BK02 rationale that BK03 made obsolete, since the book now binds the exact vector via weightsKey (probe.ts:80,89,98-101) and there is no book file in the tree at all (EMPTY_BOOK is shipped, lab/hard-ai/book/build.ts does not exist). Functionally the version is a persisted-vector schema-generation gate (loadWeights weights.ts:111-112, assertCurrentWeights :124, tuner preflight rows.ts:178 hard-codes 2), while every identity that must distinguish vectors already binds the values: weightsHash (weights.ts:157-173), ladder resolvedConfigHash (identity.ts:198-216,339-348,405-407 — includes w, material, version AND label), and the suite weightsSha256 (engine-adapter.ts:74-76,127). Recommendation: do NOT bump; the label change already happened, but the change is unrecorded — the weights.ts comments (:1-6,14,19,59-61) still describe the sparse bootstrap, no serialized freeze of the new vector exists anywhere (offline FNV hash of the HEAD vector is 14d06ba8; no JSON in the tree matches it), three literal resolved-config hashes in tests/lab/ablate.test.ts (:198,:499,:532) and the v2 floor contract's weightsSha256/engineSourceSha256 (fixtures/v2/floor-contract.json:7-8) are now stale and will fail or refuse measurement, and tests pinning the sparse vector (eval.test.ts:155,217,297-306 and, unlisted by the handoff, phasing-bootstrap.test.ts:57-58) must be rewritten. A bump would additionally break 32 repair JSONs, 2 M6 freeze artifacts, the MUJU_HARD_WEIGHTS hook, make-variants.ts and rows.ts/phasing-tune-guards pins for no gain in distinguishability.
REMAINING:
  - Decide and record: keep WEIGHTS_VERSION = 2 in /home/user/deevgames/muju/src/ai/hard/eval/weights.ts:15 and rewrite the comment at :14 to state the actual semantics (schema generation of persisted vectors; a values change is identified by weightsHash/label and by the ladder/suite identities, not by this number), and update the stale header/vector comments at :1-6, :19 and :59-61 so they no longer call the vector a 'pre-outcome' sparse accounting bootstrap.
  - Commit a freeze artifact of the shipped vector (serializeWeights(DEFAULT_WEIGHTS), expected hash 14d06ba8, label phasing-hand-priors-v1) under /home/user/deevgames/muju/docs/hard-ai/phasing/repair-2026-09-20/weights/ or a dated docs/changes record, since no JSON in the tree carries the exact HEAD numbers.
  - Rewrite /home/user/deevgames/muju/lab/hard-ai/suites/phasing/fixtures/v2/floor-contract.json engineSourceSha256 and weightsSha256 to the HEAD build (hashJson(engineSourceHashes()) and weightIdentity(DEFAULT_WEIGHTS)) and commit it ALONE, as contract.ts:89-93 and the contract's own rationale require, before any hard:suite:phasing:measure run; append the amendment to /home/user/deevgames/muju/docs/hard-ai/phasing/M5-FLOOR-PREREGISTRATION-v2.md rather than editing lines 915-923.
  - Re-pin the three literal resolved-config hashes in /home/user/deevgames/muju/tests/lab/ablate.test.ts (:198 DESKTOP_WALL3000_HASH, :499 search-iter-fit, :532 search-reach-cache) with a '4th move: 71b41a3 hand priors' note in the same style as the M6 and A4 notes, keeping the superseded values.
  - Update the sparse-vector pins in /home/user/deevgames/muju/tests/ai/hard/eval.test.ts:155,217,297-306 and /home/user/deevgames/muju/tests/ai/hard/phasing-bootstrap.test.ts:57-58 to assert the hand-prior vector (or its hash) and the contract zeros that still hold (Rent, PstMine, BankConvertible, ElementCoverage, 24-27, HangingBuy, Inv5/11/15/17/18).
  - Amend the vector record in /home/user/deevgames/muju/docs/hard-ai/phasing/M6-STATUS.md:22 and M6-BOOTSTRAP-CONTRACT.md:86-96 with a dated supersession note naming label phasing-hand-priors-v1, version 2, hash 14d06ba8 and the 53-0-11 evidence, instead of editing the frozen text.
  - Retire or relabel the now-inert '+pc' duplicates: the 4 '*+pc.json' files in /home/user/deevgames/muju/docs/hard-ai/phasing/repair-2026-09-20/weights/ and arms 'hand-priors'/'hand-priors-pc' in /home/user/deevgames/muju/lab/hard-ai/ablate/arms.ts:981-984, which are numerically the champion since 71b41a3 (same weightsHash, different label) and would run as A/A rows.
  - Correct the stale book documentation in /home/user/deevgames/muju/docs/hard-ai/DESIGN.md:877-880,1477-1484 (MUJUBK02, version-only keying) to BK03's exact descriptor, or note explicitly that no book exists and 'bump book magic' is vacuous at HEAD.
  - Record the hard-ai and ai-strength node dispositions from `python3 tools/muju-content-dag.py plan --kind ai` (both pending) in the release record, stating that the ladder identity hash moved automatically via resolvedConfigHash and that WEIGHTS_VERSION was deliberately held at 2.
RISKS:
  - Leaving WEIGHTS_VERSION = 2 with the current comment at weights.ts:14 is a self-contradiction a future reader may 'fix' by bumping, which would silently orphan 34 JSON vectors (32 repair + 2 M6 freeze) and break rows.ts:178; the comment rewrite is therefore not optional.
  - Until fixtures/v2/floor-contract.json is rewritten and committed alone, every hard:suite:phasing:measure run at HEAD throws in assertContractBuild; the handoff does not list this blocker.
  - tests/lab/ablate.test.ts (:198,:499,:532), tests/ai/hard/eval.test.ts (:155,:217,:297) and tests/ai/hard/phasing-bootstrap.test.ts (:57) fail at HEAD; re-pinning the sha256 literals requires running vitest, which the current process ban prevents, so the exact new hash values cannot be verified statically (the FNV weightsHash 14d06ba8 can, and was).
  - Historical ladder rows recorded with the old vector are distinguishable only by their configHash/label, not by version; any pooling of rows across 71b41a3 must key on aConfigHash/bConfigHash (run.ts:1799-1800), which the ai-strength DAG node already forbids across identity hashes.
  - If a BK03 book is ever generated, probe.ts:98 still requires book.weightsVersion === weights.version in addition to the exact weightsKey, so a later values-only change at version 2 is safe (weightsKey mismatch falls through to search) but the DESIGN.md text still tells readers the version alone is the key.
  - The M6 contract's accounting pins (BankLiquid 100, EconDelta 100, PendingValue 1) are preserved in the new vector, but BankExcess moved 100 -> 25, which the contract text at M6-BOOTSTRAP-CONTRACT.md:93 calls 'no arbitrary discount on cash above eight'; this is a contract deviation that needs a dated amendment, not just a code comment.
CORRECTIONS:
  - HANDOFF.md:64-65 asks whether the scorer credit is still label-gated on '+pc': at HEAD it is unconditional (engine.ts:360-364, commit 71b41a3); the '+pc' suffix on weights JSONs and the '-pc' arms no longer changes behaviour.
  - HANDOFF.md:70-71 says weights/ holds '13 reviewed vectors … my sweep vectors … and +pc copies'; the directory holds 32 JSON files (plus make-variants.ts), all version 2, 28 distinct hashes.
  - HANDOFF.md:98 lists tests/ai/hard/eval.test.ts:155,217,297 as the bootstrap pins; line numbers are correct at HEAD, but tests/ai/hard/phasing-bootstrap.test.ts:57-58 also pins the exact sparse nonzero set and is omitted, as are the three literal resolved-config hashes in tests/lab/ablate.test.ts:198,499,532.
  - HANDOFF.md:98 'suite manifests / freeze artifacts that pin the label': the started.json manifests and the m6-bootstrap-freeze weights.json are records that nothing loads; the binding pin is lab/hard-ai/suites/phasing/fixtures/v2/floor-contract.json:7-8 (weightsSha256 + engineSourceSha256), which assertContractBuild enforces and which pins by hash, not by label.
  - HANDOFF.md:99 'bumping it invalidates every JSON in weights/' is true but incomplete: a bump also invalidates the two M6 freeze artifacts under docs/changes/m6-evaluation-2026-09-19/, the MUJU_HARD_WEIGHTS hook and measure.ts --weights path (both use loadWeights), make-variants.ts:104, and lab/hard-ai/tune/rows.ts:178 (hard-coded 2) plus tests/lab/phasing-tune-guards.test.ts:18,22,140,145.
  - HANDOFF.md:18 cites engine.ts:355-360 for the scorer; at HEAD the score closure spans 355-364 with the credit on 363.
  - The DAG instruction 'bump … the ladder identity hash' cannot be done by hand: the hash is sha256 of the resolved configuration including the weights and label (identity.ts:198-216,405-407) and has already moved; the only manual step is re-pinning the test literals. 'Bump book magic' is vacuous: no book file exists and BK03 already binds the exact vector.
  - The readers' proposal of WEIGHTS_VERSION 3 (gap-hand-priors-vs-v2.md:6,125) was not adopted by 71b41a3 and, on the code's actual semantics, need not be; the version comment at weights.ts:14 is what is wrong.
====================================================================================================
ITEM round3  STATUS not-started  EFFORT hours
SUMMARY: The handoff (HANDOFF.md:101-102) says round 3 — the three workflow-reviewed vectors combined-best-guess, phasing-priors-v1 and econ-only-best-guess, each run with the scorer credit, vs Rush at 1.5 s and vs aiv2-hard-turn at 6 s — was queued but never run; HEAD confirms this: scripts/round3.sh exists (added in b2a85e6) but there is no round3* directory under results/ and no round-3 line in results/ALL-PROGRESS-LINES.txt. The script would run exactly 6 ladder rows (3 vectors x 2 opponents) as hard@desktop with MUJU_HARD_WEIGHTS pointing at the '+pc' JSON, 16 pairs = 32 games each, handicap 0, 8 shards, p1-dev openings (the first 16 rows of the 48, chosen by pair index not by seed), seed 33 vs Rush and seed 31 vs aiv2-hard-turn — the same seeds/openings as the existing hand-priors comparator rows (12-0-20 and 28-0-4), so the results would be paired comparisons; a seventh row (K48 on the hand priors) is described in a comment but line 19 is a no-op stub that was never written. All three '+pc' JSON files exist under weights/, are byte-identical to their plain copies except for the label, and pass every check loadWeights makes at HEAD (schema, featureSchema, version 2, 62 w, 18 material); since 71b41a3 made the pending-summon credit unconditional (engine.ts:359-363) and no '+pc' gate remains anywhere in src/lab/tests, the suffix is now purely documentary. The hand priors that became DEFAULT_WEIGHTS ('phasing-hand-priors-v1') are NOT one of the 13 reviewed vectors (make-variants.ts builds exactly 13, none equal to HEAD in both w and material; HEAD's w equals hp-tier13+pc's w with cost x100 material); the three round-3 vectors differ from the default in 36-39 of 62 weights and 12 of 18 material entries (see evidence). From the summary/metrics files (which record per-engine turns and meanTurnMs, not run wall time) a 16-pair row costs about 53-58 game-minutes at 6 s vs aiv2-hard-turn and 12-23 game-minutes at 1.5 s vs Rush, so the six rows are roughly 3.3-4 CPU-hours, about 1-1.2 h wall at 4 shards on this 4-core box or 2-2.3 h at the default 2 heavy slots.
REMAINING:
  - Wait until the other process releases node_modules, then in /home/user/deevgames/muju write a cloud copy of docs/hard-ai/phasing/repair-2026-09-20/scripts/round3.sh into the scratchpad (/tmp/claude-0/-home-user-deevgames/00b9be38-79a3-5f99-b547-dd1fecf37988/scratchpad/round3.sh) that sets S to that scratchpad, does `cd /home/user/deevgames/muju` (HEAD already carries the repair, no worktree), reads weights from $PWD/docs/hard-ai/phasing/repair-2026-09-20/weights/$v+pc.json (the plain $v.json is identical), calls stats via docs/hard-ai/phasing/repair-2026-09-20/scripts/stats.mjs, uses MUJU_HEAVY_SLOTS and --shards of at most 4 (this box has 4 cores; 2-3 is safer for the 1.5 s rows), and drops the pgrep wait loop (line 6) and the no-op stub (line 19).
  - Run the three Rush rows sequentially: for v in combined-best-guess phasing-priors-v1 econ-only-best-guess: `MUJU_HARD_WEIGHTS=$PWD/docs/hard-ai/phasing/repair-2026-09-20/weights/$v+pc.json node --import tsx lab/hard-ai/ladder/run.ts --a hard@desktop --b Rush --work wall:1500 --handicaps 0 --pairs 16 --seed 33 --shards 4 --openings lab/hard-ai/ladder/openings/p1-dev.jsonl --replays on --out <scratchpad>/round3/$v-pc-vs-Rush-w1500`, then `node docs/hard-ai/phasing/repair-2026-09-20/scripts/stats.mjs <out>` and `grep '^- Elo' <out>/summary.md`; check each summary.md's hard@desktop overrunRate stays under 5% (otherwise re-run with fewer shards). Expected ~12-23 game-minutes per row.
  - Run the three aiv2-hard-turn rows the same way with `--b aiv2-hard-turn --work wall:6000 --pairs 16 --seed 31 --shards 4 --out <scratchpad>/round3/$v-pc-vs-aiv2hardturn-w6000`; expected ~53-58 game-minutes each (~14 min wall at 4 shards, ~28 min at 2). Total for all six rows: ~3.3-4 CPU-hours, ~1-1.2 h wall at 4 shards on an idle box.
  - Optionally add the seventh row round3.sh:18-19 never wrote: `--a hard@ablate:k48` with MUJU_HARD_WEIGHTS unset (DEFAULT_WEIGHTS is now the hand priors, lab/hard-ai/ablate/arms.ts:481) vs aiv2-hard-turn wall:6000 seed 31 and vs Rush wall:1500 seed 33 (~1.2 CPU-hours more).
  - Copy each run's summary.md and metrics.json (not the replays) into docs/hard-ai/phasing/repair-2026-09-20/results/round3-<name>/, append the stats.mjs lines to results/ALL-PROGRESS-LINES.txt, and add one row per run to the HANDOFF.md results table, reading them as paired comparisons against the seed-33 hand-priors row (12-0-20 vs Rush) and seed-31 row (28-0-4 vs aiv2-hard-turn); record each vector's label and hash (030f6597 / fd5a13e5 / 1d6085c7) next to its out dir because the manifest names all three `hard@desktop`.
  - Update HANDOFF.md:64-73 to say the credit is unconditional at HEAD (src/ai/hard/engine.ts:363), that '+pc' labels are documentary, and that arms bootstrap-pc/bank25-pc (lab/hard-ai/ablate/arms.ts:983-984) no longer produce the bootstrap because handPriorsPatch clones the new DEFAULT_WEIGHTS; point the 'scorer fix alone' control at weights/bootstrap-control.json via MUJU_HARD_WEIGHTS.
  - Do not bump WEIGHTS_VERSION in src/ai/hard/eval/weights.ts:15 before round 3 has run, or regenerate every JSON under weights/ afterwards (loadWeights at weights.ts:112 rejects any other version).
RISKS:
  - Timing validity on this box: the harness voids a row when either arm's overrun rate exceeds 5% (lab/hard-ai/ladder/run.ts:285,1280); the laptop's 1.5 s Rush rows already ran hard@desktop at 3.0-4.2% overruns, so 4 shards saturating 4 cores may push the Rush rows over the bar — use 2-3 shards for the 1.5 s rows and check summary.md before trusting a result.
  - Sample size: each row is 16 pairs / 32 games (SE ~0.12 per synthesis.json); round 3 can separate 'broken' from 'working' vectors but cannot rank working ones — differences under ~20 points at n=32 are noise.
  - Comparability: the earlier m-*/m2-* sweep rows ran WITHOUT the scorer credit, while any vector run at HEAD always has it; compare round-3 rows only against the confirm-* rows (ALL-PROGRESS-LINES.txt lines 9-16), which used seeds 33/31 with the credit on.
  - WEIGHTS_VERSION is still 2 although DEFAULT_WEIGHTS' numbers changed (weights.ts:14-15); a later bump — which the handoff lists as a pending decision — would make loadWeights reject all 32 JSON files in weights/ and block round 3 until they are regenerated (the generators make.ts/make2.ts/make3.ts/make-variants.ts hard-code laptop paths).
  - MUJU_HARD_WEIGHTS overrides every hard@* bot in the process (hard.ts:298-300) and the manifest engine name is just `hard@desktop` (hard.ts:304-307); if the label/hash is not recorded beside each run directory the six results are indistinguishable after the fact except by the stderr line in the .log.
  - The reviewed vectors carry much larger tier-2/3 material values (MATERIAL_MODERATE or x1.75) than the default's cost x100; the laptop's m-tier-overshoot row lost 13 of 21 games by upkeep-elimination, so watch stats.mjs loss types, not only W/D/L.
  - `--replays on` writes a replay per game; the handoff says replays were dropped as too large, so either delete them after stats.mjs runs or accept distinctGames falling back to the games.jsonl tuple with --replays off.
  - The task premise that summary.md records wall time is wrong; all CPU estimates here are derived from per-engine turns x meanTurnMs and assume one core per game, so real wall time also includes engine start-up, packing and the heavy-queue wait.
CORRECTIONS:
  - HANDOFF.md:72-73 ('A label ending +pc turns the scorer credit on while it is label-gated') is stale at HEAD: commit 71b41a3 made the pending-summon credit unconditional (src/ai/hard/engine.ts:359-363) and `rg endsWith('+pc')` over src, lab and tests finds nothing; the three +pc JSONs differ from their plain copies only in the label line, so at HEAD they are interchangeable.
  - HANDOFF.md:18 cites the within-turn scorer as engine.ts:355-360; at HEAD the closure spans engine.ts:355-364 with the credit on line 363.
  - HANDOFF.md:66 lists arms bootstrap-pc and bank25-pc as usable controls; at HEAD handPriorsPatch (lab/hard-ai/ablate/arms.ts:956) clones the NEW DEFAULT_WEIGHTS, so bootstrap-pc yields the hand priors with BankExcess 100 rather than the bootstrap, and hand-priors, hand-priors-pc and plain desktop are numerically identical. The 'scorer fix ALONE gives 25-0-7' row (HANDOFF.md:13,46) is no longer reproducible from an arm; use weights/bootstrap-control.json (hash 0ae24a95) through MUJU_HARD_WEIGHTS.
  - HANDOFF.md:101-102 describes round 3 as three vectors x two opponents (six rows); round3.sh:18-19 also intended a seventh row (K 24->48 on the hand priors via hard@ablate:k48) but line 19 is a no-op stub, so that row was never scripted.
  - HANDOFF.md:70 ('13 reviewed vectors') is confirmed (make-variants.ts:128-160 builds exactly 13; synthesis.json lists 13), but the hand priors that became DEFAULT_WEIGHTS are NOT one of them — they are the separate hand-set vector from arms.ts:955-979 / make3.ts:8; HEAD's w equals weights/hp-tier13+pc.json's w exactly (material differs) and no file in weights/ equals the HEAD default in both w and material.
  - HANDOFF.md:85 recommends MUJU_HEAVY_SLOTS=8 and round3.sh uses --shards 8; this cloud checkout has 4 cores (nproc), so the equivalent is at most 4 slots/shards, and the heavy-queue default of 2 (lab/hard-ai/ladder/heavy.ts:87) is the conservative choice for wall-clock rows.
  - reports/knobs/synthesis.json randomSearchPlan calls the incumbent '90/25' and says to keep DEFAULT_WEIGHTS frozen per M6-BOOTSTRAP-CONTRACT.md:90-96; at HEAD the incumbent is the hand priors (BankLiquid 100 / BankExcess 25) and DEFAULT_WEIGHTS has already been changed without a WEIGHTS_VERSION bump, so the plan's baseline and freeze assumption need restating before the search runs.
  - The computed task's premise that the results/ summary.md files record wall time is incorrect: they record per-engine turn counts, meanTurnMs, p95/max turn ms and loadAvgMean (e.g. confirm-rep-hppc-vs-aiv2hardturn-w6000-dev/summary.md:18,28-29); the hours-per-row figures above are derived from turns x meanTurnMs.
====================================================================================================
ITEM rush  STATUS not-started  EFFORT days
SUMMARY: The handoff (HANDOFF.md:29-33 root cause 4; :103-106 step 3) claims the generator, not the weights, caps what Hard can do against scripted Rush: the purchase menu is truncated cheapest-first to fire_1 x1..4 at bank >= 12, promotions are emitted only for five missions (so ~9% of legal promotions are ever offered), one ordinary promotion fits per Prepare, and at-risk purchases earn zero service PV; best result vs Rush is 13-0-19. At HEAD (eda73b2) every cited mechanism is exactly as described and line numbers are correct (config.ts:543, purchase.ts:441-463, promote.ts:196-197/252-253, pending.ts:116, generate.ts:879/915); the only post-handoff code changes are the unconditional scorer credit (engine.ts:358-361) and hand-prior DEFAULT_WEIGHTS (weights.ts), neither of which touches the generator. None of the five proposed fixes exists in src/, and no ablation arm in lab/hard-ai/ablate/arms.ts moves purchase or promotion knobs (withGen at arms.ts:147-158 passes purchase/maxPromotions through unchanged; ArmFactor at :100-112 has no such factor), so step 3 is not started. My own trace of purchaseMultisets' DFS confirms the handoff but also shows the proposed 'sort before truncating' is insufficient on its own: the multiset list itself is DFS-truncated at maxMultisets 35 (of 209 possible), and at bank >= 14 all 35 contain fire_1, so maxPlans 36 + keepPerMultiset 1 yields 35 fire-containing plans. The knob reports and gap-v2-and-rush.md conclude Rush wins on production/income rather than tempo: after the scorer fix Hard already spends 93-97% and places ~40 bodies/game but loses every game by elimination to Rush's ~126 fire_1, and the Flood-water_1 experiment (Rush held to 0.55) says composition (a water_1 counter-build, unreachable through today's menu) is the lever, with the missing fire_1->fire_2 STRENGTH promotion second. Legality risk of the fixes is low because every plan is validated by assignOrder and every combo action by Replica.isLegal before emission; the main costs are identity-hash fallout, generator work per node, and tests that pin the current shape.
REMAINING:
  - Add a `purchase` ArmFactor and a `purchase-wide` arm (maxPlans 36, keepPerMultiset 1) in muju/lab/hard-ai/ablate/arms.ts, extending withGen (147-158) to accept a purchase override and adding the key to factorsOf/maskFactor (1101+), so the config-only test runs as `hard@ablate:purchase-wide` without moving hard@desktop's identity hash.
  - Update muju/tests/lab/ablate.test.ts (one-factor-per-arm at 235-250, hash uniqueness at 261-268, rootDiagnostic list at 330-345) for the new factor, and run its Rush row at wall:1500 / p1-dev / 16 pairs / seed 33 to compare against confirm-dev-hppc-vs-Rush-w1500 (12/0/20).
  - In muju/src/ai/hard/gen/purchase.ts replace the DFS-order write loop (441-461) with a score-then-select pass over all (multiset, assignment) candidates into a new fixed scratch buffer, and raise/restructure multiset enumeration (purchaseMultisets 132-160; maxMultisets 35 of 209, buffer ceiling MAX_MULTISETS 64 at line 70) so water_1/plant_1/metal_1 multisets survive at bank >= 14, since sorting alone still leaves all 35 DFS multisets fire-containing.
  - Keep muju/tests/ai/hard/purchase.test.ts 'replays every returned plan canonically' (278-310) green, add a test that at bank >= 14 the DESKTOP menu contains a non-fire and a mixed plan, and update muju/tests/ai/hard/interfaces.test.ts:1108, the config.ts:541-542 comment and muju/docs/hard-ai/DESIGN.md:1043,1066 if the shipped bounds change.
  - Add a STRENGTH fallback mission in muju/src/ai/hard/gen/promote.ts (Mission.STRENGTH = 5 at 59, MISSION_NAMES at 60, a final branch before `return -1` at 196-197 with benefit (dATK + dDEF) * ACTION_VALUE_CC), and rewrite muju/tests/ai/hard/promote.test.ts:137-160 ('does not invent a Prepare KILL mission'), which asserts fire_1 next to water_1 gets no candidate and will fail by design; also reconsider the -RENT_PV ordering term at 255-256 because STRENGTH benefit 60 still ranks fire_1->2 at -362 below bare purchase plans in buildCombos.
  - Allow two ordinary promotions per Prepare in muju/src/ai/hard/gen/generate.ts buildCombos (634-695) by filling promo2 for the top-2 (or 3) ordinary candidates under a pool cap like FORTIFY_PAIR_POOL (204-205), relying on runCombo's per-action isLegal checks (757-780); update muju/src/ai/hard/gen/trace.ts:109-110 multiPromotion semantics and add a two-ordinary-promotion case beside generate.test.ts:322-335.
  - Change muju/src/ai/hard/eval/pending.ts:116 to credit an integer 50% of pendingServicePVcc when risk is set, update muju/tests/ai/hard/phasing-bootstrap.test.ts:118-119,153-154,175-176,190-191, amend muju/docs/hard-ai/phasing/M6-BOOTSTRAP-CONTRACT.md:52-58 (binary selector) and note DisruptPressure(60) must stay 0 to avoid double counting; treat it as a feature-semantics change per the hard-ai DAG node (weightsVersion/book magic).
  - After each change re-measure vs Rush (and Balanced/Expand as the regression guard from synthesis.json Stage B) with scripts/stats.mjs, requiring 0 illegal / 0 divergences / 0 fallbacks and overrunRate < 5%, and treat differences under ~20 points at n=32 as noise.
  - Walk `python3 tools/muju-content-dag.py plan --kind ai`: `npm run hard:types && npm run hard:test`, then hard:perft/fuzz/determinism, then re-pin the desktop identity hash (tests/lab/ablate.test.ts:198,499) and any suite freeze manifests before any strength claim.
RISKS:
  - Illegal-turn risk (DAG hard veto) is low for all five fixes: purchase plans are validated square-by-square by assignOrder (purchase.ts:352-365, plan skipped on -1), promotion candidates use the same predicate as Replica.canPromote (promote.ts:243-250 vs state.ts:905-907), and runCombo legality-checks every BUY/PROMOTE/END_PLACE and drops the combo on failure (generate.ts:757-780) with a MAX_TURN_ACTIONS 24 guard (754); the residual risk is a scratch-buffer indexing bug in a new score-then-select pass, which purchase.test.ts:278-310 and verify/replay.ts would catch.
  - A config-only change to config.ts:543 moves hard@desktop's resolved identity hash, invalidating tests/lab/ablate.test.ts:198 and :499 literals, suite freeze artifacts and every existing ladder row's comparability (DAG: never pool across identity hashes) — same fallout class the handoff's step 1 owes for the weights label; prefer a lab arm first.
  - Sort-before-truncate alone does not fix composition: purchaseMultisets is DFS-truncated at maxMultisets 35 of 209 and all 35 contain fire_1 at bank >= 14 (my trace; inv4 finding 4), and the reference generator inherits the same cap (generate.ts:1442), so recall-style coverage checks cannot see the gap.
  - More combos per node cost GEN work and depth: inv4 measured depth 1.83 -> 1.08 when K went 24 -> 96 at 100k work; interior nodes keep only 8 place plans, and the hard@ seat already overruns 4.17% of turns at wall:1500 (VOID threshold 5%), so wider Prepare generation may void wall:1500 Rush rows.
  - STRENGTH promotions will be proposed but may still never be chosen: promotion ordering score is benefit - 422 per upkeep crystal (promote.ts:254-256), so fire_1->2 scores -362 and ranks below bare purchases in buildCombos; the leaf eval under hand priors values a tier1->2 at about -100 cc (inv3), so the mission alone may only raise candidates, not chosen promotions.
  - The 50% at-risk service credit departs from the M6 bootstrap contract's declared binary selector and would double count with DisruptPressure(60) if that weight ever becomes non-zero (inv3 flagged the overlap); it also changes the tie-break story the contract relies on (principal exactly cancels bank, service is the only positive term).
  - Post-repair Rush losses are all by elimination at 93-97% spend and ~40 bodies/game vs Rush's ~126 (ALL-PROGRESS-LINES; gap-v2-and-rush.md section 3), so buying more fire_1 may not help; the value of the menu fix hinges on planScore's ordering constants (mineCc 1, homeRaceCc 5000 dominates per inv1) actually ranking water_1/plant_1 above fire_1 once reachable — Flood-water_1 is the only responder shown to hold Rush (0.55).
  - Sample size: 32-game Rush rows have SE ~0.085; 12 vs 13 wins is noise (synthesis.json); the 'Rush with home guard' variant the gap report recommends does not exist, and most engine wins vs Rush are checkmates of an empty home, so human exploitability is understated.
  - Every ladder or test run is blocked in this session (another process owns node_modules); nothing above has been executed, only read.
CORRECTIONS:
  - '8 of 51 legal promotions were ever offered' (HANDOFF.md:30) conflates inv4's 12-position probe (51 legal -> 8 candidates, inv4.md:15,22) with the population; the population numbers are 339 of 3,842 (8.8%) at 988 Prepare points (inv0.md:26; 199 of 1,717 vs Rush) and 504 of 5,525 (9%) across 128 replays (inv2.md). Direction unchanged.
  - 'config.ts:543 (maxPlans 12, cheapest-first)': the cheapest-first order lives in core/catalog.ts:186-189 (tier1 sorted by cost, then def index) consumed by purchaseMultisets' DFS at purchase.ts:145-159; config.ts:543 only sets the caps (maxMultisets 35, maxPlans 12, keepPerMultiset 3).
  - 'better, sort before truncating in gen/purchase.ts:441-463' is necessary but not sufficient: the multiset list is itself truncated in DFS order at maxMultisets 35 (of 209 possible; buffer ceiling MAX_MULTISETS 64 at purchase.ts:70), and at bank >= 14 all 35 emitted multisets contain fire_1 (inv4 finding 4, confirmed by my trace), so 'maxPlans 36 + keepPerMultiset 1' yields 35 fire-containing plans, not a diverse menu. inv0.md:11 and synthesis.json already say 'proper fix: score every multiset then keep the best', which must include the multiset stage.
  - 'at bank >= 12 the menu is only fire_1 x1..4' is correct at HEAD given >= 4 ranked squares and >= 4 available (limit 12 includes the empty plan: [F]x3,[FF]x3,[FFF]x3,[FFFF]x2); between bank 9 and 11 lightning_1 appears via [F,F,L].
  - engine.ts:355-360 (root cause 1) and the 'see commit history for whether it is still label-gated' note are resolved: at HEAD the credit is unconditional at engine.ts:358-361 (71b41a3); the closure now spans 353-362.
  - Root cause 4 lists purchase.ts:441-461 while step 3 says 441-463; both are correct at HEAD (441 limit, 443-461 loop, 463 sortPlans).
  - 'one promotion per Prepare' is precisely 'one ordinary promotion per PlaceCombo'; a second promotion is generated only as a FORTIFY pair while an own unit holds the enemy corner (generate.ts:222-232, 709-747) and is already tested (generate.test.ts:322-335).
  - Step 3 omits that the 50% service-PV credit is a change to the M6 bootstrap contract's declared binary selector (M6-BOOTSTRAP-CONTRACT.md:52-58, 74) and that a config-only maxPlans/keepPerMultiset change moves hard@desktop's identity hash (tests/lab/ablate.test.ts:198,499) and departs from DESIGN.md:1043,1066 — neither fix is 'config-only' in test/doc fallout.
  - The 'prime suspect for the Rush losses' framing predates the measured post-repair rows: with the scorer fix + hand priors Hard spends 93-97% and places ~40 bodies/game yet loses 19-20 of 32 by elimination (ALL-PROGRESS-LINES), so the remaining gap is composition (fire_1-only menu, no water_1 counter-build, no fire_1->2 promotion) rather than spend volume; gap-v2-and-rush.md's Flood-water_1 result (Rush held to 0.55) is the strongest evidence for prioritising the menu composition fix.
====================================================================================================
ITEM time-texel  STATUS not-started  EFFORT days
SUMMARY: The handoff (HANDOFF.md:34-35, :107, :109-110) says time use is lost to work-rung rounding (~1.07 s of 1.5 s at ~50 units/ms vs the 200-600 the config assumes) with arms work-fit / search-iter-fit / k48 to try on top of the repair, and that Texel is not ready because tune/corpus.ts hard-codes muju-phasing-1, texel.ts:82 pins cash weights, and hard:spsa/hard:book point at missing files. At HEAD every one of those code claims is confirmed: chooseWork is time.ts:225-232 (budget = unitsPerMs x targetMs at :227, floor to the x2 WORK_LADDER at :76-78); the three arms exist at arms.ts:481, :531-539 and :557-565 and, since 71b41a3 made the credit unconditional (engine.ts:360-363) and DEFAULT_WEIGHTS the hand priors (weights.ts:62), any hard@ablate:<arm> already runs on the repair; the seven muju-phasing-1 literals are at lab/hard-ai/tune/corpus.ts:269,279,375 and rows.ts:146,190,227,290 (the live ruleset is muju-phasing-2 per lab/harness/types.ts:31); ACCOUNTING_PINS {2:100,3:100,58:1} is at texel.ts:82; and package.json:64-65 DOES still carry hard:spsa and hard:book while lab/hard-ai/tune/spsa.ts and lab/hard-ai/book/ do not exist (the computed task's premise that the scripts are gone is wrong). Nothing has measured the three arms under Phasing: the handoff results/ directory has no such row, round3.sh's k48 line is a stub, and the only rows anywhere are Standard-era and negative or inconclusive (work-fit 3-0-5 n=8, search-iter-fit 7-0-9 n=16, k48 never run). Two things the handoff misses: the bigger time defect is the cold profile INITIAL_UNITS_PER_MS=200 (config.ts:587), which deadline-aborts every game's first search (firstSearchAborted 64/64 and 91/96 in the committed summaries) and for which an existing calib arm (arms.ts:519-528) is the ready-made experiment; and making the hand priors the default set w[3]=BankExcess to 25 (weights.ts:31), so texel.ts:269's assertAccountingPins now throws on DEFAULT_WEIGHTS and tests/lab/phasing-tune-guards.test.ts:147-148 fails at HEAD. Texel usable on top of the hand priors needs the revision fix, a pin decision, an allowlist publisher, an equal-strength self-play corpus, and a bounded fitter, roughly days; the time-use arm rows are hours of ladder time.
REMAINING:
  - Run muju/lab/hard-ai/ladder/run.ts with --a hard@ablate:work-fit, hard@ablate:search-iter-fit, hard@ablate:work-fit-deep and hard@ablate:k48 against aiv2-hard-turn at wall:6000 (and against Rush at wall:1500) per HANDOFF.md:86-89, with MUJU_HARD_WEIGHTS unset, and commit the summary.md/metrics.json rows under muju/docs/hard-ai/phasing/repair-2026-09-20/results/ — no Phasing measurement of these arms exists today.
  - Register a two-factor arm in muju/lab/hard-ai/ablate/arms.ts combining time.ladderStep 'sqrt2' with searchFix.iterFit true (E2 showed the finer ladder alone raises work 1.36x without depth), and pin it in muju/tests/lab/ablate.test.ts beside work-fit-deep (:441-460).
  - Re-measure hard@ablate:calib (muju/lab/hard-ai/ablate/arms.ts:519-528, the existing cold-probe calibration) under Phasing, because INITIAL_UNITS_PER_MS = 200 in muju/src/ai/hard/config.ts:587 makes every game's first 1.5 s search deadline-abort at the measured ~50 units/ms (firstSearchAborted 64/64, 91/96).
  - If an overshoot rung is wanted, add an optional rungScale?: number to TimeConfig in muju/src/ai/hard/config.ts:112-157, multiply it into budget at muju/src/ai/hard/search/time.ts:227, leave it unset on every shipped shape so hard@desktop's hash at muju/tests/lab/ablate.test.ts:208 does not move, register hard@ablate:rung-x2 and pin it.
  - Give muju/lab/hard-ai/bench/run.ts a Phasing position corpus so hard:bench --calibrate stops reporting 0 units/ms, then re-derive INITIAL_UNITS_PER_MS and the PROVER price in WORK_COST (muju/src/ai/hard/search/time.ts:44,155-164) from a real measurement.
  - Replace the seven 'muju-phasing-1' literals in muju/lab/hard-ai/tune/corpus.ts:269,279,375 and muju/lab/hard-ai/tune/rows.ts:146,190,227,290 with LADDER_RULES_VERSION from muju/lab/hard-ai/ladder/ruleset.ts:52, and update the fixtures at muju/tests/lab/phasing-tune-guards.test.ts:35,40,43.
  - Decide the BankExcess pin: change ACCOUNTING_PINS in muju/lab/hard-ai/tune/texel.ts:82 to the hand priors' w[3]=25 (or unpin BankExcess with bounds) and update muju/tests/lab/phasing-tune-guards.test.ts:146-148, which fails at HEAD because DEFAULT_WEIGHTS.w[3] is 25 (muju/src/ai/hard/eval/weights.ts:31).
  - Either remove hard:spsa and hard:book from muju/package.json:64-65 (and the matching lines in muju/docs/hard-ai/DESIGN.md:227-228,258-259 and MILESTONES.md M20) or write muju/lab/hard-ai/tune/spsa.ts and muju/lab/hard-ai/book/build.ts.
  - Write the SHA-bound allowlist publisher that muju/lab/hard-ai/tune/rows.ts:140-150 expects (none exists) or relax the preflight so a corpus can be built from committed Phasing ladder runs.
  - Produce an equal-strength Hard self-play corpus from a dedicated opening pool (DEV_POOL_PATH at muju/lab/hard-ai/tune/rows.ts:38 must change) and harden fit() in muju/lab/hard-ai/tune/texel.ts:268-310 with per-parameter bounds, a fixed k and a rarity guard before trusting any fitted vector; keep the tactical/home terms hand-set since quiet rows zero them.
RISKS:
  - Arm rows vs aiv2 opponents at wall:1500 go VOID under A14 (opponent overrun > 5%; the handoff's own 1.5 s aiv2 rows are VOID), so measure the time-use arms hard-vs-hard or at wall:6000.
  - MUJU_HARD_WEIGHTS (muju/lab/hard-ai/bots/hard.ts:298-300) overrides every hard@* bot in the process, so a hard-vs-hard arm row run with it set compares identical weights on both seats.
  - The ~50 units/ms and ~1.07 s figures come from the owner's 8-core M1 under load 9-19 with MUJU_HEAVY_SLOTS=8 (another box measured 90-185); the per-turn rows they were read from (games.jsonl) were not committed, so the numbers are not reproducible from the repo and rung effects will differ per machine.
  - Any change to INITIAL_UNITS_PER_MS, DESKTOP.time or a new non-optional TimeConfig key moves hard@desktop's configuration hash (config.ts:131-132; pinned at tests/lab/ablate.test.ts:208) and orphans every row measured under it.
  - A finer ladder or an overshoot rungScale makes wall-mode moves load-dependent, which the x2 quantisation exists to prevent; fixed-work determinism goldens are unaffected, but replays of wall games become less repeatable.
  - search-iter-fit changes fixed-work output when on (config.ts:266-268), so adopting it as a default would require re-pinning determinism goldens.
  - hard:bench --calibrate is vacuous under Phasing (empirical-probe.md:23), so WORK_COST and the cold profile cannot be honestly re-derived until a Phasing bench corpus exists.
  - Bumping WEIGHTS_VERSION (handoff step 1) would invalidate every JSON in weights/ and the texel row/allowlist version checks (rows.ts:179); the Texel pin decision must be made together with that.
  - A Texel fit on lopsided ladder replays learns bot identity, not position value (gap-tuner.md:27); running the pipeline before an equal-strength corpus exists produces a vector that would be actively harmful.
CORRECTIONS:
  - HANDOFF.md:109 names only tune/corpus.ts as hard-coding muju-phasing-1; rows.ts carries four more literals (muju/lab/hard-ai/tune/rows.ts:146,190,227,290), so fixing corpus.ts alone leaves the preflight refusing every muju-phasing-2 run. (gap-tuner.md:4 lists all seven.)
  - HANDOFF.md:110 is CORRECT at HEAD: muju/package.json:64-65 still defines hard:spsa -> lab/hard-ai/tune/spsa.ts and hard:book -> lab/hard-ai/book/build.ts, and neither file nor directory exists. The computed task's premise that a grep found no such scripts is wrong; they were not moved to a lab package.json or a tune README (neither exists).
  - The handoff's step 1 test list (eval.test.ts:155,217,297) omits that making the hand priors DEFAULT_WEIGHTS sets w[3]=BankExcess to 25 (weights.ts:31), which breaks Texel's start-vector pin (texel.ts:82,269 requires 100) and fails muju/tests/lab/phasing-tune-guards.test.ts:147-148 at HEAD; hard:texel would now throw before fitting even with a valid corpus.
  - HANDOFF.md:107 (step 4) implies the three arms are ready experiments 'on top of the repair'; they are (arms.ts:481,531,557, and since 71b41a3 every hard@ablate arm inherits the hand priors and unconditional credit), but nothing has measured them under Phasing: results/ has no such row, scripts/round3.sh:16-18's k48 line is a no-op stub, and the only rows anywhere are Standard-era and unfavourable (work-fit 3-0-5 n=8 Elo -89; search-iter-fit 7-0-9 n=16 Elo -44; k48 never run, k96 -83).
  - HANDOFF.md:34-35 'the 200-600 the config assumes': precisely, INITIAL_UNITS_PER_MS = 200 (config.ts:587) is the starting profile for every engine and DESKTOP_SHAPE.unitsPerMs = 600 (config.ts:575) is only profileFor's threshold, never a starting profile; the consequence the handoff does not list under root cause 5 is that the cold 200 sizes every game's first search at the 200k rung and it is deadline-aborted (firstSearchAborted 64/64 in confirm-val-hppc-vs-aiv2hard-w1500, 91/96 in ladder-h2h-desktop-vs-aiv2hardturn-w1500), which is a larger time-use defect than the rung rounding.
  - The handoff does not mention that a 'calibrate units/ms at startup' arm already exists (hard@ablate:calib, arms.ts:519-528, time.calibrateCold) and was rejected on Standard at -27 Elo; it is the ready-made experiment for the cold-profile problem and should be re-measured under Phasing rather than reinvented.
  - The time.ts:225-233 citation is confirmed at HEAD (chooseWork spans :225-232, budget at :227). The task's path muju/src/ai/hard/tune/ does not exist; the tuner lives at muju/lab/hard-ai/tune/.
  - The handoff's ~50 units/ms and ~1.07 s are machine- and load-specific (inv1.md:33 from uncommitted games.jsonl; empirical-probe.md:28; a second reader saw 90-185 units/ms in hard-engine-code.md:30); the committed summaries carry meanTurnMs (1015-1195 ms at wall:1500) but no units/ms column.
====================================================================================================
ITEM evidence  STATUS partially-done  EFFORT days
SUMMARY: The handoff (HANDOFF.md:93-100) and commit 71b41a3's message both say the release-DAG review is OWED, and nothing at HEAD records it: `python3 tools/muju-content-dag.py plan --kind ai` yields 13 nodes (wasm-tactics, ai-search, hard-ai, ai-strength, mcp-tools, agent-guides, balance-analysis, game-validation, static-package, server-package, static-deploy, server-deploy, release-verification), all `Disposition: pending`, with no muju/docs/changes/ record for the repair and no RELEASE-*-phasing.md (only RELEASE-2026-09-18.md, a Standard-rules record whose lines 22-23 keep the Phasing guard). What PR #25 did satisfy is the in-ladder correctness veto and Elo-interval reporting for its rows (e.g. confirm-val-hppc-vs-aiv2hardturn-w6000/summary.md: 53/0/11, 0 illegal/0 divergences/0 fallbacks, Elo +273 [+161,+476]); it did NOT run perft/fuzz/determinism, did not re-measure the v2 suite floors (last measured 2026-09-19 on `phasing-accounting-bootstrap-v1`, floorPass=false; the v2 contract pins that vector's hash so it is unmeasurable at HEAD), requested no SPRT, ran no sealed row, and left tests pinning the bootstrap failing (commit message admits this; WEIGHTS_VERSION still 2 at weights.ts:15). Under the project's own rules the 53-0-11 row cannot be claimed as strength: p1-val is the VAL (model-selection) allocation (ALLOCATION-P1.md:12,22-23) and has now been read by THREE rows (two engines), Gate 2 requires p1-sealed rows 0-31 (not in the repo; owner's laptop only), seed 20260953, 32 pairs, h0+h3, wall:8000, strict legality and SPRT(0,+100;α=β=0.05) (PHASING-PREREGISTRATION:68-84), and the unlock table (:92-98) plus content-dag.json:134 require appending a NEW dated release record, never editing an old one. The M6 bootstrap contract is de facto superseded (BankExcess 100→25 breaks its 100 cc/crystal identity at M6-BOOTSTRAP-CONTRACT.md:13,106; line 1 requires deviations be recorded before outcomes) with no amendment or successor document. The roadmap's next milestone after M6 is the Phasing plan's "M7 acceptance" (correctness/responsiveness/sealed gates) then "M8 cutover" (M4-STATUS.md:118-119, M6-STATUS.md:184, m6-dev-continuation.md:15,140,144); that plan lives outside the repo, and MILESTONES.md's M7 is an unrelated, already-green Standard-era milestone.
REMAINING:
  - Create muju/docs/changes/2026-09-20-hard-phasing-repair.md from `python3 tools/muju-content-dag.py plan --kind ai` and record a disposition (changed / verified unchanged / blocked) with evidence for each of the 13 nodes; today none has one.
  - hard-ai node: update muju/tests/ai/hard/eval.test.ts:155,217,297-301 and the muju/tests/lab/ablate.test.ts hash/label pins for `phasing-hand-priors-v1`, and decide WEIGHTS_VERSION in muju/src/ai/hard/eval/weights.ts:15 (DAG says bump on weight change; m6-dev-continuation.md:142 says do not bump silently), then run `npm run hard:types && npm run hard:test`.
  - ai-strength correctness veto: run `npm run hard:perft && npm run hard:fuzz && npm run hard:determinism` at HEAD and record the artifacts (no post-repair run exists in the repo).
  - Re-measure the v2 suite floors: write a new version of muju/lab/hard-ai/suites/phasing/fixtures/v2/floor-contract.json with HEAD's engineSourceSha256 and weightIdentity(DEFAULT_WEIGHTS) and `supersedes: {ledgerSeq: 1, chain: 'd7774d6d…'}`, commit it ALONE and push, then run `npm run hard:suite:phasing:measure` in a later commit and record pass/fail per family without moving floors.
  - Append a dated amendment to muju/docs/hard-ai/phasing/M6-BOOTSTRAP-CONTRACT.md (or a successor hand-priors contract beside it) recording the deviation: BankExcess 100→25, the 40 restored coefficients, the unconditional pending-summon scorer credit, and that outcomes were seen before the deviation was recorded.
  - Append amendment A6 to muju/docs/hard-ai/PHASING-PREREGISTRATION-2026-09-18.md naming the candidate identity (`phasing-hand-priors-v1`, weights hash, engine source hash), disclosing that p1-val was read by three rows (two engines), and stating the owner's decision on whether Gate 1 (never passed) and Gate 0 item 7 remain prerequisites for Gate 2.
  - Keep Round 3 (scripts/round3.sh) and any further candidate comparison on p1-dev only; do not open p1-val again without a written selection policy and ledger (m6-dev-continuation.md:140).
  - On the owner's machine (p1-sealed is not in the repo), run Gate 2 as written: `npm run hard:ladder -- --a hard@desktop --b aiv2-hard --work wall:8000 --handicaps 0,3 --pairs 32 --seed 20260953 --sprt 0,100,0.05,0.05 --legality strict --openings <p1-sealed rows 0-31>`, plus the A1 Rush side-by-side row and Gate 3 p95 timing (desktop ≤6000 ms, phone ≤3000 ms).
  - Append a NEW file muju/docs/hard-ai/RELEASE-<date>-phasing.md (never edit RELEASE-2026-09-18.md) reporting score, Elo interval, SPRT outcome, seeds, identity hashes, responsiveness percentiles and the list of what remains unmeasured; only then flip `hardEnabled` per the unlock table.
  - Finish the downstream nodes: game-validation (`npm run server:types && npm test && npm run build`, online e2e, ai-worker e2e), static-package (`bash build-all.sh`), server-package/server-deploy (verify the Render host that auto-built master carries the new default and passes /api/muju/health, /muju/, /SKILL.md, MCP discovery), record static-deploy as blocked, and write the release-verification record.
RISKS:
  - p1-val has been consumed by three rows with two different candidate engines (results/ladder-h2h-desktop-vs-aiv2hard-w1500, confirm-val-hppc-vs-aiv2hardturn-w6000, confirm-val-hppc-vs-aiv2hard-w1500); any further val row on a new candidate is repeated peeking under m6-dev-continuation.md:140, and there is no written val selection policy (M6-STATUS.md blocker 5).
  - The v2 floor contract (fixtures/v2/floor-contract.json:7-8) pins the bootstrap's weights and engine hashes; at HEAD both changed, so the floors cannot be measured without a superseding contract version, and the last recorded measurement (2026-09-19) FAILED invariants 8/15, economy 19/20 and summon-disruption 12/14 — Gate 0 item 7 is currently unmet.
  - The 53-0-11 row is h0 only, wall:6000, seed 51, no SPRT, val not sealed, and played by ablation arm label `scratch-hand-priors-v1+pc` rather than the production label; it does not meet Gate 2 as written and cannot be pooled with a future sealed row.
  - The Render host builds master with no test gate (RELEASE-2026-09-18.md:147-150; HANDOFF.md:129), so PR #25's merge very likely already deployed the new default to production (reachable via ?phasingAi=1 per HANDOFF.md:118-120) before any DAG node was reviewed.
  - Tests pinning the bootstrap fail at HEAD by the commit's own admission, and CI was already red for 4 lab tests (HANDOFF.md:128); the DAG's hard-ai and game-validation verify commands cannot pass until pins are updated.
  - The M6 bootstrap contract's 'record deviations before outcomes' and 'freeze weights 2/3 at 100' clauses (lines 1, 106) were not followed; without an amendment the project's evaluation contract and its code disagree.
  - Gate 1 (baseline sanity) has never produced a passing row and amendment A5's per-search adapter is unimplemented; the preregistration's ordering was bypassed by the repair rows, so whether to relax or supersede the preregistration is an owner decision that no agent can make (process-lessons.md:4,8).
  - The Phasing plan that defines 'M7 acceptance' and 'M8 cutover' lives outside the repository (m6-continuation-map.md:3), while MILESTONES.md's M7 is a different, already-green milestone — easy to misread which 'M7' is next.
  - The DAG's `npm run hard:suite` verify command points at the Standard-era suite runner (package.json:43), not the Phasing floors instrument `hard:suite:phasing:measure` (package.json:67); following the DAG literally would measure the wrong suites.
  - WEIGHTS_VERSION guidance conflicts: the DAG hard-ai node says bump weightsVersion/book magic/identity hash when weights change; m6-dev-continuation.md:142 says do not silently increment the version and let content hashes distinguish coefficients; bumping invalidates every JSON under repair-2026-09-20/weights/ (HANDOFF.md:99).
CORRECTIONS:
  - HANDOFF.md:59 'p1-val has now been looked at once' is understated: three result rows read p1-val (sha cbd427df…) — master hard@desktop vs aiv2-hard w1500 (4/0/60, VOID), hand-priors-pc vs aiv2-hard-turn w6000 (53/0/11) and hand-priors-pc vs aiv2-hard w1500 (49/0/15, VOID) — i.e. two candidate engines and three seeds, though only one candidate vector was selected.
  - HANDOFF.md:18 cites the scorer at `src/ai/hard/engine.ts:355-360`; at HEAD (after 71b41a3) the scoring closure is at engine.ts:357-363 and the credit line is 363. HANDOFF.md:64's 'see commit history … whether it is still label-gated on +pc or unconditional' is resolved: 71b41a3 removed the label gate, it is unconditional.
  - HANDOFF.md:98 says to update 'suite manifests / freeze artifacts that pin the label': the v2 floor contract pins the weights HASH (fixtures/v2/floor-contract.json:8) and cannot be edited into validity — a re-measurement needs a new contract version with `supersedes` (M5-FLOOR-PREREGISTRATION-v2.md:570-574); the M6 freeze artifacts under docs/changes/m6-evaluation-2026-09-19/ are historical evidence and must not be edited.
  - The handoff's results table marks the 53-0-11 row 'valid' (harness timing rule) but does not state that under the project's rules it is development evidence only: p1-val is the VAL allocation, h0 only, wall:6000 not 8000, seed 51 not 20260953, no SPRT, Gate 0 item 7 (suites at floors) unmet and Gate 1 never passed (PHASING-PREREGISTRATION:36-84).
  - The handoff omits two process obligations the merge created: an amendment to M6-BOOTSTRAP-CONTRACT.md (its line 1 requires deviations recorded before outcomes; line 106 freezes BankExcess at 100) and a change record under muju/docs/changes/ (CONTENT_DAG.md:56-60); neither exists at HEAD.
  - HANDOFF.md:99 'decide on WEIGHTS_VERSION' should also name the conflict: the DAG hard-ai node instructs a bump on any weight change, while m6-dev-continuation.md:142 forbids silent version increments; WEIGHTS_VERSION is still 2 at HEAD (weights.ts:15).
====================================================================================================
ITEM cutover  STATUS not-started  EFFORT days
SUMMARY: The handoff's last section claims Standard is still the default in 12 places, three guards keep AI off Phasing behind ?phasingAi=1, Standard+Hard silently pack-errors into AIEngineV2, and a cheap cutover (flip defaults, open guards, reject standard on create, keep isPhasing) costs 49 vitest files/245 tests if board.ts:204 flips or 14 files/64 tests if not, plus 7/20 AI e2e and 33/72 online e2e. At HEAD eda73b2 every cited guard and default is confirmed at the cited line (board.ts:204, ModeSelect.tsx:46/109/113, src/online/OnlineLobby.tsx:30, online/client.ts:71, server/schema.ts:48, server/mcp.ts:66-69, server/observation.ts:121-129, ai/worker/handler.ts:113, GameScreen.tsx:89-103, hard/core/state.ts:615-616) except server/mcp.ts:39, which is the McpServer instructions string and not a default, and the "6 COALESCE" count, which is really 2 COALESCE sites plus 5 `?? 'standard'` fallbacks. Today with no query flag: (a) Standard + Hard sends engine:'hard' (useAI.ts:125,258, no ruleset check, hardEnabled=true at hard/config.ts:47), HardEngine.searchTurn's pack throws PackError (state.ts:615-616) and returns fallback:'pack-error' (engine.ts:521-535), useAI.ts:289 records the fallback and the turn is played by AIEngineV2's per-action loop (useAI.ts:329-345, handler.ts:181); (b) Phasing + any AI difficulty cannot start at all: ModeSelect.tsx:58/109/113/298 force or refuse Standard, GameScreen.tsx:103 disables the AI seats, and handler.ts:113 would refuse an unmarked request, so no engine runs; only ?phasingAi=1 reaches the new HardEngine. PR #25 (a02bbb7..eda73b2) touched only src/ai/hard/engine.ts, src/ai/hard/eval/weights.ts, lab/hard-ai/ablate/arms.ts and lab/hard-ai/bots/hard.ts; no guard, default, routing or server file changed, and no commit since 2026-09-19 other than 425efa4 (which created the preview) touches the guards. Per the DAG the default flips are graph-independent of hard-ai (browser-ui/server-runtime/mcp-tools descend from transitions, not hard-ai), but opening the three AI guards is policy-downstream of ai-strength (content-dag.json:127 "Keep the worker and UI guards closed ... until ai-strength passes") and, per the preregistration unlock table, of Gate 1 with hardEnabled=false; the hard-AI repair itself is not downstream of the cutover, but shipping the repaired engine to ordinary players is downstream of opening the guards. The cutover has not started at HEAD.
REMAINING:
  - Decide and record the cutover scope in muju/JUDGMENT_LOG.md and muju/SPEC.md (rule-contract node): Variant B (UI/server/MCP defaults + guards, board.ts:204 untouched) versus full Standard retirement, and whether ruleset:'standard' is rejected on create (the measured 14/64 numbers did not include that rejection).
  - Stop the silent Standard+Hard downgrade independently of the cutover: either set hardEnabled = false in muju/src/ai/hard/config.ts:47 or add `state.ruleset === 'phasing'` to the useHard condition at muju/src/hooks/useAI.ts:125, so a Standard Hard game does not pack-error every turn.
  - Decide the guard policy for muju/src/ai/phasingPreview.ts: replace the personal opt-in with a release constant (on = phasingAiEnabled || stored, keeping ?phasingAi=0 as the kill switch) so that muju/src/ai/worker/handler.ts:113, muju/src/components/ModeSelect.tsx:55-58/109/113/298 and muju/src/components/GameScreen.tsx:98-103 open together; this is policy-gated on ai-strength (content-dag.json:127) or on the preregistration unlock row (Gate 1 + hardEnabled=false) unless the owner amends.
  - Flip the defaults: muju/src/components/ModeSelect.tsx:46 and :109, muju/src/online/OnlineLobby.tsx:30, muju/src/online/client.ts:71, muju/src/components/AnalysisScreen.tsx:30, muju/server/schema.ts:48, muju/server/mcp.ts:66-69 (text and .default), muju/server/observation.ts:121-122, :129, :189-190 (text 'Built-in AI remains Standard-only' and the rulesFor default), and remove the 'AI plays Standard rules' copy at ModeSelect.tsx:282 and the preview badge at GameScreen.tsx:991-993.
  - Port the Variant B test fallout: 9 server vitest files / 48 tests that create rooms without a ruleset (mcp, rooms, staging, clock-pressure, history, clocks, lobby, phasing, crystal-handicap), 5 UI/guard files / 16 tests (tests/ai/phasing-preview.test.ts, tests/components/phasing-preview-ui.test.tsx, tests/hooks/crystal-handicap, game-setup, phasing-ai-funding), tests/ai/phasing-preview-worker.test.ts and tests/ai/hard-hook-fallback.test.ts (marker semantics), 33 online e2e tests, and the AI e2e specs (e2e/hard-ai.spec.ts ?hardMs assertion, e2e/ai-worker.spec.ts:83-91 handoff assumption, e2e/ai-timer.spec.ts 'End turn' helper, e2e/phasing-ai.spec.ts:46 guard-closed assertion).
  - Green CI before any cutover merge per gap-cutover-verification: set fetch-depth: 0 in .github/workflows/deploy.yml and install ripgrep or drop the rg dependency in muju/tests/lab/gate1-shard.test.ts; add the AI e2e specs to a CI Playwright config since none runs them today.
  - Rewrite the agent-facing text that says Standard is the default and AI is Standard-only: muju/public/skills/muju-hono-tanka/SKILL.md, muju/docs/MCP_TOOL_TAPS.md, muju/SPEC.md:3-11 (mcp-tools and agent-guides nodes).
  - Walk the release DAG (python3 tools/muju-content-dag.py plan --kind rules --kind ui --kind ai) and record changed / verified unchanged / blocked per node, then re-measure the browser sanity probe on the HEAD engine (the 2026-09-20 probes ran at a02bbb7 with bootstrap weights).
RISKS:
  - Render auto-builds master with no test gate (muju/Dockerfile per gap-cutover-verification), so a default flip that breaks ~100 tests would still deploy; the Pages path is gated but has been red since d764dd7 for environment-only failures.
  - Opening the three guards without an amendment contradicts both content-dag.json:127 ('Keep the worker and UI guards closed ... until ai-strength passes') and the preregistration unlock table, which requires Gate 1 (never passed under muju-phasing-2) even for the hardEnabled=false route.
  - With hardEnabled=true left as is, resumed Standard saves (ModeSelect.tsx:109 savedGame?.ruleset ?? 'standard') and any Standard vs-AI game keep hitting the pack-error fallback at every turn; the console still claims 'runs the real HardEngine' (hardOptIn.ts:218), which will keep generating misleading 'Hard felt weak' reports.
  - The measured fallout numbers (14/64, 49/245, 7/20, 33/72) were taken at a02bbb7 without rejecting ruleset:'standard' on create; the handoff's proposal to reject it would add server test failures beyond the 48 already counted.
  - The +pc label convention in docs/hard-ai/phasing/repair-2026-09-20/weights/ is now meaningless for gating (the credit is unconditional at engine.ts:363); lab arms or tests that still key on the label may silently measure something else.
  - The handoff's production room count (0 active / 38 archived / 28 Standard) is not sourced in any report in the checkout and cannot be verified without the live Render database.
  - The weights.ts doc comment (58-61) still calls DEFAULT_WEIGHTS 'the approved accounting bootstrap' while the label is phasing-hand-priors-v1; a reader trusting the comment will misidentify the production vector.
CORRECTIONS:
  - server/mcp.ts:39 is not a Standard-default site: it is the McpServer `instructions` string (mcp.ts:38-39) that merely describes both rulesets' end-turn actions. The mcp.ts defaults are :66 (muju_rules description), :67 (`.default('standard')`) and :69 (muju_create_room description); the source report ruleset-surface.md:5 cites only :67.
  - '6 server COALESCE(...,'standard') sites' is wrong: there are exactly 2 COALESCE(...,'standard') sites (server/rooms.ts:299 and :320). The source (ruleset-surface.md:33) said six 'missing ruleset means Standard' places, two of which are COALESCE; the other four are `?? 'standard'` at rooms.ts:522, observation.ts:23, analysis/core.ts:93 and :113. My grep finds a seventh at server/analysis/index.ts:31.
  - 'Standard is still the default in 12 places' does not match the handoff's own list. ruleset-surface.md:5 enumerates board.ts:204, ModeSelect.tsx:46 and :109, OnlineLobby.tsx:30, online/client.ts:71, server/schema.ts:48, server/mcp.ts:67, observation.ts:121-122 and :189, AnalysisScreen.tsx:30, plus four `phasing = false` component prop defaults (ActionBar:16, UnitShop:12, BlackCrystalHandicap:4, InstructionsModal:78). The handoff omits AnalysisScreen.tsx:30 and the prop defaults and adds mcp.ts:39, which is not a default.
  - 'Three guards ... (all read readPhasingAiPreview())' is only transitively true: ai/worker/handler.ts:113 reads the request marker `request.phasingPreview` and, per its own comment at 107-112, reads no flag of its own; the marker is set by src/hooks/useAI.ts:109-116 from readPhasingAiPreview(). The three direct readers are ModeSelect.tsx:55, GameScreen.tsx:98 and useAI.ts:109.
  - 'see commit history on this branch for whether it is still label-gated on +pc or unconditional' is resolved at HEAD: 71b41a3 made the pending-summon credit unconditional (engine.ts:360-363) and set DEFAULT_WEIGHTS to makeWeights('phasing-hand-priors-v1') (weights.ts:62). The weights.ts doc comment at 58-61 still says 'accounting bootstrap'.
  - The 'cheap cutover' description includes 'reject ruleset:'standard' on create', but the gap-cutover-verification patch that produced the 14/64 and 49/245 numbers only flipped defaults and forced the preview on; it did not reject Standard on create, so the numbers understate that variant.
  - 'Production holds 0 active rooms and 38 archived (28 Standard)' appears only in the handoff; no report under reports/understand/ contains it (critic.json:12 lists the server-side retirement size as an open question), so it should be treated as an unsourced owner-side observation, not a verified claim.
  - OnlineLobby.tsx lives at muju/src/online/OnlineLobby.tsx, not under src/components (the handoff gave no directory; the computed task text did, incorrectly).
====================================================================================================
ITEM ci  STATUS stale-claim  EFFORT hours
SUMMARY: The handoff (HANDOFF.md:128-129) says CI is red at HEAD for 4 environment-only lab tests, Render builds master with no test gate, and Cloudflare Pages is stale at the 09-18 bundle. That was accurate for a02bbb7: GitHub Actions run 35475103182 failed in the 'Verify Muju rules, multiplayer and MCP' step with 3 files / 4 tests (gate1-shard 'spawnSync rg ENOENT', gate1 x2 'git show a0551c8:...' on the shallow checkout, suites-phasing-contract's v1 result-path refusal on the shallow graft root). At HEAD eda73b2 (PR #25, which includes 71b41a3) run 35552456980 is still red at the same step but now with 7 files / 24 tests: the same 4 plus 20 NEW failures caused entirely by 71b41a3's DEFAULT_WEIGHTS change (eval.test.ts x5, phasing-bootstrap.test.ts x4, ablate.test.ts x10 config-hash/A-A pins, phasing-tune-guards.test.ts x1 via texel.ts's BankExcess=100 pin); the unconditional scorer credit itself broke nothing. 'npm test' is 'vitest run' over tests/**/*.test.{ts,tsx} minus a 12-file quarantine (203 files, matching CI), so tests/lab and tests/ai/hard ARE gated; Playwright e2e is a separate deploy.yml step that never ran because vitest failed first. Even a green run would not redeploy Pages: the Deploy step is 'if: CLOUDFLARE_API_TOKEN != ""' and was skipped on the last green run (d764dd7), so Pages stays frozen until credentials are configured or a manual wrangler deploy is done. Remaining work is to re-pin ~20 test expectations to the hand-priors vector, decide the texel BankExcess pin, fix the 4 env-only tests (fetch-depth: 0 plus ripgrep on the runner, or skipIf gating), then walk the release DAG.
REMAINING:
  - muju/tests/ai/hard/eval.test.ts: re-pin lines 131 (stage0 with BankExcess 25 = 1150), 155 (HomeInvaded -4000), 217 (DrawPressure -8), 298-303 (full hand-priors nonzero list) and 345 (SpawnArea 30) to the phasing-hand-priors-v1 vector, or make them read DEFAULT_WEIGHTS instead of literals.
  - muju/tests/ai/hard/phasing-bootstrap.test.ts: update the nonzero-list pin at line 58, the BankExcess pin at line 75, and the full-vector score pins at lines 88 (-2340) and 104 (-1945), documenting the new expected scores.
  - muju/tests/lab/ablate.test.ts: move DESKTOP_WALL3000_HASH (line 198) to the new '2c485153...' value and the literals at lines 499/532 to '6c8f9bb1...'/'6e3d2936...' (recording the superseded values in the existing comment chain), update live counts at line 724 (eval-no-economy 6), empty the A/A list at line 759, and invert or retire the safety-union-zero assertion at line 808.
  - muju/lab/hard-ai/tune/texel.ts:82-88 and muju/tests/lab/phasing-tune-guards.test.ts:146-148: decide whether ACCOUNTING_PINS keeps w[3]=100 (then the default violates its own texel pin) or becomes {2:100, 3:25, 58:1} / drops the BankExcess pin.
  - .github/workflows/deploy.yml: add 'with: fetch-depth: 0' to actions/checkout (line 21) and install ripgrep before 'npm test' (line 41), or gate tests/lab/gate1-shard.test.ts:456, tests/lab/gate1.test.ts:115/283 and tests/lab/suites-phasing-contract.test.ts:49 with it.skipIf(process.env.CI) as p6-stoppable-generation.test.ts:240 does, so the 4 pre-existing environment failures stop masking real ones.
  - muju/lab/hard-ai/ablate/arms.ts:982-984 and muju/lab/hard-ai/bots/hard.ts:298-299: retire or relabel the now-inert '+pc' arms and either label-scope or remove the MUJU_HARD_WEIGHTS hook marked 'not for merge as-is', so ablate identity tests stay meaningful.
  - Optionally mirror the engine.ts:363 pending-summon credit into the 8 duplicated scorer closures (lab/hard-ai/recall/run.ts:492, audit/score.ts:59, audit/gen-view.ts:75, coverage/run.ts:221, analyze/engine.ts:180, bench/p6-turn-time.ts:159, tests/ai/hard/generate.test.ts:52, tests/ai/hard/gen-trace.test.ts:52); they pass today but no longer score the way the engine does.
  - Push the test fixes to master and confirm GitHub Actions run for that commit reaches the 'Check Muju online and local browser play' and 'Check phone and tablet gameplay' steps (never exercised on any commit since d764dd7), then walk the release DAG nodes hard-ai, ai-strength, game-validation, static-deploy (blocked on Cloudflare credentials) and server-deploy (Render revision check per ONLINE.md:70-72) and write the docs/changes completion record.
RISKS:
  - Render has no test gate (Dockerfile:6 builds and type-checks only), so if it auto-deploys master as ONLINE.md:63-66 says, HEAD's hand-priors engine is already live behind ?phasingAi=1 with 24 failing tests and no e2e run; whether Render actually auto-deployed eda73b2 cannot be verified from the repo (no render.yaml; DAG node server-deploy says no verified automatic workflow).
  - Re-pinning DESKTOP_WALL3000_HASH and the arm hashes silently changes the ladder identity of hard@desktop/hard@lab; every result under docs/hard-ai/phasing/repair-2026-09-20/results was recorded under the old identity, and WEIGHTS_VERSION was left at 2 in 71b41a3 even though the DAG's hard-ai node says to bump weightsVersion when weights change (the handoff notes bumping it invalidates all 34 weights/*.json).
  - Fixing CI alone will not update Cloudflare Pages: the Deploy step is skipped whenever CLOUDFLARE_API_TOKEN is unset (deploy.yml:56-57), as it was on the last green run 35447835829; Pages stays at the 09-18 bundle until secrets are configured or a manual wrangler deploy is run.
  - The 4 environment-only failures depend on the shallow checkout and the runner lacking ripgrep; adding fetch-depth: 0 is the fix the reports recommend but it changes how measure.ts:271 resolves 'last commit touching the contract' in CI (correctly, to 66473c7), and any future contract test still needs full history.
  - Because npm test fails before the Playwright steps, no commit since d764dd7 (2026-09-19T14:08Z) has had its online e2e or phone/tablet smoke checks run in CI; the phasing preview and Hard-engine changes since then are unverified in a browser by CI.
  - hand-priors arm now equals DEFAULT_WEIGHTS and the '+pc' suffix is inert, so any ladder row labelled hard@ablate:hand-priors or hand-priors-pc is an A/A row against hard@desktop; the ablate.test.ts:759 A/A test (now expecting an empty list) will not catch this because those arms are not in ALL_WEIGHT_ARMS.
CORRECTIONS:
  - 'CI is red at HEAD for 4 environment-only lab tests' was true for a02bbb7 (run 35475103182: 3 files / 4 tests) but is stale for the current HEAD eda73b2: run 35552456980 shows 7 files / 24 tests failing (Tests 24 failed | 2843 passed), the original 4 plus 20 new failures introduced by 71b41a3's DEFAULT_WEIGHTS change.
  - Next-step 1's list of tests to update is incomplete and mis-typed: besides eval.test.ts:155,217,297, eval.test.ts:131 and :345 also fail; tests/ai/hard/phasing-bootstrap.test.ts fails at :58, :75, :88, :104; tests/lab/ablate.test.ts fails 10 tests on config-HASH pins and A/A live-weight assertions (:198/:499/:532/:595/:687/:724/:759/:808/:913/:968/:994), not on the label; tests/lab/phasing-tune-guards.test.ts:148 fails via texel.ts:88's BankExcess=100 pin. No test failed on a weights label or on a suite manifest / freeze artifact.
  - engine.ts scorer is at lines 355-364 at HEAD (not 355-360), and the handoff's open question 'whether it is still label-gated on +pc or unconditional' is resolved: 71b41a3 made it unconditional at line 363; the +pc arms in arms.ts:982-984 are now inert.
  - The Cloudflare Pages sentence omits the decisive fact from the handoff's own reports (production-ai-wiring.md:31, DAG node static-deploy): the workflow's Deploy step is skipped because no CLOUDFLARE_API_TOKEN is configured, so Pages is frozen regardless of CI colour; gap-cutover-verification.md:4's 'turn CI green so Pages can deploy again' is wrong on that point.
  - The suites-phasing-contract failure is more specific than 'contract bound to git history': it is the v1 test at suites-phasing-contract.test.ts:49-58 hitting measure.ts:285's result-path refusal because the shallow clone's graft root touches every results/ file; 5a6d2f9 guarded only the v2 test (:71-72). At HEAD the error message now also lists the repair-2026-09-20/results files, but the cause is unchanged.
  - 'Render auto-builds master' rests only on ONLINE.md:63-66 and Dockerfile:6; the repo has no render.yaml and the DAG's server-deploy node says there is no verified automatic server deployment workflow, so it should be labelled a doc claim, not a verified fact.
====================================================================================================
CRITIQUE
CONTRADICTIONS:
  - NON-ZERO WEIGHT COUNT. pins and version reports say 43; ci report says '44 entries' (quoting a CI diff) and '~39 further weights'; the task text said ~45. Adjudicated 43 by reading muju/src/ai/hard/eval/weights.ts:22-44: five accounting slots (Material, BankLiquid, BankExcess, EconDelta, PendingValue) with BankExcess assigned 100 at :24 and overwritten to 25 at :31, plus 38 priors at :32-44. The ci report's 44 is a misread of the vitest diff; 43 is right.
  - ENGINE SCORER LINE RANGE. pins, mirrors, ci and version reports cite engine.ts:355-364 with the credit at :363; the rush report cites 353-362 with the credit at 358-361; the evidence report cites 357-363. Adjudicated by reading muju/src/ai/hard/engine.ts:350-366: `score:` opens at :355, baseScore at :359, comment :360-362, `return baseScore + (p.pendCostSum[mover] - p.pendCostSum[1 - mover]) * 100` at :363, closing brace :364. The rush and evidence reports are off by two.
  - WHICH eval.test.ts ASSERTIONS FAIL. The pins report lists :155, :217, :298-301/:304 and :345 and states the 'Material/round-trip/hash' tests keep passing; the ci report (from CI run 35552456980) adds :131. Adjudicated: ci is right. muju/tests/ai/hard/eval.test.ts:131-132 expects stage0 = material + 100*(5+2) with f[BankExcess]=2 (:125); at HEAD w[BankExcess]=25 gives material+550, so it fails (CI saw 1150 vs 1300). Five failing tests in the file: :131, :155, :217, the :296-306 block, :345.
  - WHICH phasing-bootstrap.test.ts ASSERTIONS FAIL. pins and version reports name only :57-58; ci names :58, :75, :88, :104 (4 tests). Adjudicated by reading muju/tests/ai/hard/phasing-bootstrap.test.ts:44-106: :75 asserts the 9th crystal is worth 100 (now 25 because BankExcess is excess-only); :88 asserts full() of the arrived unit equals the pending score and :104 asserts score = 200+300+service — both are escrow-conservation invariants from the M6 contract that hold only when positional weights are zero. ci is right, and these two are not simple re-pins (see gaps).
  - NUMBER OF FILES IN weights/. pins says 33 JSON files, ci says 34, version and round3 say 32. Adjudicated by `ls`: 32 .json files plus make-variants.ts = 33 directory entries. 32 is the JSON count that a WEIGHTS_VERSION bump would invalidate.
  - WHETHER THE NEW ablate.test.ts HASHES ARE OBTAINABLE WITHOUT RUNNING NODE. pins says they 'cannot be computed without running node'; ci reports the new prefixes from the CI log of run 35552456980 ('2c485153...' for hard@desktop wall:3000, '6c8f9bb1...' for search-iter-fit, '6e3d2936...' for search-reach-cache). Adjudicated: both are consistent — the full 64-char values already exist in that CI log and can be read with `gh run view 35552456980 --log` (network, not node), which unblocks the re-pin before node_modules are free. Caveat from muju/docs/hard-ai/phasing/M6-STATUS.md:137: hashes must be re-pinned as a documented identity move, not 'to fit'; muju/tests/lab/ablate.test.ts:170-208 shows the convention.
  - HOW MANY TIMES p1-val WAS READ. HANDOFF.md:59 says 'looked at once'; the evidence report says three rows. Adjudicated by `grep -l cbd427df results/*/summary.md`: three files (confirm-val-hppc-vs-aiv2hard-w1500, confirm-val-hppc-vs-aiv2hardturn-w6000, ladder-h2h-desktop-vs-aiv2hard-w1500). Evidence report is right.
  - server/mcp.ts:39 AND THE '6 COALESCE' COUNT. HANDOFF.md:115-116 lists mcp.ts:39 as a default site and '6 server COALESCE sites'; the cutover report says :39 is the instructions string and there are 2 COALESCE sites. Adjudicated by reading muju/server/mcp.ts:36-40,64-70 (:38-39 is the McpServer `instructions` literal; the defaults are :66-67 `.default('standard')` and :69) and by grep: COALESCE(...,'standard') only at server/rooms.ts:299 and :320; `?? 'standard'` at rooms.ts:522, observation.ts:23, analysis/core.ts:93,:113 and analysis/index.ts:31 (seven fallback sites in total, two of them COALESCE). Cutover report is right.
  - hard:spsa / hard:book. The computed task premise (per the time-texel report) was that these scripts were gone; the handoff says they point at missing files. Adjudicated by reading muju/package.json:62-65: both scripts exist and target lab/hard-ai/tune/spsa.ts and lab/hard-ai/book/build.ts; `ls lab/hard-ai/tune/` shows only corpus.ts, rows.ts, texel.ts and `lab/hard-ai/book` does not exist. Handoff :110 is correct.
  - COUNT OF DUPLICATED SCORER CLOSURES. HANDOFF.md:98 says 'recall/run.ts and the ~8 duplicated scorer closures'; the mirrors report says exactly 8 including recall. Confirmed two of them unchanged at HEAD: muju/lab/hard-ai/recall/run.ts:488-493 and muju/tests/ai/hard/generate.test.ts:49-53 both still return plain stage0+stage1 with no pending credit. Mirrors report is right; none has been started.
  - WEIGHTS_VERSION: BUMP OR NOT. The version report recommends keeping 2; the evidence report notes the DAG hard-ai node ('Bump weightsVersion ... when ... weights change', confirmed in the planner output) conflicts with m6-dev-continuation.md:142 ('do not silently increment'). This is a decision, not a fact dispute. My inference from the code I read: loadWeights gates on version (weights.ts:14-15 comment vs behaviour), while every identity that must distinguish vectors (weightsHash, the ladder resolved-config hash, the suite weightsSha256) already binds the values; a bump would orphan 32 JSONs, rows.ts:178 and the tune-guards pins for no gain. Recommend keep 2, rewrite the :14 comment, and record the decision in the change record so the DAG instruction is answered rather than ignored.
  - 'THREE GUARDS ALL READ readPhasingAiPreview()'. HANDOFF.md:118-119 vs the cutover report. Adjudicated by reading muju/src/ai/worker/handler.ts:105-114: the worker checks `request.phasingPreview` and its own comment says it 'reads no flag of its own'; the marker is set by useAI. Cutover report is right (transitively true only).
GAPS:
  - CLAUDE.md 'start here' (CLAUDE.md:12-18) sends a fresh session to HANDOFF.md as the authority, but HANDOFF.md:64-65 ('see commit history ... label-gated or unconditional'), :72-73 ('+pc turns the credit on'), :95-99 (step 1 partly done) and :18 (engine.ts:355-360) are now stale. Once step 1 lands, CLAUDE.md must point at the dated change record (or a HANDOFF addendum) that states the production default is `phasing-hand-priors-v1` with the unconditional credit. No reader named CLAUDE.md.
  - NO CHANGE RECORD EXISTS. `ls muju/docs/changes/` has nothing dated 2026-09-20; `ls muju/docs/hard-ai/` has only RELEASE-2026-09-18.md; the planner shows all 13 ai-kind nodes 'Disposition: pending'. The evidence report named this; I add that the DAG's ai-strength node text (planner output node 4) requires 'Append a new dated release record; never edit an old one', so RELEASE-2026-09-18.md must not be amended for this change.
  - phasing-bootstrap.test.ts:88 and :104 ARE NOT RE-PINS. They assert the M6 escrow-conservation invariant (arrived unit value == pending value; score == 200+300+service). Under the hand priors an arrived unit earns positional credit (SpawnArea etc.) that a pending one does not, so the invariant is now false by design. The fix needs a decision: keep the conservation property by asserting it on the accounting slice only (stage0 + PendingValue), or record that arrival is no longer value-neutral. No reader flagged the semantic change; the ci report treated them as numeric re-pins.
  - ORDER DEPENDENCE OF THE NEW FLOOR CONTRACT. Readers (pins, version, evidence) each say to write a superseding v2 floor contract, but none noted that engineSourceSha256 covers src/ai/hard/** AND lab/hard-ai/bots/hard.ts, ablate/arms.ts and ladder/identity.ts (pins report, engine-adapter.ts:127-131). So the scorer mirror (a helper in src/ai/hard/eval/evaluate.ts), the SCRATCH-arm rebuild and the env-hook removal must all land BEFORE the contract is written, or the contract must be rewritten again. Sequence: tests/pins -> mirror -> arm/hook cleanup -> contract alone -> measure.
  - MERGED 'SCRATCH' CODE. muju/lab/hard-ai/ablate/arms.ts:953-954 says 'SCRATCH ... NOT in the repo' and muju/lab/hard-ai/bots/hard.ts:298 says 'SCRATCH EXPERIMENT HOOK (not for merge as-is)'; both were merged in PR #25. Either clean them up (rebuild bootstrap-pc from the frozen bootstrap literal, drop the inert +pc labels, label-scope or remove MUJU_HARD_WEIGHTS) or record an explicit decision to keep them. The handoff's results-table rows labelled bootstrap-pc / hand-priors-pc are no longer reproducible by arm name.
  - e2e COVERAGE OF THE SHIPPED ENGINE. muju/e2e/hard-ai.spec.ts starts every game from createInitialGameState() (:143, :160, :186, :217, :249, :280), i.e. Standard rules (board.ts:204 default per the cutover report), under which HardEngine pack-errors and falls back; so the DAG hard-ai verify text 'fallback and divergence counters stay at zero in e2e/hard-ai.spec.ts' does not exercise the Phasing engine at all. Only e2e/phasing-ai.spec.ts (behind ?phasingAi=1, :46 asserts the guard is closed) touches the hand-priors engine, and per the ci report neither spec is in playwright.online.config.ts, so no browser check of the shipped engine runs in CI. No reader named this.
  - docs/hard-ai/phasing/repair-2026-09-20/repair.patch:10 still carries the label-gated form (`if (!evaluator.currentWeights.label.endsWith('+pc')) return baseScore;`). It is a historical artifact of b2a85e6 and should be labelled as such in the HANDOFF addendum so nobody re-applies it.
  - DEVIATIONS.md HAS NO ENTRY. muju/docs/hard-ai/design/DEVIATIONS.md:1791-1802 defines the within-turn scorer (last touched at 43b87b6 per git log); the pending-principal credit is a second addition with no dated entry. The mirrors report named this; I confirm nothing was appended.
  - STALE PROSE THAT NOW MISDESCRIBES PRODUCTION: weights.ts:1-6 ('Phasing accounting bootstrap ... all overlapping coefficients are zero'), :14 (version comment), :19 ('Frozen pre-outcome vector'), :58-61 ('The approved accounting bootstrap'); vitest.config.ts:65 ('eval-correct 14/27 fail') and :71-79 (recall blocked because 'the bootstrap sets only five weights'); ablate.test.ts:755 test title 'A/A against the M6 bootstrap'; arms.ts:44-52 and :258-272 (per pins/mirrors reports); the nine reports/understand/*.md that describe the pre-repair tree; M6-STATUS.md, M5-FLOOR-PREREGISTRATION-v2.md and M6-BOOTSTRAP-CONTRACT.md (frozen — need dated supersession notes, not edits).
  - M6-BOOTSTRAP-CONTRACT.md:1 requires deviations be 'recorded and reviewed before outcomes' and :106 freezes weights 2/3 at 100 'unless the accounting model is explicitly revised'; HEAD's BankExcess=25 is exactly such a revision, and 53-0-11 outcomes were seen first. The evidence report named the amendment; the sequencing violation itself should be stated plainly in the record.
  - HAND-PRIORS ARMS ARE A/A AGAINST hard@desktop. arms.ts:981-984 clone DEFAULT_WEIGHTS (:956), so `hand-priors`, `hand-priors-pc` and `bank25-pc` now equal the champion's numbers under other labels and `bootstrap-pc` is hand priors with BankExcess 100. Any ladder row run against these arm names after 71b41a3 is an A/A row; the round3.sh comparator rows (confirm-* with hand-priors-pc) predate the change and remain valid.
  - muju/docs/PROMPT_scripted_bots_online.md:15-16 and :18-19 cite the handoff and the ?phasingAi=1 guard; still accurate (Rush still wins; guard still closed at handler.ts:113), but it is a separate feature request that itself needs the DAG walk (:32-33) and should not be started before step 1 lands, since it touches ModeSelect/worker routing that the cutover decision also changes.
  - CI GREEN DOES NOT PUBLISH PAGES (ci report, deploy.yml:56-57 guarded on CLOUDFLARE_API_TOKEN). The DAG static-deploy node already says 'record this node as blocked' (planner output). No reader recorded that Render's auto-deploy of eda73b2 is unverifiable from the repo, so whether the hand-priors engine is already live behind ?phasingAi=1 must be checked against the live host, not assumed either way.
  - THE M13 RECALL GATE ARTIFACT (lab/results/hard-ai-verify/M13.json per the mirrors report) will not reproduce once recall/run.ts:488-493 gains the credit; re-baselining it and the gates.ts brackets is a follow-on of the mirror that no ranked list carried.
RANKED:
  1. Finish handoff step 1 in one PR: (a) decide the BankExcess pin — change lab/hard-ai/tune/texel.ts:82 ACCOUNTING_PINS to {2:100, 3:25, 58:1} and tests/lab/phasing-tune-guards.test.ts:146 to match, with a dated amendment file beside docs/hard-ai/phasing/M6-BOOTSTRAP-CONTRACT.md (never edit it) recording BankExcess 100->25, the 38 priors, the unconditional credit, and that outcomes preceded the record; (b) keep WEIGHTS_VERSION = 2 and rewrite weights.ts:14 to say what the number actually gates (loadWeights/assertCurrentWeights) and that values are identified by weightsHash/label/ladder identity; (c) update tests/ai/hard/eval.test.ts:131-132, :155, :217, :296-306 (43-pair list, invariant slice, describe title), :345; tests/ai/hard/phasing-bootstrap.test.ts:57-58, :75, and decide :88/:104 (assert conservation on the accounting slice, or drop with a note); tests/lab/ablate.test.ts:198/:499/:532 as a documented fourth identity move using the full hashes from CI run 35552456980's log, :718-734 live counts (economy 6, home 10, safety 17, space 9, invariants 15, stage2 26 per pins), :755-778 (no vacuous arms), :793-809 (17 of 21 safety live); (d) fix stale prose at weights.ts:1-6,:19,:58-61, arms.ts:44-52/:258-272, vitest.config.ts:65,:71-79. Run `npm run hard:types && npm run hard:test` and `npm test` once node_modules are free.
     WHY: master's default vitest run is red with 24 failures (20 caused by 71b41a3), which masks any real regression, blocks every DAG node's verify command, and Render builds master with no test gate. Everything else (contract, mirror, measurements) is compared against this vector, so its pins and its recorded identity must be settled first.
     BLOCKED BY: another process owns node_modules (verification run only); gh access to read the CI log of run 35552456980 for the full hash literals
     EFFORT: hours (one focused session); the three sha256 literals can be read from the CI log before node_modules are free
  2. Write the change record muju/docs/changes/2026-09-20-hard-phasing-repair.md from `python3 tools/muju-content-dag.py plan --kind ai` (13 nodes, all pending), giving each node changed / verified unchanged / blocked with evidence; append a dated addendum to HANDOFF.md stating the default moved at 71b41a3, the credit is unconditional, +pc is inert, bootstrap-pc/hand-priors arms are no longer reproducible by name (use weights/bootstrap-control.json via MUJU_HARD_WEIGHTS), repair.patch is historical, and reports/understand/*.md describe the pre-repair tree; add the DEVIATIONS.md entry after :1791-1802; update CLAUDE.md:12-18 to point at the record.
     WHY: The commit message and HANDOFF.md:100 both say the DAG review is OWED; CLAUDE.md is the entry point for every fresh session and currently routes it to stale instructions. Doing this with step 1 (same PR) keeps the record honest about what was verified.
     BLOCKED BY: nothing (can be drafted in parallel with rank 1; dispositions filled as commands run)
     EFFORT: hours
  3. Mirror the scorer credit via one exported helper `withinTurnScore(ev, p, mover, sc, ply)` in src/ai/hard/eval/evaluate.ts, used by engine.ts:355-364 (byte-identical arithmetic) and the 8 duplicated closures (recall/run.ts:488-493, analyze/engine.ts:176-181, audit/gen-view.ts:71-76, audit/score.ts:45-60, coverage/run.ts:218-222, bench/p6-turn-time.ts:156-160, tests/ai/hard/generate.test.ts:49-53, tests/ai/hard/gen-trace.test.ts:49-53); add a unit test; leave pvs.ts:694 and evaluate.ts:171/185 alone; run hard:determinism; then re-baseline lab/results/hard-ai-verify/M13.json.
     WHY: The analyser (analyze.ts:550,606-612) now compares the real engine's played turns against a generator that no longer matches production, so E1/E2-style loss analyses emit spurious 'never offered' findings; and this touches src/ai/hard/**, which the floor contract's engineSourceSha256 covers, so it must land before rank 5.
     BLOCKED BY: rank 1 merged (so the test tree is green enough to see new failures); node_modules for determinism check
     EFFORT: hours
  4. Clean up the merged SCRATCH code: rebuild `bootstrap-pc` from the frozen bootstrap literal (weights/bootstrap-control.json values, hash 0ae24a95) instead of cloneWeights(DEFAULT_WEIGHTS) at arms.ts:956; drop or rename the three '+pc' arms and the four '+pc' JSON copies; label-scope or remove the MUJU_HARD_WEIGHTS hook at bots/hard.ts:296-300; fix ablate.test.ts accordingly. Also fix the 4 environment-only CI failures (deploy.yml checkout fetch-depth: 0 plus ripgrep on the runner, or skipIf gating as p6-stoppable-generation.test.ts:240 does).
     WHY: arms.ts and bots/hard.ts are inside engineSourceHashes(), so they must be final before the floor contract; the env hook overrides both seats in hard-vs-hard rows; and CI cannot go green (and never reaches the Playwright steps) until the 4 env failures are fixed.
     BLOCKED BY: rank 1
     EFFORT: hours
  5. Write a superseding v2 floor contract (new file, e.g. fixtures/v2/floor-contract-supersedes-1.json) carrying weightIdentity(DEFAULT_WEIGHTS) and hashJson(engineSourceHashes()) at the final source state, with supersedes {ledgerSeq: 1, chain: <measurement-ledger.jsonl line 1>}; commit it ALONE and push; then in a later commit run `npm run hard:suite:phasing:measure` (not the DAG's stale `hard:suite`) and record pass/fail per family without moving floors. Append the amendment to M5-FLOOR-PREREGISTRATION-v2.md rather than editing :912-921.
     WHY: Gate 0 item 7 (suites at floors) is unmet for the hand priors: the last measurement (2026-09-19, bootstrap) failed invariants 8/15, economy 19/20, disruption 12/14, and assertContractBuild refuses any measurement against the current contract. This is the first preregistered evidence the new default can earn.
     BLOCKED BY: ranks 1, 3, 4 landed (source hash must be final); node_modules
     EFFORT: hours (contract) + one measure run
  6. Run the correctness veto at HEAD: `npm run hard:perft && npm run hard:fuzz && npm run hard:determinism`, and record the artifacts under the change record's ai-strength node.
     WHY: No post-repair run exists in the repo; the DAG makes correctness a hard veto before any strength claim, and the ladder rows' 0 illegal / 0 divergence counters are not a substitute for perft/fuzz.
     BLOCKED BY: node_modules; can run in parallel with rank 5's measure
     EFFORT: hours of machine time, minutes of attention
  7. Run the queued measurements on dev openings only: (a) Round 3 — port scripts/round3.sh to this checkout (drop :3-6 laptop paths and pgrep loop, :19 stub; read weights from docs/.../weights/$v+pc.json; --shards <= 4, MUJU_HEAVY_SLOTS <= 4), six rows at seeds 33/31 as paired comparisons against the confirm-* rows, record label+hash per run because the manifest names all of them hard@desktop; (b) time-use arms hard@ablate:calib, work-fit, search-iter-fit, work-fit-deep, k48 vs aiv2-hard-turn at wall:6000 (1.5 s aiv2 rows go VOID). Commit summary.md/metrics.json only.
     WHY: These are the only strength experiments already scripted; they answer whether the reviewed vectors or the time-use arms beat the hand priors, and the cold-profile abort (firstSearchAborted 64/64, 91/96) is a larger time defect than the rung rounding. Do not open p1-val again.
     BLOCKED BY: node_modules; rank 4 if the env hook is removed (Round 3 relies on MUJU_HARD_WEIGHTS — keep a label-scoped form)
     EFFORT: hours of ladder time (~3-4 CPU-hours for Round 3, ~1-1.2 h wall at 4 shards; similar for the arms)
  8. Rush generator work, as lab arms first: add a `purchase` ArmFactor and `purchase-wide` arm (maxPlans 36, keepPerMultiset 1) so hard@desktop's hash does not move; then a score-then-select pass in gen/purchase.ts that includes the multiset stage (all 35 DFS multisets contain fire_1 at bank >= 14, so sorting alone is insufficient); STRENGTH mission in gen/promote.ts (rewrite promote.test.ts:137-160 by design); two ordinary promotions per Prepare; 50% at-risk service PV at pending.ts:116 with its contract amendment. Re-measure vs Rush (and Balanced/Expand) after each.
     WHY: Only after the default is pinned and measured; Rush losses are now by elimination at 93-97% spend, so the lever is menu composition, which needs code, tests and DESIGN.md:1043,1066 updates — days of work that should not race the record-keeping.
     BLOCKED BY: ranks 1-5 (identity hash and floor contract settled)
     EFFORT: days
  9. Owner decisions, recorded as dated amendments: (a) whether PHASING-PREREGISTRATION-2026-09-18.md's Gate 1 / Gate 0 item 7 remain prerequisites for Gate 2, disclosing that p1-val was read by three rows; (b) the Phasing-only cutover scope (Variant B: flip ModeSelect/OnlineLobby/client/schema/mcp/observation/AnalysisScreen defaults, leave board.ts:204; whether to reject ruleset:'standard' on create); (c) whether to stop the Standard+Hard pack-error fallback now by adding a ruleset check at useAI.ts:125. Then Gate 2 on the owner's machine with p1-sealed rows 0-31 and a NEW RELEASE-<date>-phasing.md.
     WHY: Both the guard opening (content-dag.json hard-ai node: 'keep guards closed until ai-strength passes') and the sealed row are policy-gated and cannot be done by an agent; surfacing the decision early avoids more val peeking.
     BLOCKED BY: owner; p1-sealed is not in the repo
     EFFORT: days (cutover fallout 14 files/64 tests minimum); owner time for the decision
  10. Texel readiness: replace the seven 'muju-phasing-1' literals (tune/corpus.ts:269,279,375; tune/rows.ts:146,190,227,290) with LADDER_RULES_VERSION, write or remove hard:spsa/hard:book (package.json:64-65), write the SHA-bound allowlist publisher, build an equal-strength self-play corpus, bound the fitter. Keep the tactical/home terms hand-set.
     WHY: Lowest priority: gap-tuner.md showed a fit on lopsided replays learns bot identity; nothing here is needed to ship the hand priors.
     BLOCKED BY: rank 1 (BankExcess pin decision) and a dev-only corpus
     EFFORT: days
HANDOFF CORRECTIONS:
  - HANDOFF.md:18 — the within-turn scorer is at src/ai/hard/engine.ts:355-364 at HEAD (base :359, credit :363), not :355-360.
  - HANDOFF.md:64-65 — resolved: 71b41a3 made the pending-summon credit unconditional; no `endsWith('+pc')` remains in src. The label gate survives only in docs/hard-ai/phasing/repair-2026-09-20/repair.patch:10, which is historical.
  - HANDOFF.md:72-73 — '+pc turns the scorer credit on' is stale; the four '+pc' JSONs differ from their plain copies only in the label line and change nothing at HEAD.
  - HANDOFF.md:66 — arms bootstrap-pc and bank25-pc no longer produce the bootstrap: handPriorsPatch (arms.ts:956) clones the NEW DEFAULT_WEIGHTS, so bootstrap-pc is hand priors with BankExcess 100 and hand-priors/hand-priors-pc/bank25-pc equal the champion under other labels. The 'scorer fix ALONE 25-0-7' row (:13, :46) is reproducible only via MUJU_HARD_WEIGHTS=weights/bootstrap-control.json (hash 0ae24a95).
  - HANDOFF.md:70-71 — weights/ holds 32 JSON files (28 distinct hashes) plus make-variants.ts; the hand priors that became DEFAULT_WEIGHTS are NOT one of the 13 reviewed vectors and no file in weights/ equals the HEAD default in both w and material.
  - HANDOFF.md:95-96 — step 1's first two parts landed in 71b41a3 (unconditional credit; DEFAULT_WEIGHTS = makeWeights('phasing-hand-priors-v1'), 43 non-zero, BankExcess 25); the rest is open: tests, mirrors, WEIGHTS_VERSION, DAG walk.
  - HANDOFF.md:97 — the pin list is incomplete and partly mis-typed. Failing at HEAD (CI run 35552456980: 7 files / 24 tests): tests/ai/hard/eval.test.ts :131, :155, :217, :296-306, :345; tests/ai/hard/phasing-bootstrap.test.ts :57-58, :75, :88, :104 (the last two are escrow-conservation invariants, not numeric pins); tests/lab/phasing-tune-guards.test.ts:146-148 via lab/hard-ai/tune/texel.ts:82 (w[3] pinned at 100 vs HEAD 25); tests/lab/ablate.test.ts :198/:499/:532 (resolved-config hash literals, five use sites), :718-734, :755-778, :808. No suite manifest pins the label; the binding pin is fixtures/v2/floor-contract.json:7-8 (engineSourceSha256 + weightsSha256, label excluded) which needs a superseding contract, and the m6-evaluation freeze artifacts are historical and must not be edited.
  - HANDOFF.md:98 — the closures to mirror number exactly 8 including recall/run.ts; none has been started (git diff a02bbb7..HEAD under lab/ and tests/ touches only ablate/arms.ts and bots/hard.ts). Sites that call stage0/stage1 but are not within-turn scorers (pvs.ts:694 futility, evaluate.ts:171/185, eval-audit.ts, bench/run.ts) must NOT receive the credit.
  - HANDOFF.md:99 — WEIGHTS_VERSION is still 2 while weights.ts:14 says it is bumped whenever the numbers change: an open inconsistency, not a future choice. A bump would invalidate the 32 repair JSONs, the 2 M6 freeze artifacts, the MUJU_HARD_WEIGHTS hook, rows.ts:178 and five pins in phasing-tune-guards.test.ts; the DAG's 'bump weightsVersion' instruction and m6-dev-continuation.md:142's 'do not silently increment' should both be answered in the change record.
  - HANDOFF.md:100 — the DAG walk has not started: all 13 nodes are pending, no docs/changes record exists, and the DAG's `npm run hard:suite` verify command points at the Standard-era runner; the Phasing floors instrument is `hard:suite:phasing:measure` (package.json:67).
  - HANDOFF.md:101-102 — confirmed not run (no round3* results directory, no round-3 progress line); round3.sh hard-codes laptop paths (:3-4) and the K48 seventh row is a no-op stub (:19).
  - HANDOFF.md:29-33 — 'sort before truncating' is necessary but not sufficient: the multiset list is DFS-truncated at maxMultisets 35 of 209 and all 35 contain fire_1 at bank >= 14; the cheapest-first order lives in core/catalog.ts:186-189, not config.ts:543; '8 of 51' is a 12-position probe, population numbers are 339/3,842 and 504/5,525.
  - HANDOFF.md:34-35 — the larger time defect is the cold profile INITIAL_UNITS_PER_MS = 200 (config.ts:587) deadline-aborting every game's first search (64/64, 91/96); an existing arm hard@ablate:calib is the ready experiment; units/ms figures are machine-specific and their per-turn rows were not committed.
  - HANDOFF.md:48, :59 — p1-val was read by three rows (two candidate engines, three seeds), and the 53-0-11 row is development evidence under the project's own rules (val allocation, h0 only, wall:6000 not 8000, seed 51, no SPRT, Gate 0 item 7 unmet, Gate 1 never passed, arm label scratch-hand-priors-v1+pc not the production label).
  - HANDOFF.md:109 — rows.ts carries four more 'muju-phasing-1' literals (:146,:190,:227,:290) beyond corpus.ts; and BankExcess 25 breaks texel's start-vector pin so hard:texel would throw before fitting.
  - HANDOFF.md:114-119 — server/mcp.ts:39 is the instructions string, not a default (defaults at :66-69); there are 2 COALESCE sites (rooms.ts:299,320) plus five `?? 'standard'` fallbacks; AnalysisScreen.tsx:30 and four `phasing = false` prop defaults are omitted; OnlineLobby is at src/online/; the worker guard reads the request marker, not readPhasingAiPreview().
  - HANDOFF.md:126-127 — the '0 active rooms / 38 archived (28 Standard)' figure is unsourced in the checkout; the cutover fallout numbers were measured without rejecting ruleset:'standard' on create.
  - HANDOFF.md:128-129 — CI is now red for 7 files / 24 tests at eda73b2 (not 4); a green run would still not publish Pages because the Deploy step is skipped without CLOUDFLARE_API_TOKEN; 'Render auto-builds master' rests on ONLINE.md and Dockerfile only (no render.yaml), so whether eda73b2 is live is unverified.
```
