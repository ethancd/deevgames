# Gate 1 runner — adopted amendment A2

A2 is pinned to commit `04097f0e5a78b54432de9363a9fee648b5a863d6` and
preregistration SHA256
`3140974aa223b820d084c83453a1cf88ea067cabc4b2b003d19f65fa1b08202d`.
The runner verifies that Git object and writes `preregistration-A2.md` in each
new evidence directory. This implementation note supersedes the A1 runner
instructions for new runs; the A1 document and evidence remain historical.

The seed-20260957 full row is void after 130 completed games because V2 purchase
pruning contaminated the scripted opponents' shared legality inventory.
Its games must not be pooled or reused. The correction restores the ordinary
Prepare inventory from T1: every affordable, rules-legal purchase and promotion,
and the matching phase end. Only V2's placement and beam candidate selection
apply the existing disruption estimate. If every remaining legal purchase is
risky, all remain candidates, including after safe squares become reserved.
No values, weights, budgets, opponent policies or acceptance thresholds changed.

“Complete” in this correction means the ordinary Prepare candidate universe.
Historical contracts outside that defect remain unchanged: AI/harness generators
omit RESIGN, their direct MOVE candidates cover one speed-length action, and
`upkeepActions` uses bounded candidate sets above 12 rent-bearing pieces.
MCP separately serves RESIGN, multi-action moves and one explicitly documented
upkeep selection. These limitations are not newly certified as full exhaustive
oracles by the Prepare regression. The dated A2 preregistration bytes are unchanged.

Replacement allocation, fixed before the corrected runs:

| Mode | Seed | Mirrored pairs per opponent/handicap | Games |
| --- | --- | --- | --- |
| Pilot | 20260959 | 1 | 16 |
| Full | 20260958 | 64 | 1024 |

Both use the canonical Phasing initial position without an opening corpus,
hard/medium fixed work 6000/3000 per own turn, Rush/Expand/Balanced/medium,
and handicaps 0 and 3. The pilot remains ineligible. Corrected runs require a
reviewed, clean, committed checkout; run the pilot before the full row into new
directories. The shared heavy queue remains mandatory.

All A1 acceptance remains unchanged: per-cell score/paired Elo criteria for
Expand, Balanced and medium; report-only Rush strength; unchanged frozen
scripted-opponent purchase/inactivity bands; at least one hard purchase in each
game exceeding ten completed player turns; at most 1% adjudication per cell;
and exact schedule/identity with zero correctness vetoes. An old A1 allocation
is rejected by the A2 report even though its historical failures remain visible.
A full result can fail. Nothing here unlocks a worker or establishes wall-clock
responsiveness, Hard correctness, sealed strength or release readiness.

From `muju/`, inspect allocation without playing:

```sh
node --import tsx lab/ai/gate1.ts --mode full --plan
```

The coordinator owns the new pilot and full-row launches after review. This
correction's verification runs focused regressions, including the existing
fixed-work repeat-game test; it does not launch a pilot or full evaluation row.
