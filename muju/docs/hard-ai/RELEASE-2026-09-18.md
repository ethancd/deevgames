# Hard AI release record — 2026-09-18

Directive (Ethan, 2026-09-18 ~02:35Z): bring the Hard AI epic to a successful
end point within eight hours — every piece of Muju work merged into one
codebase on `master`, the new engine deployed as the Hard difficulty — so the
follow-on work (engine tooling and MCP aligned to the Phasing variant, the
engine on Phasing, handicap tuning) can start from one tree. This file is the
decision record for that release (EPIC-PLAN §7 shape), written as it happens;
stamps from `date -u`; appended, never rewritten.

## What ships

- Engine: `hard@desktop` — the champion, weights `default-v1`, config hash
  `4e7afdf76b32fadfab2d11577cb610c8f9f1a491b5153e70b87b0774403600cd`, every
  `evalFix`/`searchFix` flag absent. Not a candidate: the two E3 candidates
  stay development evidence (`eval-no-safety` carries the M14 spawn-strike
  veto, `eval-correct-v1` a boundary confirmation), so nothing that lowers a
  bar ships.
- Route: the E5.1 worker route becomes the default for the Hard difficulty
  (`hardEnabled = true`, DESIGN §6.4), with an opt-out (`?hardAi=0`) to the
  legacy `AIEngineV2`, budget `TURN_BUDGET_MS.hard` 8,000 ms (the A5 release
  allowance) and a `?hardMs=` page override for faster play. Phasing keeps its
  guard: the engine plays Standard rules only.
- Branch: `claude/hard-ai-e4` (which contains every earlier Hard AI branch and
  `origin/master`) merged to `master` by pull request; CI (build-all, server
  types, full vitest, Playwright online e2e, phone smoke) publishes the site to
  Cloudflare Pages and Render rebuilds the online server.

## Release ledger (seed = 20260950 + ordinal)

| # | seed | arm | vs | openings | pairs | out | result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | 20260951 | `hard@desktop` (shipping) | `aiv2-hard` (shipped Hard) | `e1-sealed.jsonl` rows 0–31 (SEALED, first use, h0/h3) | 32 | `lab/results/hard-ai-release/r1-sealed-wall8000` | preregistered 2026-09-18T02:40:37Z; launches when row #6's slots free, 6 shards, 8 slots; DONE 04:07:48Z (launched 03:10:38Z at e078a41a, 6 shards): 42/2/20, 0.672, +124 [+38, +229], LOS 99.8%; h0 22/1/9 .703, h3 20/1/11 .641; overrunRate 2.0% (hard) / 4.1% (aiv2), not voided; p95 8,036 / 8,073 ms, max 8,711 / 8,642 ms; illegal 0, divergences 0; loadAvgMean 260 (the box was overloaded by an unrelated process storm) → rule MET; R2 rerun on a quiet box for clean timing |
| R2 | 20260952 | `hard@desktop` (shipping) | `aiv2-hard` | `e1-sealed.jsonl` rows 0–31 (same block; R1's timing columns were taken under load 260, the openings leak nothing — nothing is tuned) | 32 | `lab/results/hard-ai-release/r2-sealed-wall8000` | preregistered 04:09:19Z; launches now on the quiet box, 6 shards, same command and prediction as R1 |

### R1 preregistration (2026-09-18T02:40:37Z)

- Both arms at wall:8000 (A5: the product allowance), h0/h3, `--legality
  strict`, 32 pairs on the sealed pool (E6's held-out set, untouched until
  now; this is its release use), seed 20260951, `--shards 6`,
  `MUJU_HEAVY_SLOTS=8`, from `~/src/deevgames-e4-run` at the commit that
  carries this file (`src/ai/hard` flag-absent behaviour identical to the E3
  rows: hash unchanged, golden 48/48 at every E4 merge).
- Prediction, falsifiable: score > 0.5 with the pair-level 95% interval
  excluding 0 (priors at wall:3000: E1 +203 [+145, +273], E2 re-pin +215
  [+156, +287]; E1's 8-pair check at wall:8000 was 12/4). If ≤ 0.5 or the
  interval covers 0: the engine does NOT ship as the default; the opt-in
  route stays and this record says so.
- Responsiveness read from the same row: p95 and max turn ms per seat,
  overrun rate (A14 voids at 5%), the P6 flag (> 20 s), abort share; these
  go into the record as what the player will see at 8 s.
- Correctness: illegalActions 0, replicaDivergences 0 required; fixed-work
  golden identical at the release commit.
- Not measured tonight, recorded as red: phone/tablet device timing (E5.2),
  memory, p99, the P8/P7 tail (worst turns ~25 s on 30-body home races at
  3 s; at 8 s the search half is capped by the deadline, the canonical half
  is not), tactical suites at the shipped allowance.

## Not in this release — first follow-on items (02:49:37Z)

- **Codex's uncommitted rules WIP** in the main checkout
  (`~/src/deevgames`, branch `codex/muju-local-wip-2026-09-16`, 52 files,
  +3,335/−1,872, reported by the coordinating session at 02:49:37Z): it changes
  `src/game` (board, homeCheckmate, resourceMap, rules, types, units, a new
  analysis.ts), tests, server, online and components, and does not touch
  `src/ai`. It is NOT merged tonight: the Hard engine keeps a replica of the
  rules and verifies every plan canonically, so a rules change needs a
  parity pass first (`hard:perft` and `hard:fuzz` numbers, replica
  divergence 0 over a ladder row, the fixed-work golden re-pinned). That
  pass is the first task after this release, together with the Phasing
  alignment Ethan named.
- Campaign artifacts: ladder replays and game logs are archived in
  `~/src/deevgames-hard-ai-campaign-2026-09-18.bundle` (434 MB, verified)
  and the local campaign branches, not in `master`
  (`lab/results/hard-ai-ARCHIVE.md` in the release commit).

## R1 read and R2 preregistration (04:09:19Z, `date -u`)

- R1 (artifacts `lab/results/hard-ai-release/r1-sealed-wall8000/`): the
  shipping engine beat the shipped Hard 42/2/20 on the sealed pool at the
  8,000 ms allowance, +124 [+38, +229], both strata above 0.5; the rule as
  preregistered (score > 0.5, interval excluding 0) is MET; hygiene clean.
  Caveat stated plainly: the row ran while the box carried an unrelated
  process storm (mean load 260; 5.5 s of an 8 s turn were lost to
  contention on both seats alike), so its overrun and abort columns (2.0% /
  4.1%, abort 17.8%) describe that box, not the product; the harness did not
  void it (A14's 5% bar holds) and the score is an equal-time result under
  equal contention.
- R2: the same row, seed 20260952, on the quiet box, to record the
  responsiveness columns the player will see. The merge does not wait for
  it; it is appended when read. If R2 fails the rule, `hardEnabled` is
  flipped to `false` in a follow-up commit and this record says so.
