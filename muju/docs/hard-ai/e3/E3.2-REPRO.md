# E3.2 — the row #1 reproduction record (lane 10, 2026-09-17)

E3-PLAN.md's "Row #1 preconditions" says three things must be recorded under
`lab/results/hard-ai-e3/repro/` before the screening row launches:

1. the static reproduction with the arm's weights (+60 / −60),
2. the arm's exam end key on `loss-g4-s6_3_5-B-white-t1`,
3. a fresh-engine work sweep at that root for the arm.

All three ran before the launch. This file is that record, plus what each of
them found that the preregistration did not predict. The row was then launched
and is running; section 5 is its launch record.

Everything below is `hard@ablate:eval-no-safety` — the 19 safety weights of
`lab/hard-ai/audit/eval-groups.ts` at 0 — against `hard@desktop`, both at the
E3 head `50b6a94c`. The arm's weights are resolved through
`lab/hard-ai/bots/hard.ts hardEnginePatch('ablate:eval-no-safety')` in every
instrument below; both new options assert `weights.version !== 0` and that the
label is `default-v1-no-safety` and not `default-v1` (E0's I2 lesson).

- `hard@desktop` at `wall:3000`: `4e7afdf76b32fadfab2d11577cb610c8f9f1a491b5153e70b87b0774403600cd` (unchanged; re-checked at the end of this lane).
- `hard@ablate:eval-no-safety` at `wall:3000`: `66edf9cdf58f6b037186b25d05a939ed521e8a76e07051937dcbc611eb343aaf`.

## 1. Static: the +60 and the −60 reproduce exactly

- Instrument: `lab/hard-ai/audit/loss-judgment.ts --arm eval-no-safety`, the
  option this lane added. It re-reads THE SAME reconstructed end states with
  the arm's price list; no engine is built, no root is re-searched, nothing
  about the corpus moves.
- Command, from `muju/`:
  `node --import tsx lab/hard-ai/audit/loss-judgment.ts --exposed lab/results/hard-ai-e2/analysis/e1.1-losses-exposed --sweep lab/results/hard-ai-e2/analysis/e1.1-losses-work-sweep-fresh/sweep.json --replays lab/results/hard-ai-e1/e1.1-diag/replays --out lab/results/hard-ai-e3/repro/static-eval-no-safety --arm eval-no-safety --no-aiv2 --label e1.1-eval-no-safety`
- Result: 12 distinct positions from 14 analysed turns, the same 12 lane 6 read.
  - `g4-s6_3_5-B-white` + `g5-s7_3_7-B-white`, black, turn 1, root `74b71f89ae7abb64`: `staticDeltaCc` **+60** for the arm against **−275** for the champion. Predicted +60. Confirmed.
  - `g4-s2_3_1-B-white`, black, turn 2, root `8e7c6996a650e7f7`: `staticDeltaCc` **−60** for the arm against **−1,475** for the champion. Predicted −60. Confirmed. It does not flip.
- The default path is unchanged. The same script with no `--arm`, run into a
  scratch directory, reproduces lane 6's committed artifacts field for field on
  `staticDeltaCc`, `searchedDeltaCc`, `verdict`, `rootKey`, both end keys, all
  58 `featureDeltas`, the five `groupDeltas`, both `Decomposition`s, `judge1`
  and `judge4` at all 12 positions.

### The full 12, arm next to champion

`static Δ` is the adviser's end state minus the played one, centi-crystals,
from the seat that lost. Positive means that engine's leaf prefers the turn the
adviser preferred. `safety Δ` is the champion's safety-group share of its own
delta; the arm's shift is exactly its negation, which is what "the arm is the
safety block at zero" means arithmetically.

| position | seat | turn | class | champion static Δ | arm static Δ | shift | champion safety Δ | reading |
| --- | --- | ---: | --- | ---: | ---: | ---: | ---: | --- |
| `g2-s20_3_15-A-white` | white | 3 | strong-candidate-misjudged | +432 | +92 | -340 | +340 | both prefer the adviser |
| `g2-s20_3_15-B-white` | black | 7 | fixed-work-divergence | +395 | +670 | +275 | -275 | both prefer the adviser |
| `g2-s5_0_2-A-white` | white | 4 | fixed-work-divergence | +281 | +746 | +465 | -465 | both prefer the adviser |
| `g4-s10_0_8-A-white` | white | 3 | fixed-work-divergence | -385 | +15 | +400 | -400 | **flips to the adviser** |
| `g4-s10_0_8-B-white` | black | 7 | fixed-work-divergence | -2162 | -1412 | +750 | -750 | both prefer the played turn |
| `g4-s10_3_9-A-white` | white | 2 | strong-candidate-misjudged | -360 | -360 | +0 | +0 | both prefer the played turn |
| `g4-s2_3_1-B-white` | black | 2 | fixed-work-divergence | -1475 | -60 | +1415 | -1415 | both prefer the played turn |
| `g4-s6_0_4-A-white` | white | 4 | fixed-work-divergence | +192 | +192 | +0 | +0 | both prefer the adviser |
| `g4-s6_3_5-A-white` + `g5-s7_3_7-A-white` | white | 4 | fixed-work-divergence | -361 | -246 | +115 | -115 | both prefer the played turn |
| `g4-s6_3_5-B-white` + `g5-s7_3_7-B-white` | black | 1 | strong-candidate-misjudged | -275 | +60 | +335 | -335 | **flips to the adviser** |
| `g5-s11_0_10-B-white` | black | 1 | fixed-work-divergence | -409 | -134 | +275 | -275 | both prefer the played turn |
| `g5-s11_3_11-B-white` | black | 2 | strong-candidate-misjudged | -2809 | -1399 | +1410 | -1410 | both prefer the played turn |
- The arm moves the static delta toward the adviser at 10 of 12 positions and
  is exactly neutral at 2 (`g4-s10_3_9-A-white` t2 and `g4-s6_0_4-A-white` t4,
  where the safety group scores the two end states identically).
- It changes the champion's static PREFERENCE at 2 of 12: the named development
  case, and `g4-s10_0_8-A-white` white t3 (−385 → +15), which the
  preregistration does not mention. That one is a 400 cc shift crossing zero by
  15 cc — recorded as a flip, not leaned on.
- It moves the delta AWAY from the adviser at 1 of 12: `g2-s20_3_15-A-white`
  white t3, +432 → +92 (safety was worth +340 to the adviser there). That
  position is the one the synthesis routes to E4 as a search tie-break, so the
  arm loses a leaf preference the search was not acting on anyway.
- Verdict counts, champion → arm: `static-prefers-played` 8 → 6,
  `static-and-search-agree-on-adviser` 3 → 4,
  `static-prefers-adviser-search-does-not` 1 → 2.

Artifacts: `lab/results/hard-ai-e3/repro/static-eval-no-safety/` (12 position
files, `summary.json`, `summary.md`). The summary records
`weightsLabel: default-v1-no-safety`, `arm: eval-no-safety`,
`weightsVersion: 1`.

## 2. Searched: the arm's exam end key is the AVOIDED key, not the preferred one

- Instrument: `npm run hard:exam -- --engine <e> --stratum dev --ids <three
  cases> --work <w> --no-heavy`. `--ids` already existed; nothing was changed
  in the exam runner.
- The three ids, all `kind: judgment`, `stratum: dev`:
  `loss-g4-s6_3_5-B-white-t1` and `loss-g5-s7_3_7-B-white-t1` (the same
  position, `stateDigest 38c7e20b…`, preferred key `559e2d60610127d8`, avoid
  key `4844d47f0e9b8049`), and `loss-g4-s2_3_1-B-white-t2` (preferred
  `dd8b039153935d77`, avoid `82ee31872ff9ee0a`).

| case | work | `hard@desktop` end key | `hard@ablate:eval-no-safety` end key | preferred | arm matches? |
| --- | ---: | --- | --- | --- | :-: |
| `loss-g4-s6_3_5-B-white-t1` | fixed:25,000 | `4844d47f0e9b8049` (avoid) | `4844d47f0e9b8049` (avoid) | `559e2d60610127d8` | no |
| `loss-g5-s7_3_7-B-white-t1` | fixed:25,000 | `4844d47f0e9b8049` (avoid) | `4844d47f0e9b8049` (avoid) | `559e2d60610127d8` | no |
| `loss-g4-s2_3_1-B-white-t2` | fixed:25,000 | `dd8b039153935d77` (preferred) | `dd8b039153935d77` (preferred) | `dd8b039153935d77` | yes, and so does the champion |
| `loss-g4-s6_3_5-B-white-t1` | fixed:200,000 | `4844d47f0e9b8049` (avoid) | `4844d47f0e9b8049` (avoid) | `559e2d60610127d8` | no |
| `loss-g5-s7_3_7-B-white-t1` | fixed:200,000 | `4844d47f0e9b8049` (avoid) | `4844d47f0e9b8049` (avoid) | `559e2d60610127d8` | no |
| `loss-g4-s2_3_1-B-white-t2` | fixed:200,000 | `82ee31872ff9ee0a` (avoid) | `187dc5a8404d0867` (a third turn) | `dd8b039153935d77` | no |

- **This is the precondition's real answer and it is negative.** The arm's leaf
  prefers the adviser's end state at `g4-s6_3_5-B-white` t1 by +60 cc, and the
  arm's SEARCH still plays the avoided turn at both exam rungs. A leaf flip of
  60 cc does not survive a depth-2/3 search at this root.
- The synthesis §3.5 caveat about lane 5's dev exam ("judgment 5/22 for the arm
  against 4/22 but it does not name which row changed") is answered for these
  three rows: none of them is the row that changed. Both engines match 1 of 3
  at 25k and 0 of 3 at 200k on this slice.
- The 200k row for `loss-g4-s2_3_1-B-white-t2` is a real difference between the
  engines (`82ee31872ff9ee0a` vs `187dc5a8404d0867`), but it is avoid-vs-third,
  not a move toward the preferred key.
- Caveat, stated: these are FIXED-work rungs. The row is `wall:3000`. A fixed
  rung is the only way to hold the two engines to the same search size, and it
  is not what the seat will do in the row.

Artifacts: `lab/results/hard-ai-e3/repro/exam/` — four `*.json` and four
`*.md`, named `<engine>-fixed<work>`. The arm's runs record
`configHash: hard:ablate:eval-no-safety:fixed:25000#570d584695e21b38…`.

## 3. Sweep: the arm flips at 400k, to a turn the champion cannot even list

- Instrument: `lab/hard-ai/analyze/work-sweep.ts --engine hard@<label>`, the
  option this lane added. It swaps the PRODUCTION engine only. The adviser that
  scores whatever the search chose stays the artifact's own `hard@desktop`,
  because `adviserBestDeepCc` — the yardstick every flip is measured against —
  was produced by it. Swapping the adviser too would move the question.
- Mode `fresh`: a new engine per (turn, work), the napkin's E2 lesson.
- Commands, from `muju/`, source a one-artifact directory holding
  `e1.1-losses-exposed/g4-s6_3_5-B-white.json` with its `replay` field
  re-pointed at this worktree's byte-identical copy of the E1 replay:
  `node --import tsx lab/hard-ai/analyze/work-sweep.ts <src> --out lab/results/hard-ai-e3/repro/sweep/desktop --mode fresh`
  and the same with `--engine hard@ablate:eval-no-safety --out …/sweep/eval-no-safety`.

Root `74b71f89ae7abb64` (`g4-s6_3_5-B-white` = `g5-s7_3_7-B-white`, black,
turn 1). Adviser's best `559e2d60610127d8` at −1,797 cc; played
`4844d47f0e9b8049` at −2,473 cc; flip tolerance 300 cc.

| rung | `hard@desktop` chooses | adviser's value | `hard@ablate:eval-no-safety` chooses | adviser's value | arm flip? |
| ---: | --- | ---: | --- | ---: | :-: |
| 100,000 | `4844d47f0e9b8049` (played) | −2,473 | `4844d47f0e9b8049` (played) | −2,473 | no |
| 200,000 | `4844d47f0e9b8049` (played) | −2,473 | `4844d47f0e9b8049` (played) | −2,473 | no |
| 283,000 | `4844d47f0e9b8049` (played) | −2,473 | `4844d47f0e9b8049` (played) | −2,473 | no |
| 400,000 | `4844d47f0e9b8049` (played) | −2,473 | `7a4e0a31c5259add` (a third turn) | −2,032 | yes, within tolerance |
| 566,000 | `4844d47f0e9b8049` (played) | −2,473 | `7a4e0a31c5259add` | −2,032 | yes |
| 800,000 | `4844d47f0e9b8049` (played) | −2,473 | `7a4e0a31c5259add` | −2,032 | yes |

- `flipWork`: champion **never** (up to 800k), arm **400,000**. The
  preregistration predicted "the arm's search prefers the adviser's key at some
  rung ≤ 400k where the champion never does". The rung is right and the
  direction is right; the KEY is not. The arm never chooses `559e2d60610127d8`.
  It chooses a third turn that the champion's own adviser scores 441 cc above
  the played turn and 235 cc below the adviser's own best — inside the sweep's
  300 cc tolerance, which is what makes it a flip by the instrument's rule.
- Say it plainly: the arm searches its way to a turn that is better than the
  played one by the adviser's reckoning, at a rung the seat can afford. It does
  not vindicate the adviser's specific turn.
- **The arm is not a leaf-only change, and the sweep proves it at this root.**
  The generator's candidate list is built with `cfg.weights`
  (`GenFamilyLister(cfg.gen, cfg.weights)`), so the 19 zeros move the list too:
  - `hard@desktop` lists **18** candidates at `74b71f89ae7abb64`; the arm lists
    **19**.
  - `7a4e0a31c5259add` — the turn the arm's search plays from 400k up — is the
    arm's candidate #9 and is **not in the champion's list at all**.
  - Both lists contain the adviser's `559e2d60610127d8` (#7) and the played
    `4844d47f0e9b8049` (#6).
  - Consequence for the row report: row #1 prices "the 19 safety weights
    wherever the engine reads them", which includes the generator's static
    ordering and its K-cut, not only the search leaf. Synthesis §3.2's "what
    changes" bullet lists the 19 `w[]` entries, which is true, and this is what
    those entries reach. `tests/lab/ablate.test.ts`'s one-factor invariant is
    unaffected: the factor IS `weights`.
- The default path is byte-identical. The champion sweep re-run here reproduces
  E2's `e1.1-losses-work-sweep-fresh/sweep.json` on this turn exactly: same six
  `chosenEndKey`s, same `depth` (2, 3, 3, 3, 3, 3), same `candidates` and
  `searched` (18/18 at every rung), same `chosenDeepCc` (−2,473 at every rung),
  same `flipWork` (null).

Artifacts: `lab/results/hard-ai-e3/repro/sweep/desktop/{sweep.json,sweep.md}`
and `lab/results/hard-ai-e3/repro/sweep/eval-no-safety/{sweep.json,sweep.md}`.
The arm's report records `engine: hard@ablate:eval-no-safety` and
`engineWeightsLabel: default-v1-no-safety`; the champion's records `null` for
both, which is how a default-path sweep is told from a swapped one.

## 4. What the three preconditions add up to

- Precondition 1 (static): **met, exactly as predicted.**
- Precondition 2 (searched exam end key): **met as a record, negative as a
  result.** The arm does not reach the preferred key at either rung on either
  spelling of the development case.
- Precondition 3 (fresh sweep): **met, and half-predicted.** The arm flips at
  400k where the champion never flips, to a third turn rather than to the
  adviser's.
- None of this falsifies the row's own prediction, which is about the score of
  64 games at equal time, not about this root. It does bound what the row's
  report may claim: the development case shows the arm's leaf and search moving
  toward turns the adviser rates higher, and does not show the arm finding the
  adviser's turn.

## 5. Row #1 launch record

- Go file `…/scratchpad/e3-row1-go` present, stamped `2026-09-17T06:32:07Z`.
- Launched `2026-09-17T06:34:15Z` from `/Users/ashkie/src/deevgames-e3-run/muju`,
  a detached worktree verified at `50b6a94c` before launch.
- `nohup npm run hard:ladder -- --a hard@ablate:eval-no-safety --b hard@desktop
  --work wall:3000 --handicaps 0,3 --pairs 32 --openings
  lab/hard-ai/ladder/openings/e1-val2.jsonl --openings-skip 0 --seed 20260931
  --legality strict --shards 1 --out
  lab/results/hard-ai-e3/ablate/eval-no-safety/screen`, logging to
  `lab/results/hard-ai-e3/ablate/eval-no-safety/screen.nohup.log`.
- Launcher pid 97431; the ladder shard took heavy slot 1 at
  `2026-09-17T06:34:16.317Z` as pid 97550 (slot 0 is E2's re-pin row,
  `hard@desktop`-vs-`aiv2-hard`, from `deevgames-e2-run2`, started 06:12:37Z).
  It did not wait for a slot.
- `manifest.json` at `06:34:16.315Z`: `pairs 32`, `seed 20260931`,
  `openings.path lab/hard-ai/ladder/openings/e1-val2.jsonl`,
  `openings.sha256 fe3b9c98ad216773fc1766ef618f8aed7efa2e990f04ea409b6ed9bce49d2007`,
  `openings.skip 0`, `handicaps [0, 3]`, `work wall:3000`, `legality strict`,
  `shards 1`, `openingsIndependent true`, `independenceWarning null`,
  `overrunTolerance {spec: "max(10ms,1%)", frozen: true}`,
  `git 50b6a94c2d5322769dff11ea83fb18ecfc1e74b2`.
- Both arms resolved in the manifest, checked three ways (E0's I2 lesson):
  - `aConfigHash hard:ablate:eval-no-safety:wall:3000#66edf9cdf58f6b03…`,
    `aResolvedConfig.config.weights.label default-v1-no-safety`, version 1, the
    19 safety indices all 0.
  - `bConfigHash hard:desktop:wall:3000#4e7afdf76b32fadfab2d11577cb610c8f9f1a491b5153e70b87b0774403600cd`,
    `weights.label default-v1`, version 1, the 19 safety indices at their
    `DEFAULT_WEIGHTS` values (−20, −50, −30, −25, −10, +20, +35, −40, −120,
    −150, −250, −100, −120, −150, −150, −40, −60, −150, −250).
- `gitDirty: true` in the manifest. The only untracked paths in that worktree
  are the row's own output directory and its nohup log; no tracked file is
  modified. Recorded, not treated as a contamination.
- `--pairs 32` is 32 pairs ACROSS both handicaps, i.e. 16 per handicap, i.e.
  `e1-val2.jsonl` rows 0–15, which is the preregistered block (the napkin's E2
  lesson). `manifest.openings.ids` lists all 32 ids in the file, not the 16 in
  use; the block actually played is readable from `games.jsonl`'s `openingId`s
  when the row finishes.
- Progress at `06:37:13Z`: `status running`, `pairsPlanned 32`,
  `pairsCompleted 0`, 1 game written. At that pace the row is a ~2–3 hour run,
  in line with the synthesis's 1–3 h estimate. This lane did not wait for it.
- `…/scratchpad/e3-row1-launched` written with the pid, the shard pid, the
  launch time, the out directory and the worktree.

## 6. What this lane changed, and what it did not

- `lab/hard-ai/audit/loss-judgment.ts`: `--arm <name>`, `armWeights()` (the two
  assertions), `newCtxWithWeights()`, a `weights` parameter on
  `decomposeFromSeat`/`judgeOne`, and `arm`/`weightsLabel` on the summary. With
  no `--arm` the script still calls lane 4's `newCtx()` and the artifacts are
  unchanged.
- `lab/hard-ai/analyze/work-sweep.ts`: `--engine hard@<label>`,
  `resolveEngineLabel()` (which refuses version-0 weights and unknown labels),
  `engine`/`engineWeightsLabel` on `SweepReport`, and a production-only config
  path. With no `--engine` the search decisions are byte-identical, checked
  against E2's artifact.
- `tests/lab/analyze-work-sweep.test.ts`: five new assertions covering the CLI
  flag, the arm's resolved weights, that the production factory gets the arm
  and the adviser factory gets the champion, that the default path still gets
  only `default-v1`, and that an unknown label throws.
- No `src/ai/hard/**` file was touched. No fixture was edited. No gate
  criterion, plan or synthesis section was changed.
