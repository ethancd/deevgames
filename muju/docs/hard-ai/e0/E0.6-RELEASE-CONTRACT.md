# E0.6 release-contract inventory (re-inventoried at 1e0466de, 2026-09-16)

Inventory only; nothing here changes a requirement. Every mechanics status is AS OF `1e0466de`; the first pass ran against `b1f3d4e`, and each row that moved says so. Owner decisions: §6, PENDING; proposals: `AMENDMENTS-PENDING.md` A1-A15.

Keys (under muju/; docs = docs/hard-ai, lab = lab/hard-ai): MS = docs/MILESTONES.md; D = docs/DESIGN.md; EP = docs/EPIC-PLAN-2026-09-16.md; G = lab/verify/gates.ts; R = lab/ladder/run.ts; H = lab/bots/hard.ts; S = lab/suites/run.ts; SP = lab/ladder/sprt.ts; EN = lab/ladder/engines.ts. Line numbers are on 1e0466de; MS, D, EP, G and S are byte-identical to b1f3d4e, R/H/SP/EN moved.
Class: RB release blocker, OM owning-milestone requirement, DG diagnostic. Sibling lanes A, B, C, D and F landed by 1e0466de; claims are read off the merged code, not a lane report. P3 applies to every section-1 row. Two commits land AFTER it: 56835ef8 an openings set, 055fddda an independence guard, an overrun tolerance and forecast/manifest fixes. Rows stay read at 1e0466de; A14 and A15 record what landed.

Provenance. P1: MS M19 L354-364, G L591-599, f865174/e700f01 (2026-09-14, "Claude"), from D §8 L1741-1742 citing measurement-first.md L1249-1250; no owner sign-off (UNVERIFIED). P2: MS M20 L366-378, G L600-606, same commits, from D §5.11.5/§5.14/§5.15. P3: EP §5 L392-399, b1f3d4e (2026-09-15, Ethan Ashkie), owner-authored, "no inherited requirement is relaxed" (L402-403). P4: MS M14 amendments L294-295, re-homed to M18 L349-352, 83f53a8 (2026-09-15, "Claude"), implementer-applied; POSTMORTEM L47-63 records no owner acceptance. P5: MS M15 L311 from D §6.3 L1580-1581, f865174.

## 1. Inherited requirements (EP §5 table, L392-399)

| # | Requirement (verbatim) | Source | Class | Mechanics | Command today | Produced vs named | Prov |
|---|---|---|---|---|---|---|---|
| I1 | Desktop strength: Hard vs shipped Hard, wall:3000, h0/h3, 300 pairs cap, SPRT 0/+100, alpha=beta=.05, H1 | EP L394; MS L361-363 | RB | unsupported, see M19-2 | none | M19-2 | P1 |
| I2 | Turn-path calibration: AIv2-turn vs AIv2, wall:3000, h0, 150 pairs, 0/+50, not-H0 | EP L395; MS L361 | DG (report separately) | see M19-1 | M19-1 | M19-1 | P1 |
| I3 | Phone strength: Hard mobile vs shipped Medium, wall:1500, h0, 200 pairs, 0/0, H1 | EP L396; MS L361 | RB | impossible, see M19-3 | none can pass | M19-3 | P1 |
| I4 | Responsiveness: phone p95 <= 3,000 ms; desktop p95 <= 6,000 ms; phone depth-1 <= 150 ms across corpus | EP L397; MS L311, L363; D L1580-1581 | RB (EP L397) | partly supported at 1e0466de: runner.ts keeps a wall-clock bucket per turn per seat (L220-224, L257); R reports `timing.{a,b}.p95TurnMs`, `maxTurnMs`, `overruns` off them (L537-546, L784-796). Unsupported: no phone-device row (M19-3); no `depth1MaxMs`; `lab/bench/latency.ts` (MS L307) absent; M15 a stub (G L558-565); desktop bound in D only, no gate row; overruns untolerated (A14) | any ladder row (`metrics.timing` is always written) | lab-box p95TurnMs per engine vs phone.p95TurnMs, phone.depth1MaxMs, desktop p95 (unnamed) | P5 |
| I4d | Shipped Hard's browser allowance is `TURN_BUDGET_MS.hard = 8000` per turn (engine-v2.ts L39; useAI.ts L55, L124; production 0f1d5f1 same); I1's arm gets wall:3000 split across decisions (EN L24-35, L135-142) | code | RB (defines the opponent) | unsupported at 1e0466de: allowance undecided (A5) | - | - | none |
| I5 | Correctness: zero illegal actions; zero replica divergences; canonical checks; adjudication <= 1% | EP L398; MS L293, L363; D L1686, L1688 | RB | runnable at 1e0466de: illegalActions (R L591), replicaDivergences (R L594; M14's criterion, not M19's), adjudicationRate voiding above 0.01 (R L771-772). No M19/M20 command passes `--legality`; R defaults to `as-shipped` (L246) where D L1688 says `strict` (A7). Canonical checks still UNVERIFIED as a metric: worker sets `checkInvariants: true` (worker.ts L292), nothing counts them | ladder as written plus `--legality strict` | illegalActions, replicaDivergences, adjudicationRate vs === 0, === 0, <= 0.01; canonical checks unnamed | P1 P4 |
| I5s | Suites no worse than M14; fixture versions frozen | EP L398; MS L363 | RB | unsupported: see M19-4; no version pin in G | M19-4 | M19-4 | P1 |
| I6 | Deferred M14 obligations: invariants >= .90; Rush smoke Elo >= 0 at wall:500 | EP L399; MS L349-352 | OM (M18); release link PENDING | see D1, D2 | D1, D2 | D1, D2 | P4 |

## 2. M19 rows (MS L361-363; G L591-599)

G's M19 row is `notImplemented` (G L24-35: criterion always false), so `hard:verify --gate M19` cannot pass. MS L357's `hardEnabled = true` flip has no target: no such symbol in src (comments only, worker/protocol.ts L24-25); useAI.ts L55-57 routes every difficulty to AIEngineV2. Both unsupported at 1e0466de; E5. Every M19/M20 row carries `--shards 12` (queue cap 2); `--seed`/`--out` omitted below.

EARLY STOP at 1e0466de, common to every ladder row: the decision is the SEQUENTIAL SPRT in pairIndex order; the stop is real at `--shards 1` (R L1037-1083, L1172-1180), weak at k>1, where contiguous shard slices hold the prefix behind shard 0 and most of the schedule plays anyway (R L75-85, `pairsPlayedBeyondDecision`).

| # | Row (verbatim) | Class | Mechanics | Command today | Produced vs named | Prov |
|---|---|---|---|---|---|---|
| M19-1 | `--a aiv2-hard-turn --b aiv2-hard --work wall:3000 --handicaps 0 --pairs 150 --sprt 0,50,0.05,0.05`; `calib.decision !== 'H0'` | DG (EP L395), still an M19 clause | runnable as written (shards aside); both arms resolve (EN L284-293); unchanged at 1e0466de apart from the early stop above. A cap with no crossing yields `continue` | same with `--shards 2` | decision in {H0,H1,continue,void}; `!== 'H0'` is satisfied by `continue` and by `void` (A8) | P1 |
| M19-2 | `--a hard@ship --b aiv2-hard --work wall:3000 --handicaps 0,3 --pairs 300 --sprt 0,100,0.05,0.05`; `desktop.decision === 'H1' && adjudicationRate <= 0.01 && illegalActions === 0 && handicaps.includes(3) && seatMirrored === true` | RB | unsupported: `hard@ship` throws (H L180-201: lab, lab-dfpn, lab-refined, desktop, midrange, phone/mobile). `seatMirrored` IS produced at 1e0466de (R L596-614, L800-814): every PLAYED pair finished in both orientations with a pair row and zero failures; schedule completeness is `pairsCompleted`/`missingGames`/`status` (A4: owner to confirm the split). | nearest, not the row: `--a hard@desktop ...` (not frozen) | decision, adjudicationRate, illegalActions, handicaps[], seatMirrored, bothSeatsPlayed vs the named list; `hard@ship` is what is still missing | P1 |
| M19-3 | `--a hard@mobile --b aiv2-medium --work wall:1500 --handicaps 0 --pairs 200 --sprt 0,0,0.05,0.05 --profile phone`; `phone.decision === 'H1' && phone.p95TurnMs <= 3000` | RB | impossible: elo0 = elo1 gives t0 = t1 (SP L247-248), LLR identically 0, decision always `continue`. At 1e0466de `validateSprtParams` throws and R refuses the row at parse time naming A1 (R L203-213). Also unsupported: `--profile` is REJECTED, not recorded (R L191-193); `hard@mobile` runs the PHONE shape at lab-box speed anyway (H L193-195), so no row measures a phone; `p95TurnMs` is lab-box only (I4). Repair (EP L416-422): (a) non-inferiority, margin fixed before results (-25/0); (b) superiority 0/+50; PENDING (A1) | does not parse: `--profile` throws, and `--sprt 0,0,0.05,0.05` throws naming A1 | nothing: the row is refused before a game is played | P1 |
| M19-4 | `hard:suite --all --engine hard@ship --work 400000`; `suites.every(s => s.score >= M14.baseline[s.name])` | RB | unsupported at 1e0466de: `--all` throws `unrecognised argument` (S L163); `hard@ship` throws (S L413 -> H L197-200); the artifact carries per-suite `{cases,passed,points,pointsWon}` and top-level `tactics`, `spawnStrike`, `homeMate`, `invariantsEval`, `invariantsSearched` (S L194, L600), no `score`; G has no `M14.baseline`; no `home-force.suite.json` (M16). M14-suite.json: tactics .918, spawnStrike .95, homeMate 56, invariantsEval .60 | `hard:suite --suites tactics,spawn-strike,home-mate,invariants --engine hard@desktop --work 400000` | per-suite passed/points vs s.score, M14.baseline | P1 |

## 3. M20 steps (MS L375-377; G L600-606 stub "see MILESTONES.md")

All nine: OM (M20 feature activation, EP L403-413); a feature enabled by default without its H1 makes its row RB. Provenance P2. Every row is UNSUPPORTED at 1e0466de with no command today; the cell says why. Ladder rows use `--work fixed:400000 --handicaps 0,3 --pairs 1500 --sprt 0,10,0.05,0.05` unless quoted.

| # | Step (verbatim) | Mechanics | Produced vs named |
|---|---|---|---|
| M20-1 | `hard:corpus --games 20000 --work 25000 --handicaps 0,3 --shards 12` | package.json L44 names lab/tune/corpus.ts, lab/tune/ absent | none vs positions >= 200000, drawShare < 0.70 |
| M20-2 | `hard:texel --corpus ... --iterations 20` | tune/texel.ts missing (L45) | none vs heldOutLossAfter < Before |
| M20-3 | `hard:ladder --a hard@texel --b hard@m14` | both labels throw (H L197-200); `m14` needs a frozen weights snapshot | decision vs texelSprt.decision === 'H1' |
| M20-4 | `hard:ladder --a hard@dfpn --b hard@texel` | `hard@dfpn` throws; `lab-dfpn` exists (H L185-186) but tactics/dfpn.ts is the M14 stub (MS L279), M16 not started | decision vs dfpnSprt.decision !== 'H0' (passes on `continue`) |
| M20-5 | `hard:ladder --a hard@refined --b hard@texel --each-flag` | `hard@refined` throws (`lab-refined` exists, H L187-188); `--each-flag` is still ignored (R reads named flags only), so one SPRT would run, not one per flag; no `perFlag` | none vs perFlag.every(f => f.decision !== undefined) |
| M20-6 | `hard:spsa --iterations 20 --batch 400 --work 100000` | tune/spsa.ts missing (L46) | none vs configWritten === (decision === 'H1') |
| M20-7, M20-8 | `hard:book --nodes 50000 --work 500000 --handicap 0`, then `--handicap 3` | lab/book/build.ts missing (L47); src/ai/hard/book has only format.ts, probe.ts | none |
| M20-9 | `hard:ladder --a hard@book --b hard@nobook --work wall:3000 --pairs 300 --sprt 0,10,0.05,0.05` (handicaps 0,3) | both labels throw; `HardConfig.book` exists (config.ts L150) but every profile passes `null` (L275-279); no `public/muju-book-h0.bin`. EP L408-409 notes the wall:3000/300-pair deviation | decision vs bookSprt.decision !== undefined |

## 4. Deferred M14 obligations (MS L349-352; provenance P4)

| # | Requirement | Class | Mechanics | Command today | Produced vs named |
|---|---|---|---|---|---|
| D1 | `invariantPairs === 20 && invariantsEval >= 0.90` | OM (M18); release link PENDING | runnable, unchanged at 1e0466de; G M18 is a stub (L582-590), nothing enforces it; MS L350 calls the bar unreachable without re-authored fixtures | `hard:suite -- --suites invariants --engine hard@lab --work 400000 --out <dir>/M18-invariants.json` | invariantsEval (0.60 at M14) vs >= 0.90 |
| D2 | `elo >= 0` over the 16 smoke games | OM (M18); release link PENDING | runnable (shards aside); at 1e0466de `elo` is the pair-level interval with a Jeffreys prior on degenerate counts (elo.ts L31-38, L168), so D2's M14 number is incomparable (A12) | `hard:ladder -- --a hard@lab-400k --b Rush --work wall:500 --handicaps 0 --pairs 8 --seed 9 --shards 2 --out <dir>/M18-rush` | elo (-254.7 at M14, M14-smoke/metrics.json) vs >= 0 |

## 5. Missing per unsupported item (proposed owner)

- `hard@ship`, `hard@m14`: labels bound to frozen snapshots (config, weights, WASM hash); E0.1 landed the hashes, snapshots are E6.1; until then `hard@desktop`, diagnostic (A3).
- `hard@texel/dfpn/refined/book/nobook`: labels and features; M18/M16/M17 (EP: E3 evaluation, E4 search, E7 book/df-pn, L313-316).
- `--legality strict` on release rows; R defaults to `as-shipped` (A7). `seatMirrored` is produced; its definition awaits the owner (A4).
- Phone-device p95, depth1MaxMs, desktop row: `p95TurnMs` is produced, lab box only; a device run recorded as such, a depth-1 producer and a desktop row are missing (E5, A4). Tolerance unfrozen (A14).
- `hardEnabled`, worker `engine:'hard'`: the browser flip and a Hard route in the worker; E5. `--profile phone` is refused, not missing.
- `--each-flag`, `perFlag`: R parse and per-flag loop, M17/E4 (A9). `--shards 12`: restated at the cap of 2 (A2).
- `hard:suite --all`, `s.score`, `M14.baseline`, fixture pin: S and G (A6). `hard:corpus/texel/spsa/book`: four scripts, M18. Real G M19/M20 rows: E6.2 after approval.

## 6. Release-decision status

No requirement is relaxed here, and no row counts as met that was not met at b1f3d4e. Distinct items: 1 impossible (M19-3's 0/0 statistic, now refused at parse time instead of run to a meaningless `continue`) and 16 unsupported — three fewer than the b1f3d4e pass, because `seatMirrored` and `p95TurnMs` are produced and `--profile` is refused. `--shards 12` and the missing `--legality strict` stay deviations on the runnable rows M19-1, I5, D1, D2; the weak early stop at k>1 is documented (§2), not a defect. Pending (Ethan): A1-A15, of which A14 (overrun tolerance) and A15 (pair independence) are new and each blocks a release row quoting a timing count or an interval; and whether D1/D2 are release blockers or M18-only. Release readiness needs those decisions and an E6.2 run whose metrics match the named ones.
