# M6 feature/weights lane — implementation checkpoint

Checkout: `work/deevgames-phasing-m6`, branch `codex/phasing-m6`, base `5d31740791b8c3aa17797a29f9a5eac87a85b844`. Coordinator approved `muju/docs/hard-ai/phasing/M6-BOOTSTRAP-CONTRACT.md`. This is an uncommitted implementation checkpoint. Parent owns shared-queue validation and independent review; the first diagnostic is preserved below, and corrections await a second run.

## Owned changes

- `config.ts`: checked62 count, `PHASING_EVAL_SCHEMA=muju-phasing-eval-1`, additive Weights.featureSchema, updated placeholder shape; coordinator-requested optional BK03 Book fields. No routing flag change.
- `eval/features.ts`: preserved0–57 and appended PendingValue58/ArrivalThreat59/DisruptPressure60/RentShortfall61 atstage2. Feature23 consumes only exact chronological root-live PV before signed whole-crystal truncation. Pending service/principal and diagnostic signed values are attributed separately. Legacy name HangingBuy29 now reports arrival-dependent victim value; Hanging28 excludes that same subset. Both remain zero-weight.
- `eval/pending.ts`: computes actual pending principal plus risk-selected per-commitment service; no-move status uses chronological pendingArrival. Bounded one-existing-live-mover geometry distinguishes remaining current AP/flags from a future reset, gives settled Prepare no intervening movement window, preserves alternate anchor rectangles, excludes refunded principal from disruption gain, and takes the maximum denied service across mutually exclusive move endpoints. ArrivalThreat compares identical nextAct occupancy with/without paid-arrival attacker candidates; it never infers necessity merely from a selected plan containing an arrival.
- Narrow `tables/kill.ts`: optional includePendingAttackers=false removes only pending attacker candidates. Projected occupancy/healing, current horizon behavior, live candidate enumeration and default callers are unchanged.
- `eval/weights.ts`: hand-derived sparse version2 vector, material catalogue priors, cash2/3=100, live forecast23=100, pending58=1, all other weights0. Explicit persisted file/feature schemas; load/serialize/runtime constructor/setWeights reject stale schema/version/shape. Input integers are checked against signed32 range before typed-array conversion. Historical generated source is not edited or padded. Feature schema enters weight identity.
- `eval/evaluate.ts`: every stage computed for every window; old unproved bound replaced with Infinity. PROVER work charges monotone table-counter deltas in finally, preserving executed work when a forecast veto throws; cached forecasts are not recharged. Existing EVAL charges retained. full() has no meter by contract and retains table counters for diagnostics.
- Narrow `gen/generate.ts` / `search/pvs.ts`: private forecast call/cap deltas are published in `finally`, including typed forecast vetoes. Prepare generation records fresh per-generation counters; `generateAt` counts them without pricing. Direct search L2 builds price new calls at existing PROVER class 8, and leaf diagnostics record the evaluator's already-priced deltas without charging twice. Separate per-search economy counters preserve the main Replica's pre-existing capped lifetime gauge. No work cost or cutoff semantics changed.
- `lab/hard-ai/audit/eval-groups.ts`: every new feature grouped once, invariant range fixed to20 rather than FEATURE_COUNT-38.
- `tests/ai/hard/phasing-bootstrap.test.ts`: authored finite checks for indices/schema/weights, stale shape/int overflow, cash split, escrow/arrival/refund principal conservation, finite delayed mining attribution, actual timing/flags, alternative-anchor intrusion, rent-before/after-income distinction, paidzero-bank arrival/current refusal/healing/livealternative, signed extraction and unchanged input, unconditional window equality, cached work counters and veto propagation. Three additional production-boundary seam checks cover generator count-only veto, search L2 priced veto, and leaf diagnostics without duplicate charging; these inject typed failures and do not claim proof parity.

## Verification state

`git diff --check` passed. No typecheck, test, search, engine, corpus, opening, val, sealed, or tuning command was run by this lane. Source-only hashes are in `work/m6-feature-ready-hashes.json`. No test success is claimed. The original `eval.test.ts` and `interfaces.test.ts` historical58/oldweight failures were preserved by root; their semantic ports are detailed in the correction checkpoint below. No suite was excluded or weakened.

## Limits and coordinator dependencies

- ArrivalThreat is an optimistic table differential, not an exhaustive canonical kill proof. Its live-only comparator is a controlled same-occupancy counterfactual, not a separately reachable state.
- Movement exposure is bounded to one surviving root-live mover on the actual chronological enemy-Act snapshot, omitting captures, coordinated moves and arrival movers. Prior releases and paid blockers are included. Exact finite remaining income plus bank must fund the mover's own rent; the owner may prioritize it in any legal keep. A full post-move forecast/terminal counterproof and effects of other release choices remain outside this probe. risk0 means not marked by that probe, not proven safe. Chronological status separately handles actual pass-only releases/terminal stops.
- The exact funded economic policy is conditional pass-only defaultUpkeep(false), not adversarial prediction. Its absolute-square tie rule is not rot180-seat equivariant for equal-cost competitors. The coordinator approved retaining that named canonical policy and recording the limitation in the M6 contract. Parent owns raw-key bootstrap book identity; no rotational forecast/full-evaluation equivalence is claimed. Side-of-view sign antisymmetry for a fixed state remains required. Independent canonical mirrored-reference checks remain meaningful without forcing equal rotated values.
- Old zero-weight invariant/geometry diagnostics still need their separately leased M6 semantic audit, including leadCc escrow, Inv7/14/19 and stale immediate-placement assumptions. This lane does not claim their old language is rederived merely because weights are zero.
- Parent owns nonempty BK03 compatibility, book/corpus/tuner/identity integration and frozen tuning coordinates. Initial tuning must retain cash2/3=100, pending58=1 and the fire1 material scale unless the accounting contract is explicitly revised.
- Full evaluation is deliberate and unbenchmarked. No search strength, responsiveness, suite floor, golden, baseline readiness, milestone completion, deployment or releasegreen claim follows.


## First diagnostic and correction checkpoint

Root scheduled the first source-frozen shared-queue diagnostic. `work/m6-first-focused-1/tests.json` records `success:false`, total222, passed202, failed20, pending0; its `numFailedTestSuites` is20 (nested-suite counter), while the seven failed files are `eval.test.ts`, `interfaces.test.ts`, `invariants.test.ts`, `phasing-book.test.ts`, `phasing-bootstrap.test.ts`, `phasing-invariants.test.ts`, and `purchase.test.ts`. `work/m6-first-types-1/command.log` preserves the initial type failures. Root reported no source drift. No evidence was overwritten, no suite was skipped, and this lane ran no engine/test command independently.

Corrections made after root released source:

- The sole owned bootstrap failure was a mock boundary mistake: initial forced completion scores at L1 before the Prepare L2 build. The test now passes L1 through and injects the same typed veto at exactly the L2 boundary. It retains call/cap/price assertions.
- Existing eval/interface tests now pin62 indices and the approved sparse accounting arithmetic rather than historical weights. Canonical diagnostic assertions,20 invariant slots, weight independence and schema rejection remain. Book metadata declaration includes its three exact keys. Initial failures remain evidence, not waived checks.
- Exported `buildSearchTables` covers PVS/root/quiescence L2 calls. Quiescence accounts completed work even on veto. Engine pre-rung forecast proof work is explicitly separate preparation telemetry and survives cold-probe stats replacement without changing the rung. Calibration uses a Phasing initial position and charges table/evaluator forecast deltas at the existing price. Both engine generic search catches propagate `PhasingEconomyProofCutoff` instead of converting it to fallback.
- Engine's time-budget book hint uses root's exact-compatible book helper, so rejected stale books cannot shorten the default target. Root owns the helper/raw full-turn keys/mirror corrections; this lane only changes the engine call.
- Independent review found the root-board exposure check retained an anchor that the named pass forecast released before the enemy Act. Galileo added `pendingEnemyAct` / `pendingOwnAct` reusable snapshots to that same forecast without additional makes/proofs. Exposure now uses surviving anchors/occupancy/current or reset flags and AP. Mirrored hand-authored canonical intrusion/refund tests include an actual funded surviving-alternate-anchor control.
- An intruding renter that cannot survive its own ensuing bill is excluded from both risk and pressure. The necessary cash calculation sums current bank plus finite income of every live own body, relocating only the mover; it does not require funding all renters. Mirrored canonical zero-cash dry-renter cases arrive after forced release, while one-crystal controls mark exposure.
- ArrivalThreat59 now compares current kill tables on the actual first incoming owner Act, after actual batch resolution and healing. `KillOpts.excludedAttackerSlots` suppresses only new friendly attacker identities in the live-only comparison while keeping occupancy. Count only surviving root-live enemy identities, checked by birth sequence to reject reused slots. An explicit refund-after-anchor-release fixture proves the old static table could invent an attacker and the chronological diagnostic refuses it.
- Slot28 remains the residual old static optimistic hanging indicator, excluding the exact root target identities counted by arrival-only slot29. It is not relabelled a chronological proof. Slots28/29/59 retain coefficient0. The contract records all snapshot, survivor, identity, conditional-pressure and rotation caveats.

No tests or types have run on this correction checkpoint yet. Source hashes are in `work/m6-feature-corrections-hashes.json`; the earlier ready inventory remains preserved. `git diff --check` passes. Source is paused for root's second combined diagnostic.

New review dependency after this source pause: Galileo owns extending the context table-cache guard to the root birth/commit order, IDs and sequence counters required by retained snapshot identity. Equal Kturn and slotSquares alone do not bind those cold identity inputs. Root requested an equal-hash/equal-board changed-identity regression; no acceptance is claimed before that guard is validated.

## Broad lazy-test semantic port

Root preserved `work/m6-broad-regression-1/tests.json`: successfalse,1102passes/4failures/0skips,total1106,source drift0. All4failures were legacy assumptions in `tests/ai/hard/lazy.test.ts`: finite integer bound, zero-weight bound0, window early exits returning a partial score, and conditional EVAL2 charging. Root authorized only this test file. It now tests the approved Infinity/full-evaluation contract; preserves300 positions and both original/stretched weight vectors with exact3× stage2 arithmetic, preserves300×12 windows and strengthens each to full-score equality, checks actual stage2 invocation even outside the score window, and checks exactly one EVAL1/EVAL2 charge on cached high/low/straddling evaluations. No production source, numeric floor, timeout, skip or exclusion changed. Diff check passes; no execution after the correction by this lane.
