// Feature flags. Flip a value to `true` to bring hidden content back.
// Any deck item tagged with `flag: '<key>'` is shown only when that key is true.
export const flags = {
  // Less-common minerals trimmed from the Mineral Formulas core set
  // (Orthoclase, Gypsum, Malachite, Olivine, Talc, Apatite). Hidden by default.
  showExtraFormulas: false,
}
