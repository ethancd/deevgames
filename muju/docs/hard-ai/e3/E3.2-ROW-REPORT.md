# E3.2 row report — the safety block priced at equal time (rows #1 and #2, 2026-09-17)

Rows #1 and #2 of the `E3-PLAN.md` ledger, branch `claude/hard-ai-e3`, head
`49e0548a`. Both rows are `hard@ablate:eval-no-safety` against `hard@desktop`
at `wall:3000`, h0/h3, `--legality strict`, `--shards 1`, 32 pairs each.

This report is written to `EPIC-PLAN-2026-09-16.md` §5 (what a result must
report) and §7 (the decision-record block), and to `E3-PLAN.md`'s rule that
every E3 row report leads with the feature it changed and the judge that
motivated it. Every number below was read from the artifact named beside it.
Section 10 lists the places where an artifact and a prose claim disagree.

## 1. The feature block that changed, and the judges that motivated the row

### 1.1 The 19 weights

- The arm is `hard@ablate:eval-no-safety`, `lab/hard-ai/ablate/arms.ts`, factor
  `weights`, patch `zeroWeights('safety', EVAL_GROUPS.safety)`.
- The set is `EVAL_GROUPS.safety` of `lab/hard-ai/audit/eval-groups.ts`, which
  is one part of a partition of the 58 features of
  `src/ai/hard/eval/features.ts` into five groups.
- All 19 are set to 0; the other 39 weights and the 18 catalogue material
  priors stay at `default-v1`.
- Weights below are `default-v1`'s, read from `src/ai/hard/eval/weights.ts`;
  indices are read from `src/ai/hard/eval/features.ts`.

| # | feature | index | `default-v1` weight | sub-block |
| ---: | --- | ---: | ---: | --- |
| 1 | `Exposure` | 17 | −20 | threat stack |
| 2 | `Hanging` | 28 | −50 | threat stack |
| 3 | `HangingBuy` | 29 | −30 | threat stack |
| 4 | `ApproachRetreat` | 30 | −25 | threat stack |
| 5 | `ApproachStrand` | 31 | −10 | threat stack |
| 6 | `StrandPunish` | 32 | +20 | threat stack |
| 7 | `KillAvailable` | 33 | +35 | threat stack |
| 8 | `CleaveExposure` | 34 | −40 | threat stack |
| 9 | `AnchorFragility` | 35 | −120 | anchor |
| 10 | `BlockingDeficit` | 36 | −150 | anchor |
| 11 | `Inv3RetreatSquare` | 40 | −250 | safety invariants |
| 12 | `Inv4StrandUnpunished` | 41 | −100 | safety invariants |
| 13 | `Inv6FragileAnchor` | 43 | −120 | anchor |
| 14 | `Inv8NoPreAdjacency` | 45 | −150 | safety invariants |
| 15 | `Inv9ChipAcrossTurn` | 46 | −150 | safety invariants |
| 16 | `Inv12CleaveLine` | 49 | −40 | safety invariants |
| 17 | `Inv17SelfBlock` | 54 | −60 | safety invariants |
| 18 | `Inv19SoftMinerExposed` | 56 | −150 | safety invariants |
| 19 | `Inv20StrandNoRetreat` | 57 | −250 | safety invariants |

- Σ|w| removed is 1,770 centi-crystals of weight: 230 in the threat stack, 390
  in the anchor triple, 1,150 in the eight remaining safety invariants
  (`E3.2-SAFETY-SUBARMS.md`; the per-weight values above sum to those three).
- The index list matches `E3-PLAN.md`'s "Row #1 preregistration" exactly:
  17, 28–36, 40, 41, 43, 45, 46, 49, 54, 56, 57.
- Σ|w| is not a measure of how much judgment an arm removes: the threat-stack
  features are `div100` centi-crystal quantities and the invariants are 0/1
  indicators, so the 230 buys the larger share of the evaluator's output
  (lane 4's contribution audit: threat stack 16.98% of mean Σ|w·f| on
  `exam-dev` against 2.25% for the anchor triple and 2.41% for the safety
  invariants).
- The 19 weights are not read only at the search leaf. The candidate generator
  builds its list with `cfg.weights`, so the zeros move the static ordering and
  the K-cut too: at the development root `74b71f89ae7abb64` the champion lists
  18 root candidates and the arm lists 19, and the turn the arm plays from 400k
  up is not in the champion's list at all (`E3.2-REPRO.md` §3). The row prices
  the 19 weights wherever the engine reads them.

### 1.2 The judges that motivated the row (the E3-PLAN judge rule)

`E3-PLAN.md`'s judge rule admits four judges and forbids the champion at more
work from deciding. The row was chosen on these, in this order of weight.

- Judge 1 (the game outcome), out of sample, the real motivation: the
  descriptive fixed-work row `lab/results/hard-ai-e3/ablate/eval-no-safety/fixed100k/metrics.json`
  — `fixed:100000`, 8 pairs on 4 `e1-dev` openings, seed 32 — scores 0.875
  (14/0/2), Elo +338 [+168, +1200], h0 0.875 (7/0/1) and h3 0.875 (7/0/1).
- Judge 1, in sample: at the 12 distinct E1.1 loss positions the safety group
  moves the static leaf toward the played (losing) turn at 9 of 10 positions
  where it moves at all, Σ −5,100 cc
  (`lab/results/hard-ai-e3/loss-judgment/summary.json`, reproduced by
  `E3-CRITIQUE.md` table row 1).
- Judge 4 (a canonical fact): at the two development end states the rules
  enumerate 0 one-step kills and 0 hangable value crystals for either side, so
  the `Hanging` charge rests on kills the rules cannot see in one step
  (`loss-judgment/g4-s6_3_5-B-white.json`, critique table row 11).
- Judge 3 (an authored judgment): DESIGN row 17 says stage 2 refines Exposure;
  the code adds a second charge on top of it (`E3.1-SYNTHESIS.md` §3.1).
- Judge 2 (`aiv2-hard` as a second opinion) is recorded and did not decide: at
  the development case it is nearer the adviser's contribution vector by 3%
  (L1 3,427 against 3,522), and lane 6's group-level reading is nearer the
  played turn at 8 of 12 (`E3-CRITIQUE.md` C3).
- What the judges do NOT establish is stated in §7 below: the named concept
  ("the price of a threatened own body") does not generalise beyond the 12
  in-sample positions.

## 2. The preregistration and the rule

Both rows were preregistered in `E3-PLAN.md` before launch.

- Arm: `hard@ablate:eval-no-safety`, resolved config hash at `wall:3000`
  `66edf9cdf58f6b037186b25d05a939ed521e8a76e07051937dcbc611eb343aaf`.
- Champion: `hard@desktop`, resolved config hash
  `4e7afdf76b32fadfab2d11577cb610c8f9f1a491b5153e70b87b0774403600cd`,
  weights `default-v1`.
- Both hashes are recorded in each row's `manifest.json` (`aConfigHash`,
  `bConfigHash`) and are identical across the two rows.
- Seed rule: `20260930 + <row ordinal>`, so 20260931 for row #1 and 20260932
  for row #2; both are recorded in the manifests.
- Prediction for row #1, falsifiable: score > 0.5; strong form, the 95%
  interval excludes 0.
- Falsification for row #1: score ≤ 0.5 means the +338 was a fixed-work or
  development-opening artifact, the safety block earns its keep at equal time,
  and the arm closes.
- Rule for row #1: SCREENING. It retains nothing alone.
- Rule for row #2: retain as champion CANDIDATE only with score > 0.5 AND a
  95% interval excluding 0.
- Non-regression thresholds, preregistered before row #2 (critique B3):
  tactics ≥ 73/79 and home-mate 56/56 under real weights; spawn-strike within
  one case of the champion's 16/20; exam exact at `fixed:200,000` within three
  cases of the champion. Judgment counts reported, never gated.
- Void and flag rules: A14 (overrun rate above 5% for either arm voids the
  row), P6 (any turn over 20 s flagged), `illegalActions` 0,
  `adjudicationRate` reported.
- Mechanism check, preregistered inside the row: the arm's per-turn rung
  distribution must not sit above the champion's and its abort rate must not
  exceed the champion's, or the row priced search rather than judgment.
- M20's contract: a retained arm is development evidence and a champion
  candidate, not a shipped default.

## 3. Row #1 — screening

Source: `lab/results/hard-ai-e3/ablate/eval-no-safety/screen/`
(`metrics.json`, `manifest.json`, `summary.md`, `elo.json`, `games.jsonl`,
`pairs.jsonl`, `replays/`).

### 3.1 Provenance

- Launch commit `50b6a94c2d5322769dff11ea83fb18ecfc1e74b2`, one commit for both
  arms, from the detached worktree `~/src/deevgames-e3-run` (`manifest.git`).
- `manifest.gitDirty` is `true`; the identity that holds is the two resolved
  config hashes and the per-arm weight vectors in `aResolvedConfig`, not the
  commit.
- Started `2026-09-17T06:34:16.315Z`, finished `2026-09-17T08:39:02.768Z`,
  wall 7,486,573 ms (2 h 04 m 47 s), single shard.
- Box: Apple M2 Max, 12 cores, node v24.11.1; loadavg at launch 3.30 / 5.24 /
  5.83 (`manifest.loadavg`).
- `baseline.ai` (a sha256 over the whole `src/ai` tree)
  `81d011ea20659c61db8194fce3e40f2ac16d44c15f8084684b671dba2d2d4f27`;
  `baseline.game` `442226339ec2dd2c…`; `wasmSha256` `da9b1cdf25e4bcd6…`.

### 3.2 Openings

- File `lab/hard-ai/ladder/openings/e1-val2.jsonl`, sha256
  `fe3b9c98ad216773fc1766ef618f8aed7efa2e990f04ea409b6ed9bce49d2007`
  (verified against the file in this worktree: 32 rows, same digest).
- `--openings-skip 0`; the pool after the skip is 32 rows; `openingsUsed` is
  the first 16 of them, rows 0–15: `e1v2-g4-s10`, `e1v2-g4-s50`,
  `e1v2-g3-s65`, `e1v2-g4-s70`, `e1v2-g3-s85`, `e1v2-g2-s100`, `e1v2-g4-s110`,
  `e1v2-g4-s130`, `e1v2-g4-s150`, `e1v2-g3-s165`, `e1v2-g4-s170`,
  `e1v2-g4-s175`, `e1v2-g5-s195`, `e1v2-g4-s210`, `e1v2-g4-s230`,
  `e1v2-g4-s250`.
- That list is byte-identical to rows 0–15 of the file (checked by reading the
  file's `id` fields in order).
- `openingsIndependent` true, `independenceWarning` null.
- A15 makes the openings the independence source: 16 openings, so the 32-pair
  interval is optimistic by roughly √2.
- 16 openings × {h0, h3} × both seats = 64 games; `distinctGames` A-white 32
  and B-white 32, digested from the replays, `duplicates` false.

### 3.3 Score, record and uncertainty

- 32 pairs scheduled, 32 completed; 64 games scheduled, 64 played; 0 missing,
  0 unattributed, 0 failures; replays 64 of 64.
- Arm record W/D/L 45/2/17; score 0.71875.
- Paired five-category counts (`elo.json` `counts`, loss-loss to win-win):
  1 / 1 / 14 / 1 / 15 over 32 pairs.
- Score interval 0.620–0.817 (`muLo`, `muHi`); pair standard error 0.1007.
- Elo +162.99, 95% interval [+85.12, +260.36], LOS 99.9993%.
- No SPRT was requested; `sprt`, `sprtSequential` and `decision` are null; the
  row is a fixed sample read against its preregistered rule.

### 3.4 Per-handicap strata

| handicap | pairs | games | score (arm) | W/D/L | Elo | 95% interval | LOS |
| ---: | ---: | ---: | ---: | --- | ---: | --- | ---: |
| 0 | 16 | 32 | 0.7344 | 23/1/8 | +176.66 | [+61.58, +347.85] | 99.913% |
| 3 | 16 | 32 | 0.7031 | 22/1/9 | +149.78 | [+50.12, +281.21] | 99.877% |

### 3.5 Per-seat strata

Computed from `games.jsonl` (`orientation`, `players.<side>.bot`, `winner`);
the arm is White in `A-white` games and Black in `B-white` games.

| seat of the arm | games | W/D/L | score |
| --- | ---: | --- | ---: |
| White (`A-white`) | 32 | 21/1/10 | 0.6719 |
| Black (`B-white`) | 32 | 24/1/7 | 0.7656 |
| White, h0 | 16 | 11/0/5 | 0.6875 |
| White, h3 | 16 | 10/1/5 | 0.6562 |
| Black, h0 | 16 | 12/1/3 | 0.7812 |
| Black, h3 | 16 | 12/0/4 | 0.7500 |

- `bothSeatsPlayed` true and `seatMirrored` true: every opening was played from
  both seats at both handicaps.

### 3.6 Game length and win types

- Turns per game over all 64 games: mean 33.36, median 32.5, min 5, max 62
  (`games.jsonl` `turns`).
- By handicap: h0 mean 36.72 and median 37.5 over 32 games; h3 mean 30.00 and
  median 29.5 over 32 games.
- Win types, arm's 45 wins: `home-checkmate` 21, `elimination` 21,
  `upkeep-elimination` 3.
- Win types, champion's 17 wins: `elimination` 8, `home-checkmate` 7,
  `home-occupation` 2.
- Draws: 2, both `inactivity` — natural ten-quiet-turn game outcomes, not
  harness adjudications (`inactivityDraw` true on both).

### 3.7 Correctness and hygiene columns

- `adjudicationRate` 0; no game was adjudicated by the harness turn limit
  (`adjudicated` false on all 64).
- `illegalActions` 0, summed over both seats of all 64 games.
- `replicaDivergences` 0.
- `invariantViolation` null on all 64 games; `anomalies` empty on all 64;
  `timingAnomalies` 0.
- `failures.jsonl` is empty (0 bytes).

### 3.8 Timing, overruns and search stopping

Allowance `wall:3000`; frozen tolerance `max(10ms, 1%)` = 30 ms at this
allowance (`overrunTolerance.frozen` true).

| engine | turns | mean turn ms | p95 turn ms | max turn ms | overAllowance | overruns | overrun rate |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `hard@ablate:eval-no-safety` | 2,122 | 1,714.8 | 3,001 | 3,007 | 122 | 0 | 0.00% |
| `hard@desktop` | 2,110 | 1,822.3 | 3,002 | 3,656 | 154 | 4 | 0.19% |

- A14: the void bar is 5% for either arm; the observed rates are 0.00% and
  0.19%, so the row is NOT voided (`voided` false, `voidReason` null).
- P6: the longest turn in the row is 3,656 ms (`hard@desktop` as Black in pair
  `e1v2-g3-s85:0:8`); 0 turns exceed 20 s; no P6 flag.
- `budgetExhausted` 0 and `reSearches` 0 for both arms.

| engine | searches | aborted | abort rate | first search aborted | empty plans |
| --- | ---: | ---: | ---: | ---: | ---: |
| `hard@ablate:eval-no-safety` | 2,122 | 142 | 6.69% | 47 | 0 |
| `hard@desktop` | 2,110 | 191 | 9.05% | 45 | 0 |

- `firstSearchAborted` counts games whose first search stopped on the clock;
  47 of the arm's 64 seat-games and 45 of the champion's.
- Startup, packing and verification overhead is not separated in this row's
  artifacts; `decisionMs` per seat and `durationMs` per game are recorded, and
  EPIC-PLAN §5's "report overhead separately" is unmet here (§10).

### 3.9 Outcome against the preregistered rule

- Score 0.71875 > 0.5: the weak form of the prediction is MET.
- 95% Elo interval [+85.12, +260.36] excludes 0: the strong form is MET.
- Row #1 is a screening row and retains nothing. Per the rule, row #2 was
  funded.

## 4. Row #2 — confirmation

Source: `lab/results/hard-ai-e3/ablate/eval-no-safety/confirm/`.

### 4.1 Provenance

- Launch commit `a0f03c4e028fc96aeaf66dd00d276a84390bbd8d`, one commit for both
  arms, from `~/src/deevgames-e3-run` re-detached at that commit.
- `manifest.gitDirty` is `true`, as in row #1.
- Started `2026-09-17T10:09:53.457Z`, finished `2026-09-17T12:12:07.294Z`,
  wall 7,333,996 ms (2 h 02 m 14 s), single shard.
- Box: the same Apple M2 Max; loadavg at launch 5.19 / 7.70 / 6.50.
- `baseline.ai` `cf814f2826b0b2ba2e8608e8c7827d5c48538f1cf0f70cec57a3700ddc463ffe`
  — DIFFERENT from row #1's `81d011ea…`, because lanes 9–12 added the
  correctness flags to `src/ai/hard/**` between the two commits (`git diff
  50b6a94c a0f03c4e -- src/ai`: 7 files, 288 insertions, 18 deletions).
- Both arms' resolved config hashes are unchanged between the rows, and the
  fixed-work golden at `108a1215` against `50b6a94c` was 48 of 48 rows
  identical on `scoreCc`, `depth`, `work`, `nodes`, `endKey` and `source`
  (`lab/results/hard-ai-e3/correct/cross-commit/rows-head-108a1215.json`, as
  recorded in `E3-PLAN.md`). The champion's play is unchanged; its source bytes
  are not (§10).

### 4.2 Openings

- File `lab/hard-ai/ladder/openings/e2-val.jsonl`, sha256
  `df0c99ccc1249445d35b746b3add51e50c2182c15261a12fcd464cb6bc333f52`
  (verified against the file in this worktree: 64 rows, same digest).
- `--openings-skip 32`; `openingsUsed` is 16 ids, rows 32–47: `e2-g4-s430`,
  `e2-g5-s475`, `e2-g4-s490`, `e2-g4-s510`, `e2-g3-s525`, `e2-g2-s530`,
  `e2-g5-s535`, `e2-g3-s545`, `e2-g5-s575`, `e2-g3-s585`, `e2-g5-s595`,
  `e2-g3-s605`, `e2-g4-s610`, `e2-g4-s615`, `e2-g5-s635`, `e2-g3-s645`.
- That list is byte-identical to rows 32–47 of the file (checked by reading the
  file's `id` fields in order), which is critique B4's arithmetic confirmed:
  32 pairs over h0/h3 consume 16 openings, not 32.
- First and only use of this block; rows 48–63 remain reserved.
- `openingsIndependent` true, `independenceWarning` null; `distinctGames`
  A-white 32 and B-white 32, digested from the replays, `duplicates` false.
- Independence width is 16 openings, the same √2 caveat as row #1.
- Rows #1 and #2 draw from different opening files, so the two rows are 32
  distinct unseen validation openings in total.

### 4.3 Score, record and uncertainty

- 32 pairs scheduled, 32 completed; 64 games played; 0 missing, 0
  unattributed, 0 failures; replays 64 of 64.
- Arm record W/D/L 42/2/20; score 0.671875.
- Paired five-category counts: 4 / 0 / 12 / 2 / 14 over 32 pairs.
- Score interval 0.554–0.789; pair standard error 0.1199.
- Elo +124.50, 95% interval [+37.94, +229.50], LOS 99.793%.
- No SPRT requested; the row is a fixed sample read against its preregistered
  rule.

### 4.4 Per-handicap strata

| handicap | pairs | games | score (arm) | W/D/L | Elo | 95% interval | LOS |
| ---: | ---: | ---: | ---: | --- | ---: | --- | ---: |
| 0 | 16 | 32 | 0.7188 | 23/0/9 | +162.99 | [+32.19, +365.54] | 99.352% |
| 3 | 16 | 32 | 0.6250 | 19/2/11 | +88.74 | [−21.68, +221.04] | 94.167% |

- The h3 stratum's interval covers 0; the row's retention rule is read on the
  whole row, not on a stratum.

### 4.5 Per-seat strata

| seat of the arm | games | W/D/L | score |
| --- | ---: | --- | ---: |
| White (`A-white`) | 32 | 21/1/10 | 0.6719 |
| Black (`B-white`) | 32 | 21/1/10 | 0.6719 |
| White, h0 | 16 | 11/0/5 | 0.6875 |
| White, h3 | 16 | 10/1/5 | 0.6562 |
| Black, h0 | 16 | 12/0/4 | 0.7500 |
| Black, h3 | 16 | 9/1/6 | 0.5938 |

- `bothSeatsPlayed` true and `seatMirrored` true.
- The seat split is even in row #2 (0.6719 both seats) where row #1 favoured
  the arm as Black (0.7656 against 0.6719).

### 4.6 Game length and win types

- Turns per game over all 64 games: mean 32.69, median 31.5, min 5, max 84.
- By handicap: h0 mean 31.56 and median 26.5; h3 mean 33.81 and median 32.5.
- Win types, arm's 42 wins: `home-checkmate` 25, `elimination` 14,
  `upkeep-elimination` 3.
- Win types, champion's 20 wins: `elimination` 10, `home-checkmate` 9,
  `home-occupation` 1.
- Draws: 2, both `inactivity`, natural game outcomes.

### 4.7 Correctness and hygiene columns

- `adjudicationRate` 0; no harness turn-limit adjudication in 64 games.
- `illegalActions` 0 over both seats of all 64 games.
- `replicaDivergences` 0.
- `invariantViolation` null on all 64; `anomalies` empty on all 64;
  `timingAnomalies` 0; `failures.jsonl` empty.

### 4.8 Timing, overruns and search stopping

| engine | turns | mean turn ms | p95 turn ms | max turn ms | overAllowance | overruns | overrun rate |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `hard@ablate:eval-no-safety` | 2,064 | 1,724.2 | 3,001 | 3,495 | 118 | 3 | 0.15% |
| `hard@desktop` | 2,055 | 1,833.9 | 3,001 | 11,918 | 147 | 3 | 0.15% |

- A14: both rates are 0.15% against the 5% void bar; the row is NOT voided
  (`voided` false, `voidReason` null).
- P6: the longest turn is 11,918 ms, by `hard@desktop` as Black in pair
  `e2-g5-s575:0:16` at h0; that is under the 20 s P6 bar; 0 turns exceed 20 s;
  no P6 flag.
- The second-longest turn in the row is 3,762 ms, also the champion's; the
  11,918 ms turn is a single outlier on a loaded box, and it is the champion's
  seat, not the arm's.
- `budgetExhausted` 0 and `reSearches` 0 for both arms.

| engine | searches | aborted | abort rate | first search aborted | empty plans |
| --- | ---: | ---: | ---: | ---: | ---: |
| `hard@ablate:eval-no-safety` | 2,064 | 141 | 6.83% | 44 | 0 |
| `hard@desktop` | 2,055 | 178 | 8.66% | 34 | 0 |

### 4.9 Outcome against the preregistered rule

- Score 0.671875 > 0.5: MET.
- 95% Elo interval [+37.94, +229.50] excludes 0: MET.
- Both halves of the retention rule are satisfied on the confirmation row.

## 5. Mechanism check — did the rows price judgment or search?

The preregistered check is that the arm's rung distribution must not sit above
the champion's and its abort rate must not exceed the champion's.

### 5.1 The fields that exist

- Per-seat per-turn rows ARE recorded for a Hard-vs-Hard row, at
  `players.<side>.hardTiming.turnRows` in `games.jsonl`.
- Each row carries: `turn`, `rung`, `work`, `elapsedMs`, `searchMs`,
  `fundedMs`, `depth`, `stopReason`, `unitsPerMsBefore`, `unitsPerMsAfter`,
  `deadlineCut`. All eleven fields are present on every row read.
- `rung` is the work rung the search was funded to; `unitsPerMsAfter` is the
  throughput estimate the seat carried out of the turn; `depth` is the depth
  the search reports for the turn.
- Computation: a throwaway Python script over `games.jsonl`, run outside the
  repository; no engine was run for this report.

### 5.2 Row #1, 4,232 turn rows (2,122 arm, 2,110 champion)

| rung | arm | champion |
| ---: | ---: | ---: |
| 50,000 | 3 (0.1%) | 3 (0.1%) |
| 100,000 | 1,213 (57.2%) | 1,125 (53.3%) |
| 200,000 | 839 (39.5%) | 914 (43.3%) |
| 400,000 | 67 (3.2%) | 68 (3.2%) |

- Mean `unitsPerMsAfter`: arm 64.40, champion 66.40 (medians 59 and 63).
- Mean `unitsPerMsBefore`: arm 68.60, champion 70.65.
- Mean `depth`: arm 2.362, champion 2.427; medians 2 and 2; maxima 12 and 12.
- Mean depth over rows that did not abort: arm 2.352 (n = 1,980), champion
  2.425 (n = 1,919).
- `stopReason`: arm abort 142 / work 1,933 / complete 47; champion abort 191 /
  work 1,894 / complete 25.
- `deadlineCut` true on 142 arm rows and 191 champion rows, matching the abort
  counts exactly.

### 5.3 Row #2, 4,119 turn rows (2,064 arm, 2,055 champion)

| rung | arm | champion |
| ---: | ---: | ---: |
| 100,000 | 1,048 (50.8%) | 950 (46.2%) |
| 200,000 | 952 (46.1%) | 1,041 (50.7%) |
| 400,000 | 64 (3.1%) | 64 (3.1%) |

- Mean `unitsPerMsAfter`: arm 68.09, champion 70.23 (medians 65 and 68).
- Mean `unitsPerMsBefore`: arm 72.23, champion 74.40.
- Mean `depth`: arm 2.387, champion 2.432; maxima 12 and 12.
- Mean depth over rows that did not abort: arm 2.382 (n = 1,923), champion
  2.452 (n = 1,877).
- `stopReason`: arm abort 141 / work 1,881 / complete 42; champion abort 178 /
  work 1,852 / complete 25.

### 5.4 Per-seat rung distribution

Read from the same rows, split by the arm's seat.

| row | orientation | engine | seat | rows | 100k | 200k | 400k | mean depth | mean units/ms |
| --- | --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| #1 | A-white | arm | white | 1,016 | 53.5% | 43.2% | 3.1% | 2.424 | 65.69 |
| #1 | A-white | champion | black | 1,000 | 49.8% | 47.0% | 3.2% | 2.422 | 67.56 |
| #1 | B-white | arm | black | 1,106 | 60.5% | 36.2% | 3.2% | 2.306 | 63.22 |
| #1 | B-white | champion | white | 1,110 | 56.5% | 40.0% | 3.2% | 2.432 | 65.36 |
| #2 | A-white | arm | white | 912 | 49.5% | 47.0% | 3.5% | 2.406 | 68.42 |
| #2 | A-white | champion | black | 907 | 43.9% | 52.6% | 3.5% | 2.442 | 71.27 |
| #2 | B-white | arm | black | 1,152 | 51.8% | 45.4% | 2.8% | 2.372 | 67.83 |
| #2 | B-white | champion | white | 1,148 | 48.1% | 49.1% | 2.8% | 2.423 | 69.42 |

- The pattern is the same in all four seat splits: the arm sits on the 100k
  rung more often and on the 200k rung less often than the champion facing it,
  and the 400k tail is equal to within 0.1 percentage points.

### 5.5 Reading

- The arm's rung distribution is BELOW the champion's in both rows: more mass
  at 100,000 units, less at 200,000, the same at 400,000.
- The arm's abort rate is below the champion's in both rows: 6.69% against
  9.05% in row #1, 6.83% against 8.66% in row #2.
- The arm's mean completed depth is below the champion's in both rows: 2.362
  against 2.427 in row #1, 2.387 against 2.432 in row #2.
- The arm's mean turn is shorter in both rows: 107 ms shorter in row #1, 110 ms
  shorter in row #2.
- The preregistered condition is met in every column. The rows did not price
  extra search.
- The fixed-work cost probe bounds the confound from the other direction: at
  `fixed:100000` the arm spends 555.0 mean nodes against the champion's 536.83,
  +3.4%, with 1,078.3 evals against 964.67 and mean completed depth 3.33
  against 3.17 (`lab/results/hard-ai-e3/ablate/cost-probe.json`, 6 `e1-dev`
  positions). A cheaper evaluator buys slightly more nodes per unit at fixed
  work; at equal time the arm still searched less.

### 5.6 Box load caveat

- Two wall-clock rows shared the box during each of these rows: E2's re-pin row
  held heavy slot 0 while row #1 ran on slot 1, and the same arrangement held
  for row #2.
- Recorded loadavg at launch: 3.30 / 5.24 / 5.83 for row #1 and 5.19 / 7.70 /
  6.50 for row #2; the critique observed 6.6–9.0 at 06:40Z during row #1.
- Within-row fairness is unaffected: both seats run in one process under one
  clock, so load lowers both alike, and the units/ms columns record it (row #2
  ran about 6% faster per unit than row #1 on both seats).
- What load DOES affect is any comparison of these rung distributions against
  another row's. Critique C8 and C13 ask for per-game load recording; that
  instrument does not exist, so these rung percentages are not comparable to
  E2's.
- The champion's 11,918 ms turn in row #2 is the visible cost of that load; it
  is inside the P6 bar and it is the champion's seat.

## 6. Non-regression columns

Both arms measured under the weights each one plays with, after A7-1 landed
(`E3.2-PRECONDITIONS.md`; `suites/run.ts` previously built searched engines
with the `placeholder-m4` version-0 vector).

### 6.1 Suites, each case at its own budget

Artifacts: `lab/results/hard-ai-e3/preconditions/suites-desktop/after-default-v1.json`
and `.../suites-eval-no-safety/after-default-v1-no-safety.json`, 205 cases each,
`--shards 1`.

| suite | n | `hard@desktop` (`default-v1`) | arm (`default-v1-no-safety`) | threshold | met? |
| --- | ---: | ---: | ---: | --- | --- |
| `tactics` | 79 cases / 73 points | .9178 (67/73 points, 73/79 cases) | .9178 (67/73) | arm ≥ 73/79 | MET, equal |
| `spawn-strike` | 20 | .80 (16/20) | .75 (15/20) | arm within one case of 16/20 | MET |
| `home-mate` | 56 | 56/56 | 56/56 | arm 56/56 | MET |
| `invariantsSearched` | 20 pairs | .30 (6/20) | .40 (8/20) | none preregistered | reported |
| `invariantsEval` | 20 pairs | .60 (12/20) | .60 (12/20) in this artifact | none preregistered | reported, see below |
| `economy` | 30 | 1.000 (30/30) | 1.000 (30/30) | none preregistered | vacuous (L7-F2) |

- `illegalTurns` 0 and `replicaDivergences` 0 in both suite runs.
- `tactics` misses the same six cases in both engines, all
  `tactics-plugged-*-vs-fire_3`.
- `spawn-strike`: the arm fails everything the champion fails and additionally
  `spawn-strike-purchase-1`.
- `invariantsSearched`: the arm passes everything the champion passes and
  additionally `inv13-turtle` and `inv17-self-block`, both pairs whose
  invariant sits in the zeroed block.
- `invariantsEval` .60 for the arm in the committed precondition artifact is
  the CHAMPION's vector read under the arm's name (L9-F1: the column was scored
  with a module-level `Evaluator` carrying `DEFAULT_WEIGHTS`). After lane 13's
  A9-1 fix the arm's `invariantsEval` is .50 (10/20) and the champion stays .60
  (`lab/results/hard-ai-e3/followups/suites/after-a9-1-ablate-eval-no-safety.json`,
  read directly). Both numbers are reported; neither is a threshold.
- A6's `spawnStrike` .95 baseline is UNMET by the champion under its real
  weights (16/20 = .80). That is a red obligation and stays visible; it is
  recorded, not amended.
- M14's pass criterion `suite.spawnStrike >= 0.80` measured with the gate's own
  engine and work is exactly .80 with zero margin
  (`preconditions/suites-desktop/m14-gate-spawn-strike-lab-400k.json`,
  `hard@lab`, `--work 400000`, 16/20).

### 6.2 Exam, dev stratum, both budgets, both arms

Artifacts: `lab/results/hard-ai-e3/preconditions/exam-{desktop,eval-no-safety}-fixed{25k,200k}/dev.json`,
149 cases each (127 exact + 22 judgment). Values read from the artifacts.

| engine | work | exact | judgment matched | unmatched | won | dead | wall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `hard@desktop` | fixed:25,000 | 121/127 | 1 | 14 | 3 | 4 | 18.9 s |
| `hard@desktop` | fixed:200,000 | 120/127 | 0 | 15 | 3 | 4 | 63.1 s |
| arm | fixed:25,000 | 121/127 | 2 | 13 | 3 | 4 | 19.1 s |
| arm | fixed:200,000 | 117/127 | 0 | 15 | 3 | 4 | 64.4 s |

- Preregistered threshold: exam exact at `fixed:200,000` within three cases of
  the champion. Observed 117 against 120, a gap of exactly three: MET AT THE
  BOUNDARY. A further loss would fail it.
- The three cases the arm loses at 200k are
  `authored-tactics-tactics-plugged-fire_2-vs-fire_3`,
  `authored-spawn-strike-spawn-strike-purchase-1` and
  `authored-spawn-strike-spawn-strike-purchase-3`; nothing goes the other way.
- At 25,000 units the two engines are level at 121/127, trading
  `plugged-fire_2-vs-fire_3` (champion passes) for
  `spawn-strike-retreat-1` (arm passes).
- "Exam exact unchanged" is a 25,000-unit fact only and must never be quoted
  without its budget.
- The champion itself loses `spawn-strike-purchase-4` between 25,000 and
  200,000 units (121 → 120), which is why both budgets are carried.
- Judgment counts are four numbers since A7-2 and A7-3:
  `matched + unmatched + won + dead = 22` in all four runs; the three `won` and
  four `dead` rows are the same seven cases in every run, because `dead` is a
  property of the root and not of the engine.
- Judgment counts are REPORTED, NEVER GATED. They are budget-fragile: the
  champion's single agreement at 25k is gone at 200k, where both engines match
  0 of 22.

### 6.3 The thresholds, collected

| threshold (preregistered before row #2) | champion | arm | verdict |
| --- | --- | --- | --- |
| tactics ≥ 73/79 under real weights | 73/79 | 73/79 | MET |
| home-mate 56/56 under real weights | 56/56 | 56/56 | MET |
| spawn-strike within one case of 16/20 | 16/20 | 15/20 | MET |
| exam exact at fixed:200,000 within three cases | 120/127 | 117/127 | MET at the boundary |
| judgment counts | reported | reported | not gated |
| A14 overrun ≤ 5% both arms | 0.19% / 0.15% | 0.00% / 0.15% | not voided |
| illegalActions 0 | 0 | 0 | MET |
| replica divergences 0 | 0 | 0 | MET |
| P6: no turn over 20 s | max 11,918 ms | max 3,495 ms | no flag |

## 7. What the two rows establish, and what they do not

### 7.1 What they establish

- The 19 safety weights AS AUTHORED cost `hard@desktop` strength at equal time
  on unseen validation openings.
- The evidence is two rows, 64 pairs, 128 games, 32 distinct validation
  openings never used before: +163 [+85, +260] and +124 [+38, +229].
- The effect survives both handicaps in both rows (four strata, all scores
  above 0.5), and both seats in both rows (four seat splits, all above 0.5).
- The rows priced judgment, not search: the arm's rungs, abort rate, completed
  depth and mean turn time are all below the champion's (§5).
- Neither row is voided; there is no illegal action, no replica divergence and
  no harness adjudication in 128 games.

### 7.2 What they do not establish

- **This is a GROUP ABLATION, not a fix of a named concept.** The block
  contains `AnchorFragility`, `BlockingDeficit`, `Inv6FragileAnchor`,
  `Inv17SelfBlock`, `Inv8NoPreAdjacency`, `Inv9ChipAcrossTurn` and
  `Inv12CleaveLine`, none of which is "the price of a threatened own body"
  (critique C1). The rows say the union is net-negative; they do not say which
  weight is wrong or by how much.
- **The named concept is in-sample only.** Lane 10's 47-loss extension over 39
  distinct roots: safety → played 17 of 30, economy → adviser 16 of 16, the
  joint claim 6 of 39, against the in-sample 9 of 10, 8 of 11 and 6 of 12.
  Judge 1's material gap at the decision is −0.69 crystals over 39 roots, so
  the seat was not ahead when it chose. What generalises is the routing (static
  prefers the played turn at 28 of 39) and judge 1's collapse (−18.95 at three
  turns later, negative at 34 of 39). "E3.2 fixes one misconception" is NOT
  claimed for this arm.
- **The development case does not reproduce under the search the row prices.**
  `E3.2-REPRO.md`: the static +60 / −60 holds exactly, but the arm still plays
  the AVOIDED key `4844d47f0e9b8049` on `loss-g4-s6_3_5-B-white-t1` at both
  `fixed:25,000` and `fixed:200,000`, and the fresh sweep flips only at 400k,
  only within the 300 cc tolerance, and to a third turn `7a4e0a31c5259add`,
  never to the adviser's `559e2d60610127d8`. A 60 cc leaf flip does not survive
  a depth-2/3 search at that root.
- **No sub-block reproduces the block.** Descriptive fixed-work rows at
  `fixed:100000` on the four `e1-dev` openings, 8 pairs each: threat stack
  0.688 (11/0/5, +137 [+14, +309], LOS 98.6%), anchor 0.562 (9/0/7, +44 [−36,
  +129]), safety invariants 0.625 (10/0/6, +89 [−74, +307]), against the whole
  block's 0.875 (14/0/2, +338 [+168, +1,200]). All three are at or above 0.5
  and removing all nineteen beats removing any part. Per critique fix 8 the
  union stays the candidate and no sub-arm gets its own screening row.
- **The prior was a selected maximum.** The +338 was the best of six group arms
  on the same four development openings (per opening 3/4, 4/4, 4/4, 3/4; sign
  test p = 1/16; seat split 6/8 as White and 8/8 as Black). Row #1's +163 on 16
  unseen openings is the number to quote from here on.
- **The comparison against shipped Hard is not in hand.** Row #3
  (`hard@ablate:eval-no-safety` vs `aiv2-hard`, `e1-baseline.jsonl`, 100 pairs,
  seed 20260933, descriptive) launched 12:15:21Z and has no artifact in this
  worktree. The reference numbers it will be read against are E1's baseline
  `hard@desktop` vs `aiv2-hard` 152/1/47, 0.7625, +202.6 [+144.7, +273.0]
  (`lab/results/hard-ai-e1/baseline/metrics.json`, 100 pairs) and E2's re-pin
  of that row at the P6-fixed code, 155/0/45, 0.775, +215 [+156, +287], which
  `E3-PLAN.md` reports and whose artifact lives outside this worktree (§10).
- **M20's contract stands.** Feature-activation of a changed weight vector as a
  shipped DEFAULT needs M20's fixed-work SPRT and E6's held-out release
  campaign. A retained arm here is development evidence and a champion
  candidate. Nothing in these two rows changes a shipped default.
- **Correctness is a separate column from strength** (EPIC-PLAN §7: no umbrella
  status). The arm's correctness column is "no regression against the
  preregistered thresholds", not "correct": it zeroes `Inv3RetreatSquare`,
  whose missing conjunct is a known bug, rather than fixing it.
- **A15 bounds the intervals.** The independence source is 16 openings per row,
  so each 32-pair interval is optimistic by roughly √2.
- **Neither row separates startup, packing and verification overhead** from
  total elapsed time, which EPIC-PLAN §5 asks for; the artifacts carry
  per-seat `decisionMs` and per-game `durationMs` only.

## 8. Decision record (EPIC-PLAN §7 shape)

```text
Candidate/source/config hashes:
  candidate: hard@ablate:eval-no-safety, resolved wall:3000 hash
    66edf9cdf58f6b037186b25d05a939ed521e8a76e07051937dcbc611eb343aaf,
    weights default-v1-no-safety (version 1); arms.ts factor `weights`
  incumbent: hard@desktop, resolved wall:3000 hash
    4e7afdf76b32fadfab2d11577cb610c8f9f1a491b5153e70b87b0774403600cd,
    weights default-v1
  row #1 commit 50b6a94c2d5322769dff11ea83fb18ecfc1e74b2 (gitDirty true),
    src/ai tree 81d011ea20659c61…, src/game 442226339ec2dd2c…, wasm da9b1cdf25e4bcd6…
  row #2 commit a0f03c4e028fc96aeaf66dd00d276a84390bbd8d (gitDirty true),
    src/ai tree cf814f2826b0b2ba… (lanes 9–12's correctness flags, default off),
    src/game and wasm unchanged; champion config hash and fixed-work golden unchanged
  one commit for both arms in each row; node v24.11.1; Apple M2 Max x12 darwin/arm64
Hypothesis and changed parameter(s):
  hypothesis: the champion's 19 authored safety weights are net-negative at equal time
  changed: w[17,28,29,30,31,32,33,34,35,36,40,41,43,45,46,49,54,56,57] = 0
  unchanged: the 18 catalogue material priors, w[Material], and the other 39 weights
  the zeros are read by the candidate generator's ordering and K-cut as well as the search leaf
Correctness: pass against the preregistered thresholds; NOT a correctness claim for the arm
  illegalActions 0 and replicaDivergences 0 in 128 games across both rows
  adjudicationRate 0 in both rows; invariantViolation null on all 128 games
  tactics .9178 (73/79 cases, 67/73 points) = champion; home-mate 56/56 = champion
  spawn-strike 15/20 against the champion's 16/20 (within one case: met)
  exam exact fixed:200,000 117/127 against 120/127 (within three: met at the boundary)
  exam exact fixed:25,000 121/127 for both engines
  judgment (matched/unmatched/won/dead) 25k: arm 2/13/3/4, champion 1/14/3/4; 200k: 0/15/3/4 both — reported, not gated
  invariantsSearched .40 (arm) against .30 (champion); invariantsEval .50 (arm, post-A9-1) against .60 (champion)
  hard@desktop unchanged: config hash identical in both rows; fixed-work golden 48 of 48 rows identical
  A6's spawn-strike .95 baseline is UNMET by the champion under default-v1 (16/20) — a visible red obligation
Strength vs shipped Hard: NOT MEASURED for the candidate
  row #3 (hard@ablate:eval-no-safety vs aiv2-hard, e1-baseline.jsonl, 100 pairs, h0/h3, seed 20260933,
    descriptive) launched 2026-09-17T12:15:21Z; no artifact in this worktree
  reference: hard@desktop vs aiv2-hard, E1 baseline, 100 pairs, wall:3000: 152/1/47, 0.7625, +202.6 [+144.7, +273.0]
  reference: E2's re-pin of that row at the P6-fixed code: 155/0/45, 0.775, +215 [+156, +287] (E3-PLAN; artifact outside this worktree)
Strength vs previous candidate: none — E2 retained no candidate
  strength vs the incumbent hard@desktop, equal time, wall:3000, h0/h3, --legality strict, --shards 1:
    row #1 (screening), seed 20260931, e1-val2.jsonl rows 0–15, 16 openings, 32 pairs, 64 games:
      45/2/17, score 0.71875, Elo +163 [+85, +260], LOS 99.999%
      h0 0.734 (23/1/8, +177 [+62, +348]); h3 0.703 (22/1/9, +150 [+50, +281])
      arm as White 21/1/10 (0.672); arm as Black 24/1/7 (0.766)
    row #2 (confirmation), seed 20260932, e2-val.jsonl rows 32–47, 16 openings, 32 pairs, 64 games:
      42/2/20, score 0.671875, Elo +124 [+38, +229], LOS 99.8%
      h0 0.719 (23/0/9, +163 [+32, +366]); h3 0.625 (19/2/11, +89 [−22, +221])
      arm as White 21/1/10 (0.672); arm as Black 21/1/10 (0.672)
    A15: independence width is 16 openings per row, so each interval is optimistic by about √2
    prior (descriptive, fixed:100000, 4 e1-dev openings, 8 pairs, seed 32): 0.875, 14/0/2, +338 [+168, +1200]
      — the selected best of six group arms on those four openings; superseded as the number to quote
Responsiveness: desktop only; Apple M2 Max x12, darwin/arm64, node v24.11.1
  row #1: arm mean 1,714.8 ms, p95 3,001 ms, max 3,007 ms, overruns 0 of 2,122 turns (0.00%)
  row #1: champion mean 1,822.3 ms, p95 3,002 ms, max 3,656 ms, overruns 4 of 2,110 turns (0.19%)
  row #2: arm mean 1,724.2 ms, p95 3,001 ms, max 3,495 ms, overruns 3 of 2,064 turns (0.15%)
  row #2: champion mean 1,833.9 ms, p95 3,001 ms, max 11,918 ms, overruns 3 of 2,055 turns (0.15%)
  tolerance frozen at max(10ms,1%) = 30 ms; A14 void bar 5%; neither row voided
  P6: no turn over 20 s in either row; the 11,918 ms outlier is the champion's seat under box load
  fallbacks: emptyPlans 0, budgetExhausted 0, reSearches 0 for both arms in both rows
  abort rate (clock-stopped searches): row #1 6.69% arm / 9.05% champion; row #2 6.83% / 8.66%
  firstSearchAborted: row #1 47 / 45; row #2 44 / 34
  peak memory: NOT MEASURED — the ladder harness records no memory column
  p99 latency: NOT MEASURED — the harness records p95 and max only
  completed depth: mean 2.362 (arm) / 2.427 (champion) in row #1; 2.387 / 2.432 in row #2
Representative loss and where the strong turn disappeared:
  g4-s6_3_5-B-white, black, turn 1, root 74b71f89ae7abb64 (= g5-s7_3_7-B-white t1); never flips at any work ≤ 800k for the champion
  champion static delta −275 cc = safety −335 (Hanging −250, ApproachStrand −50, KillAvailable −35) + RelocationDebt +60
  with the 19 weights at 0 the same two end states score +60 cc for the adviser's turn — reproduced exactly
  but the arm's SEARCH still plays the avoided key 4844d47f0e9b8049 at fixed:25,000 and at fixed:200,000
  and the arm's fresh sweep flips only at 400,000 units, within the 300 cc tolerance, to a third turn 7a4e0a31c5259add
  second case g4-s2_3_1-B-white black t2: champion −1,475 cc, arm −60 cc; it does not flip
  so the strong turn does not reappear under the search the row prices; the row is a group ablation, not a repaired misjudgment
Decision: RETAIN hard@ablate:eval-no-safety as the CHAMPION CANDIDATE
  rule: score > 0.5 AND a 95% interval excluding 0 on the confirmation row — both met (0.672, [+38, +229])
  all preregistered non-regression thresholds met, one (exam exact at 200k) at its boundary
  development evidence under EPIC-PLAN §5 campaign 4; NOT a shipped default (M20's fixed-work SPRT, E6's held-out release)
  mechanism: the rows priced judgment, not search (arm's rungs, abort rate, depth and turn time all below the champion's)
Unresolved criteria or proposed amendments:
  which of the 19 weights carries the effect at equal time — no sub-block reproduces it (0.688 / 0.562 / 0.625 against 0.875, descriptive)
  whether a CORRECTION rather than a removal does better — eval-safety-once is registered as a follow-on, never run
  A6's spawn-strike .95 is unmet by the champion under default-v1 (16/20); M14's 0.80 bar is met with zero margin — MILESTONES owner's call
  M17's relative spawn-strike criterion compares a default-v1 number with a placeholder-m4 one unless both sides are re-measured
  A13-1 (rootDiagnostic flip) and A13-2 are built but not applied; three ablate tests pin the old reading
  A9-4: dev.jsonl and its seeder disagree by four dead judgment rows; no fixture was edited
  critique C13: per-game box load is not recorded, so these rung distributions are not comparable across rows
  EPIC-PLAN §5's startup/packing/verification overhead column is not separated by the ladder harness
Next bounded task:
  1. read row #3 (candidate vs aiv2-hard on e1-baseline, 100 pairs) against E1's +203 and E2's re-pin +215
  2. record per-game box load in the ladder harness (critique C13)
  3. price the correctness bundle eval-correct-v1 against BOTH hard@desktop and this retained candidate (needs a two-factor `combined` arm, preregistered separately)
  4. preregister the sub-block or correction question as its own row if an opening block is funded for it
Links to raw results and replays:
  lab/results/hard-ai-e3/ablate/eval-no-safety/screen/{metrics.json,manifest.json,summary.md,elo.json,games.jsonl,pairs.jsonl,failures.jsonl,replays/}
  lab/results/hard-ai-e3/ablate/eval-no-safety/confirm/{metrics.json,manifest.json,summary.md,elo.json,games.jsonl,pairs.jsonl,failures.jsonl,replays/}
  lab/results/hard-ai-e3/ablate/eval-no-safety/fixed100k/{metrics.json,manifest.json,games.jsonl,replays/}
  lab/results/hard-ai-e3/ablate/{eval-no-threat-stack,eval-no-anchor,eval-no-safety-inv}/fixed100k/
  lab/results/hard-ai-e3/ablate/cost-probe.json
  lab/results/hard-ai-e3/preconditions/exam-{desktop,eval-no-safety}-fixed{25k,200k}/dev.{json,md}
  lab/results/hard-ai-e3/preconditions/suites-{desktop,eval-no-safety}/*.json
  lab/results/hard-ai-e3/followups/suites/after-a9-1-{desktop,ablate-eval-no-safety}.json
  lab/results/hard-ai-e3/repro/{static-eval-no-safety,exam,sweep}/
  lab/results/hard-ai-e3/loss-judgment/{g4-s6_3_5-B-white.json,g4-s2_3_1-B-white.json,summary.json}
  lab/results/hard-ai-e3/correct/cross-commit/rows-head-108a1215.json
  lab/results/hard-ai-e1/baseline/metrics.json (the shipped-Hard reference row)
```

## 9. Raw artifacts, by claim

| what | path |
| --- | --- |
| row #1 score, strata, timing, aborts | `lab/results/hard-ai-e3/ablate/eval-no-safety/screen/metrics.json` |
| row #1 provenance, openings, config hashes | `.../screen/manifest.json` |
| row #1 per-game records, win types, turn rows | `.../screen/games.jsonl` |
| row #1 per-pair scores | `.../screen/pairs.jsonl` |
| row #1 paired Elo detail | `.../screen/elo.json` |
| row #1 rendered summary | `.../screen/summary.md` |
| row #1 replays (64) | `.../screen/replays/` |
| row #2, same six files plus replays | `lab/results/hard-ai-e3/ablate/eval-no-safety/confirm/` |
| the descriptive fixed-work prior row | `lab/results/hard-ai-e3/ablate/eval-no-safety/fixed100k/` |
| the three safety sub-arm rows | `lab/results/hard-ai-e3/ablate/{eval-no-threat-stack,eval-no-anchor,eval-no-safety-inv}/fixed100k/` |
| fixed-work cost probe (nodes, evals, depth) | `lab/results/hard-ai-e3/ablate/cost-probe.json` |
| exam, both engines, both budgets | `lab/results/hard-ai-e3/preconditions/exam-*/dev.json` |
| suites, both engines, real weights | `lab/results/hard-ai-e3/preconditions/suites-*/*.json` |
| suites after A9-1 (invariantsEval with the engine's own weights) | `lab/results/hard-ai-e3/followups/suites/after-a9-1-*.json` |
| the development-case reproduction (static, exam, sweep) | `lab/results/hard-ai-e3/repro/` |
| the loss-position decompositions | `lab/results/hard-ai-e3/loss-judgment/` |
| the fixed-work golden across the launch commits | `lab/results/hard-ai-e3/correct/cross-commit/rows-head-108a1215.json` |
| the shipped-Hard reference row (E1 baseline) | `lab/results/hard-ai-e1/baseline/metrics.json` |
| the feature group definition | `lab/hard-ai/audit/eval-groups.ts` |
| the arm definition | `lab/hard-ai/ablate/arms.ts` |
| the feature indices | `src/ai/hard/eval/features.ts` |
| the `default-v1` weight values | `src/ai/hard/eval/weights.ts` |

## 10. Discrepancies found while checking the artifacts

Each of these is a prose claim that does not match the artifact it rests on.
None of them changes either row's decision.

1. **Row #1's finish time.** `E3-PLAN.md`'s ledger says "DONE ~10:00Z" and its
   correction bullet says "row #1 read 10:01Z". The manifest says
   `finishedAt` `2026-09-17T08:39:02.768Z` with `wallMs` 7,486,573. The row
   finished at 08:39Z and was read at 10:01Z; the ledger's "~10:00Z" is the
   reading time, not the completion time.
2. **The `src/ai` tree is not identical across the two rows.** `manifest.baseline.ai`
   is `81d011ea…` for row #1 and `cf814f28…` for row #2; `git diff 50b6a94c
   a0f03c4e -- src/ai` shows 7 files and 288 insertions (lanes 9–12's
   correctness flags, default off). The champion's resolved config hash is
   identical in both manifests and the fixed-work golden was 48 of 48 rows
   identical, so play is unchanged — but "byte-identical engine" is false as
   stated and the golden, not the hash, is the evidence (critique B5's point,
   confirmed).
3. **Both rows ran with `gitDirty` true.** `manifest.git` records the commit and
   `manifest.gitDirty` records `true` for row #1 and row #2 alike. The
   "one commit for both arms" rule is satisfied by the two resolved config
   hashes and the recorded weight vectors, not by a clean tree. Recorded, as
   the critique's N3 recorded the same for lane 5's rows.
4. **The rung histogram has a fourth rung the plan's correction omits.** Row
   #1 carries 3 turn rows at rung 50,000 for each arm (0.1% each), beside the
   100k / 200k / 400k rungs the plan's mechanism bullet lists. The percentages
   the plan quotes are otherwise exact. Row #2 has no 50,000 rows.
5. **`summary.md` mislabels its openings line.** Both rows' `summary.md` print
   "openings: used 16 of 32" and then list all 32 ids of the post-skip pool
   rather than the 16 in `openingsUsed`. `metrics.json` `openingsUsed` and
   `manifest.openings.used` both carry the correct 16. A reader of
   `summary.md` alone would over-count the independence width by a factor of
   two; this report uses the metrics field.
6. **`invariantsEval` for the arm is reported as two different numbers in two
   committed places.** `E3.2-PRECONDITIONS.md` §1.2's table says .60 and its
   appended coordinator note (A13-3) says .50; the artifacts hold both — .60 in
   `preconditions/suites-eval-no-safety/after-default-v1-no-safety.json` (the
   pre-A9-1 reading, which is the champion's vector under the arm's name) and
   .50 in `followups/suites/after-a9-1-ablate-eval-no-safety.json`. The .50 is
   the number that has the arm in it.
7. **E2's re-pin row cannot be verified from this worktree.** `E3-PLAN.md`
   quotes `hard@desktop` vs `aiv2-hard` 155/0/45, 0.775, +215 [+156, +287].
   No such metrics file exists under `lab/results/hard-ai-e2/`; the artifact
   lives in `~/src/deevgames-e2-run2`, which this session does not touch. The
   E1 baseline it reproduces IS in this worktree and reads 152/1/47, 0.7625,
   +202.6 [+144.7, +273.0] over 100 pairs, which matches the plan's "E1's +203
   [145, 273]".
8. **Row #3 has no artifact here.** `lab/results/hard-ai-e3/ablate/eval-no-safety/`
   contains `screen`, `confirm`, `fixed100k` and `exam` only; there is no
   `vs-shipped` directory. The shipped-Hard comparison is genuinely pending.
9. **Two EPIC-PLAN §5 columns cannot be filled from these artifacts.** Peak
   memory and p99 turn latency have no field in `metrics.json`, `manifest.json`
   or `games.jsonl`; startup, packing and verification overhead is not
   separated from total elapsed time. They are recorded as NOT MEASURED in the
   decision record rather than estimated.

## Addendum — row #3, the shipped-Hard comparison (2026-09-17 14:3xZ)

- `eval-no-safety` vs `aiv2-hard`, wall:3000, `e1-baseline.jsonl` 50 × h0/h3,
  seed 20260933, 4 shards at 55ae23d8: 181/1/18, 0.9075, +397 [+330, +495];
  h0 0.890, h3 0.925; overrun 0.07%, abort 13.2%, max turn 3,200 ms, load
  per game mean 10.0. Artifacts:
  `lab/results/hard-ai-e3/ablate/eval-no-safety/vs-shipped/`.
- Champion on the same set (E2 re-pin, `lab/results/hard-ai-e3/reference/e2-repin/`):
  155/0/45, 0.775, +215 [+156, +287].
- Decision-record field "Strength vs shipped Hard": 0.9075 (181/1/18), 100
  pairs, +397 [+330, +495], per handicap 0.890 / 0.925.

## Corrections at the E3 close (2026-09-17 18:04:46Z, `date -u`, appended; from `E3-CLOSE-CRITIQUE.md`)

- §8 "Correctness: pass against the preregistered thresholds" is restated:
  the row #2 bars ("within one case of 16/20", "within three of 120/127")
  were written at 10:06Z after the arm's numbers were committed at 06:52Z and
  sized to them, so they are not evidence. The arm REGRESSES the champion by
  one spawn-strike case (`purchase-1`) and three exam exact cases at 200k;
  tactics and home-mate are equal. Retention rests on the strength rule alone
  (B1). At M14's gate work (fixed 400,000, desktop profile, 18:02Z) the arm
  scores 15/20 = .75 against `>= 0.80` (champion 16/20), a release-contract
  red obligation recorded in `E3-PLAN.md` and `E3-CLOSE.md`.
- §8 intervals: the opening-clustered re-analysis gives row #1 [+91, +251]
  and row #2 [+41, +226] beside the pair-level [+85, +260] and [+38, +229];
  row #2's h3 stratum [−22, +221] covers 0 in every spelling.
- Addendum row #3: the abort rate 13.2% is unexplained; the single-shard
  re-pin of the same set shows the same class of rate (15.1%); first-turn
  aborts are 28% of the aborts (C5). "Close to" is withdrawn.
- §6.1's bundle line: `eval-fix-b6`'s descriptive row is an A/A confirmed
  (identical moves, 8/0/8; ±85 is the prior's width) (N3).
