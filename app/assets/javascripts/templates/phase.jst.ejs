<h3
  <% if (ready){ %>
    class = "ready">Waiting
  <% } else { %>
    ><%= phase.split("_").map(function(word){
      return word.charAt(0).toUpperCase() + word.slice(1);
    }).join(" ") %>
  <% } %>
</h3>