# Gate 1 A1 and baseline purchase freeze — scoped completion

Prepared, verified, **uncommitted** on `codex/gate1-baseline`, base `c43c7280`.
Rules remain `muju-phasing-1`. No deployment or full Gate 1 row was authorized
in this task; neither ran. [Pilot and diagnosis](../../lab/ai/results/t2c-gate1-pilot-2026-09-19/README.md)
pin the dirty-source identity and full evidence.

A1 now replaces historical-margin acceptance with absolute per-cell strength
conditions for Expand/Balanced/medium; Rush strength is report-only. Behavioral
bands remain frozen and every hard game longer than 10 completed player turns
requires a purchase. The adopted preregistration is verified from Git commit
`0a6f8db5`; the Claude-owned source document is unchanged here.

The baseline formerly discarded every buy when all legal squares were at risk
of next-turn disruption. It now keeps legal risky commitments when no safe
ones exist, with existing discounted pending value/income. Saved Rush positions
prove the defect and its repair. No weights, difficulty presets, rules, budgets,
worker guards or Hard-engine files changed.

Plan: `python3 tools/muju-content-dag.py plan --files muju/src/ai/moves.ts muju/src/ai/planner/summons.ts muju/lab/ai/gate1.ts`.
The planner's `summons.ts` input was reviewed but needed no edit: its existing
risky pending value equals refunded bank value at default weights and its
pending-income term is zero. No new DAG surface was added.

| Affected node | Disposition and evidence |
| --- | --- |
| ai-search | **Changed:** legal-risk fallback in move generation; planner comment, diagnostic CLI, A1 runner/report/reference metadata. Saved-state purchase regressions, fixed-work determinism, 16-game pilot and full non-Hard AI suite pass. |
| hard-ai | **Verified unchanged:** no replica, Hard evaluation, adapter, book, weights or frozen document edits. Baseline identity changes must be carried into later Claude-owned strength rows. |
| ai-strength | **Changed:** A1 pass/fail paths verified synthetically; new 16-game pilot has zero correctness failures and zero must-buy violations. Gate 1 is unpassed: pilot is ineligible, all one-pair intervals contain zero, Expand h0 draw band fails. Full rows await scheduling. |
| mcp-tools | **Changed transitively:** observation legal-action lists and bounded tactical analysis import the purchase generator and inherit its legal fallback. No service source edit needed; 59 MCP/analysis/server Phasing tests and server types pass. No semantics or claim change. |
| agent-guides | **Verified unchanged:** no new tool, rule or user-facing claim; existing legal actions remain legal, no guide edit needed. |
| balance-analysis | **Verified unchanged:** rules, catalogue, solver and current-static sources are unchanged. No static-value or improved-strength claim; old experiments preserved. |
| game-validation | **Changed:** 267 AI/lab tests plus 59 server consumer tests pass (326 total), four type configurations, all 2,686 pilot actions independently replayed with snapshots/invariants. DAG tests 14/14. |
| static-package | **Changed:** local Muju Vite production build passes with unchanged ABI 7 WASM; no root hub or other game changes. Full multi-game release assembly is outside this scoped task. |
| server-package | **Verified unchanged:** container/configuration untouched; updated shared AI source and client bundle are prepared, server types and consumer tests pass. Container publication is outside scope. |
| static-deploy | **Blocked for broader release:** existing Pages credentials pause; no deployment requested or attempted. |
| server-deploy | **Verified unchanged:** no deployment requested or attempted; live release unchanged. |
| release-verification | **Changed:** this completion record and pilot evidence; prepared only. No live verification or Gate 1/0/2/3 release claim. |

Prerequisites `transitions` and `wasm-tactics` are unchanged and exercised by
the legality/replay, pending/refund and JS/WASM tests. No changes to saves,
rooms, rule semantics or Academy content. Historical experiments, T2b evidence,
proposal and frozen bands are preserved. No other track's worktree was written.

Remaining integration work: Claude review/commit; run full Gate 1 only in the
agreed quiet window; report hard@desktop versus Rush alongside V2 at Gate 2.
The pilot's Rush losses and Expand h0 draw-band failure remain visible; no
additional outcome-driven tuning or rerun was performed.
