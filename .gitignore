*.gem
*.rbc
.bundle
.config
coverage
.DS_Store
InstalledFiles
lib/bundler/man
log
pkg
rdoc
spec/reports
test/tmp
test/version_tmp
tmp

# YARD artifacts
.yardoc
_yardoc
doc/
# Ignore application configuration
/config/application.yml

# Environment variables
.env
.env.*

# Node.js
node_modules/
package-lock.json
!muju/package-lock.json

# Build output
dist/
_site/

# Claude Code on the web: transient agent worktrees (linked working trees,
# never committed into the parent repo)
.claude/worktrees/
# Local forge-ai worktree checkout (branch claude-forge-ai), not repo content
forge-ai/
# Vite/TS build caches
.vite/
*.tsbuildinfo

# The bare `coverage` rule above is for test-coverage output. Hard AI's E2.2
# stage-trace tool and its results happen to live in directories of that name
# and ARE repo content (docs/hard-ai/e2/E2.2-COVERAGE-TRACE.md).
!muju/lab/hard-ai/coverage/
!muju/lab/results/hard-ai-e2/coverage/

# Python bytecode cache
__pycache__/
*.pyc
