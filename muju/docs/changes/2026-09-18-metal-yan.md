# Metal v2.9 — preparation and release evidence

User request: Metal ATK/DEF/SPD/MINE 1/3/0/3, 1/4/1/4, 2/5/2/5; metal_1 renamed Yan. Local game implementation verified. Academy speech is synthesized and verified after explicit authorization; all eight affected videos and the complete course package are verified locally. Academy deployed at website commit 3b67c3e32b2b8eb10fb08540a6ee79c51a3f958c; game deployed and verified at 17dc8894 (Cloudflare e4d2bc77; Render dep-damoke6k1f9s73bjqc40). This document retains the original preparation evidence below; the release addendum supersedes its unpublished-scope statements. Source HEAD 1ac8026802461569cb1ac0a27c17341737e78a68 plus existing dirty working tree and this change. Unrelated work preserved. Closure: python3 tools/muju-content-dag.py plan --kind piece-stats.

## 1. [x] catalogue — Canonical piece definitions and elemental relationships

Reason: change kind: piece-stats

Paths: `muju/src/game/units.ts`, `muju/src/game/types.ts`, `muju/src/game/elements.ts`

Read/confirm current prerequisites: rule-contract

- Update: Change canonical definitions, stable IDs, stats, costs and elemental relationships. Check promotion cost differences and tier/stock limits. Read the applicable SPEC sections even when starting here.
- Verify: Exercise changed stat boundaries and elemental matchups against real combat, movement, mining and promotion behavior; verify all consumers use current definitions.

**Disposition: changed.** Evidence: Stable metal_1/2/3 IDs; Yan/Mazask/Tanka = 1/3/0/3, 1/4/1/4, 2/5/2/5. Costs 5/9/17 and promotion gaps 4/8 retained. tests/game/metal-v29.test.ts.

## 2. [x] transitions — Canonical legality and state transitions

Reason: upstream changed: catalogue

Paths: `muju/src/game/`, `muju/src/ai/simulate.ts`, `muju/src/hooks/useGameState.ts`, `muju/lab/harness/`

Read/confirm current prerequisites: board-rules

- Update: Review legality, combat, movement, promotion, spawning, upkeep, mining, turn timing, inactivity, victory and home-checkmate. Keep UI, server and simulations on the same transition; adjust lab invariants where the rule itself changes.
- Verify: Add or adapt meaningful regression cases for the changed rule, including illegal-action behavior and identical resulting states in local, AI and server paths.

**Disposition: changed.** Evidence: Movement rejects Speed 0 and shared movementActionCost handles stationary adjacent attacks without NaN. Home-defense optimistic bounds retain Yan attacks. UI reducer / AI transition parity, promotion and finite reserve income regressions pass.

## 3. [x] rules-docs — Current rules, AI explanation and public design copy

Reason: upstream changed: transitions

Paths: `muju/SPEC.md`, `muju/AI_ENGINE_README.md`, `muju/docs/AI_IMPLEMENTATION_STATUS.md`, `muju/docs/STRATEGY_GUIDE-2026-09-12.md`, `muju/docs/strategy-guide-codex-vs-claude.md`, `docs/game-design-dossier.md`, `portfolio/index.html`

- Update: Synchronize current rule tables, examples, costs, terminology and supported strategy claims. Review dated guides used as current advice; mark superseded claims or issue a new revision instead of silently rewriting historical evidence.
- Verify: Search by piece ID, display name, stat label and old numeric phrase across active docs. Check examples with the current engine. Do not claim old balance studies establish new strategic strength.

**Disposition: changed.** Evidence: SPEC v2.9 and JUDGMENT_LOG updated; dated strategy guides marked historical. Current AI implementation note updated. Root dossier/portfolio and AI plans reviewed: no changed stat claim; old architecture plans already carry supersession notices.

## 4. [x] browser-ui — Browser controls, instructions and visual representations

Reason: upstream changed: transitions

Paths: `muju/src/components/`, `muju/src/App.tsx`, `muju/src/main.tsx`, `muju/src/hooks/useAI.ts`, `muju/src/index.css`, `muju/src/utils/colors.ts`, `muju/index.html`, `muju/public/previews/`

- Update: Inspect UnitShop, UnitInfo, InstructionsModal, VisualKey, AnalysisScreen, MapPainter, action/reach previews and unit artwork for copied facts or numeric assumptions. Reuse canonical data where possible. Regenerate affected previews.
- Verify: Inspect desktop and phone play: instructions, stat cards, legal highlights, purchase/promotion, turn endings and analysis. Ensure the AI worker still runs.

**Disposition: changed.** Evidence: Instructions explain stationary Yan. Shop, UnitInfo, VisualKey, MapPainter and artwork derive stats/labels or contain no changed claim. Metal desktop/phone tests verify cards, keyboard/click nonmovement, adjacent capture and save reload. Existing preview is reserve-only. Full site phone/tablet smoke passes.

## 5. [x] persistence — Saved games, migrations, history and replay compatibility

Reason: upstream changed: transitions

Paths: `muju/src/game/migrate.ts`, `muju/src/game/replay.ts`, `muju/src/game/moveHistory.ts`, `muju/src/utils/persistence.ts`, `muju/src/components/TurnReplay.tsx`, `muju/src/online/RoomHistory.tsx`

- Update: Decide how existing saves, active rooms and historical replays behave under the change. Add migration/version handling if needed; preserve old evidence and stored histories.
- Verify: Load an existing save/room, undo where supported, restart the server and replay a completed game. Explicitly report incompatibilities and their handling.

**Disposition: changed.** Evidence: Existing saves/rooms retain stable IDs and adopt current catalogue. Replay expansion preserves historical Speed-1 Metal move snapshots/labels instead of looping at Speed 0. Historical command fixture uses its explicitly scoped old Metal catalogue, restored immediately after playback. Arbitrary historical commands are not guaranteed legal under v2.9; snapshots remain viewable and exploration uses current rules.

## 6. [x] wasm-tactics — Compiled tactical solver and canonical witness validation

Reason: upstream changed: transitions

Paths: `muju/assembly/tactics.ts`, `muju/src/ai/wasm/`, `muju/asconfig.json`

- Update: Review duplicated tactical semantics and ABI assumptions. kernel.ts packs stats and attack powers from canonical definitions; do not invent a second stat table. Rebuild tactics.wasm when affected.
- Verify: Replay solver witnesses through canonical legality/transitions. Compare TS and WASM on changed combat/movement/promotion cases, including bounded-search unknown results.
- Command (cwd `muju`): `npm run ai:wasm` — Rebuilds generated WASM; also runs automatically before npm test/build.

**Disposition: changed.** Evidence: WASM bound skips unreachable Speed-0 approaches and retains adjacent attacks, movement loop explicitly skips Speed 0; kernel and canonical TS use safe cost helper. Rebuilt tactics.wasm. Differential full-catalogue tests plus new adjacent/distant Yan fixtures pass.

## 7. [x] ai-search — Deterministic game AI, evaluation, planning and worker

Reason: upstream changed: transitions; upstream changed: wasm-tactics

Paths: `muju/src/ai/`, `muju/lab/ai/`, `muju/AI_ENGINE_PLAN.md`, `muju/AI_ENGINE_QUESTIONS.md`

- Update: Review moves, evaluation, beam plans, MCTS, placement strategies, budgets, tactical sharpening and worker protocol. Rules changes may alter reach, affordability and threat assumptions even when stats import automatically. Historical design plans are context, not implementation authority.
- Verify: Check legal complete turns, reproducibility under fixed seeds/work budgets, worker execution and deadlines. Separate rule correctness from strength; run targeted matchups when balance claims are made.
- Command (cwd `muju`): `npm run ai:tactics` — Runs tactical cases and writes a fresh lab result directory; inspect results.

**Disposition: changed.** Evidence: Strategic evaluation stays finite for stationary Yan, including at enemy home. All regression tests and 7 worker browser tests pass. Fresh tactical run: 28 fixtures, 84 decisions, 42/42 expected rescues cleared; lab/results/ai-wasm-tactics-1789744884022. No new strategic-strength claim.

## 8. [x] server-runtime — Authoritative multiplayer host, schemas and online client

Reason: upstream changed: transitions; upstream changed: persistence

Paths: `muju/server/rooms.ts`, `muju/server/schema.ts`, `muju/server/http.ts`, `muju/server/index.ts`, `muju/server/clockPressure.ts`, `muju/src/online/`, `muju/ONLINE.md`

- Update: Review room transitions, validation, clocks, staging, invitations, observations, persistence and client/server compatibility. Document any protocol change and handling of active games.
- Verify: Check create/join/play, revision and idempotency handling, clocks/staging where relevant, persisted rooms and reconnects.

**Disposition: verified unchanged.** Evidence: Room/schema protocol and clock behavior need no content copy. Server types and 121 server/MCP tests pass, including persisted rooms/restarts, revision/idempotency and HTTP/stdio. Online suite 46/48 initially passed; two pre-existing 10-reserve expectations corrected to 8 and both history tests pass.

## 9. [x] mcp-tools — MCP rules, legal actions, previews and analysis helpers

Reason: upstream changed: server-runtime; upstream changed: ai-search

Paths: `muju/server/mcp.ts`, `muju/server/stdio.ts`, `muju/server/observation.ts`, `muju/server/agentSchema.ts`, `muju/server/notation.ts`, `muju/server/analysis/`, `muju/tools/benchmark-analysis.ts`

- Update: Review muju_rules text and catalogue, tool descriptions, schemas/defaults, legal actions, preview and play parity. Recheck geometry, economy, units, tactics and exchange helpers for hardcoded budgets/claims and changed search bounds.
- Verify: Assert rules/analysis return current stats and legal witnesses, then exercise HTTP MCP and stdio as applicable. Distinguish bounded estimates from proofs; update benchmark evidence when helper behavior changes.

**Disposition: changed.** Evidence: Rules prose describes Speed 0. Analysis damage bound handles adjacent stationary pieces; tests assert Yan attacks remain discoverable. HTTP/stdio catalogue tests assert exact requested Metal stats, names and prices.

## 10. [x] agent-guides — Published agent skills and MCP usage documentation

Reason: upstream changed: mcp-tools; upstream changed: rules-docs

Paths: `muju/public/skills/`, `muju/server/skills.ts`, `muju/docs/MCP_TOOL_TAPS.md`, `muju/docs/ANALYSIS_TOOLS.md`, `muju/ONLINE.md`

- Update: Update public Muju and time-awareness skill files, TAPs, analysis documentation and live tool examples. Vite copies public skills into dist; the production server serves those copies.
- Verify: Compare documentation to tool discovery and actual responses. Check both /SKILL.md on the Node host and the static /muju/skills/ copies after release.

**Disposition: changed.** Evidence: Public Muju skill adds Metal v2.9 table and movement behavior; MCP tool TAP example renamed. Time-awareness/analysis docs reviewed, unaffected. Updated skill is copied in dist and _site. Live copies intentionally unpublished.

## 11. [x] balance-analysis — Current static balance report and relevant fresh experiments

Reason: upstream changed: transitions; upstream changed: ai-search

Paths: `muju/lab/solver/`, `muju/lab/results/current-static/current.json`, `muju/lab/results/current-static/current.md`

- Update: Review solver assumptions and regenerate current-static. Run new experiments only when needed to support changed claims; record source/rules hashes, configuration and seeds. Leave frozen map studies, baselines and dated results intact.
- Verify: Check catalogue/model freshness and explain changed dominance or witness results rather than weakening checks merely to pass. Static values are separate from AI weights.
- Command (cwd `muju`): `npm run balance:static` — Regenerates current-static outputs.
- Command (cwd `muju`): `npm run balance:check && npm run balance:types` — Checks current outputs and solver types.

**Disposition: changed.** Evidence: Static model accepts nonnegative speed, includes adjacent strikes, excludes unreachable approaches, reports null for unbounded mean/savings instead of NaN. Regenerated current-static with new hashes. balance:check and balance:types pass: 18 distinct profiles, no dominance, no missing sole witness. Historical baselines untouched.

## 12. [x] academy-data — Academy current rule snapshots, catalogues, map and matchup matrices

Reason: upstream changed: transitions

Paths: `muju/academy/export-rules.ts`, `muju/academy/rules-snapshot/`, `muju/academy/catalog.json`, `muju/academy/map.json`, `muju/academy/bonk-matrix.json`, `muju/academy/rules-verification.json`, `muju/academy/production/R??/source-rules/`, `muju/academy/production/R??/src/catalog.json`, `muju/academy/production/R??/src/map.json`, `muju/academy/production/R??/src/bonk-matrix.json`

- Update: Read Academy README and STATUS first. Adapt export-rules.ts assertions/version to the intended rule change, export from live src/game, then propagate current data to every affected active episode. Audit imports and provenance of rules-snapshot and each episode source-rules: they contain historical assumptions and rendering subsets, so do not blindly replace them.
- Verify: Check all ordered full-health matchups and affected demonstrations, map totals and copies. A single attack/defense change can alter incoming/outgoing matrices in every element lesson R11–R16. Regeneration of JSON alone does not update narration or rendered videos.
- Command (cwd `muju`): `node --import tsx academy/export-rules.ts` — Writes Academy JSON after all assertions pass.

**Disposition: changed.** Evidence: Export validates all 324 matchups, Metal stat rows, no-move Yan and reserve-limited income before writing. All 16 catalogue/matrix copies and changed source snapshots verified. Rendering subset types retained; provenance in academy/metal-v29-provenance.json. Prior copies archived.

## 13. [x] academy-lessons — Academy lesson scripts, examples and renderer sources

Reason: upstream changed: academy-data; upstream changed: rules-docs; upstream changed: browser-ui; upstream changed: server-runtime

Paths: `muju/academy/README.md`, `muju/academy/STATUS.md`, `muju/academy/BIBLE.md`, `muju/academy/ALL-SCRIPTS.md`, `muju/academy/CURRICULUM-AND-BATCHES.md`, `muju/academy/PIPELINE-NOTES.md`, `muju/academy/production/R??/episode.json`, `muju/academy/production/R??/src/`, `muju/academy/production/R??/scripts/`, `muju/academy/supplement.tsx`, `muju/academy/update-visuals.py`

- Update: Audit active R01–R16 episode.json and renderer sources for spoken facts, cards, boards, examples, captions and summaries. Record affected episode IDs and why others are unchanged. Preserve repaired current scripts; revise.py and propagate-economy.py are one-time historical migrations and must not be rerun over current production.
- Verify: Validate each changed example against current engine state, action budget and timing. Cross-element counter matchups matter. Check visible revision/date labels and current source provenance; do not resurrect retired R17–R27.

**Disposition: changed.** Evidence: R05 shop card, R06 ladder, R11–R16 matrices/renaming, R16 income/roster/Speed-0 exercise updated. Eight other episodes have no changed claims. Existing repaired narration preserved unless content changed. All scripts/curriculum updated. R16 preview frames inspected for exact stats, matrix changes, v2.9 label and text fit.

## 14. [x] academy-audio — Academy narration, local transcription and caption timing

Reason: upstream changed: academy-lessons

Paths: `muju/academy/audio-batch.py`, `muju/academy/prepare-audio.py`, `muju/academy/retake-openai.mjs`, `muju/academy/transcribe-local.py`, `muju/academy/align-current.py`, `muju/academy/timeline.mjs`, `muju/academy/production/R??/public/audio/`, `muju/academy/production/R??/speech-directions.json`

- Update: Regenerate only changed speech with the established OpenAI cast and pronunciation, preserve unchanged takes, and refresh the changed-clip inventory consumed by transcribe-local.py. Verify locally with faster-whisper, align captions, and regenerate timelines. Inspect hardcoded temporary runtime/model paths before running scripts.
- Verify: No unresolved spoken-fact, missing-word, pronunciation or caption-timing errors. Bind ASR and caption evidence to current audio hashes. Old clip inventories and successful historical logs do not verify new narration.

**Disposition: changed.** Evidence: The user explicitly authorized speech synthesis on 2026-09-18, resolving the earlier automatic approval rejection. All 32 changed clips across R06/R11/R12/R13/R14/R16 use the established OpenAI voices. Seven clips received targeted repairs for omissions or name pronunciation. Local small.en/medium.en verification has no unresolved differences; ASR and aligned captions match current audio hashes in academy/logs/metal-v8-speech-current.json. Consecutive Göl/Gölge ASR token joining is normalized without dropping words. Unchanged takes and every replaced take/QA record are preserved.

## 15. [x] academy-video — Final videos, mastering, posters, transcripts and visual QA

Reason: upstream changed: academy-audio

Paths: `muju/academy/render-batch.py`, `muju/academy/render.mjs`, `muju/academy/master.mjs`, `muju/academy/remaster-batch.py`, `muju/academy/final-frames.py`, `muju/academy/stills-batch.py`, `muju/academy/review-final.py`, `muju/academy/production/R??/output/`, `muju/academy/production/R??/qa/`, `muju/academy/production/R??/public/music/`, `muju/academy/production/R??/public/art/`

- Update: Render affected episodes, master with existing soundtracks, and regenerate affected posters/transcripts. Review actual final exports before recording review-final.py --visual-reviewed evidence. Inspect batch script/runtime paths and current episode configuration first.
- Verify: Require full decode, source-audio correlation, complete duration, quiet holds, loudness/peak checks, readable cards/boards and correct matrices. final-review.json must identify exact final video and timeline hashes; never mechanically assert that unseen frames were reviewed.

**Disposition: changed.** Evidence: All eight v8 exports (R05, R06, R11–R16) are rendered, mastered, fully decoded and visually reviewed. All 324 exported speech clips pass correlation (minimum 0.90994, maximum offset 0.048s); duration, loudness/peak and quiet-hold checks pass. Thirty reviewed final frames cover all 18 element-tier matrices, the shop, promotion ladder, Yan demonstrations, Metal roster/mining and Speed0 exercise. Final reviews identify exact video/timeline/frame hashes. Previous media and QA are archived. Full evidence: academy/logs/metal-v8-final-release-summary.json.

## 16. [x] game-validation — Game, AI, server, MCP and browser regression gates

Reason: upstream changed: rules-docs; upstream changed: browser-ui; upstream changed: persistence; upstream changed: wasm-tactics; upstream changed: ai-search; upstream changed: server-runtime; upstream changed: mcp-tools; upstream changed: agent-guides; upstream changed: balance-analysis

Paths: `muju/tests/`, `muju/e2e/`, `muju/vitest.config.ts`, `muju/playwright.config.ts`, `muju/playwright.online.config.ts`, `muju/server/tsconfig.json`, `muju/tsconfig.json`

- Update: Adapt meaningful changed-rule regression cases and fixtures. Retain explicit historical compatibility fixtures; do not replace every occurrence of an old number. Investigate failures against intended behavior.
- Verify: Run targeted checks during iteration, then release gates. For general local browser tests first serve the built game at MUJU_BASE_URL (default http://127.0.0.1:8927/muju/); the online suite starts its own isolated host.
- Command (cwd `muju`): `npm run server:types && npm test && npm run build` — Checks types/tests and rebuilds WASM plus browser artifacts.
- Command (cwd `muju`): `npm run test:online:e2e` — Runs Playwright against an isolated in-memory Node host on port 8928.
- Command (cwd `muju`): `npm run test:e2e -- e2e/ai-worker.spec.ts` — Requires the built game served at MUJU_BASE_URL and an installed browser.

**Disposition: changed.** Evidence: 808/808 tests and server types/build passed. Subsequent exact MCP catalogue assertions passed in the 121-test server suite. Online tests: 46 initially plus 2 corrected history tests pass. AI worker 7/7; Metal desktop/phone 2/2; staged site smoke at 390/834px passes. Fixed stale smoke comparison to ignore save timestamp only. DAG check and 10 planner tests pass.

## 17. [x] static-package — Complete deevgames static release artifact

Reason: upstream changed: game-validation

Paths: `build-all.sh`, `tools/verify_site.py`, `tools/smoke-site.cjs`, `muju/vite.config.ts`, `muju/package.json`, `muju/package-lock.json`, `index.html`, `README.md`

- Update: Build all three games with locked dependencies and Node 24; stage only the generated _site directory. Confirm current rules, skill assets and compiled AI land in the artifact. Check hub/game navigation and public dossier copy.
- Verify: The full build verifies links, required pages and file sizes. Serve _site on an unused port and run tools/smoke-site.cjs against it; inspect actual gameplay.
- Command (cwd `.`): `bash build-all.sh` — Builds muju, forge and oracle and replaces generated _site; their dependencies must already be installed.

**Disposition: changed.** Evidence: bash build-all.sh on Node v24.11.1 built all three games; site links/assets verified. _site staged and browser smoke passes, including save/refresh. Source and asset hashes in 2026-09-18-metal-evidence/artifact-hashes.json.

## 18. [x] server-package — Node host release with persistent room storage

Reason: upstream changed: game-validation

Paths: `muju/Dockerfile`, `muju/compose.yaml`, `muju/.dockerignore`

- Update: Build the Node host including its own browser dist and copied public skill files. Verify deployed source revision, persistent disk configuration, public URL/CORS and protocol compatibility.
- Verify: Test the built image and restart persistence. Preserve /app/data/rooms.sqlite and existing room data. Static Pages publication does not update this service.

**Disposition: changed.** Render built the Docker image from 17dc8894, started successfully, and retained the existing 1 GB /app/data disk. The room created before deployment survived; live MCP purchase, stationary movement, mining and promotion passed. Local Docker is unnecessary after verified remote build/restart.
## 19. [x] academy-package — Versioned Academy site package and historical redirects

Reason: upstream changed: academy-video

Paths: `muju/academy/build-release.py`, `muju/academy/verify-live.py`, `muju/academy/verify-withdrawal.py`

- Update: Adapt release-specific v7/v2.8 assumptions, summaries and expected economy in builder/verifier for the new release. Locate a current ashkie-pages checkout, review its instructions/workflow, choose a fresh archive outside the site, then validate before packaging. Do not assume the old /private/tmp checkout still exists.
- Verify: Run build-release.py with production root, site checkout and fresh external archive using --check-only before packaging. Verify exact hashed videos/posters/transcripts, course page, release.json, offline-manifest handling and static-before-wildcard redirects. The builder explicitly verifies the Metal v2.9 mixed v7/v8 release.
- External: ethancd/ashkie-pages: muju-academy/, _redirects, offline-manifest.json, ./check, tools/verify_muju_videos.py and deployment workflow

**Disposition: changed.** Evidence: build-release.py --check-only passes all16 final exports; mixed v7/v8 package built at /private/tmp/muju-academy-v8-metal-site from website snapshot 6145c0f6025c80fc08bc4da293182defa8dd24ca. The shared checkout was not changed. Local exact-byte verification passes 48 assets, 48 seek ranges, 120 historical redirects and 33 retired assets. Desktop/phone page and Metal seeking visually reviewed. Offline manifest fresh at 7,681 URLs, exactly 16 poster/transcript additions and 16 removals. Cache behavior passes with browser globals supplied to the pre-existing VM test harness. Prior website assets archived outside staging. Prepared, not deployed; a future publish requires a fresh website-source check.

## 20. [x] static-deploy — Publish Cloudflare Pages deevgames

Reason: upstream changed: static-package

Paths: `.github/workflows/deploy.yml`

- Update: Follow root README deployment flow. Publish validated _site through the workflow or documented Wrangler command; master is production and other branches are previews. Check fetched origin/master freshness and preserve unrelated work.
- Verify: Record source revision, artifact identity, actual publish result and deployment URL. A green workflow can mean checks-only when credentials are absent; confirm the deploy step actually ran.
- External: Cloudflare Pages project deevgames; documented public base https://deevgames.pages.dev

**Disposition: changed.** Published the validated _site directly with the existing Pages OAuth login. Source 17dc8894; deployment https://e4d2bc77.deevgames.pages.dev. Live production assets match the local artifact; nine live Metal/AI-worker tests pass.
## 21. [x] server-deploy — Publish authoritative multiplayer/MCP host

Reason: upstream changed: server-package

Paths: external release surface

- Update: Inspect the current Render service and deploy the verified matching source/image through its configured release path. The repository has no verified automatic server deployment workflow; discover the live service configuration instead of inventing a deploy command. Follow ONLINE.md persistent disk and health settings.
- Verify: Record service/release identity and revision. Verify /api/muju/health, /muju/, /SKILL.md and MCP discovery/rules. Exercise a changed-rule preview and legal action in a fresh disposable room; verify existing storage survived.
- External: Documented Node/MCP host https://deevgames-muju.onrender.com; Render web service with /app/data persistence

**Disposition: changed.** Render auto-deployed 17dc8894 as dep-damoke6k1f9s73bjqc40. Health, browser gameplay, all 15 MCP tools, exact Metal catalogue and public skills verified. Phasing retained. Pre-deployment room persisted; QA game resigned after checks.
## 22. [x] academy-deploy — Publish Muju Academy on ashkie.com

Reason: upstream changed: academy-package

Paths: external release surface

- Update: Publish the prepared Academy-only changes via the current ashkie-pages workflow, following that checkout's release checks. Preserve unrelated website changes and previous media archives.
- Verify: Record website revision/deployment URL. Install the adapted verify-live.py as tools/verify_muju_videos.py in the website checkout before running it there. Verify page and release manifest, exact media bytes/hashes, range seeking, historical redirects and retired assets, then inspect the live course.
- External: https://ashkie.com/muju-academy/; ethancd/ashkie-pages deployment workflow

**Disposition: changed.** Academy published at website commit 3b67c3e3 via Actions 35381455284. Exact 48 assets, 48 seek ranges, 120 historical redirects, 33 retired assets, offline checks, desktop/phone visuals and actual Metal seeking all pass.
## 23. [x] release-verification — Cross-surface completion evidence

Reason: upstream changed: static-deploy; upstream changed: server-deploy; upstream changed: academy-deploy

Paths: external release surface

- Update: Write a change-specific completion record with every affected node disposition, exact source/artifact revisions, tests, affected lesson IDs, publish results and live verification evidence. Update current Academy STATUS and append decision context as appropriate.
- Verify: Compare the changed piece/rule across browser UI, local AI behavior, online engine, MCP rules and analysis, public agent skills and live Academy lessons/transcripts. Every applicable release must be verified or explicitly blocked. Do not call an end-to-end update complete when a surface is still stale or a publish is unverified.

**Disposition: changed.** All affected live surfaces verified. See 2026-09-18-metal-evidence/deployment-summary.json for source/deployment identities and logs. Shared checkout changes and historical media preserved.


## Scope and cleanup

Published and verified all three targets. The current rules and eight revised
Academy videos are live. Phasing remains available as a separate variant. The
QA room was resigned and all local preview servers stopped. Existing user edits,
room storage, historical results and archived media were preserved.

## Authorized release addendum (2026-09-18)

The user explicitly authorized publication and deployment. Release work uses an
isolated checkout based on current `origin/master` 57a70c1a7914bba649df0d8fcf30d16ebf5b34b7,
preserving Phasing, room lifecycle changes, and the recently shipped Hard AI.
The shared source checkout remains untouched by Git operations.

That newer Hard AI required Speed-0 support in packed action generation, move
costs, kill and home-defense bounds, approach/retreat classification, nearest-owner
tables and promotion candidates. Dedicated Yan parity tests cover stationary
movement and adjacent attacks; Yan can promote for movement on an exhausted mine.
Historical E3 evaluations, A6 suite scores, exam witnesses and E1 replay corpora
retain their v2.8 catalogue in isolated test mocks; their source artifacts remain
unchanged. Current-rule differential tests use v2.9. Static balance was regenerated
for the current four-action model (18 distinct profiles, no dominance).

Academy: website source based on current `origin/main` 565028b0; release commit
3b67c3e32b2b8eb10fb08540a6ee79c51a3f958c. GitHub Actions run 35381455284 successfully
published via Cloudflare. Live verification passed all 48 exact assets, 48 range
requests, 120 redirects and 33 retired assets. Desktop and phone visuals and
Metal seeking to 110s of 347.4333s were checked. Offline manifest remains 7,681 URLs,
with exactly 16 additions and 16 removals.

Render service srv-dahbp4ht0dsc73fdqn10 auto-deploys master using Docker. The
existing 1 GB disk is mounted at /app/data. A disposable room was created before
release to verify persistence across deployment; credentials remain private.
Game live results and final release IDs are recorded in the local DAG ledger
and release evidence after deployment completes.

Release gates completed locally: Node 24 build of all three games; server and
balance types; balance freshness; 72 online browser cases (69 initial passes,
three verified on an isolated retry); nine Metal/AI-worker cases; phone/tablet
site smoke; 167 focused Hard AI/historical-evidence cases with the revised
mobile-spare fixture; twelve direct Yan/promotion cases; eleven deadline cases.
The pre-existing slow-runner deadline assertion now respects its own minimum
sample-work floor instead of always requiring a profile update.

## Final live completion

Game: 17dc8894d1539824b203c2dc388bd15e50d4a9e7, Cloudflare e4d2bc77,
Render dep-damoke6k1f9s73bjqc40. Academy: 3b67c3e3, workflow 35381455284.
The full production-base run passed 2,178 tests; its two fixture failures were
corrected and both files passed (15 tests). Additional Yan/promotion cases pass
(12), as does the slow-runner deadline correction (11). No unresolved local
regression failure remains. Online: 72 passing cases across initial/retry runs.
Live Pages: nine browser cases; live Render: two browser cases and real MCP room
checks. The runtime JavaScript and public skill bytes match across both game
hosts; Docker-generated CSS filenames differ and were checked in the browser.
Current default-branch CI is separately re-running the same release tests; direct
Pages publication and Render deployment are already verified independently.
