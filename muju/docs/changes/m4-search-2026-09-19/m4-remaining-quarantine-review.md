# Remaining M4 quarantine review

Read-only review of `codex/phasing-m4` while combined diagnostics run. No tests executed, historical JSON/corpora/openings read, or source/test edits made for this review. Source refs below are under `muju/`. All four exclusions remain in `vitest.config.ts:72–77` at review time.

| Test | Executable historical reads | Obsolete assumptions | Bounded current-rule coverage needed |
|---|---|---|---|
| `tests/ai/hard/search-tie-break.test.ts` | None found. It creates fresh initial states and the engine defaults to an empty book. | Calls to `createInitialGameState()` at lines 36, 76, 98 and 134 construct Standard (default in `src/game/board.ts:204`); `prepare` packs directly (`search-fixture.ts:29`). This reaches the Phasing pack guard before ordering/search. | Construct explicit fresh Phasing states using the fourth constructor argument. Preserve root TT suppression versus interior TT priority, actual tied-score ordering and absent-versus-empty fixed-work equality. Keep a non-vacuous tied-key control; if a newly generated position has no ties, use a small authored tie fixture instead of deleting `ties > 0` / reverse-order controls. Assert real canonical complete-turn replay so equality between two fallbacks cannot pass. |
| `tests/ai/hard-engine-fallback-elapsed.test.ts` | None found. | The two intended search exceptions at lines 56 and 66 instead fail Standard packing before the mocked `searchRoot`. Their expected `engine-error`/250 ms therefore does not test the intended path. | Only replace those valid fixtures with explicit Phasing initial states. Preserve invalid-pack case, exact 120/250 ms accounting, zero fixed-work clock reads, and add/assert the throwing spy is reached for the two search-error cases. This is a cheap deterministic mock test, suitable for immediate unquarantine after verification. |
| `tests/ai/hard/p6-stoppable-generation.test.ts` | Module imports of `p6-g2-black-t20.json` and `p6-g5-white-t19.json` at lines 54–55. These are reconstructed historical replay snapshots, per this file's header. No JSON contents inspected. | Historical fixed-work `BEFORE` at 220–232 explicitly encodes `END_PLACE → attacks/moves → END_ACTION`, a Standard macro. Old work/key/output identities and measured pathology wall times cannot become Phasing oracles. `actions.length > 1` and `replaysLegally` only prove a legal prefix, not a completed Phasing handoff. The authored expired-deadline case at 182–208 already uses the Phasing fixture builder and is salvageable. | Add a dedicated small Phasing stoppability file: deterministic generation stop/deadline control; nonempty canonical complete macro fallback/salvage; no opponent actions after handoff; required upkeep/Prepare closure; generator-prover telemetry with a nonzero exercised call and exact current accounting; repeated fixed-work determinism on the new fixture. Mock the stop clock/trigger instead of demanding the historical 3-second pathology or spending minutes. Preserve the old snapshot/pin test as explicitly historical quarantined evidence rather than relabeling its state/rules or replacing its old constants. |
| `tests/ai/hard/p8-rescue-cap.test.ts` | Imports historical `p8-black-t23.json` and `p8-white-t25.json` at lines 58–59, plus `tests/fixtures/metal-v28-catalogue.ts` at line 2. That fixture intentionally mocks current Metal values to the old catalogue. No snapshot contents inspected. | `CHAMPION_PIN` at 135–149 explicitly buys then ends Place and moves, and the fixed work/depth/node pins at 187–191 plus `ARM_PIN`/seven-call deltas are old-rule evidence. Removing the catalogue mock and retaining these numbers would not be a valid port. The legal replay helper likewise accepts an incomplete prefix. | Add a dedicated current-catalogue Phasing rescue-cap test with a small authored rescuable home occupation. Drive the existing injectable witness and `RescueCap` deterministically: exactly N accepted calls, no callback after cap refusal, exact charged PROVER count, `rescueCapped`, shared cap across generators, reset per searchTurn, and `generateAt` truncation/TT suppression. Replay any emitted witness through full Phasing macro completion. Include an uncapped-versus-inert-cap equality control and repeated capped determinism without old numeric output pins. Preserve historical JSON, catalogue mock and old pins under explicit historical quarantine. |

The P8 production mechanism remains testable without its heavy historical game: `RescueCap` constructor/take/reset/remaining are public at `src/ai/hard/gen/generate.ts:110–134`; `setRescueCap` at 327 and the witness setter immediately above it provide bounded injection. `injectRescue` at 1013–1021 refuses before invoking the witness, records `rescueCapped`, and charges the accepted call. `src/ai/hard/search/pvs.ts:509–514` marks a refused rescue as truncation. A cap-only unit test would be insufficient: the test must exercise this generator-to-search wiring and the complete-turn result.

The two simple files can be ported in place and unquarantined. For P6/P8, distinguish replacement Phasing structural coverage from preserved historical fixed-work/timing experiments. Leaving the old files explicitly quarantined is honest only if the new unquarantined tests exercise the production contracts and the quarantine ledger names the remaining historical-only scope. Do not report M4 accepted solely because the four old exclusions remain quiet. No strength, latency-envelope or new fixed-work golden claim is established by this read-only review.
