{
  "name": "deev-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DeevGames shared game-infrastructure kits: deterministic core, balance lab, game AI, content pipeline, LLM services, UI shell.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
